# Catfishing 项目本地约定

本文件补充全局 `AGENTS.md`，适用于 `D:\develop\Catfishing`。

## Git 提交信息

- Codex 创建 Git commit 时，提交信息必须以中文为主；代码符号、类名、文件名和无法准确翻译的专有名词可以保留英文。
- 提交标题采用“动作：具体内容”的格式，直接说明本次提交产生的结果。常用动作包括：`修复：`、`实现：`、`重构：`、`优化：`、`测试：`、`文档：`、`构建：`、`清理：`。
- 标题应具体、简洁，例如：`修复：角色倒地后无法重新交互的问题`、`实现：鱼竿耐久度同步功能`、`重构：拆分生存属性与角色生命周期职责`。禁止使用“更新代码”“修改问题”“一些调整”等无法判断改动目的的描述。
- 每个 commit 只表达一个主要意图；如果改动同时包含彼此独立的修复、功能或重构，应拆分提交，避免标题无法准确覆盖内容。
- Bug 修复提交除标题外，原则上应在正文写明以下信息；如果某项无法从现有证据确认，不得猜测，应明确写“未确认”或省略该项：

  ```text
  触发情况：在什么操作、状态或环境下出现问题
  问题原因：导致问题的直接原因或机制
  修复方法：本次如何修复，以及为什么这样处理
  验证方式：执行了哪些测试或检查，结果如何
  ```

- 功能实现或重构提交在仅看标题不足以理解设计选择时，也应在正文补充背景、实现方式、影响范围和验证方式。
- 提交信息只能描述该 commit 实际包含并已验证的内容，不得把计划中的工作、尚未完成的行为或未运行的测试写成已经完成。
- 每个新功能或新 Bug 修复在达到可验证状态后，Codex 应主动创建一个独立 commit 作为代码状态检查点；不需要为了等待后续任务而把多个已完成事项长期堆在工作区。
- 后续发现问题时继续追加新的修复 commit，保留可读的线性改动轨迹；除非用户明确要求，不得通过 amend、rebase、reset 或强制推送改写已经形成的检查点。
- 自动提交不等于自动推送。只有用户明确要求 push 时才更新远端；提交前仍必须检查并保留用户的并行改动，不能为了制造干净提交而撤销或覆盖它们。

## 源码目录分类

- 新增或迁移 C++ 类型前，先判断类型本身隶属于哪个系统，再决定目录；不要按“它服务谁”“它挂在哪个 Actor 上”“当前谁调用它”来归类。
- `Character/` 只放角色本体、身体生命周期、Pawn/Character 直接职责。挂在角色身上的系统组件不因此自动属于 `Character/`。
- Gameplay Ability、AbilitySet、AttributeSet、Ability 诊断类和 Ability 配置属于 `AbilitySystem/`。例如 `UCatSurvivalAttributeSet` 归 AbilitySystem，而不是 Character。
- 条件、倒地、潮湿、恢复等状态域属于 `Condition/`，即使它们由角色组件承载。
- UI Subsystem、Widget、UI 配置和只读 View DTO 属于 `UI/`。
- Online Session、旅行、邀请、联机策略与对应类型属于 `Online/`。
- GameMode、GameState、PlayerState、PlayerController 等 UE 游戏框架宿主属于 `Framework/Game/`；不要继续把这类“项目大父类”留在模块根目录。
- 模块根目录只保留模块入口、Build 配置和确实跨系统且没有更稳定归属的极少数文件。发现根目录开始堆业务类时，应在验收前整理。

整理目录时必须同步完成文件移动、include 路径修正、构建验证和必要文档路径更新；不要留下“能编译但导航混乱”的半整理状态。

## 旧代码清理

- 每次新增、迁移或更新功能前，必须先查找同一功能的旧入口、旧目录、旧脚本检查、旧文档口径和旧资产生成路径；不能只把新代码接上而让旧的不合适路径继续误导后续开发。
- 本轮改动范围内发现的旧实现、错误归属、重复入口、过期检查和错误文档口径，必须在同一轮删除、迁移或改正；如果保留会形成双入口或错导航，就不能视为完成。
- 逻辑或功能改动后，如果已经通过源码引用搜索、调用链、配置入口和必要的资产引用检查确认旧方案不再被任何运行路径调用，必须在同一轮自行安全清理旧代码及其配套入口；不得仅以“兼容方案”“以后可能会用”为由保留已无引用的旧实现。只有仍存在已确认的兼容消费者，或引用状态无法安全确认时才允许暂留，并必须说明具体消费者或不确定性。
- 对二进制资产、用户未提交改动、外部引用不明或删除风险高的旧内容，不得硬删；必须在交付说明里明确写出为什么不能删、当前剩余哪条旧路径、后续应该用编辑器迁移还是另开清理任务。
- 清理必须保持最小充分原则：只处理与本轮功能同一职责链上的旧内容，不借机重构无关系统。

## Harness 与进度入口

- `Docs/Development/需求对齐差距清单.md` 是本项目唯一人工进度入口；`.codex/state/current-harness.json` 与 `.codex/state/current-graph.json` 只保存机器执行状态，不另建业务进度清单。
- Harness 中声明为 `single_atomic_module` 的模块只能维护一个模块级状态。内部实现切面、Agent 节点和验证步骤不得拆成可独立关闭的业务事项；只有整套端到端验收同时成立后才能把模块标为完成。
- 多个分流任务并行写同一工作区时，各模块必须使用 `.codex/state/<module>-harness.json`、`<module>-graph.json` 和 `<module>-context.json` 隔离机器状态，并在 `module_delivery` 中声明对应路径；这些文件不是新的人工进度账本。
- 本项目测试 Agent 必须把证据分成 `contract`、`runtime_behavior`、`presentation_delivery` 三层。Automation、FullLoop、静态盘点或编译绿灯只能证明代码契约或运行链路，不能关闭正式资产、正式 WBP/UI、整场 Run/Environment/Social 行为验收或 Delivery readiness；这些高层缺口必须继续挂在对应模块级状态下。
