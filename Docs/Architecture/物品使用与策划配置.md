# 物品使用与策划配置

文档状态：2026-09-17 源码及现有资产已接入；正式双账号与完整表现验证未闭合，模块状态仍以 `Docs/Development/需求对齐差距清单.md` 为准。

事实来源：本次用户确认的 Lyra 职责重构方案；`Source/Catfishing/AbilitySystem/Items/`、`AbilitySystem/Costs/`、`Equipment/CatEquippedDefinition.h`、`Equipment/CatEquippedInstance.cpp`、`Inventory/Fragments/CatItemUseFragment.h`、`Environment/CatChumPlacementService.cpp`；正式配置在 `Content/Catfishing/Data/`。下文未定规则是待策划裁决，不能据此创建可售占位资产。

## 制作一件可使用物品

程序编写行为流程。策划在物品定义的 `UCatItemUseFragment` 选择程序提供的 C++ 行为类，填写成本、前摇、蒙太奇、GE 和命名数值；音效和特效由 GE 关联的 GameplayCue 配置。新增同类内容复用行为类；新的交互机制需要程序实现新的能力行为，策划不编排蓝图执行节点。

| 配置 | 含义、单位与默认值 | 读取者和限制 |
| --- | --- | --- |
| AbilityClass | 使用动作，无默认类 | 随身归属授予对应 Spec；抽象类不可配置 |
| ConsumeCount | 每次成功消耗件数，默认 1，0 为保留原物品 | `UCatAbilityCost_Item` 在服务器支付；装备装配/拿出为 0，正式窝料沿原配置迁为 1 |
| CommitDelay | 生效前摇，秒，默认 0.5 | GA 两端启动任务；正式鱼为 1 秒，装备瞬时入口为 0 |
| Montage | 使用动作，可空 | GAS 负责启动、取消及复制；不再用额外多播重播同一动作 |
| Effects | 提交成功施加给使用者的 GE，默认空 | 自用效果与进食行为读取；领域型装备行为不自动解释这组效果 |
| Magnitudes | GE 使用的 SetByCaller 命名数值，默认空 | 值须有限；鱼经验由实物重量覆盖，客户端不能传经验值 |

物品身份、重量、鱼竿耐久属于实例事实。物品定义和 Fragment 不申请效果、不保存一次使用的执行上下文。`UCatEquipmentItemDefinition` 是库存物品配置；`UCatEquippableItemFragment` 引用独立 `UCatEquippedDefinition`，后者配置装备 Actor 和 AbilitySets。`UCatEquippedInstance` 在装备生命周期内授予/回收能力，SourceObject 指向装备实例，实例关联原物品。

编辑器从物品定义递归验证 Fragment，报告无效能力、负数量、非法秒数和效果参数，并核查装备片段所引用的能力集与世界类。效果型使用必须配置 GE；鱼进食还要求单条成本和成长经验效果类型；装备操作与窝料拒绝不会执行的自用效果。这里不检查 GE 内部修饰器数量，也不把通用 Fragment 限制为经验效果。保存不会自动证明玩法条件、表现或任意自定义 GE 组合正确。

## 当前行为和入口

| 行为 | C++ 入口 | 提交与表现 |
| --- | --- | --- |
| 鱼进食 | `UCatGA_ConsumeFish` | 相同 GA 支持随身、嘴叼、公共容器；权威成本消费原鱼，GE 写成长元属性，保留鱼种经验系数×实际千克和既有取整/溢出/三选一 |
| 自用效果 | `UCatGA_ApplyItemEffects` | 精确来源成本＋配置 GE；可用于小鱼干同类内容，未创建未定数值的商品 |
| 拿出鱼竿 | `UCatGA_DeployFishingRod` | 调用已有部署事务；原竿身份、耐久与物理保持 |
| 抄网 | `UCatGA_UseScoopNet` | TargetData 冻结视线，服务器重算，保留搏斗中可抄鱼的例外；帧末裁决仍归钓鱼域，取消撤销待裁决请求 |
| 饵与漂 | `UCatGA_SelectFishingLoadout` | 只装配精确实例；真咬扣饵保持原时点 |
| 窝料 | `UCatGA_FishingChum` | 预测蓄力/预览，标准释放任务计时；领域完成落点准备后调用 GA 的成本提交，再发布窝点和库存变化 |
| 鱼护 | 原搬运、放置和容器交互 | 没有虚构的 Use 效果；外壳及内部库存继续由原 Actor 保管，不搬到私人背包后再吃 |

选中格子只改变焦点；使用才拿出。物品栏和菜单通过 `UCatItemAbilityComponent` 按行为类及具体 SourceObject 匹配 Spec。随身来源离开库存即取消并撤销其能力；物品正在提交时由 GAS 作用域锁延迟收尾，已付成本后的既定结算不会被同步通知打断。公共/嘴叼进食由默认角色 AbilitySet 授予无固定来源的进食能力。

能力或来源尚未复制时，本机只保留一次输入意图，最多等 2 秒。等待中的取消丢弃意图；连续输入在等待期间松开，随后准备完成时按零蓄力执行。已经激活的释放绑定原 Spec，切格不换结算对象，也不会让同类物品一起响应。

## 网络与提交边界

使用 `AbilitySpec`、`PredictionKey`、`TargetData` 和标准输入事件。动画、蓄力及本地预览可以预测；库存、经验、图鉴、三选一和世界产物只在服务器确认后生效。

每次提交再次核对原来源、可访问性和数量。公共鱼被拿走或先消费后，后来者失败；不能扣另一条同类鱼。前提交取消不扣量，后提交取消不撤销已确认收益。TargetData 最多接受一次；5 秒未收到目标结束等待。日志用 RequestId、物品实例和能力类关联请求及终态。

`CommitAbility` 不是跨系统事务。窝料的环境准备阶段在调用付款前完成水域、范围和配额校验；付款入口仅调用 GA 成本，不接收任意效果执行。库存只扣精确资源，暂缓通知直到窝点终态确认，不保存整包快照回滚。新增成本/GE 若引入同步外部副作用，需要单独审核提交顺序，不能承诺任意效果自动原子回滚。

开发存档版本为 9。旧版本保留磁盘文件并明确拒绝恢复；物品数字编号及实物 GUID 不重编号。

## 七件新道具的接入契约

以下是程序与策划共同遵守的接入边界，不代表七件道具已做成商品。

| 道具 | 接入方式和状态所有者 | 提交、结束与待定规则 |
| --- | --- | --- |
| 小鱼干 | 自用效果 GA，件数成本，绿色体力恢复 GE | 提交前检查非搏斗条件，成功消费一次；恢复量/回满规则及正式表现按当前策划裁决制作 |
| 爪爪套 | 使用 GA 支付物品，角色保留待生效 GE；钓鱼域消费状态 | 指定钓鱼事件开始本场效果，结局移除；生效事件、减耗比例、取消是否保留和重复使用规则待确定 |
| 四叶草 | 使用 GA 建立下一竿状态；选鱼入口读取权重 | 只改抽取权重，不能修改已冻结的鱼；消耗状态的具体阶段、倍率及失败保留规则待确定 |
| 湿毛器 | 目标型 GA；水量是实例持久字段，专用成本精确支付 | 装水为独立动作；服务器复核目标、距离及水源，湿毛用 Cue/状态表现；容量、单次用量、射程及装水条件待确定 |
| 假鱼 | 目标型 GA；机关状态归目标鱼护并复制 | 放置确认才消费；触发者、触发条件、解除/回收及保存边界待确定；不能用使用者本地变量代替鱼护状态 |
| 响响筒 | 输入确认能力；次数归具体实例，确认后扣一次 | 取消输入不扣次数；消息表现发给本队，重复网络确认不重复播报；次数、内容、频率和目标频道待确定 |
| 渔网 | 投掷 GA＋件数成本；调用钓鱼产出与水域影响接口 | 先校验落点/产出许可再提交，生成与水域影响共用请求身份；鱼数/权重、禁入鱼、持续时间、购买和携带限制以当前正式规则裁决 |

这些机制不能用物品名称判断、通用 Fragment 内的分支或策划蓝图节点补齐。规则不完整时保留契约，不加入商店目录。

## 本次替换与消费者衔接

下表记录架构替换结果，不代替唯一模块进度入口。源码位置均相对工程根，公共前缀为 `Source/Catfishing/`。

| 功能/环节 | 当前位置与引用证据 | 现有行为与目标差异 | 处理方式与目标位置 | 衔接依赖与顺序 | 回归风险与验证方式 | 处理结果与证据 |
| --- | --- | --- | --- | --- | --- | --- |
| 使用输入与来源 | `Framework/Game/CatfishingPlayerController.cpp`、`UI/Inventory/CatInventoryPageController.cpp` 调用 `AbilitySystem/Items/CatItemAbilityComponent` | 原库存使用 RPC 与实例行为改为精确来源 Spec；选格不拿竿 | 删除旧 Use/End RPC 和实例使用流程；保留一次待处理意图 | 先授予和复制来源，再接输入，最后删除旧入口 | 弱网、切格、来源尚未复制 | 真实双端快捷栏和 300ms 来源延迟场景通过 |
| 进食与成长 | `CatItemGameplayAbility.cpp` → `Costs/CatAbilityCost_Item.cpp` → `Attributes/CatGrowthAttributeSet.cpp` | Fragment 不再执行效果，成长元属性不再携带事务回执 | 删除 ConsumableEffectFragment、ItemEffectApplication 和库存效果回调/整包快照回滚 | 先成本与成长接收方，再迁移 14 条正式可食用鱼 | 原重量、经验、来源消失、取消和争抢 | 背包/嘴叼/公共来源、争食、取消和超距双端场景通过；没有恢复中毒或增益 |
| 装备与操作能力 | `Equipment/CatEquipmentItemDefinition` → `CatEquippableItemFragment` → `CatEquippedDefinition/CatEquippedInstance` | 物品身份与拿出后的能力生命周期分离 | 装备实例关联来源并管理 AbilitySet 句柄，全量重存后移除临时类重定向 | 先新定义及实例，再迁移 18 份正式及 4 份旧基础装备关联资产 | 同一竿身份、耐久、拿出/收回、跨玩家拾竿 | 正常和弱网的跨玩家拿竿/收回通过；引用核查后已删除旧类重定向及临时迁移字段 |
| 抄网、饵、漂 | `CatEquipmentItemAbilities.cpp` → `Fishing/Integration/CatFishingCommandComponent.cpp` 与 Equipment | 实例不再执行行为；保留原目标裁决和真咬扣饵 | C++ GA 构建冻结领域上下文；取消抄网时移除待裁决请求 | 先领域完成回调，再替换实例与菜单入口 | 搏斗例外、目标不可替换、取消不捕获、扣饵一次 | 实际 GA 搏斗抄鱼、异步去重与真咬扣饵回归通过 |
| 窝料 | `AbilitySystem/Fishing/InputAbilities/CatFishingChumAbility.cpp` → `Environment/CatChumPlacementService.cpp` | 原物品蓄力和直接投放 RPC 改为预测 GA 与标准释放任务 | 删除行为实例类和投放 RPC；落点预检后提交来源 Cost，再发布窝点及库存 | 先准备窝点，再支付一次；最终删除旧 ChumUse 集合 | 快速切格、松开、取消、最后一份与重入 | 正常/弱网窝料→抄网双端通过；支付后异常公开真实扣量并记录错误，不假装回滚任意 GE |
| 鱼护与表现 | `Items/` 搬运/容器链与 `Fishing/Presentation/` 的 ScoopSwing | 原容器仍持有内鱼，不增设虚构 Use；进食蒙太奇改走 GAS | 保留符合职责的搬运/放置；挥网保持既有单一权威表现入口，禁止同时配置重复预测挥网 | 使用入口切换后复查正式 UI、嘴叼和内鱼身份 | 正式页面布局、搬运后内鱼、动作重复 | 有渲染双端鱼护搬运/放置/丢弃通过；完整美术演出和 Steam 双端手验仍未完成 |
| 数据、保存及脚本 | `Save/CatRunSaveGame`、`Save/CatSaveSubsystem`；`Scripts/migrate_equipment_definitions.py`、`migrate_existing_fish_use.py` | 开发旧档不再兼容，物品数字身份不改变 | 版本升为 9 并明确拒旧；编号 6 的旧窝料迁入同链，引用审计后删旧能力集 | 先迁移所有目录消费者，再删无引用包，最后 Cook | 二进制引用、目录漏项、存档身份与磁盘旧档 | 42 个物品资产验证通过；冷盘保存及旧档拒绝通过；编号未重排 |
| 诊断与清理 | `CatItemAbilityComponent.cpp`、`CatItemGameplayAbility.cpp`、现有领域日志 | 从请求、目标、权威终态关联同一来源 | RequestId＋Item＋Ability；无目标超时另记 PredictionKey；Development 默认落盘 | 清除旧入口后检查源码、资产和当前文档；保留明确标注的历史记录 | 不能用单端成功替代跨端证据 | 注释扫描与独立语义复核已执行；旧行为实例、旧回执和旧库存执行 API 已删除 |

旧基础定义另保留编号 1、14、31、33：槽名由旧 `Slot_*` 迁为当前槽语义，原片段数值不改。编号 31 原部署类为空，补用正式一级竿已有的世界表现类；这不把旧基础商品加入正式商店，也不重编号。旧类与旧行为实例的项目资产引用归零后，迁移重定向与临时字段均已清理。
