# 七条长期红项排查报告

日期：2026-09-13。分支 `feature/design-backlog-batch1`，HEAD `c900c04`。**本轮因自动化启动时的缓存权限阻塞停止实施：修掉 0 条，未完成最终全量回归。** 以下判定针对当前源码和已有失败日志；不是修复验收结论。

用例名称下表省略共同前缀 `Catfishing.Unit.`。

| 用例 | 判定 | 根因一句话 | 本轮是否修掉 |
| --- | --- | --- | --- |
| Fishing.BiteTiming.WorldFieldsDriveFormalStateTreeAndBobber | A | 手工置为 prepared，却漏建选鱼必需的 FisherCharacter 与已准入玩家上下文，水域匹配也未确认 | 否，权限阻塞前未改源码 |
| Fishing.Effort.ActualWorkAndSupportPreservePricingIndependenceUnderFiniteReel | A | 夹具沿用默认回体速率 0，却断言放线产生正恢复 | 否 |
| Fishing.Effort.ExhaustionAndReleasedSlackRespectEffortBoundaries | A | 要验证恢复通道，却没有提供正恢复速率这一前置输入 | 否 |
| Fishing.Effort.LineLimitStopsRecoveryOnArrivalAndUsesFinalPaidLength | A | 满线边界断言正常，重新有线后的恢复断言依赖夹具未配置的正费率 | 否 |
| Fishing.Runner.OperatorSamplesAndSingleASCSettlementRemainConservative | A | 受阻前移仍按旧支撑价格判断，另有默认零速率下要求恢复的问题 | 否 |
| Fishing.Runner.PrimaryOperatorStrengthAndCostsIgnorePhysicalHelpers | B | ASC 把极小非零绿色扣款当零，返回成功而没有扣款；另叠加 A 类恢复夹具问题 | 否 |
| Inventory.EquipmentItemPickupRejectsFullBagAndPreventsReentrantDoubleGrant | A | 销毁断言落后于拾取保留原 Actor、隐藏入库并供再次丢弃的正式生命周期 | 否 |

## 执行边界、基线与阻塞

已显式读取 `AGENTS.md`、`CLAUDE.md`、09-12 分支说明（重点第二、五、七节）、第六批报告、设计目录 README，并核对本次相关设计裁决和实现。Git 只读核对：分支和 HEAD 与委托一致，开始时 `status --short` 无改动。没有 add、commit、stash、restore、切分支或修改 Git 配置。用户本轮“我审核并提交／不要动 git”优先于仓库自动 checkpoint 约定。

本轮人工写入仅本报告；未修改源码、配置、脚本、资产或禁止目录。UBT 与编辑器正常生成的构建产物和日志不是业务源码改动。

### 实际执行结果

在 PowerShell 中按本机安装信息设置 `$env:UE_ROOT` 并确认 `Build.bat` 存在，然后走 README 的入口：

```powershell
& "$env:UE_ROOT/Engine/Build/BatchFiles/Build.bat" CatfishingEditor Win64 Development -Project="$PWD/Catfishing.uproject" -WaitMutex -NoHotReload
```

**构建成功，退出 0，26.45 秒，9 个 action。** UBA 正常参与执行，没有再次出现上一批阻止构建的存储目录权限错误。日志包含 `Some action result store tasks did not succeed`，但最终明确为 `Result: Succeeded`；另有既有 `CatPhysicalBodyComponent.cpp:149` double 转 float 警告。本轮没有修改该处。未启动第二个 target，没有并发 UBT。

随后尝试全量基线：

```powershell
& "$env:UE_ROOT/Engine/Binaries/Win64/UnrealEditor-Cmd.exe" "$PWD/Catfishing.uproject" '-ExecCmds=Automation RunTests Catfishing.Unit; Quit' -unattended -nopause -nosplash -NullRHI -log -LOG=LongRed-Baseline.log
```

**编辑器在执行测试前退出 1。** 实际落盘文件是 `Saved/Logs/Catfishing.log`，没有生成命令中命名的 `LongRed-Baseline.log`，不得引用后者为证据。

- `Saved/Logs/Catfishing.log:1018`：ZenLocal 服务连接失败。
- `:1020`：本机通用 DerivedDataCache 写入探针失败。
- `:1027`、`:1036`：`Unable to use cache graph 'Installed' because it has no writable nodes available.`
- `:1054` 附近：错误退出路径；没有本轮 `Test Completed. Result={...}`，没有正常 Automation 完成证据。

这是编辑器 DDC 阻塞，不是本次 UBT 编译失败，也不是“测试失败正常返回 255”。按用户明确要求“如果还是被权限挡住，立刻停下写报告”，发现后停止修码、构建和测试尝试，只读核对现有证据并写本报告。**没有自行更换构建方案，也没有在失败后切换内存 DDC 重试。** README 虽列有 `-DDC-ForceMemoryCache` 的另一条测试命令，本轮仍服从遇到权限阻塞即停止的指令。

解除条件：运行环境提供可用、获准写入的编辑器 DDC，或者用户明确接受 README 的内存 DDC 测试入口后恢复工作。需要重新跑当前源码基线，再逐项实施，最终全量回归；这不是要恢复 UBT 缓存授权，UBT 此次已经成功。

### 现有日志与历史的证据边界

按 `Test Completed\. Result=\{Success\}` / `Test Completed\. Result=\{Fail\}` 整句计数：

| 日志（工程根相对路径） | 通过 | 失败 | 证据性质 |
| --- | --- | --- | --- |
| `Saved/Logs/Catfishing-backup-2026.09.13-07.22.02.log` | 88 | 7 | 任务开始前已有日志；本报告七项失败明细的主要依据 |
| `Saved/Logs/Catfishing-backup-2026.09.12-08.40.06.log` | 81 | 7 | 既有历史日志，含同名失败及断言明细 |
| `Saved/Logs/Catfishing-backup-2026.09.12-14.01.54.log` | 81 | 7 | 另一份既有历史日志 |
| `Saved/Logs/Catfishing.log`（本次尝试） | 0 | 0 | 初始化失败，实际测试执行 0 条；绝不是“零失败通过” |

下文把第一份已有日志简称 **L**，第二份简称 **H**。日志里的时间为 UTC，文件名时间是备份时点；不把备份时点当测试开始时点。

采用用户已核实的“七个用例名在 `99ff6ef` 前已经红”事实，未执行 stash 或历史 checkout。本轮只读源码历史补充如下：

| 用例文件／产品位置 | 可确认的历史 | 不能据此声称的结论 |
| --- | --- | --- |
| `Fishing/Tests/CatFishingCatWorkTests.cpp`、`CatFishingEffortTests.cpp` | 09-11 `dcfd1dc` 已调整出力测试；09-12 `824a81f` 又按移动口径更新；更早有 `43cb6b2`、`11e4730` 等做功及线长边界提交 | 不能只因批 2 改过公式就把所有旧红归因给批 2 |
| `Fishing/Tests/CatFishingOperatorIntegrationTests.cpp` | 最新文件变更为 09-11 `82ac481`，此前有 `dcfd1dc`、`e51e5a0` | 测试文件未改，不表示被测默认值和计费实现没改 |
| `Fishing/Tests/CatFishingParticipantStrengthTests.cpp` | 最新文件变更为 09-11 `e51e5a0`；源头包含 09-10 `6b04247` 的单主控切换 | 同名失败不证明每个失败断言有相同历史根因 |
| `Fishing/Simulation/CatFishingFightSimulator.h` | `git show 99ff6ef:...` 中回体默认为 **1.5**；`9bfb4d5` 改为 **0** | 当前三条 Effort 的零速率根因不能倒推成 `99ff6ef` 当时的根因 |
| `AbilitySystem/Core/CatAbilitySystemComponent.cpp` | `99ff6ef` 直接把非零 delta 交给 GE；`4319c31` 加绿色／黄色分流时引入 `IsNearlyZero(GreenDelta)` | 当前 near-zero 提前返回不是原始分叉点源码已有的分支 |
| `Items/CatItem.cpp` | 09-11 `1681d48` 改为保留拾取原 Actor；该提交同步了 Editor 世界物回归，没有同步本条 Inventory 测试 | 不能为迎合旧销毁断言撤销正式保留链 |

上述 Source 子路径均以 `Source/Catfishing/` 为根。历史日志没有嵌入可独立验证的 Git HEAD 与构建产物绑定；H 已包含演出时序修改后的断言文案，**不能因为它也是 81/7，就宣称它运行的 DLL 对应 `99ff6ef`**。精确到旧提交的首个失败断言和根因仍未完成重现。本报告明确保留这一缺口，避免把同名红项的后续新增失败原因掩盖成“始终如此”。

## 1. BiteTiming.WorldFieldsDriveFormalStateTreeAndBobber

**判定：A，选鱼前置夹具不完整。** 这不是已证明的产品演出失败，也不是“只缺匹配鱼种”。

**本来要抓的 bug：** 窝料世界采样没有进入实际等待计时器；预警不足 1.5 秒；正式 StateTree 没进入试探／真咬；鱼影晚于提竿；扣饵过早或真咬不扣；入夜错误中断已成立窗口；窗口超时后错误再给机会。核心断言位于 `Source/Catfishing/Fishing/Tests/CatFishingBiteTimingWorldTests.cpp:154`、`:178`、`:184`、`:185`、`:187`、`:198`、`:211`。0／1／5 份窝料三种场景仍是测试范围。

**根因及链路证据：**

1. 测试 `:117` 创建 BaitCharacter，`:121` 只设置 PlayerState，`:134` 起手填部分 Session 字段，`:143` 直接 `bPrepared = true`。整个用例没有设置 `Session->FisherCharacter`，也没有建立已准入的 PlayerController。
2. 生产 `Source/Catfishing/Fishing/CatFishingSession.cpp:672` 在正式准备阶段写 FisherCharacter；`:682` 写捕获归属。夹具跳过了这些前置。
3. `ResolveHookSelectionFromAuthority` 的阶段与依赖门在同文件 `:1115` 附近检查 `!FisherCharacter.IsValid()`，失败直接返回；它在 Catalog 选鱼之前。因此“因为没有匹配鱼种而失败”不是当前调用链的第一根因。
4. 即使只补 FisherCharacter，`Source/Catfishing/Fishing/CatFishingService.cpp:1866` 仍从已具备战力资格的 Controller 枚举参与者。夹具仅造 Character／PlayerState 不提供这个上下文。`Source/Catfishing/Data/CatFishCatalogSettings.cpp:117` 起要求至少一个有效参与者及正力量、正体力；补一条弱引用不足以证明修复完成。
5. `CatFishingBiteTimingWorldTests.cpp:47` 自造 `BiteTimingTestWater`。Catalog `:11` 要求逐鱼 `RegionIds.Contains(RegionId)`，这是后续必须核对的独立前置。**本轮未通过引擎读取正式鱼资产的 RegionIds，不能把“所有正式鱼都不匹配”当成已确认事实。**

L `:4668` 起报观察不到 Probe、真咬未开、鱼影为空和时长连锁失败；此前调度日志可见 `Controller=None PlayerState=None StableNetId=Invalid`，与不完整夹具相符。没有 `fishing_fish_selection_resolved` 的同链路选鱼成功证据。预警与 Probe 的失败不能靠扩大帧容差解决。

**改了什么／为什么没改：** 未改。环境停机指令已触发。恢复实施时需先建立正式准入和权威 Session 输入，再选择经引擎确认有正式鱼候选的测试水域；保留正式 StateTree、Hook BP、Catalog、选鱼及 Encounter 生成调用，不能预塞 FishDefinition 或直接把阶段写成 TrueBiteWindow。

**修完还能抓住原 bug 吗：** 按上述只补前置的方案，能保留现有计时、鱼影、扣饵、跨夜和终局断言；把选鱼重新推迟到提竿、取消鱼影创建或提前开窗仍应失败。**该方案尚未实施和运行，不能声称已经证明。** 试探时长的逐鱼覆盖与参数页优先级仍留待原设计裁决，不在夹具里暗改。钓鱼规则 §4.2 的瞬断报废排期锁本轮没有解除。

## 2. Effort.ActualWorkAndSupportPreservePricingIndependenceUnderFiniteReel

**判定：A，恢复前置漏配；核心调价独立性没有在已有日志中失败。**

**本来要抓的 bug：** 费用倍率反过来改变鱼位移、线长、卷线速度、张力、鱼耗体或磨损；受阻转杆凭空产生实际做功；持续支撑费用过高或无负载微调免费。`Source/Catfishing/Fishing/Tests/CatFishingCatWorkTests.cpp:71` 起同时验证这些合同和最后的放线豁免。

**根因：** `:10` 的 `Config()` 配置固定步 0.05 秒等输入，未配置 `SlackStaminaRegenPerSecond`；`Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.h:78` 当前默认 0。用例 `:138` 却要求 `Recovery.CatStaminaDrain < 0`。生产 `CatFishingFightSimulator.cpp:637` 使用 `-min(剩余上限, 速率 × dt)`，零速率只能得到零。L `:4731` 唯一错误就是恢复这一句，`:4732` 记录轻调 0.227、受阻 2.000、重操作 7.568 点/秒。

**设计依据：** `Knowledge/Design/GDD 系统分册/钓鱼系统/钓鱼规则.md:213` 明确基础放线不回体，成长项可以提供速率；`设计修改记录.md:283` 保留基础 0。不能把产品默认改回 1.5 来迎合测试。

**改了什么／为什么没改：** 未改，遵守权限阻塞后停机。恢复测试的正确前提可以是显式带正速率的受控成长场景，同时必须保留零速率产品场景的证据；这不同于改变断言或放宽费用容差。具体迁移尚未实施。

**修完还能抓住原 bug 吗：** 若只补恢复场景的输入，能；调价不改物理的整组断言和正恢复断言均可保留。若只是把 `< 0` 改成 `<= 0`，就抓不住恢复失效，不采用这种做法。当前新默认不能解释 `99ff6ef` 的旧失败，历史原因缺口见前表。

## 3. Effort.ExhaustionAndReleasedSlackRespectEffortBoundaries

**判定：A，要求恢复却未配置恢复能力。**

**本来要抓的 bug：** 已力竭鱼收尾继续扣猫、鱼或分项费用；放线未满时错误沿用角力收费；恢复突破上限。这里的 Exhaustion 首先指**鱼**力竭，不能与“猫归零是否瞬时结场”的未裁事项混为一谈。

**根因：** `Source/Catfishing/Fishing/Tests/CatFishingEffortTests.cpp:14` 的共享 `MakeConfig()` 没给恢复速率，`:285` 使用它，`:305` 要求负 CatStaminaDrain；生产零速率公式同第 2 条。L `:4775` 仅此断言失败，前面的力竭收尾、分项免耗、鱼不耗体和上限检查没有报错。

**改了什么／为什么没改：** 未改，环境阻塞。若恢复实施，应在本用例的恢复场景明确提供正速率，不给共享 `MakeConfig()` 全局加回体而悄悄改变其他用例前提。现有同文件 `:343` 的 `RightButtonWaivesCostsOnlyWhileLineRemainsAvailable` 已显式设置 2.75 点/秒，可作为正确构造前置的参考，不能以它替代或禁用本用例。

**修完还能抓住原 bug 吗：** 只补本场景前置则能，鱼力竭免耗和放线费用边界断言都可保留；若删“恢复应发生”断言则不能。未实际验证。不会借机裁定猫力竭终局。

## 4. Effort.LineLimitStopsRecoveryOnArrivalAndUsesFinalPaidLength

**判定：A，有限线杯边界测试遗漏正恢复速率输入。**

**本来要抓的 bug：** 到达上限当步仍错误恢复；岸线修正后的最终放线长度没有重新参与结算；Finalize 重入重复收费／磨损；鱼回游产生几何余线被误当成线杯容量恢复；真正收短后保留的右键不能再生效。

**根因：** `Source/Catfishing/Fishing/Tests/CatFishingEffortTests.cpp:419` 使用零恢复速率的 `MakeConfig()`；`:444`、`:460` 分别把“岸线修正后有容量”和“实际收短”与负 CatStaminaDrain 绑定。`CatFishingFightSimulator.cpp:461` 的 Finalize 按最终结果计算，`:637` 再乘配置速率。因此在恢复权限重新成立时，金额仍为零。L `:4794`、`:4795` 对应这两处，满线当步收费、重复结算不叠加和余线不等于杯容量等断言没有报错。

**改了什么／为什么没改：** 未改，环境阻塞。应为“有恢复能力的人遇到线杯边界”显式构造正速率，保持上限、最终长度、余线与费用断言及 1e-9 容差；另保留基础零速率覆盖。不能把杯容量判定换成鱼距或几何松线。

**修完还能抓住原 bug 吗：** 按此前置修复能；正速率反而使“到上限还在恢复”和“最终恢复资格没重算”可观察。删掉负恢复检查或把它改成零也通过，会废掉后一类保护，本轮没有这样做。

## 5. Runner.OperatorSamplesAndSingleASCSettlementRemainConservative

**判定：A，夹具同时沿用旧受阻计费判断和未配置的恢复前置。**

**本来要抓的 bug：** 同一身体 100 ms 的观察被两个 50 ms 固定步重复收费；瞬移被算作自愿位移；真正受阻的一段输入漏收费；被动位移收费；一份账单重放写 ASC 两次；带外力或互相抵消的外力仍错误回体。

**根因一：** `Source/Catfishing/Fishing/Tests/CatFishingOperatorIntegrationTests.cpp:184` 推进 0.05 秒，前向输入、身体被挡住，`:187` 仍要求 `LastOperatorStaminaDrain > 0.09`。当前 `Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:337` 读取每段输入方向和时间，`:342` 读取前／后移费率；`CatFishingOperatorWorkModel.cpp:21` 起仅按方向选按秒速率。钓鱼规则 `:201` 的前移为 1.5 点/秒，所以这个受控 50 ms 前移账单是 **0.075**，过不了旧支撑阈值。此前位移缺失支撑的数值不再是腿部附加标准，不能恢复旧费用公式让它过。

**根因二：** 本用例也未配置 `Runner->Config.SlackStaminaRegenPerSecond`；末尾“无负载放线确实恢复”在默认 0 下不成立。L `:8670`、`:8672` 正好报告这两处。

L `:8671` 的观察是两种采样排程都前移 30 cm、都扣 0.150002 点；这只是已有日志支持的局部守恒证据，不是本轮重跑结果。

**改了什么／为什么没改：** 未改，环境阻塞。受阻输入应检验当时已裁的按秒账单及其守恒，恢复分支应明确声明正速率场景。用户禁止直接改期望值迎合当前行为，本轮没有改阈值，也没有删除受阻扣费断言；后续迁移必须以设计 `:201` 的公式为依据，并实际证明“漏掉受阻整个时间段”仍会失败。

**修完还能抓住原 bug 吗：** 只有保留受阻非零账单、同一采样时段只结算一次、重放拒绝、瞬移与被动位移免计费、外力阻止恢复等全部合同，才算能。旧 `> 0.09` 原样不能验证 0.075 的已裁前移场景；本轮不冒充已完成此契约迁移，也不拿一个更大的账单掩盖它。

## 6. Runner.PrimaryOperatorStrengthAndCostsIgnorePhysicalHelpers

**判定：B，真实非零账单被 ASC 吞掉；此外恢复段也有第 2 条所述 A 类夹具缺口。** 主判定按会影响产品资源结算的错误归 B，不能只补夹具后声称本条解决。

**本来要抓的 bug：** 助手力量错误进入主控卷线模型；低体力比例削弱力量；极小正余额无法扣尽；账单重复扣款；钓鱼结算改写助手 ASC；按住左右键跨线杯上限时发生错误优先级与恢复切换。

**根因：**

1. `Source/Catfishing/Fishing/Tests/CatFishingParticipantStrengthTests.cpp:133` 输入 60、30、1e-9 的正体力；`:157` 之后要求真实账单扣款、一次 ASC 修改以及刷新后主控力量为零。
2. `Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:352` 把应付金额裁到当前余额，`:360` 起只拒绝精确零 delta，然后调用 ASC，使用实际余额回读。
3. `Source/Catfishing/AbilitySystem/Core/CatAbilitySystemComponent.cpp:217` 得到非零 `GreenDelta=-1e-9`；`:223` 却以 `FMath::IsNearlyZero(GreenDelta)` 提前返回 **true**，没有应用 `:229` 的 GE。评论说“绿段已空”，但此处实际上有一段尚未支付的非零余额。
4. `Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:268` 明确按 `FrozenOperatorStamina > 0.0` 保留完整力量。余额未扣导致力量继续是 50，助手并没有进入这条数值公式。
5. L `:8793`、`:8794` 中 ASC 修改次数为 0 而非 1；`:8795`、`:8796` 中期望零力量却得到 50。这和提前成功返回的直接后果一致。

还有一个测试盲点：该测试的“扣至零”使用浮点 `TestEqual` 默认容差，可能将 1e-9 当成零而让那一句通过。后面的 delegate 次数及力量断言才真正抓住未扣余额。**不能通过删 delegate 断言、扩大容差或增加测试初始余额绕过。**

**设计依据与修复边界：** `钓鱼规则.md:170` 规定力量不随正体力衰减，`:213` 规定基础恢复为零；`设计修改记录.md:303`、`:309` 确认单主控物理抓握和统一出力池。支付非零余额而非假报成功不需要设计决定。可修方向是精确区分“没有绿色金额”和“金额很小但非零”，保留正式 GE 写口及绿先黄后，不设第二个补账写口。该函数还有抓／推／爬等共享调用，实施前必须回归统一出力池的消费者。

**改了什么／为什么没改：** 未改，原因是本轮明确的权限停机条件，不是把产品 bug 丢给设计。`4319c31` 引入该提前返回；`99ff6ef` 并无这个分支，所以它是当前已查清的根因，不能冒充原始红项的唯一历史根因。恢复段 `:218`、`:222` 还需独立修复前置，光修 ASC 不保证整条变绿。

## 7. Inventory.EquipmentItemPickupRejectsFullBagAndPreventsReentrantDoubleGrant

**判定：A，测试的旧生命周期要求与已经生效的原 Actor 保留链冲突。**

**本来要抓的 bug：** 满包时丢失世界物或错误入账；失败后不能重试；库存通知回调重入使同一世界物发货两次；成功后世界中仍有可重复拾取的副本。

**根因与引用证据：**

- `Source/Catfishing/Inventory/Tests/CatInventoryInstanceTests.cpp:203` 注册用例；`:238` 起验证满包拒绝；`:244` 起在库存通知中重入；`:253` 起检查成功、回调次数、重入失败、数量为一。L 中这些断言没有报错，唯一错误是 `:258` 的 `Pickup->IsActorBeingDestroyed()`。
- `Source/Catfishing/Items/CatEquipmentItem.cpp:24` 把装备转为单件库存批次，继承 `ACatItem::Interact_Implementation`。
- `Source/Catfishing/Items/CatItem.cpp:126` 把原 Actor 交给实例，`:129` 在入库前锁定拾取，`:154` 保留成功的原 Actor 并关闭物理／碰撞／可见性。只有没有实例保留它的批次才走 `:160` 的 Destroy。
- L `:11546` 为满包拒绝，`:11547` 为重入拒绝，`:11548` 是旧销毁断言失败；此前真实提交日志 `Event=item_pickup_committed` 明确 `RetainedActor=1`。
- `Source/Catfishing/Inventory/CatInventoryComponent.cpp:2401` 再次丢弃完整堆栈时取 `SourceItem->GetWorldActor()`，`:2448` 起重新设置世界变换、显示和碰撞。这是保留 Actor 的运行消费者，不是无引用死代码。
- `Source/CatfishingEditor/Inventory/Tests/CatInventoryWorldActionsTests.cpp:351` 已要求拾回隐藏原 Actor，`:353` 检查实例保管，`:354` 禁重复拾取，`:364` 验证再放出复用原 Actor。09-11 `1681d48` 同步了这条 Editor 回归，却未迁移当前 Unit 末尾的销毁断言。

**改了什么／为什么没改：** 未改，权限停机；也没有把生产保留链改回 Destroy。若迁移此测试，不能只把“销毁”为真改成“未销毁”为真；需要同时证明隐藏、无碰撞、不可交互、实例保管原 Actor、继续重入不再发货，才保护其原本的资源安全目的。相关 Editor 用例不等于本条 Unit 已经通过。

**修完还能抓住原 bug 吗：** 对“满包物品不丢、回调不双发、成功无可拾取副本”的原业务 bug，完整迁移上述后置条件能继续抓住。**如果把原 bug 严格定义为“成功后这个 Actor 必须 Destroy”，则不能：正式运行路径就是保留它，是否迁移这条生命周期断言需要审核者决定，不能把原断言删掉后声称原义仍保留。** 本轮没有改这条断言。

## 现有实现与目标实现对照表（未实施）

本轮未修改功能源码；此表记录已完成的职责链盘点与待修边界，不能当作完成清单。源码简称沿用上述各节可定位完整路径。

| 功能/环节 | 当前位置与引用证据 | 现有行为与目标差异 | 处理方式与目标位置 | 衔接依赖与顺序 | 回归风险与验证方式 | 处理结果与证据 |
| --- | --- | --- | --- | --- | --- | --- |
| Bite 入口／权威／生命周期 | BiteTiming 测试 `:117`、`:143` → Session `:672`、选鱼 `:1115`；Service `:1866` | prepared 标志不代表角色、身份和战力真的齐备；生产要求有效输入 | 补测试输入及准入，保留生产准备／选鱼契约 | 先角色和准入，再会话，再计时 | 不允许预写鱼影或目标阶段；验证完整三个份数场景 | 未实施；已定位先于鱼种筛选的缺失前置 |
| Bite 配置／资产／表现 | `Config/DefaultGame.ini:252` Hook BP、`:253` Encounter、`:284` ST_FishingSession；Catalog `:11` 水域门 | 真实树／漂需保留；自造水域的正式鱼资产匹配未确认 | 只读引擎核鱼 RegionIds 和树绑定；测试用合法输入 | 先资产读取，后 fixture 接入 | 仍要证明鱼影先出现、漂轻点、猛沉、扣饵与跨夜 | 引擎未启动成功；资产引用值未确认，不删任何资产或接口 |
| Effort 核心公式／单位／默认值 | Simulator h `:78`、cpp `:637`；三条 Effort 测试 | 速率单位点/秒，默认 0；测试恢复场景要求正值 | 局部构造有成长恢复的输入；产品默认不变 | 先明确场景，再验证单条及 Effort 全组 | 价格不影响动力学；容量和最终长度不混用；保留容差 | 未实施；已有日志指向恢复断言 |
| Runner 采样／ASC／副作用 | Integration 测试；WorkModel `:21`；Runner `:324` | 新前移 1.5 点/秒与旧 50 ms 支撑阈值冲突；单 ASC 结算保留 | 按已裁方向／时间验证金额，不改产品费用 | 先受控输入与采样，再实际 ASC 与重放检查 | 100 ms/2×50 ms 守恒、瞬移、被动位移、外力恢复门 | 未实施；已有日志两排程都 0.150002 |
| ASC 精确结算／复制消费者 | ASC `:223` → Stamina GE → AttributeSet → Runner `:268`；共享体力池 | 非零扣款被当零并假成功；目标仍通过唯一 GE 精确付余额 | 修非零分支；保留绿黄顺序、属性委托和复制 | 先盘点共享消耗调用，再修 ASC，再验 Runner | 极小余额精确归零、一次委托、助手零写入；统一出力池回归 | 未实施；当前直接产品错误已定位，其他下游是否有第二道小量过滤尚未运行排除 |
| Inventory 状态／引用／退出清理 | CatItem `:126`、`:154` → Inventory `:2401`；Editor 世界物测试 `:351` | 旧测试要求 Destroy；生效链保留原实例载体，关闭世界交互 | 待审核后迁移测试后置条件，保留正式保管和丢弃链 | 先确认原义，再修断言组，最后验证再次丢弃/拾回 | 满包不丢、重入不双发、成功不可拾、状态不重建 | 未实施；生效方案与旧测试口径已报告，没有删保留链 |
| 持久化／日志／脚本／Cook／文档 | Inventory 原实例链；`Scripts/create_fishing_fight_balance_asset.py`；DefaultGame 包引用；本报告 | 不新增资源或 schema；现有测试失败不能当正式产品验收 | 本轮仅报告；后续若修共享 ASC，日志和运行消费者需同验 | 先解除运行环境阻塞 | contract / runtime_behavior / presentation_delivery 分层 | 本轮无持久化写入逻辑修改、无生成/迁移/Cook 入口变化；正式 BP/WBP 消费者未全部反序列化，不能以文本搜索否认引用 |

UI/WBP、动画、多人复制与打包双端本轮没有修改；没有新的 UPROPERTY、配置键、资产类或软路径迁移。由于引擎未进入自动化，二进制引用与表现实际送达仍未确认。只读源码能确认流程要求，不能补足这些运行证据。未删除兼容入口、资产或旧测试。

## 我故意没改什么、为什么

- **未改任何源码与测试**：遇到 DDC 权限阻塞后执行用户停机要求；不是完成了修复却少跑一遍测试。
- **没有削弱测试**：没有删断言、放宽容差、改期望、改名、拆分、Disabled 或 AddExpectedError；对必须迁移语义的旧断言只报告。
- **没有恢复正的默认放线回体**：设计明确基础为零，测试应有明确的恢复能力输入。
- **没有修改猫归零终局、搏斗外回体三道闸、试探时长事实源**：这些原设计／实现争议不需要为了本轮测试自行裁决。没有新增 C 类裁决来替代已经能解释的 A／B 根因。
- **没有启用“瞬断报废已验收”的结论**：BiteTiming 尚未变绿，正式表现也未验收，排期依赖仍在。
- **没有改 Destroy 生产策略**：库存中保存的原 Actor 有再次丢弃的已确认消费者。
- **没有修改任何 `.uasset`／`.umap`、Knowledge、gap-analysis、README、AGENTS、CLAUDE、Git 状态或 UBT 方案**。本报告不是新的业务进度账本，也没有关闭任何模块。

## 查过但没问题／证据强度

- UBT/UBA 本轮正常构建，当前阻塞在编辑器 DDC，不需要再归咎上一批 UBA 授权。
- 力量读取仍明确只用主控 ASC，极小体力问题不是把助手 5000 力量加进来了；当前异常能由漏付绿色余额解释。
- 回体金额乘配置速率的公式与基础零速率设计一致；正速率模型回归在现有测试中有明确输入，不应删除恢复通道。
- Inventory 正式拾取在入库通知前置占用，失败释放占用；现有日志没有报告双发货，报的是成功后的销毁旧期望。
- 同类 WorldActions 测试已经有保留原 Actor 的断言；发现了旧、新测试口径冲突，但未在证据不足或用户禁令下改测试。
- 历史七个红项没有因本轮变成更多红项的证据，但**本轮未跑成测试，也就不能声称“88 条无回归已经验证”**。

`contract`：本轮构建通过；静态调用链、历史 diff 和已有日志已审。没有本轮 Automation 通过证据。

`runtime_behavior`：使用已有 L 的失败及局部观察；本轮实际用例执行 0 条。尚未证明修复后的消费者衔接，更没有 Win64 Development 游戏 target 或双端联机运行证据。

`presentation_delivery`：未验证。正式 StateTree/Hook 配置路径已定位，不等于看到了正式演出或客户端鱼影。没有本轮 `<打包根目录>/Catfishing/Saved/Logs` 房主／客户端日志。

## 最终全量结果与仍红清单

**最终全量测试未运行成功。** 本轮唯一全量尝试在初始化时被阻塞，0 条执行、退出 1；不将它计作 0 失败的通过结果。最近一次已有完整日志为 **95 条，88 通过／7 失败**，以下七条均未修复、未关闭：

```text
Catfishing.Unit.Fishing.BiteTiming.WorldFieldsDriveFormalStateTreeAndBobber
Catfishing.Unit.Fishing.Effort.ActualWorkAndSupportPreservePricingIndependenceUnderFiniteReel
Catfishing.Unit.Fishing.Effort.ExhaustionAndReleasedSlackRespectEffortBoundaries
Catfishing.Unit.Fishing.Effort.LineLimitStopsRecoveryOnArrivalAndUsesFinalPaidLength
Catfishing.Unit.Fishing.Runner.OperatorSamplesAndSingleASCSettlementRemainConservative
Catfishing.Unit.Fishing.Runner.PrimaryOperatorStrengthAndCostsIgnorePhysicalHelpers
Catfishing.Unit.Inventory.EquipmentItemPickupRejectsFullBagAndPreventsReentrantDoubleGrant
```

交付状态：**已完成受阻排查报告，未完成“失败数下降且原 88 条保持通过”的任务目标。** 恢复工作后优先补齐 BiteTiming 选鱼前置并运行正式时序，再处理共享 ASC 非零账单，随后迁移有明确设计依据的费用与生命周期测试；每个单元保留原 bug 检测能力，最后必须再跑一次全部 95 条。
