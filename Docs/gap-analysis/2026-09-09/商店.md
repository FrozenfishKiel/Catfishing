## 商店

### Features

| # | Design Requirement | Status | Code Reference | Notes |
|---|-------------------|--------|---------------|-------|
| 1 | 设计禁区：不做真钱入口（金币只在单局内流转）、不做消费审批流程、不设生人局限花钱开关（商店.md:43-46） | ✅ Implemented | `Source/Catfishing/ShopEconomy/CatShopEconomyService.h:207`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.h:74` | 三条都是否定要求。全库无任何 IAP／充值／商城入口；购买写口只有 `SubmitCartFromKiosk` 一条，没有审批、投票、房主批准分支；公款是 WorldSubsystem 内的单一 `Wallet`，不进 Profile 存档，天然只在单局内流转 |
| 2 | 金币为全队共用的单一钱包，无个人分账，收入与支出对全队可见（商店.md:25、81） | ✅ Implemented | `Source/Catfishing/ShopEconomy/CatShopEconomyService.h:207`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:660-684`、`Source/Catfishing/Framework/Game/CatfishingGameState.h:104-105` | 全局只有一份 `FCatShopWalletSnapshot Wallet`；`BuildPublicSnapshot` 把余额＋本局全部公开流水（含操作者 PlayerState、WalletDelta）复制到 GameState，所有客户端都能读到。是否被界面显示是另一条（第 38、41 行） |
| 3 | 钓到的鱼有卖／吃／献三条互相竞争的处置出口（商店.md:23、50） | ✅ Implemented | `Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.cpp:113`、`Source/Catfishing/Items/CatFishConsumptionCoordinator.h:21`、`Source/Catfishing/Run/CatSacrificeCoordinator.cpp:144` | 三条出口的服务器写口都存在。卖这条当前因收鱼价未裁而 fail-closed（见第 20 行），所以三路取舍在运行期实际只有两路可用 |
| 4 | 任何玩家出售任何来源的鱼——自己的鱼、缸里的鱼和偷来的鱼——所得统一进入公款（商店.md:52、67） | ⚠️ Partial | `Source/Catfishing/Items/CatItemsService.cpp:317-345`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:417` | 可售容器白名单只有 `FishGuard` 与 `SharedFishTank`；鱼护还额外要求 `Fish.OwnerStableNetId == SellerStableNetId`（CatItemsService.cpp:341）。偷来的鱼在 Social 的 escrow／叼嘴态里，既不在这两类容器里、`OwnerStableNetId` 也仍是受害者，因此走不到售鱼写口——`Source/Catfishing/Social/CatSocialService.cpp:139-167` 的偷鱼协议只有「窗口到期吃掉」和「被追回原位返还」两个出口，没有卖。所得进公款这一半 ✅ |
| 5 | 白天不可献祭，夜晚由全队统一开启献祭（商店.md:67） | 🔄 Divergent | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1183`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1234` | 代码把献祭贡献 gate 成 `Phase == DayActive && bQuotaOpen`，即只有白天能献、夜晚拒 `InvalidPhase`，与设计正好相反。设计已更新于 09-04（设计修改记录.md:452-456「献祭移到夜晚收尾并等于翻天」，局与进程 v1.25／商店 v1.5 同批落地），代码未跟进；工程自补决策记录里无对应 D 条目 |
| 6 | 用公款购买新竿、鱼饵、鱼漂、道具（商店.md:52、71） | ⚠️ Partial | `Config/DefaultGame.ini:150`、`Config/DefaultGame.ini:56`、`Scripts/verify_equipment_shop_runtime.py:135-143`、`Source/Catfishing/ShopEconomy/CatShopInventoryComponent.cpp:110` | 目录机制通用（任意 DefinitionId 均可上架），正式目录指向 `DT_ShopCatalog_Default`。**复核更正**：脚本里的 `expected_rows` 是「必须包含」的子集断言（`missing_rows = expected_rows - row_names`），并不锁定行数，日志还把实际行数当变量打（`CatalogRows={len(catalog_rows)}`），所以据它断定「正式表就 14 行、鱼漂没有商店行」是过度解读——鱼漂行在不在表里属资产（③），本轮未跑引擎读不到。立得住的是抄网：`Docs/Development/需求对齐差距清单.md:286-287` 明写它当前靠临时开关 `bAutoGrantStarterScoopNet=True`（`Config/DefaultGame.ini:56`）发放，删除条件正是「商店的抄网商品接通」；`Rod_ShopT2` 已以 240 售价接通（同文件同段） |
| 7 | 鱼缸容量升级＝唯一设施类商品：不进团队装备库、直接作用在营地那口缸上、按档位买、随局清空；初始 10 条，两档 20／30，价 300／700 金币（商店.md:71） | ❌ Missing | `Config/DefaultGame.ini:139`、`Source/Catfishing/Items/CatItemsSettings.h:19`、`Source/Catfishing/Items/CatFishTankActor.cpp:50-51` | 复核维持。缸容量是单一固定配置 `SharedFishTankCapacity=20`，由 `GetContainerCapacity` 在 `ACatFishTankActor::BeginPlay` 注册容器时一次读死，既没有档位、也没有升级写口，连「初始 10」这个已拍值都对不上。`FCatShopCatalogEntry` 只有 DefinitionId＋PurchaseQuantity 一种交付形态，`RunCartOrder` 唯一收货方是营地公共仓库（`Trading/CatShopTradeController.cpp:96-97`）。搜过：`TankCapacity`／`FishTankCapacity`／`CapacityTier`／`UpgradeTier`／`Facility`／`SharedFishTank`，全库无设施类商品或容量升级概念。C++ 与 Config 里可查而确实没有 |
| 8 | 使用公款支付，商品明码标价且不可讲价（商店.md:71） | ✅ Implemented | `Source/Catfishing/ShopEconomy/Trading/CatShopTradingTypes.h:223-234`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:259`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.h:77-79` | 复核抽验：原稿的 `CatShopTradingTypes.h:124-134` 落在 `FCatShopTransactionRecord` 尾部，不是它要引的 `FCatShopCartLineCommand`（真实位置 223-234），已改。语义成立：该结构只有 EntryId＋CartCount，客户端提交不了价格；`ResolveCatalogCartForAuthority`／`PurchaseCatalogCart` 都从服务器冻结目录重算总价。`UnitPrice` 默认哨兵 -1＝未填价，`IsRuntimeReady` 拦住漏价条目（`Catalog/CatShopCatalogTypes.cpp:8-17`） |
| 9 | 每笔购买向全队广播，并记录购买者、商品、价格和购买时刻，供 Playtest 回看（商店.md:52、97） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:349`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradingTypes.h:56-131`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:151-157` | 事件与账本都在：首次提交时 `OnPublicTransactionCommitted` 广播一次（重放和拒绝不发），GameMode 订阅后重建 `FCatShopPublicEconomySnapshot` 复制给全队。购买者（StableNetId→PlayerState）、商品（EntryId／DefinitionId）、价格（WalletDelta）齐全，但**没有购买时刻**——`FCatShopTransactionRecord` 无时间戳／天序号字段，`Event=shop_cart_submitted` 日志也只有 RequestId 和行数，不含买家与金额。表现层缺失另见第 41 行 |
| 10 | 购买物进入团队装备库：竿和漂放入公共架，其他备装自取，消耗品从公库领取（商店.md:53、71） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.cpp:96-97`、`Source/Catfishing/Camp/CatCampInventoryActor.cpp:363`、`Source/Catfishing/Camp/CatCampInventoryActor.cpp:682` | 「先入库、玩家再自取」这条链已按 D-04 建成：扣款前预检公共仓库整批容量，支付成立后统一 `AddItemsFromAuthority`，玩家用 `WithdrawToEquipmentFromAuthority` 取入随身 Equipment。但只有一个 `ACatCampInventoryActor`，设计的「公共架（竿／漂）／公库（消耗品）」二分在代码里不存在，所有品类进同一容器 |
| 11 | 消耗品单次携带上限以「钓鱼系统」2.7 为准（商店.md:71） | 🔄 Divergent | `Config/DefaultGame.ini:64`、`Source/Catfishing/Equipment/CatEquipmentSettings.h:45-77` | 复核改判（原 ⚠️ Partial）。原稿只说「5 是 D-13 暂定值、跨册指针失效」，漏了正面对数：现行口径是**鱼饵 8 份／窝料 5 份**（`数值模拟与参数记录.md:51`「携带上限」行，2026-08-19 由 5 改；`GDD 系统分册/道具/道具.md:85` 与 `GDD 系统分册/钓鱼系统/钓鱼规则.md:133` 两处同值），而代码是单一的 `InventoryQuantityStackCapacity=5`——既对不上 8，也表达不了「饵和窝料两个不同上限」。有 D-13（08-20）登记为暂定值，且设计改动（08-19）早于该条目，故记 D 编号而不记「设计已更新」。另：商店册引用的「钓鱼系统 2.7」在现行〈钓鱼规则〉页确已无该编号（§2 只到 2.6），指针本身也失效 |
| 12 | 特殊饵与特殊道具每日限量进货（商店.md:73） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:60-73`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:609`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1060-1066` | 机制齐全并已接线：`bDailyRestock`＋`DailyRestockQuantity` 字段、`AdvanceShopDay` 只重置标了每日进货的有限库存、Run 进入 `DayActive` 时按正式天序号调用一次。哪些条目真的标了每日进货、进货量多少写在 `DT_ShopCatalog_Default.uasset` 里，属资产（③），本轮未跑引擎无法读；`IsRuntimeReady` 会拦住「标了进货却没给数量」的行，所以配置错误不会静默通过 |
| 13 | 装备每日刷新（货架每天换一轮）（商店.md:73、127） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:632`、`Source/Catfishing/ShopEconomy/CatShopInventoryComponent.cpp:132`、`Source/Catfishing/ShopEconomy/CatShopInventoryComponent.h:47` | 复核维持并加固。随机换货架的整条实现都在（`bAlwaysStocked`／`RefreshWeight`／`RandomEntryCount`／带种子的不放回抽取／RequestId 幂等），但 `RefreshShopInventoryFromCatalog` 在全仓库没有任何调用方——服务层那个只被自己的 `.h` 声明引用，组件层那个只被服务层调；`BeginPlay` 只调 `RebuildInitialInventoryFromCatalog` 建一次开局货架。两处都**不是 UFUNCTION**（该组件全文仅 3 个 UFUNCTION，均为 BlueprintPure 查询），蓝图也调不到，可排除「刷新时机藏在 BP 里」。跨天只补货不换架，货架整局不变 |
| 14 | 基础竿和基础补给永不缺货；1 级竿可免费自取，普通饵提供免费保底（商店.md:73） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:36-50`、`Source/Catfishing/UI/Shop/CatShopModel.cpp:359-366` | 复核维持。表达手段齐备：`bUnlimitedStock` 表达永不缺货，`UnitPrice=0` 是显式免费（注释点名「免费普通饵就靠它」），UI 把 0 价渲染成「免费」、无限库存渲染成「库存：不限」。哪几行真配成 0 价／无限写在 DataTable 资产里（③），本轮读不到。设计侧的「营地免费自取」（`GDD 系统分册/道具/道具.md:85`：普通饵营地免费自取、保底件之一）在代码里没有独立写口：搜过 `免费`／`Free`／`Grant`，全库唯一的无偿发放是临时抄网开关 `GrantStarterScoopNetIfConfigured` 与默认关闭的 `bAutoConfigureStarterLoadout`，正式路径只能走商店 0 价购买 |
| 15 | 鱼竿断裂后该鱼竿直接消失，系统不自动返还、不自动替换、不回退为 1 级竿（商店.md:75；DeiSFL.csv 第7行 首轮通过条件） | ⚠️ Partial | `Source/Catfishing/Equipment/CatEquipmentComponent.cpp:1340-1353`、`Source/Catfishing/Save/CatRunSaveGame.h:32-34`、`Source/Catfishing/UI/Inventory/CatInventoryModel.cpp:159` | 复核维持并加固。「不返还、不替换、不回退」✅——商店无退款写口，断竿不触发补发；`Equipment/Tests/CatRodReplacementTests.cpp` 那条链是玩家自己去公共仓库换竿，不是系统自动替换。「直接消失」没有：耐久归零只把实例置 `bRodBroken=true` 并留在库存格（存档 DTO 保留），库存 UI 还专门给它渲染后缀「，已断」，可见废竿是被设计成继续挂在背包里的。D-21（09-04 用户确认）只裁到「耐久归零仍为 RodBroken 并同步真实损坏」，未裁实例是否移除 |
| 16 | 赠送与丢弃统一为「扔下」行为（商店.md:77） | ❌ Missing | — | 复核维持。搜过：`DropToWorld`／`DropItem`／`ThrowItem`／`Discard`／`Abandon`／`Litter`／中文「扔下」「丢弃」「赠送」，全库无装备与道具的落地写口——`Items/CatItemTypes.h:184-218` 的 Drop 是格子间拖放，命中的中文「丢弃」全是「丢弃事件负载／丢弃草稿／丢弃旧视图」这类无关语义；世界里唯一能落地的是鱼（`Items/World/CatFishPickupActor.h:102` 的嘴叼释放）。`需求对齐差距清单.md:271` 也自陈「当前未新增拾取／扔出玩家操作」 |
| 17 | 水面设空气墙，物品不能丢入湖中（商店.md:77） | ❓ Unverifiable | — | 阻挡体属关卡资产（`.umap` 里的 BlockingVolume／碰撞设置），C++／Config／Scripts 里没有也不该有对应文本。属可检查程度③④，本轮不跑引擎，不能据文本判缺失。注：即使有空气墙，第 16 行没有丢弃动作，这条当前无从触发 |
| 18 | 落地后至次日清晨仍无人拾取的物品，自动回收到营地（商店.md:77） | ❌ Missing | — | 复核维持。搜过：`Recover`／`Reclaim`／`Cleanup`／`DayRollover`／中文「回收」「翻天」，无任何掉落物回收或翻天清理逻辑；前置的「扔下」本身也不存在（第 16 行）。另需注意本条设计已过期：`设计修改记录.md:477`（09-05 李前臻拍）把「次日清晨回营地」改为**翻天时消失**，道具册 v1.30 已落地，商店册 §3.1.2 此句未同步。按最新裁决同样是未实现 |
| 19 | 除结算夜收摊后，商人猫全天营业持续收鱼（含夜晚）（商店.md:63、87） | ✅ Implemented | `Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.cpp:126`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:769-772` | 商店链路用的是宽 gate `CanAcceptGameplayCommand`（只看 authority＋`bRunCommandsOpen`＋玩家在场），不是 `CanAcceptFishingCommand` 的白天 gate，所以夜晚照常可买可卖；只有 `CloseCommands` 会关（第 27 行） |
| 20 | 收购价以鱼体重为主要锚点（商店.md:63、128） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomySettings.h:29-30`、`Source/Catfishing/ShopEconomy/CatShopEconomySettings.h:57-65`、`Config/DefaultGame.ini:153` | 复核维持，阻塞点收窄。体重档位机制实现完整（整表校验：非空、重量严格递增、价格不递减；不外推、不插值、不给兜底价），但 `FishPurchasePricePolicy=Unset` 且 `FishPurchasePriceAnchors` 为空，`TryAppraiseFishSale` 恒 false，整条售鱼链 fail-closed，`Scripts/verify_equipment_shop_runtime.py:261-263` 还把「保持 Unset」写成运行期断言。更正一处：档位**阈值**其实已拍（`数值模拟与参数记录.md:54`，09-06 重拍：小 w<1／中 1≤w<5／大 5≤w<15／巨影 w≥15），代码的档位表结构正好能表达它；缺的只是每档价格——参数页至今没有「收购价」这一行 |
| 21 | 窝料轴可作为收购价修正系数（商店.md:63） | ❌ Missing | `Source/Catfishing/ShopEconomy/CatShopEconomyService.h:90` | 复核维持。搜过 `TryAppraiseFishSale`／`Appraise`／`PriceModifier`／`Chum` 与售鱼命令载荷：估价签名只有 `double WeightKilograms` 一个入参，`FCatShopFishSaleCommand` 也只带重量、鱼实例与 Items 提交证据，没有窝料或来源维度的修正位。设计原文写作「可在数值阶段作为修正系数」，属可选项而非硬要求，不构成当前欠账 |
| 22 | 合力捞起的巨物形成全队可感知的大额进账（商店.md:63） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:206-220`、`Source/Catfishing/Data/CatFishDefinition.h:12-22` | 复核维持，措辞更正。档位表能表达「越重越贵」，理论上可把巨影档配成大额；但没有任何「合力／巨物」专门加成，且档位表为空（第 20 行），当前产不出任何进账。更正原稿「商店侧无对应枚举」的写法：鱼侧存在的是 `ECatFishBodyClass`（Unknown／Standard／Giant）这个**协作档位**枚举，不是四档体重档，而且它根本不进估价入参——估价只按连续重量取档。设计侧的四档（小／中／大／巨影）在 `数值模拟与参数记录.md:50、54` 已与经验、供奉共用同一套阈值。「播报」那一半归第 41 行：数据已复制给全队，缺的是表现层 |
| 23 | 收购价、吃鱼成长收益与献祭分量使用一套可对照的等价换算表（商店.md:65、125） | ❌ Missing | `Source/Catfishing/Data/CatFishDefinition.h:78`、`Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:206-220` | 复核维持并补设计侧证据。搜过 `换算`／`等价`／`Equivalent`／`ExchangeRate`／`Conversion`：三条链在代码里完全独立——收购价走 `FCatShopFishWeightPrice` 档位表（按实际重量），献祭走 `CatFishDefinition::SacrificeContribution` 这个**按鱼种写死的整数**，吃鱼走 `CatFishConsumptionCoordinator`＋FishDefinition 效果，没有共享标尺、换算函数或对照配置。设计侧其实已给了标尺：`数值模拟与参数记录.md:50` 明写「供奉值挂体重档，与经验／抄网／收购价同阈值」，`:54` 给了阈值——所以这条是工程未建位，不是产品未给表；顺带暴露一处待查：按鱼种写死的 `SacrificeContribution` 与「按每条鱼实际重量分档」对不上，该口径归局与进程册 |
| 24 | 每局设启动金，具体金额在数值阶段确定（商店.md:83、129） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomySettings.h:42-43`、`Config/DefaultGame.ini:151` | 字段与「未裁哨兵」机制设计得当：默认 -1 表示没人拍过，此时整个 ShopEconomy fail-closed，`StartingTeamWalletBalance=0` 才是「裁定白手起家」。但当前配置是 `999999`，是开发期放行值而不是产品裁定；D-14（08-20）登记的暂定起始公款是 **10**，改成 999999 未回写台账，D-14 需复核 |
| 25 | 金币跟局走，局末通过结算流程归零（商店.md:81） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:23-40`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:709-733`、`Source/Catfishing/Save/CatRunSaveGame.h:200-263` | 跨局不带钱 ✅：公款是 WorldSubsystem 状态，`Deinitialize` 清账本与幂等缓存，新 World 重新从 Settings 冻结余额。但没有「结算流程归零」这个显式写口，靠 World teardown 兜底；另外 `FCatRunSaveGame` 保存了天数、献祭进度、玩家装备和营地公共仓库，**唯独没有公款**——局内存档读档后余额会回落到启动金 |
| 26 | 商人猫是世界里第一个 NPC，猫猫在他这里进行买卖（商店.md:87） | ❓ Unverifiable | `Source/Catfishing/ShopEconomy/CatShopKioskActor.h:15` | 交易角色由 `ACatShopKioskActor`（摊位 Actor＋交互组件＋货架组件）承担，C++ 里没有任何猫形 NPC 类、AI 或对话。摊位是不是被做成一只猫，写在 `Content/UI/Shop/BP_CatShopKiosk.uasset` 与其网格资产里（③④），本轮不跑引擎读不了；`Content/UI/Texture/World/T_UI_World_ShopSign.uasset` 说明至少有招牌类表现资源 |
| 27 | 商人猫收摊＝买卖冻结：购物车支付、售鱼入账、交付确认与每日进货全部停止受理新命令（商店.md:54、87） | ✅ Implemented | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:690`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:2552-2559`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1108`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1115` | 四个写口＋`AdvanceShopDay` 统一受 `bCommandsOpen` 管；关闭后查询照读、既有 RequestId 重放仍返回首次终态，语义正确。两种结算夜相位切换与 World teardown 都调用，重复调用无第二次副作用 |
| 28 | 成功局＝把世界进度顶到 100% 的那次上供当场转成结算夜（不翻天），商人猫在此收摊（商店.md:54） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1110-1116`、`Source/Catfishing/Run/CatRunSettings.h:47-50` | `SuccessSettlementNight` 相位入口直接 `CloseShopForSettlementNight()`，不再翻天。触发点判定（是否由 100% 那次上供触发）归局与进程册，本册只核对「商人猫在此收摊」这一半 |
| 29 | 收摊后把剩余公款／装备换成小鱼干，给猫猫们在篝火旁娱乐（商店.md:54、87） | ❌ Missing | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:690-693` | 复核维持。搜过：`小鱼干`／`DriedFish`／`Snack`／`Treat`／`Bonfire`／`Campfire`——篝火侧只有 `UCatGA_BodyActionCampfirePlayback` 与 `ACatCampHubActor::RequestCampfirePlayback` 这条表演链，与公款、库存无任何连接；`CloseCommands` 只把命令 gate 置否，注释明写「不清公款、库存、账本」，没有余额兑换、装备折算或篝火发放逻辑 |
| 30 | 失败局不进入结算夜，直接执行失败结算（商店.md:55） | 🔄 Divergent | `Source/Catfishing/Framework/Core/CatRunContracts.h:17-18`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1103-1109` | 代码里有独立的 `ECatRunPhase::FailureSettlementNight` 相位，进入时同样走 `CloseShopForSettlementNight()`，还需要等 `SettlementComplete` 才收口。设计已更新于 09-07（设计修改记录.md:429「失败改为进度归零，且不进结算夜，采商店册第 47 行的口径」，局与进程 §3.1.3 与营地册两处已改），代码未跟进；工程自补决策记录里无对应 D 条目 |
| 31 | 失败局无论剩余多少，本局金币与装备全部清零；已解锁的图鉴保持不变（商店.md:55） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:23-40`、`Source/Catfishing/Collection/CatRunImprintService.cpp:29-44` | 图鉴不受影响 ✅：`FishRecorded` Grant 在捕获时就写进 durable Profile，与售／吃／献无关。金币与装备的「清零」没有任何显式写口——失败结算相位只关商店命令门，余额与营地仓库靠 World teardown 一起消失；而 `FCatRunSaveGame` 会把营地公共仓库和玩家装备落盘，存档路径下「失败即清零」不成立 |
| 32 | 金币只买功能性补给（竿、饵、漂、道具），表达类内容（外观、庆祝动作、图鉴皮肤）走解锁不进商店（商店.md:91） | ✅ Implemented | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:19-20`、`Source/Catfishing/Framework/Core/CatProfileContracts.h:12` | 目录项的 `DefinitionId` 只交付给营地公共仓库的装备／消耗品库存，没有外观、动作或皮肤类交付通道；表达类内容走 Profile Grant（`ECatProfileGrantKind`），与商店链完全分离 |
| 33 | 解锁＝商店上新货：解锁的品类进入货架，具体件仍需使用公款购买（商店.md:91） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:56-58`、`Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.cpp:8-17` | 「解锁后仍要花钱买」这一半 ✅（解锁不发实物，只影响上架）。但 `RequiredShopUnlockId` 目前是**只挡不放行**的字段：注释与 `IsRuntimeReady` 都写明「当前没有商店解锁事实源，非空即 fail-closed」，即配了解锁条件的商品永远上不了架，等于这条链没接通 |
| 34 | 解锁清单跟玩家走，使下一局初始货架包含已解锁品类；具体装备和公款只跟当前局走（商店.md:93） | ⚠️ Partial | `Source/Catfishing/Profile/CatProfileSubsystem.h:46`、`Source/Catfishing/Framework/Game/CatfishingPlayerController.h:220-225`、`Source/Catfishing/ShopEconomy/CatShopInventoryComponent.cpp:110` | 「解锁跟人走」✅（durable Profile 的 `UnlockIds`，D-04 已让 owning client 上报并由 PlayerState 复制校验）、「装备与公款跟局走」✅（第 25、31 行）。缺的是把两者接起来的那一步：`RebuildInitialInventoryFromCatalog` 只读本摊位 DataTable，不读任何玩家解锁快照，开局货架与已解锁品类无关 |
| 35 | 钓到瞬间即计入图鉴，记录不受后续出售、吃掉或献祭影响（商店.md:107-108） | ✅ Implemented | `Source/Catfishing/Collection/CatRunImprintService.cpp:29-44`、`Source/Catfishing/Items/World/CatFishPickupActor.cpp:705` | 捕获提交时按 CaptureRequestId 一次性建立不可变 `FishRecorded` Grant 并投递给 Profile；售鱼、吃鱼、献祭三条链都不回写它。登记时点与 09-08「上钩揭剪影→上岸登记收集层」分层裁决一致（设计修改记录.md:523） |
| 36 | 偷卖后的追逐、追回与抓捕规则归联机社交，商店只负责收购端（商店.md:67、109） | ✅ Implemented | `Source/Catfishing/Social/CatSocialService.h:27-49`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.h:64-85` | 职责边界成立：偷鱼协议、追回窗口、抓捕与印记全在 `UCatSocialService`，商店侧一行都不碰。注意这条边界成立不代表第 4 行成立——偷来的鱼当前根本卖不掉 |
| 37 | 确定商店位置，并可验证钓点—鱼缸—商店之间的追逐距离（商店.md:99；GaHeFr.csv 第5行 待对齐内容列） | ❓ Unverifiable | `Source/Catfishing/ShopEconomy/CatShopKioskActor.h:69-70` | 摊位在关卡里摆在哪、离钓点和鱼缸多远，是 `.umap` 里的 Actor 摆放（③），本轮不跑引擎读不到。代码侧只有 `InteractionRadiusCentimeters=300` 这个交互半径，不表达世界布局 |
| 38 | 公款余额对全队常时可见（商店.md:100、133；GaHeFr.csv 第9行 待对齐内容列） | ❌ Missing | `Source/Catfishing/UI/Shop/CatShopModel.cpp:253-255`、`Source/Catfishing/Framework/Game/CatfishingGameState.h:52`、`Source/Catfishing/UI/HUD/CatHUDModel.h` | 复核维持，排除理由更正。余额只在商店页打开时显示（`WalletText`＝「商店：团队公款 %d」）；`Source/Catfishing/UI/HUD/` 全文无 Wallet／Coin／公款字段，`GetShopEconomySnapshot()` 不是 UFUNCTION，`UCatShopModel` 全类零 UFUNCTION，HUD 蓝图确实拿不到余额。**更正**：蓝图并非完全无路——`UCatShopWidget::GetLastShopViewState()` 是 `UFUNCTION(BlueprintPure)` 且 `FCatShopViewState::Economy` 为 BlueprintReadOnly，但那个 Widget 只在商店页打开时存在，正是「常时可见」要排除的场景。故本条仍判未实现 |
| 39 | 缺货、每日刷新、免费保底与入库位置均需明确反馈（商店.md:100、135） | ⚠️ Partial | `Source/Catfishing/UI/Shop/CatShopModel.cpp:359-366`、`Source/Catfishing/UI/Shop/CatShopModel.cpp:113`、`Source/Catfishing/UI/Shop/CatShopModel.cpp:136` | 复核维持。四项里三项有：缺货（「库存：已售罄」＋加购拦截「商店：这件商品已经售罄」）、免费保底（0 价渲染成「免费」）、入库位置（支付成功提示「请在营地公共仓库查看新物品」）。每日刷新在 C++ 侧零反馈——`ShopDayIndex` 只出现在服务与快照结构里，UI 一个字都没用它（何况第 13 行本来就没换架）。留一个 ④ 口子：`ShopDayIndex` 位于 BlueprintReadOnly 的 `Economy` 里（`Trading/CatShopTradingTypes.h:208-210`），商店页 WBP 理论上能自行显示天序号，本轮不跑引擎读不到 |
| 40 | 商人猫收摊需同时通过动作、音效与商店不可交互状态传达（商店.md:136） | ❓ Unverifiable | `Source/Catfishing/ShopEconomy/CatShopKioskActor.h:64-66`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:690-693`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradingTypes.h:196-219` | 复核改判（原 ❌ Missing）。原稿称 `bInteractionEnabled` 是「纯编辑器字段」——不成立：它是 `EditAnywhere, BlueprintReadWrite`，注释明写「蓝图写入后会同时影响提示、打开页面和服务端下单距离证明」，而 Run 相位（`SuccessSettlementNight`／`FailureSettlementNight`）本来就复制到客户端，BP 完全可以据此在本地把摊位置为不可交互。动作与音效本就是 ④。C++ 侧能证的只有：`CloseCommands` 只关服务器写口、不碰摊位，全库无该字段的运行时写入点，公开经济快照里也没有「已收摊」位——手段具备、C++ 未接，按硬要求不能判「未实现」，改 ❓ 并留给引擎入口核对 `BP_CatShopKiosk` |
| 41 | 购买广播作为全场事件，至少显示购买者、商品和支出（商店.md:134） | ❌ Missing | `Source/Catfishing/Framework/Game/CatfishingGameState.h:52`、`Source/Catfishing/UI/Shop/CatShopModel.cpp:227`、`Source/Catfishing/UI/Shop/CatShopWidget.h:264-266` | 复核维持，排除理由更正。数据已复制到每个客户端（第 9 行），C++ 侧唯一消费者是 `UCatShopModel`，且只用余额与库存，`Transactions` 一次都没渲染，HUD 与聊天侧无任何 toast 或流水。**更正**：原稿「WBP 拿不到、可排除藏在蓝图里」不成立——`UCatShopWidget::GetLastShopViewState()` 是 `UFUNCTION(BlueprintPure)`，`ViewState.Economy.Transactions` 蓝图可读，因此「商店页里有没有一条流水列表」属 ④ 查不了。能证伪的是「**全场**」那半：商店页只对打开它的那个玩家存在，而 HUD／GameState 路径上 `GetShopEconomySnapshot()` 非 UFUNCTION、`UCatShopModel` 无 UFUNCTION，全场事件在 C++ 侧确实无从产生 |
| 42 | Playtest 可采集的经济指标：每局购买广播次数、每日余额、局末金币与装备剩余率、偷卖次数（DeiSFL.csv 第3行、第5行、第6行 记录指标列） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomyService.h:210`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:140-150` | `TransactionLedger` 逐笔留了操作者、商品、金额、公款版本和交付状态，理论上能数出购买次数和局末余额；但记录没有时间戳也没有天序号（第 9 行），「每日余额」这类按天切片的指标算不出来，也没有任何导出、统计或落盘入口，账本随 World 一起清空 |
| 43 | 交易过程需要具备明确的猫味演出（商店.md:113） | ❓ Unverifiable | — | 设计原文标为软性边界、「具体动作、台词与视觉资产由内容／美术初版补齐」。演出是动画蒙太奇／音效／WBP 资产（③④），C++ 侧只有 `UCatShopInteractionComponent` 的开关页面逻辑，本轮不跑引擎无法判定有无 |
| 44 | 设计禁区：不让货币放大数值军备——购买提供选择与花样，不形成不可追赶的碾压（商店.md:44） | ❓ Unverifiable | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:9-93` | 复核补行：原稿第 1 行只收了 §1.4 四条禁区里的三条，漏掉本条。目录结构本身不带属性词条、随机强化或叠加位（只有 DefinitionId＋数量＋价格＋库存＋展示字段），商店不会自己制造军备；但「买到的竿会不会形成碾压」取决于装备资产的强度／耐久数值与鱼价曲线（③ 资产＋数值阶段未拍），C++ 侧既无也不该有军备上限机制。本轮不跑引擎、参数页也未给收购价，无法判定 |

### Summary
- Total features: 44
- ✅ Implemented: 10
- ⚠️ Partial: 17
- ❌ Missing: 8
- 🔄 Divergent: 3
- ❓ Unverifiable: 6
- Inspectable: (44 - 6) / 44 * 100% = 86.4%
- Coverage of inspected: (10 + 0.5 * 17) / (44 - 6) * 100% = 48.7%

### Scan Scope

**项目声明（由主流程给定，未自行探测）**：engine=unreal 5.8；project_root=`D:/LocalGameProject/Unreal/Catfishing`；tech_stack=C++/Blueprint（模块 Catfishing、CatfishingEditor）。代码以 2026-09-09 10:40 拉新后的工作区当前文件为准。

**扫过的目录与文件类型**
- `Source/Catfishing/ShopEconomy/`（含 `Catalog/`、`Trading/`）——全部 `.h`/`.cpp` 通读
- `Source/Catfishing/UI/Shop/`——`CatShopTypes.h`、`CatShopModel.cpp/h`、`CatShopInteractionComponent.h`、`CatShopKioskActor` 相关段落
- `Source/Catfishing/Camp/`——`CatCampInventoryActor.h`、`CatCampHubActor.cpp/h` 相关段落
- 顺链扩展扫描：`Source/Catfishing/Items/`（`CatItemsService.cpp` 售鱼容器白名单、`CatFishTankActor`、`CatFishPickupActor`）、`Source/Catfishing/Social/CatSocialService.cpp`（偷鱼协议出口）、`Source/Catfishing/Framework/Game/`（`CatfishingGameModeBase.cpp` 的相位切换／收摊／献祭 gate、`CatfishingGameState.h/.cpp` 的快照复制、`CatfishingPlayerController` 的商店 RPC）、`Source/Catfishing/Equipment/`（断竿与 Starter 兜底）、`Source/Catfishing/Run/`、`Source/Catfishing/Save/`、`Source/Catfishing/Profile/`、`Source/Catfishing/Collection/`、`Source/Catfishing/UI/HUD/`
- `Config/DefaultGame.ini`——`CatShopEconomySettings`、`CatEquipmentSettings` 两段
- `Scripts/verify_equipment_shop.ps1`、`Scripts/verify_equipment_shop_runtime.py`——只作为**静态文本**读，未执行；其中的断言（正式商店 DataTable 14 行行名、`PricePolicy=Unset`、临时抄网发放开关）用作资产内容的间接证据

**检查方法**
- Grep 定位（类名 `UCatShopEconomyService`/`ACatShopKioskActor`/`ACatCampInventoryActor`、字段名 `StartingTeamWalletBalance`/`FishPurchasePriceAnchors`/`bDailyRestock`/`RequiredShopUnlockId`/`bRodBroken`、日志事件名 `shop_cart_submitted`/`shop_fish_sale_submitted`/`shop_economy_snapshot_received`、中文注释「收摊」「公款」「免费」、Config 段名），再 Read 片段核对，未通读大文件
- 台账对照：`Docs/Development/工程自补决策记录.md`（D-01…D-32 全表通读，商店相关为 D-04、D-13、D-14、D-21）、`Docs/Development/需求对齐差距清单.md`（Equipment / Shop 模块段）
- 设计侧对照：`Knowledge/Feishu/GDD 系统分册/商店.md` 全篇＋两张嵌入 CSV；`Knowledge/Feishu/裁决同步/设计修改记录.md` 按日期核对 09-04／09-05／09-07／09-08 与商店相关的裁决（结算夜触发点、鱼缸升级开口子、掉地物品改翻天消失、失败不进结算夜、鱼缸档位 10/20/30 与升级价 300/700）；`裁决同步 · 道具与商店（王甜甜）.md` 只作参考

**跳过与未做的事**
- `Content/` 下 `.uasset`/`.umap` 未作文本扫描（约 1838 个），只用 Glob 确认文件存在。因此 `DT_ShopCatalog_Default.uasset` 的逐行价格／库存／每日进货配置、`BP_CatShopKiosk` 与 `WBP_CatShop*` 的图逻辑、关卡里的摊位摆位与水面阻挡体**均未读取**——报告里第 17、26、37、43 行判为「查不了」而非「未实现」，第 6、12、14 行的资产侧内容也在 Notes 里单独标出
- 未跑引擎、未编译、未执行 `Scripts/verify_equipment_shop*`（`Automation` 模式本身也已被脚本主动阻断，见 `verify_equipment_shop.ps1:148`）
- `Source/**/Tests/` 下没有任何商店或经济相关测试文件，因此本次没有可引用的自动化证据；所有 ✅ 都给了 Source/Config 里的 file:line
- 「鱼／鱼的行为.md」按任务约定不作评分对象；物品本体（清单、属性、解锁表达）归道具册，本报告只判交易、经济与商店侧反馈

### 复核记录

复核者独立到 `Source/`、`Config/`、`Scripts/` 重查了全部 24 行待驳行（原判 ❌／🔄／⚠️），另抽验 9 行 ✅（#2、#3、#8、#19、#27、#28、#32、#35、#36）与全部 4 行 ❓（#17、#26、#37、#43）。结果：3 行改判、9 行证据或排除理由更正、1 行补入。

| # | 原判 | 改判 | 依据 file:line 或搜过的关键词 |
|---|------|------|------------------------------|
| 6 | ⚠️ Partial | ⚠️ Partial（Notes 更正） | `Scripts/verify_equipment_shop_runtime.py:135-143` 是子集断言（`missing_rows = expected_rows - row_names`），不锁行数；「表就 14 行、无鱼漂行」是过度解读，改标 ③。抄网结论保留：`Config/DefaultGame.ini:56`、`需求对齐差距清单.md:286-287` |
| 7 | ❌ Missing | ❌ Missing（补证据） | `Config/DefaultGame.ini:139` `SharedFishTankCapacity=20`、`Items/CatItemsSettings.h:19`、`Items/CatFishTankActor.cpp:50-51`；搜过 `TankCapacity`／`CapacityTier`／`UpgradeTier`／`Facility`／`SharedFishTank` |
| 8 | ✅ Implemented | ✅ Implemented（引用改正） | `CatShopTradingTypes.h:124-134` 落在 `FCatShopTransactionRecord` 尾部；`FCatShopCartLineCommand` 真实位置 `CatShopTradingTypes.h:223-234`（`grep -n "struct FCatShop"` 核过全部结构体边界） |
| 11 | ⚠️ Partial | 🔄 Divergent | 现行携带上限＝鱼饵 8／窝料 5：`数值模拟与参数记录.md:51`、`GDD 系统分册/道具/道具.md:85`、`钓鱼系统/钓鱼规则.md:133`；代码单值 `Config/DefaultGame.ini:64` `InventoryQuantityStackCapacity=5`（D-13，08-20 登记暂定，设计改动 08-19 在前） |
| 13 | ⚠️ Partial | ⚠️ Partial（加固） | `CatShopInventoryComponent.h:47` 与 `CatShopEconomyService.h:122` 均非 UFUNCTION（该组件全文 3 个 UFUNCTION 都是 BlueprintPure 查询），蓝图也调不到，排除「刷新时机藏在 BP 里」 |
| 14 | ⚠️ Partial | ⚠️ Partial（补设计出处） | 「营地免费自取」出处 `GDD 系统分册/道具/道具.md:85`；搜过 `免费`／`Free`／`Grant`，无偿发放只有临时抄网与默认关闭的 `bAutoConfigureStarterLoadout` |
| 15 | ⚠️ Partial | ⚠️ Partial（加固） | `UI/Inventory/CatInventoryModel.cpp:159` 给断竿渲染后缀「，已断」——废竿留在背包是既定行为；`Equipment/Tests/CatRodReplacementTests.cpp` 是玩家手动换竿，不构成自动替换 |
| 16 | ❌ Missing | ❌ Missing（关键词入 Notes） | 搜过 `DropToWorld`／`DropItem`／`ThrowItem`／`Discard`／`Abandon`／`Litter`／「扔下」「丢弃」「赠送」；中文命中全是「丢弃事件负载／草稿／旧视图」 |
| 18 | ❌ Missing | ❌ Missing（关键词入 Notes） | 搜过 `Recover`／`Reclaim`／`Cleanup`／`DayRollover`／「回收」「翻天」；设计已过期见 `设计修改记录.md:477` |
| 20 | ⚠️ Partial | ⚠️ Partial（阻塞点收窄） | 档位阈值已拍：`数值模拟与参数记录.md:54`（09-06 小 w<1／中 1≤w<5／大 5≤w<15／巨影 w≥15）；缺的只是每档价格，参数页无「收购价」行 |
| 21 | ❌ Missing | ❌ Missing（补引用） | `CatShopEconomyService.h:90` 估价签名只有 `double WeightKilograms`；`FCatShopFishSaleCommand`（`CatShopTradingTypes.h:298-325`）也无窝料位 |
| 22 | ⚠️ Partial | ⚠️ Partial（措辞更正） | 鱼侧枚举是 `Data/CatFishDefinition.h:12-22` 的 `ECatFishBodyClass`（Unknown/Standard/Giant），是协作档位不是体重四档，且不进估价入参；设计四档见 `数值模拟与参数记录.md:50、54` |
| 23 | ❌ Missing | ❌ Missing（补两侧证据） | 献祭走 `Data/CatFishDefinition.h:78` 按鱼种写死的 `SacrificeContribution`；设计侧标尺已存在（`数值模拟与参数记录.md:50、54`），故属工程未建位而非产品未给表；搜过 `换算`／`等价`／`Equivalent`／`ExchangeRate`／`Conversion` |
| 29 | ❌ Missing | ❌ Missing（关键词入 Notes） | 搜过 `小鱼干`／`DriedFish`／`Snack`／`Treat`／`Bonfire`／`Campfire`；篝火只有 `AbilitySystem/BodyAction/Camp/CatCampBodyActionAbilities.cpp:162-261` 与 `Camp/CatCampHubActor.cpp:250-345` 的表演链 |
| 38 | ❌ Missing | ❌ Missing（排除理由更正） | 「蓝图完全拿不到」不成立：`UI/Shop/CatShopWidget.h:264-266` 是 `UFUNCTION(BlueprintPure)`；但该 Widget 只在商店页存在，「常时可见」仍不成立。HUD 侧确无路径（`UI/HUD/` 无 Wallet 字段、`GetShopEconomySnapshot` 非 UFUNCTION、`UCatShopModel` 零 UFUNCTION） |
| 39 | ⚠️ Partial | ⚠️ Partial（留 ④ 口子） | `ShopDayIndex` 在 BlueprintReadOnly 的 `Economy` 里（`Trading/CatShopTradingTypes.h:208-210`），WBP 可自行显示；C++ 侧无任何刷新文案 |
| 40 | ❌ Missing | ❓ Unverifiable | `ShopEconomy/CatShopKioskActor.h:64-66` 的 `bInteractionEnabled` 是 `EditAnywhere, BlueprintReadWrite`（非「纯编辑器字段」），注释明写蓝图写入即影响提示／开页／服务端距离证明；Run 相位已复制，BP 可据此本地置否 → ④ 查不了，按硬要求不得判未实现 |
| 41 | ❌ Missing | ❌ Missing（排除理由更正） | 同 #38：`CatShopWidget.h:264-266` BlueprintPure 可读 `Economy.Transactions`，商店页内的流水列表属 ④；能证伪的只有「全场」那半 |
| 44 | （原稿无此行） | ❓ Unverifiable | 补行：`商店.md:44`「不让货币放大数值军备」是 §1.4 四条禁区之一，原稿第 1 行只收了其余三条；目录结构无属性词条／强化位（`Catalog/CatShopCatalogTypes.h:9-93`），但军备强度取决于装备资产数值（③） |

维持不改的待驳行与复核依据：#4（`Items/CatItemsService.cpp:317-345` 白名单＋鱼护 owner 校验；`Social/CatSocialService.cpp:152` 偷鱼后 `OwnerStableNetId` 仍是受害者，`HandleTheftWindowExpired` 只有「吃掉／原位返还」两个出口）、#5（`CatfishingGameModeBase.cpp:1183、1234` 两处 gate 同为 `DayActive && bQuotaOpen`，`:1076` 只在 DayActive 开 `bQuotaOpen`；设计 09-04 见 `设计修改记录.md:450-456`）、#9／#42（`Trading/CatShopTradingTypes.h:57-128` 逐字段核过，确无时间戳或天序号）、#10（`Camp/CatCampHubActor.h:76` 只有一个 `PublicInventory`，`ResolveDeliveryInventoryForShopOrder` 唯一收货方）、#12（`CatfishingGameModeBase.cpp:1060-1066` 已在 DayActive 入口调用，条目配置属 ③）、#24（`Config/DefaultGame.ini:151` = 999999，D-14 登记暂定 10，参数页无启动金行）、#25／#31（`CatShopEconomyService.cpp:23-40` Deinitialize 不重置 Wallet 只清账本；`Save/CatRunSaveGame.h:212-265` 逐字段核过，存了天数／献祭进度／玩家／营地仓库，唯独没有公款）、#30（`Framework/Core/CatRunContracts.h:17-18` 与 `CatfishingGameModeBase.cpp:1103-1109` 独立失败结算夜相位；设计 09-07 见 `设计修改记录.md:429`）、#33／#34（`Catalog/CatShopCatalogTypes.cpp:8-17` 非空即 fail-closed；`CatShopInventoryComponent.cpp:110` 只读本摊位 DataTable）。

抽验的 ✅ 行：#2（`CatShopEconomyService.h:207` 单一 Wallet、`.cpp:659-684` BuildPublicSnapshot 复制余额与全部流水、`CatfishingGameState.h:104-105` 复制属性，位置与语义均对）、#3（`Trading/CatShopTradeController.cpp:113` 售鱼、`Items/CatFishConsumptionCoordinator.h:21` 吃、`Run/CatSacrificeCoordinator.cpp:144` 献，三条写口都在）、#19（`CatShopTradeController.cpp:126` 走宽 gate `CanAcceptGameplayCommand`，定义在 `CatfishingGameModeBase.cpp:769-772`，确实不含白天限制）、#27／#28（`CatfishingGameModeBase.cpp:1108`／`:1115` 两种结算夜都调 `CloseShopForSettlementNight`，实现在 `:2552-2559`，另在 `:268`、`:2465` 也有调用点；`CatShopEconomyService.cpp:690-693` 只置命令 gate）、#32（`Framework/Core/CatProfileContracts.h:9-19` 的 `ECatProfileGrantKind` 与商店交付链分离）、#35（`Collection/CatRunImprintService.cpp:29-44` 按 CaptureRequestId 建一次不可变 Grant，`Items/World/CatFishPickupActor.cpp:705` 在捕获提交处调用）、#36（`Social/CatSocialService.h:27-49` 偷窃协议与商店零耦合）。全部位置存在且语义相符，未改判。

抽验的 ❓ 行：#17（水面阻挡属 `.umap`；C++ 侧只有 `Environment/CatWaterQuerySubsystem` 这类烘焙水域查询，且前置的「扔下」不存在，维持）、#26（搜过 `AIController`／`UBehaviorTree`／`NPC`／`商人猫`，C++ 无任何 NPC/AI 类；形象在 `Content/UI/Shop/BP_CatShopKiosk.uasset` 与网格资产里，维持）、#37（`Scripts/verify_equipment_shop_runtime.py` 只校验 PlayerStart／WaterRegion／资产，未断言摊位摆位，维持）、#43（演出为动画／音效／WBP 资产，维持）。四行均确属 ③④，没有把 C++/Config 能查的东西推给「查不了」。
