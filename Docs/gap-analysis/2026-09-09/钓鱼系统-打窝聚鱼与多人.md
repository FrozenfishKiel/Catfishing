## 钓鱼系统-打窝聚鱼与多人

### Features

| # | Design Requirement | Status | Code Reference | Notes |
|---|-------------------|--------|---------------|-------|
| 1 | 窝点维护 `{X腥, P香, J酵}` 三个存量，每格是窝点里对应偏好鱼的条数；打窝＝逐分量加算，存量恒为整条鱼（钓鱼规则.md:35） | 🔄 Divergent | Source/Catfishing/Environment/CatWaterTypes.h:142-156；Source/Catfishing/Environment/CatChumFieldTypes.h:45-96 | 代码是 `FCatChumVector{Fishy, Fragrant, Fermented}` 连续 double「浓度贡献」，不是鱼的条数；无库存语义、无整数约束。D-30（08-28）登记了这套面积/容量/曲线方向；设计已更新于 09-07／09-08（钓鱼规则 §2 重写为整数存量），D-30 需复核 |
| 2 | 窝点是以首次窝料落点为圆心、半径 5 米的圆，初始存量＝首次投料三属性增量（钓鱼规则.md:37） | 🔄 Divergent | Source/Catfishing/Environment/CatChumFieldTypes.h:50-51；Config/DefaultGame.ini:266-267 | 代码每次投放各自生成一个 field，半径来自窝料资产 `ChumInfluence.RadiusCentimeters` × 全局 `InfluenceAreaMultiplier=8.0`（注释算出直径约 16.97 m），不存在「首次落点定圆」这一身份。D-31（09-01）登记的暂定值是 `InfluenceAreaMultiplier=2.0`（半径乘 √2），当前 Config/DefaultGame.ini:267 已是 8.0，与台账取值不一致，D-31 需复核 |
| 3 | 服务器维护共享存量，同一窝点的所有浮漂读同一份数据（钓鱼规则.md:37） | ⚠️ Partial | Source/Catfishing/Environment/CatChumFieldSubsystem.cpp:337-384 | 服务器权威、所有浮漂读同一个 field 集合 ✅；但读法是「按采样点把所有覆盖该点的 field 按距离/时间衰减加权求和」，不是一份可增可减的共享存量，因而不存在「同一窝点」这个共享账户 |
| 4 | 新投料落在已有窝点圆内→向该窝点加算存量并沿用原圆心半径；否则新建窝点（钓鱼规则.md:39） | ❌ Missing | — | `PrepareField`/`ActivatePreparedFieldDeferred` 一律新建 field（CatChumFieldSubsystem.cpp:189-275），无并窝分支；重叠只在采样时相加 |
| 5 | 几何归属唯一规则：近者优先（比圆心距离）、等距早者优先（创建序号小者）、边界算内（距离 ≤ 半径）（钓鱼规则.md:39） | ❌ Missing | — | 采样端按 GUID 排序累加全部覆盖 field（CatChumFieldSubsystem.cpp:358-380），没有「选一个窝点」的归属判定，也就没有近者/等距/边界三条 |
| 6 | 叠加没有设计上限，代码只留正常游玩碰不到的工程安全夹（钓鱼规则.md:41） | ✅ Implemented | Config/DefaultGame.ini:268-269；Source/Catfishing/Environment/CatChumFieldSubsystem.cpp:186-196 | `MaxActiveFieldsPerRegion=32`、`MaxRawContributionPerRegion=100000.0` 是工程夹，超限返回 `FieldCapacityExceeded`；无产品级容量上限 |
| 7 | 投窝料和抛竿是同一套操作：点击定点、无蓄力、无散布，点哪落哪（钓鱼规则.md:43） | 🔄 Divergent | Source/Catfishing/Fishing/Integration/CatFishingCommandComponent.cpp:779-796,1179-1186 | 代码是 Q 蓄力弹道：按下记时间戳、松开按 `ChargeAlphaFromHeldSeconds` 换算蓄力比再 `PredictChumThrow` 算落点。设计修改记录 09-08「窝料投掷与抛竿同构：点击定点、无蓄力、无散布」；钓鱼系统册 v1.23 也把「蓄力投料」列为需改的旧口径。无 D 条目登记 |
| 8 | 射程限制，超程提示够不到（钓鱼规则.md:43） | ⚠️ Partial | Config/DefaultGame.ini:270-271；Source/Catfishing/Environment/CatChumPlacementService.cpp:168-178 | 服务端射程 `MaxPlacementRangeCentimeters=2000`（20 m）＋视角偏离 60° 限制，超程返回 `PlacementOutOfRange` ✅；「够不到」的玩家提示是 UI 控件，Widget 在 Content 里，本轮查不了 |
| 9 | 投到岸上的窝料变成掉落物，可以捡回（钓鱼规则.md:43） | ❌ Missing | Source/Catfishing/Environment/CatChumPlacementService.cpp:162-167；Source/Catfishing/Fishing/Integration/CatFishingCommandComponent.cpp:1187-1192 | 非水面落点直接以 `InvalidWaterTarget` 拒绝并原样保留库存，不生成任何掉落物；`ACatFishPickupActor` 只承载鱼，Items/World 下没有窝料掉落物类 |
| 10 | 投料落水泛涟漪、出现鱼影（钓鱼规则.md:43） | ❓ Unverifiable | Source/Catfishing/Environment/Presentation/CatChumFieldPresentationActor.cpp:31-43,46-60 | 复核确认：C++ 只有占位网格（引擎自带 Cube/Sphere + BasicShapeMaterial，31-43）与 `ApplyPublicState → BP_OnFieldAdded/BP_OnFieldChanged/BP_OnFieldRemoved` 三个蓝图钩子（46-60）。真正的涟漪/鱼影落在 `PresentationClass`（`TSoftClassPtr<AActor>`）指向的蓝图与材质里；④ 类，文本搜索证明不了。搜过 `涟漪/Ripple/FishShadow/鱼影/Niagara` 均无 C++ 承载 |
| 11 | 叼起窝料再次抛入同一个窝执行叠加，不重置衰减计时（钓鱼规则.md:45） | ❌ Missing | — | 每次投放生成独立 field，各带自己的 `StartServerTime/ExpireServerTime` 与 `TimeFalloff`（CatChumFieldTypes.h:198-210），后投的料就是一条新的衰减曲线；且窝料落地不成掉落物（见 #9），叼回这条路本身不存在 |
| 12 | 补窝受道具总闸门约束：正在搏斗的猫不能掏窝料；其他处于可用道具状态的玩家可以为同一个窝补料（钓鱼规则.md:45；多人钓鱼附篇.md:75） | 🔄 Divergent | Source/Catfishing/Fishing/Integration/CatFishingCommandComponent.cpp:779；Source/Catfishing/Environment/CatChumPlacementService.cpp:133-140 | 代码注释写明「Q 打窝蓄力：与是否有会话无关，等口/遛鱼中都可以补窝」，`PlaceChum` 只拒 `bDowned` 与全局白天 gate，没有任何搏斗态闸门。队友替补料这半是通的（任一玩家都能投）。无 D 条目登记 |
| 13 | `Total = X+P+J`（条数）；Total>0 → T_base 15 秒，Total=0（含落点不在任何窝点内）→ T_base 120 秒（钓鱼规则.md:51-56） | 🔄 Divergent | Config/DefaultGame.ini:227-235；Source/Catfishing/Fishing/Simulation/CatFishingBiteTimingModel.cpp:36-58 | 代码是「无窝/一份/五份」三个均值锚点 20/14/6 秒，按连续贡献量 `SingleChumContribution=1.7`／`FullChumContribution=8.5` 分段线性插值出目标均值。没有 15/120 两档，无窝均值 20 秒而不是 120 秒 |
| 14 | `T_actual = T_base / (1 + Total/K)`，`K = 100`（钓鱼规则.md:58） | ❌ Missing | — | 全库无该公式与 K 常量；代码改用「目标均值 → 二分求解截顶指数分布速率 → 采样等待秒数」（CatFishingBiteTimingModel.cpp:45-74）。参数页 09-08 已把 K 行改成「不再乘 5」，说明设计侧 K 仍在 |
| 15 | 计时器挂在浮漂上，每漂独立（钓鱼规则.md:60） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:869-880,908-914 | 每个 `ACatFishingSession`（＝一根竿一次抛投）各自持有 `ProbeTimerHandle`/`BiteWarningTimerHandle`，互不干涉 |
| 16 | 存量因补窝、衰减或钓走改变后，剩余时间按 `新剩余 = 旧剩余 × 新间隔 / 旧间隔` 折算，不重开完整计时（钓鱼规则.md:60） | ❌ Missing | Source/Catfishing/Fishing/CatFishingSession.cpp:837-880 | 窝料只在抛竿落水那一刻采样一次并一次性排好定时器，等待期间补窝、衰减都不会重算剩余时间；代码里没有任何折算路径 |
| 17 | 窝空时间隔从约 15 秒跳到 120 秒，不加平滑、最低间隔或保底（钓鱼规则.md:62） | 🔄 Divergent | Config/DefaultGame.ini:233-235；Source/Catfishing/Fishing/Simulation/CatFishingBiteTimingModel.cpp:38-44 | 代码明确设了下限 `MinimumBiteDelaySeconds=3.0`（:233）＋`BiteWarningSeconds=1.5`（:235）与上限 `MaximumBiteDelaySeconds=40.0`（:234，模型侧字段名为 `MaximumWaitSeconds`），并对贡献量做分段线性平滑——正是设计写明「不加」的三样。复核订正了原稿把 ini 键名写成 `MaximumWaitSeconds` 的笔误 |
| 18 | 某个浮漂计时到点，服务器执行一次原子事务：读该漂主人当前的饵 → 选类别 → 选鱼种 → 当场扣 1 条库存（钓鱼规则.md:68） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:908-914,944-1007 | 到点只发 `ProbeTriggered` 进试探；鱼种直到玩家在真咬窗内提竿成立才 `ResolveHookSelectionFromAuthority` 选出，且没有任何库存扣减动作。09-08 裁「抽中即出鱼影」也依赖抽鱼提前到到点这一刻 |
| 19 | 两步选鱼：先按 `X/Total、P/Total、J/Total` 选偏好类别，再在该类候选里按漂主当前饵的偏好权重选鱼种（钓鱼规则.md:70） | 🔄 Divergent | Source/Catfishing/Data/CatFishCatalogSettings.cpp:246-281,282-335 | 复核订正：代码确实是两段抽样，但第一段抽的是**挑战档**（Comfort/势均力敌/高风险，按配置带权重 roll，:269-281），不是设计要的 X/P/J 偏好类别；第二段才在选中带里按 `SpawnWeight × ChumModifier(三轴亲和度经半饱和+曲线) × BaitModifier × ChallengeModifier` 归一化 roll 鱼种（:282-335）。三轴占比只以连续亲和度进入乘法权重，不作为一次独立的类别抽样 |
| 20 | 窝料表「目标鱼种加权」的窝料，在第二步对该鱼种权重再乘表里倍率（钓鱼规则.md:70） | ❌ Missing | — | `FCatChumInfluenceSpec` 只有三轴基础贡献、半径、时长、两条衰减曲线（CatChumFieldTypes.h:45-77），没有目标鱼种字段；选鱼端也无对应消费。该列是 09-08 晚⑤号裁决新加的 |
| 21 | Total=0 从基础池选、按当前饵权重、但不扣存量；候选为空或总权重为零时也落基础池（钓鱼规则.md:70） | ⚠️ Partial | Source/Catfishing/Data/CatFishCatalogSettings.cpp:316-319；Source/Catfishing/Fishing/CatFishingSession.cpp:844-854 | 无窝时按 `TotalChum=0` 走同一条选鱼链、仍读饵权重，语义上接近「基础池」✅；但没有独立的基础池概念，`TotalCandidateWeight<=0` 时直接返回未选中（`NoEligibleFish`）而不是兜底出鱼——设计要求的「兜底体验」这半没有 |
| 22 | 库存在抽中那一刻就扣，抽扣同一原子事务，抽中即受惊离窝，之后任何结局都不回滚（钓鱼规则.md:74） | ❌ Missing | — | 无窝料存量模型，因此没有「扣 1 条」这个动作，也无从谈原子性与不回滚。整个 Environment 模块搜不到任何 chum 存量递减 |
| 23 | 饵的数量在真咬成立时扣当时挂着的那 1 份，试探期提竿不损饵（钓鱼规则.md:74） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:1088-1097；Source/Catfishing/Equipment/CatEquipmentComponent.cpp:1506 | 抛竿时只预约（Deferred），真咬提竿并成功选出鱼后才 `CommitFishingBaitDeferred` 落扣；试探期提竿走空竿分支不提交。D-13 登记的普通饵一局消耗品口径 |
| 24 | 同窝多漂同刻到点按服务器时间戳判序，时间戳并列按席位序；A 漂抽走最后一条后 B 漂自然落基础池（钓鱼规则.md:76） | ❌ Missing | — | 依赖 #1/#22 的共享存量与扣减；当前每漂各自采样、各自出鱼，彼此之间没有可争抢的资源，因而这条判序规则在代码里没有对应实现 |
| 25 | 每 30 秒统一执行 `新存量 = floor(旧存量 × 0.9)`，逐分量向下取整，不为负（钓鱼规则.md:82） | 🔄 Divergent | Source/Catfishing/Environment/CatChumFieldTypes.h:62-63,88-96；Source/Catfishing/Environment/CatChumFieldSubsystem.cpp:373-379,388-400 | 代码是每个 field 一条 `TimeFalloffCurve`（烘焙成 65 点采样表）连续衰减，到 `ExpireServerTime` 整体下线；没有 30 秒节拍、没有 0.9 系数、没有 floor 取整。曲线数值在 Content 资产里（③），但「30 秒 × 0.9 整数衰减」这套机制在 C++ 里确定不存在 |
| 26 | 三类存量都达到门槛后，每次投料独立判定一次触发；触发后咬钩加速、鱼群密度演出增强；期间再投料不重复触发；没有冷却（钓鱼规则.md:92） | ❌ Missing | — | 全库无「聚鱼窗口」状态：搜不到门槛判定、触发 roll、窗口计时或咬钩倍率。D-25（08-20）登记了聚鱼两个触发源与 cd 未裁，其处理是「撤回旧自然涌现概率与 `AggregationMomentCooldownSeconds`」；设计已更新于 09-08（聚鱼冷却删除、单一投料触发路径），D-25 需复核 |
| 27 | 聚鱼建议快照：每类门槛 30 条、每次合格投料触发概率 8%、持续 45 秒、咬钩速度 2.5 倍（钓鱼规则.md:96-99） | ❌ Missing | — | Config 与 Source 里都没有这四个量的任何承载字段；`CatChumFieldSettings`／`CatFishingSettings` 两段配置全查过 |
| 28 | 聚鱼只有投料一条触发路径，范围＝触发它的那个窝点；环境事件触发归完整版、不在本版（钓鱼规则.md:103） | 🔄 Divergent | Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1646；Source/Catfishing/Environment/CatEnvironmentSettings.h:30；Source/Catfishing/Environment/CatChumFieldAnchor.h:9 | 代码有第二条路径：公共自然事件按 `NaturalChumDefinitionId + NaturalChumAnchorId` 向 ChumField 写口提交一次「自然聚鱼」，去重键 RunId+DayIndex+EventId+AnchorId。设计已把环境事件触发划出本版。D-25 登记，设计已更新于 09-08，D-25 需复核 |
| 29 | 渔网是一次性道具：按落点窝点当前概率抽 5～8 条、稀有不加权、湖心巨影永不入网，产出落地先到先得（钓鱼规则.md:109） | ❌ Missing | — | `ECatEquipmentKind` 只有 Rod/Bait/Float/ScoopNet/Chum/Herb/Driftwood/Utility（CatEquipmentTypes.h:11-31），没有渔网；全库 grep `CastNet/ThrowNet/渔网` 只在 Docs/Architecture 里作为「当前不是完整可售装备定义」出现 |
| 30 | 渔网使用后该窝点进入 30 秒死水，所有浮漂咬钩计时挂起，结束后按折算规则恢复；死水期内再撒网正常结算、死水重置 30 秒不叠加（钓鱼规则.md:109,111） | ❌ Missing | — | 无死水状态、无计时挂起路径；`ACatFishingSession` 的 Probe/Warning 定时器没有 pause/resume 接口 |
| 31 | 渔网逐条按抽中鱼的偏好类别扣存量，实际条数 `min(5～8 随机数, Total)`；无窝点或空池按基础池给 1～2 条低保、不扣存量；渔网产出不扣竿的基础磨损（钓鱼规则.md:111） | ❌ Missing | — | 随 #29 一并缺失；且没有存量可扣（见 #22） |
| 32 | 单局上限 4 人（多人钓鱼附篇.md:27） | ✅ Implemented | Source/Catfishing/Framework/Game/CatfishingGameModeBase.h:24；Source/Catfishing/Online/CatOnlineSubsystem.cpp:874,2296 | `MaxCampSpawnPlayers = 4` 同时作为房间 `NumPublicConnections` 与营地出生位上限，且在房间设置校验里复核 |
| 33 | 游戏机制层没有房主：钓鱼、移动、献祭都没有身份特权（多人钓鱼附篇.md:27） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingService.cpp:660-692；Source/Catfishing/Environment/CatChumPlacementService.cpp:38-84 | 打窝、占竿位、抄网、拾取的服务端裁决只看身份合法性/距离/阶段，不查 host 身份；D-05 登记的房主争议是房间层 Host Migration，不落在这三条玩法上 |
| 34 | 参与角色有主钓手、辅助手和岸上替补；合力发生在搏斗拉竿阶段（多人钓鱼附篇.md:35-41） | ⚠️ Partial | Source/Catfishing/Fishing/Actors/CatFishingRodActor.h:104-132；Config/DefaultGame.ini:236-237 | 竿上有紧凑的操作位数组（0 号＝主，其余＝辅），`MaximumRodOperatorSlots=4`、`RodOperatorSlotSpacingCentimeters=140`，主/辅两类角色成立 ✅；「岸上替补」在代码里不是一种角色状态，只是「没占竿位的人」，因而替补专属的恢复/接手规则无处挂（见 #37、#48） |
| 35 | 力量计入：钓鱼位 100%、辅助位 75%（多人钓鱼附篇.md:39-40,53） | 🔄 Divergent | Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:349-350；Source/Catfishing/Fishing/Config/CatFishingFightBalanceDefinition.h:49；Scripts/create_fishing_fight_balance_asset.py:28 | 机制成立（主 ×1.0、辅 ×`HelperStrengthMultiplier`），但取值是 0.5 不是 0.75：C++ 默认值 0.5，生成资产脚本 `helper_strength_multiplier: 0.5`。运行时真值在 `DA_FishingFightBalance_Default` 资产里（③），脚本是唯一可读的落值证据 |
| 36 | 消耗倍率 K_pos：钓鱼位 1.0、辅助位 1.5（多人钓鱼附篇.md:39-40,53） | ❌ Missing | Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.cpp:182-198 | 共享账单（收线/转竿/顶住）用的是「按剩余余额从小到大等额注水」的均摊，与位置无关；个人位移消耗也只有 `CatMovementStaminaMultiplier` 一个全局倍率。全库无 K_pos 字段 |
| 37 | 岸上替补搏斗外恢复 5 点/秒（多人钓鱼附篇.md:41） | 🔄 Divergent | Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:384；Scripts/create_fishing_fight_balance_asset.py:43；Source/Catfishing/AbilitySystem/Core/CatAbilitySystemComponent.cpp:309-330 | 代码里唯一的搏斗内恢复是主钓手放线时的 `SlackStaminaRegenPerSecond`（脚本落值 3.0），且只给竿上成员；不在竿上的猫没有逐秒恢复，体力改为在会话开始/结束时一次性回满 |
| 38 | 入位：靠近搏斗中的主钓手 1.5 米内按 F；退出：再按 F 或走出半径，自由来去无惩罚（多人钓鱼附篇.md:45） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingService.cpp:686-692；Source/Catfishing/Fishing/Integration/CatFishingCommandComponent.cpp:735-768 | 键位是 R（代码注释「R 的鱼竿三态」）而不是 F，半径是 250 cm（`FMath::Square(250.0)`／`FindNearestOperableRod(..., 250.0)`）而不是 150 cm；「再按 R」退出成立 ✅，但「走出半径自动退出」没有实现。实际按键绑定在 Content 的 InputAction 资产里（④），此处以 C++ 注释与半径常量为据。参数页 09-08 待办里「交互半径 1.5 米」尚未挂号 |
| 39 | 辅助手体力归零就弹出辅助位、2 秒喘气演出、不落水，恢复到 20% 以上才能重入（多人钓鱼附篇.md:45） | ❌ Missing | Source/Catfishing/Fishing/CatFishingService.cpp:1126；Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.cpp:49-50 | 代码是反向口径：注释写明「零体力只影响出力而不影响操竿资格」，`ActiveStrength` 归零但成员身份保留，不弹出、不喘气、无 20% 重入门槛 |
| 40 | 辅助人数不另设上限，受 4 人上限自然约束，主 1 加辅最多 3（多人钓鱼附篇.md:45） | ✅ Implemented | Config/DefaultGame.ini:236；Source/Catfishing/Fishing/Actors/CatFishingRodActor.h:131-132 | `MaximumRodOperatorSlots=4` 是唯一上限，占满返回 `RodOccupied`；没有单独的辅助位配额 |
| 41 | F 目标选择：看着鱼按 F 是抓鱼、看着主钓手按 F 是入位，两者难分时先算抓鱼；准星态提示当前 F 目标（多人钓鱼附篇.md:47） | ❌ Missing | Source/Catfishing/AbilitySystem/Tags/CatFishingAbilityTags.cpp:5,8；Source/Catfishing/Interaction/CatInteractionTargetingComponent.cpp:66-85 | 抄网与占竿位是两个独立输入意图（`Input_Fishing_Scoop` / `Input_Fishing_RodInteract`），各自绑定不同按键，服务端也没有「同一键的目标优先级」裁决。复核补查了万能交互链：`UCatInteractionTargetingComponent` 确有准星射线取焦（`FocusStencilValue`／`BeginLocalFocus`），但 `ACatFishingRodActor` 与水中 `ACatFishEncounterActor` 都不实现 `ICatInteractable`（实现者只有鱼护/鱼缸/营地箱/商店/落地 `ACatFishPickupActor`），因此设计要的「鱼 vs 入位」两个 F 目标根本不在这套裁决里 |
| 42 | `总力量 = 主钓手当前力量 × 100% + Σ 辅助手当前力量 × 75%`，辅助力量不设上限；合力变化触发强度检查，提交 F_total 与各参与者 K_pos（多人钓鱼附篇.md:53） | ⚠️ Partial | Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.cpp:44-68；Source/Catfishing/Fishing/CatFishingSession.cpp:2371-2408 | 求和公式与「不设上限」成立，且每个固定步按当前操作位重建参与集合并 `PublishGroupSummaryFromAuthority` 发布 F_total ✅；缺的是提交各参与者的 K_pos（见 #36），且辅助系数是 0.5（见 #35） |
| 43 | 位置落水只判持竿者；辅助手归零弹出辅助位、不落水（多人钓鱼附篇.md:57） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:1635-1656 | `HandleCatEnteredDangerousWaterFromAuthority` 对**任一占用操作位的角色**都成立（判据是 `GetOperatorSlotIndex(...) != INDEX_NONE`），辅助手入水同样触发落水表现并被释放竿位；设计要求只判持竿者。09-08 裁 Splash 落水两支（体力≤0／位置）也只指向持竿者 |
| 44 | 主钓手放线时全员零消耗，辅助手不必退出（多人钓鱼附篇.md:57） | ✅ Implemented | Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:382-384,421 | `bFreeEffort = Step.bSlackRecoveryActive \|\| State.bFishExhausted` 时共享账单与每人位移消耗一并置零，覆盖全体成员；成员身份不受影响 |
| 45 | 辅助手没有放线权；只有主钓手能触发主动放弃（多人钓鱼附篇.md:57） | ✅ Implemented | Source/Catfishing/Fishing/Integration/CatFishingCommandComponent.cpp:620-640；Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:480-488,529 | 非主位提交线杯输入直接被拒（`NotFisher` / 诊断标 `AuxiliaryMovementOnly`），`RefreshCatAction` 只读主位的 `bSlackHeld/bPullHeld`；`ApplyFightInputFromAuthority` 也要求 `Participant->bPrimary` |
| 46 | 主钓手任意时刻可按 E 发起换人请求、再按 E 取消；请求无时限挂起、本竿结束自然失效、没有超时出口（多人钓鱼附篇.md:63） | ❌ Missing | — | 代码里没有「换人请求」这个对象：`ECatFishingCommandType` 无对应命令，`ACatFishingRodActor` 的复制状态里也没有挂起请求字段。换人在代码里是无握手的（见 #48） |
| 47 | 体力不高于 5% 时 UI 给濒死强提示（多人钓鱼附篇.md:63） | ❌ Missing | Source/Catfishing/UI/HUD/CatHUDWidget.h:50-69 | HUD 数据模型只有体力值/上限/归一化比例，没有濒死阈值标志或告警字段；C++ 侧无 5% 判定 |
| 48 | 岸上替补自愿按 E 接手，先按先得、同刻并列按席位序；体力恢复到 50% 以上的替补才能接手（多人钓鱼附篇.md:65） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingService.cpp:707-745,1126-1158 | 代码是两条无握手路径：①主位离开时按加入顺序自动晋升下一位（`RemoveOperatorAndReconcileSession`）；②他人主动占 0 号位触发 `bNeedsSessionTakeover` 抢主。先按先得/席位序在「加入顺序」意义上部分成立，但 50% 体力门槛完全没有——注释写明「零体力只影响出力而不影响操竿资格」 |
| 49 | 接手瞬间完成：鱼与竿的状态完全继承，猫体力各是各的，接手者用自己的（多人钓鱼附篇.md:65） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingService.cpp:1437；Source/Catfishing/Fishing/CatFishingSession.cpp:398-493 | `TransferSessionFisher` → `bFightTakeover` 走 `FightRunnerRebind`，同步迁移 ASC/力量/体力/输入域，鱼与竿状态不重置；Runner 每步从各人自己的 ASC 读体力（FightRunner.cpp:290-297） |
| 50 | 图鉴归属不变，原始上钩者保留（多人钓鱼附篇.md:65；09-08 晚裁：收集层归上钩者） | 🔄 Divergent | Source/Catfishing/Items/World/CatFishPickupActor.cpp:695-705 | `ArchiveCommittedCapture` 把 `FishRecorded` Grant 发给 `PickerStableNetId`（拾取/叼走的人），不是上钩者；换人或抢拾之后图鉴会记到接手者/拾取者名下。设计侧口径 08-27→09-08 变过两次，现行是「归上钩者」 |
| 51 | 多人搏斗到收鱼仍是两条路：可抄鱼贴岸抢抄，或翻肚落岸后抢拾；资格与并列判序见主文（多人钓鱼附篇.md:73） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:502-519；Source/Catfishing/Items/World/CatFishPickupActor.cpp:660-692 | `RequestScoop` 对任何满足参战能力谓词的 Controller 开放（不限竿主）；`ACatFishPickupActor` 的拾取同样对任一未倒地角色开放，服务端按到达顺序裁决并缓存终态 |
| 52 | 操作贡献者＝上钩钓手＋合力中按过左键至少一次的猫＋抄中或拾取的猫；打窝与围观不算（多人钓鱼附篇.md:81） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:1711-1719,1820-1830,2371-2406 | 两处不同：①判据是「当前占着竿位」而不是「按过左键至少一次」，且成员离开时会被 `UnregisterFightStaminaParticipantFromAuthority` 移出集合，中途帮过忙的猫会掉名单；②非巨物的鱼只把钓手（＋抄/拾者）计入，`FightParticipantIds` 仅在 `Snapshot.bGiant` 时带入。打窝/围观确实不计入 ✅ |
| 53 | 贡献只用于演出（合影名单、头衔类），不进图鉴、不刷新个人最佳重量（多人钓鱼附篇.md:81） | ⚠️ Partial | Source/Catfishing/Items/World/CatFishPickupActor.cpp:710-729 | 参与者名单确实只喂给印记成像候选 `CreateCapturePlansForParticipants`（演出向）✅；但图鉴 Grant 走的是拾取者而非上钩者（见 #50），所以「不进图鉴」这条在结果上仍被破坏 |
| 54 | 举鱼展示、围观互动、巨物合力成功的慢镜头合影（多人钓鱼附篇.md:83） | ❓ Unverifiable | Source/Catfishing/Collection/CatRunImprintService.h:44-49 | C++ 只有成像候选/投递的协调骨架；慢镜头、合影构图、举鱼动作都是蓝图与动画资产（④），文本搜索证明不了 |
| 55 | 求助由玩家手动发出：急促猫叫加头顶图标（多人钓鱼附篇.md:87） | ✅ Implemented | Source/Catfishing/Social/CatSocialService.cpp:364-405；Source/Catfishing/AbilitySystem/BodyAction/Social/CatSocialBodyActionAbilities.cpp:53-149；Config/DefaultGame.ini:303-304 | `ECatHelpSignalKind::ManualFishing` 手动求助链完整（Ability → Social 服务 → GameState 信号），`ManualHelpRadiusCentimeters=500`、`ManualHelpCooldownSeconds=6`；猫叫与图标的具体资源在表现层 |
| 56 | 只有巨物保留全体提示（多人钓鱼附篇.md:87） | ✅ Implemented | Source/Catfishing/Social/CatSocialService.cpp:435-443 | `GiantFishSystem` 信号 `bGlobal = true` 由系统在巨物 Session 开始时发布；手动求助只按半径传播（见 #55），两者区分正确 |
| 57 | 主辅位的反馈与角色区分不做力量百分比条（多人钓鱼附篇.md:87） | ⚠️ Partial | Source/Catfishing/UI/HUD/CatHUDWidget.h:46-69；Source/Catfishing/UI/CatFishingViewTypes.h:40-46 | 数据模型确实没有「力量百分比」字段，但把 `FishingStrength`、`CombinedFishingStrength`、`ActiveCombinedFishingStrength`、`ActiveHelperCount`、`NormalizedTotalFightStamina` 都暴露给了 HUD，做不做成条取决于蓝图 Widget（④）。主辅角色区分复核确认无：`CatHUDModel.cpp` 全文 grep `IsPrimaryOperator/Primary/Helper` 零命中，`ActiveHelperCount` 只是人数不是本人角色标记，Widget 无法从数据层知道自己是主是辅 |
| 58 | WASD 控制移动；`F_net = Σ(猫力量 × 方向单位向量)`、`S = Σ猫力量`（按键者）、`ρ = \|F_net\| / S`；任意方向输入直接进合力求和（多人钓鱼附篇.md:93） | 🔄 Divergent | Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.cpp:51-53,63-66,73-76 | 代码算的是按力量加权的意图向量和 `MovementStrengthWorld` 与加权期望速度 `DesiredVelocityCentimetersPerSecond = Σ(intent×speed×strength)/ΣStrength`，斜向输入直接求和 ✅；但没有 `S`、没有 `ρ` 这个标量，后续三档判定因此无输入。D-02 只登记了 WASD 键位 |
| 59 | 无人按键（S = 0）不进 ρ 计算，直接按静止处理，不存在除零（多人钓鱼附篇.md:93） | ✅ Implemented | Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.cpp:73-77 | `if (Result.TotalActiveStrength > 0.0)` 才做除法，否则期望速度保持零向量并过有限性校验 |
| 60 | ρ 三档：齐步走 ρ≥高阈（0.8）正常移动、拖行居中走龟速、僵局 ρ≤低阈（0.2）（多人钓鱼附篇.md:95-99） | ❌ Missing | — | 全库无阈值常量、无档位枚举；`Config/DefaultGame.ini` 亦无对应段。速度是连续的加权平均，不存在「龟速档」。参数页 09-08 待办里 ρ 阈值 0.8/0.2 尚未挂号 |
| 61 | 僵局细分：两组共线反向就对顶——恰 2 人顶牛、4 人 2v2 拔河，其余组合自然推拉、无特定演出；方向散乱就打转（多人钓鱼附篇.md:99） | ❌ Missing | — | 没有对顶/打转状态机，也没有按人数分支的判定；对向输入在代码里只是向量抵消 |
| 62 | 打转持续超过 5 秒：恰 3 人随机甩飞一只，4 人连环摔倒，两者互斥（多人钓鱼附篇.md:101；09-08 裁：连环摔倒恰 4 人、不产生搏斗结局） | ❌ Missing | — | 无打转状态即无计时，无甩飞/连环摔倒的任何触发或效果 |
| 63 | 移动合力是全额力量相加，钓鱼合力才有辅助位 75% 折扣，两处口径独立（多人钓鱼附篇.md:101） | 🔄 Divergent | Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.cpp:49-53 | 代码只有一个 `ActiveStrength`（已含主/辅倍率），移动意图与拉竿支撑共用它：`MovementStrengthWorld = Intent × ActiveStrength`。辅助位的折扣因此也落到了移动合力上，两处口径没有分开 |
| 64 | 移动消耗表：正常移动 1、龟速被拖者 8／顺行者 2、原地打转 5、顶牛拔河 6（点/秒/人）（多人钓鱼附篇.md:110-114） | 🔄 Divergent | Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.cpp:116-157；Source/Catfishing/Fishing/Simulation/CatFishingFightWorkModel.cpp:3-15 | 代码是位移驱动做功模型：`WorkStaminaDrain = f(实际位移 × 标准力量 × CostPerStrengthCentimeter, 负载)` ＋ 受阻附加 `SupportStaminaPerSecond × Δt × BlockedEffortRatio²`，没有按档位取常数的表。位移驱动本身与 09-07 裁决同向，但五档常数无对应实现 |
| 65 | 无人移动（静止）恢复 5 点/秒/人（多人钓鱼附篇.md:115） | ❌ Missing | Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:384 | `RecoveryPerParticipant` 只在主钓手放线（`bSlackRecoveryActive`）时非零；静止且不放线时恢复为 0 |
| 66 | 移动中体力归零的猫走不动：不能主动移动、没有拖/背交互、原地恢复并随队被拖行、不阻塞编组（多人钓鱼附篇.md:107,123） | ⚠️ Partial | Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.cpp:49-50,177-180 | 归零者 `ActiveStrength=0`：不贡献力量、不被扣费、不阻塞其他成员结算 ✅（「不阻塞编组」成立）；但没有「禁止主动移动」的状态、没有原地恢复（见 #65），也没有随队被拖的位移施加；确实也没有拖/背交互 ✅ |
| 67 | 恢复速率由主文基础自然恢复提供：搏斗外一律 5 点/秒，静止、岸上替补、摸鱼与归零随队被拖都是这一个数、没有场景乘数（多人钓鱼附篇.md:123） | 🔄 Divergent | Source/Catfishing/AbilitySystem/Core/CatAbilitySystemComponent.cpp:309-330；Source/Catfishing/Fishing/CatFishingSession.cpp:120-131 | 代码没有搏斗外的逐秒恢复：`InitializeFishingStaminaForSession` 在会话开始/结束时把 FightStamina 一次性拉满，等于「瞬间回满」而不是 5 点/秒。设计 09-08 晚①刚把这条统一成 5 点/秒无乘数 |
| 68 | 不按键者不进计票：不贡献力量向量、不拖慢队伍，随队被拖、自己照常恢复（多人钓鱼附篇.md:125） | ✅ Implemented | Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.cpp:51-53,66；Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.cpp:129-132 | 意图为零 → `MovementStrengthWorld` 为零、不进 `WeightedVelocity`、位移消耗直接返回零；被动位移明确不计为做功（FightRunner.cpp:322-329 注释「Passive dragging without input ... cannot become active work」）。「自己照常恢复」这半见 #65/#67 |
| 69 | 编组就是同房间全员一队、没有子编组，加入退出即入局离局（多人钓鱼附篇.md:125） | 🔄 Divergent | Source/Catfishing/Fishing/Actors/CatFishingRodActor.h:15-28,236-237；Source/Catfishing/Fishing/Actors/CatFishingRodActor.cpp:222-265 | 代码的「组」＝同一根竿的操作位集合，`FCatFishingGroupMotionState` 只在 `bFightActive` 期间生效；不在竿上的房间成员完全不进组移动。同房间全员一队没有实现 |
| 70 | UI：屏幕上方按 W/A/S/D 图标显示每个队友当前方向；移动状态文字「齐步走／被拖行／原地打转／顶牛中」；每个角色头顶体力条、力竭变灰（多人钓鱼附篇.md:129） | ❌ Missing | Source/Catfishing/UI/HUD/CatHUDWidget.h:36-69；Source/Catfishing/UI/CatFishingViewTypes.h:34-46 | HUD 数据模型只有本人属性＋同竿体力合计，没有逐队友的方向、状态或体力条数据；三项所需字段在 C++ 侧都不存在，Widget 再怎么画也拿不到数据 |
| 71 | 六个群体动作演出：顶牛、被拖着走、原地打转、连环摔倒、被甩飞、拔河拉长（多人钓鱼附篇.md:132-138） | ❌ Missing | — | 依赖 #60-#62 的档位判定；`CatFishingAbilityTags` 的 Cosmetic 标签只有 ScoopSwing/HookPull/LineBroken/LineCut/CatInWater 五个，没有任何群体动作事件标签 |
| 72 | 多人不绕过当前可用协作能力的鱼候选过滤；加入、退出、换人与在局人数变化是能力变化的来源（多人钓鱼附篇.md:27,49） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingService.cpp:1618-1643；Source/Catfishing/Fishing/CatFishingSession.cpp:993-999；Source/Catfishing/Data/CatFishCatalogSettings.cpp:219；Source/Catfishing/Data/CatFishSelectionTypes.cpp:19-23 | 复核补行（原稿漏了附篇 §1 末句与 §2.1 末段这条）。选鱼前 `BuildFightCapabilitySnapshot` 实时遍历全部 PlayerController 按参战能力谓词统计人数/合计力量/合计体力，冻进 `FrozenSelectionContext`；候选管线用 `FCatFishEligibilityPolicy::PassesActivePlayerCount`（`ActivePlayerCount >= Definition.MinimumFightParticipants`）过滤，人数是实时事实、不走单人旁路 |
| 73 | 稀有度本身不进抽鱼概率，鱼种分布与出现条件由鱼册提供（钓鱼规则.md:70） | ✅ Implemented | Source/Catfishing/Data/CatFishDefinition.h:84-86,100；Source/Catfishing/Data/CatFishCatalogSettings.cpp:305-311 | 复核补行。`RarityTierId` 只作稳定内容 ID 参与 `IsRuntimeDefinitionReady` 校验，全库 grep `Rarity` 在选鱼链上零命中；最终权重只由 `SpawnWeight × ChumModifier × BaitModifier × ChallengeModifier` 构成，稀有度经由内容侧的 SpawnWeight 表达而不在代码里硬编码档位概率 |
| 74 | 连携激励只做演出（慢镜头、合影、头衔类），没有任何数值加成（多人钓鱼附篇.md:83） | ✅ Implemented | Source/Catfishing/Items/World/CatFishPickupActor.cpp:710-729；Source/Catfishing/Collection/CatRunImprintService.cpp:166-186；Source/Catfishing/Framework/Core/CatProfileContracts.h:8-19 | 复核补行。参与者名单唯一的下游是 `CreateCapturePlansForParticipants`（印记成像计划），授予物只有 `ECatProfileGrantKind` 四种（FishRecorded/FishSilhouette/Imprint/Unlock），没有按参与人数发的力量/体力/经济修正。唯一受人数影响的是候选过滤（见 #72），那是过滤不是奖励 |

### Summary
- Total features: 74
- ✅ Implemented: 17
- ⚠️ Partial: 8
- ❌ Missing: 25
- 🔄 Divergent: 22
- ❓ Unverifiable: 2
- Inspectable: (74 - 2) / 74 * 100% = 97.3%
- Coverage of inspected: (17 + 0.5 * 8) / (74 - 2) * 100% = 29.2%

### Scan Scope

**工程根**：`D:/LocalGameProject/Unreal/Catfishing`（engine=unreal 5.8，tech_stack=C++/Blueprint，模块 Catfishing / CatfishingEditor）。代码以 2026-09-09 10:40 拉新后的工作区文件为准。

**扫过的目录与文件类型**

- `Source/Catfishing/Environment/**`（.h/.cpp）：ChumField 类型/子系统/设置/放置服务/锚点/表现，以及 WaterTypes 里的 `FCatChumVector`。
- `Source/Catfishing/Fishing/**`（.h/.cpp）：`CatFishingSession`、`CatFishingService`、`CatFishingSettings`、`Simulation/`（GroupModel、FightRunner、BiteTimingModel、FightSimulator、WorkModel）、`Integration/`（CommandComponent）、`Actors/`（RodActor、FishEncounterActor）、`Config/`（FightBalanceDefinition）。
- `Source/Catfishing/Social/`、`Source/Catfishing/AbilitySystem/`（Tags、BodyAction/Social、Core/CatAbilitySystemComponent、Fishing/InputAbilities）。
- `Source/Catfishing/Items/World/CatFishPickupActor.*`、`Source/Catfishing/Collection/`、`Source/Catfishing/Data/CatFishCatalogSettings.cpp`、`Source/Catfishing/Equipment/CatEquipmentTypes.h`、`Source/Catfishing/UI/HUD/`、`Source/Catfishing/UI/CatFishingViewTypes.h`、`Source/Catfishing/Online/`、`Source/Catfishing/Framework/Game/`、`Source/Catfishing/Character/`、`Source/Catfishing/Input/`。
- `Config/DefaultGame.ini` 全段（重点 `CatFishingSettings`、`CatChumFieldSettings`、`CatSocialSettings`、`CatOnlineSettings`）。
- `Scripts/`：`create_fishing_fight_balance_asset.py`（读取资产落值）、`configure_run_environment_social_map.py`、`verify_showcase_formal_fish_region.py`。
- 代码侧台账：`Docs/Development/工程自补决策记录.md`（D-01…D-32 通读）、`Docs/Development/需求对齐差距清单.md`（作为线索，未作为 ✅ 依据）。
- 设计侧：`Knowledge/Feishu/GDD 系统分册/钓鱼系统/钓鱼规则.md` §2 全文、`多人钓鱼附篇.md` 全文、`裁决同步/设计修改记录.md`（09-07／09-08 段）。

**检查方法**

Grep 定位 + Read 片段，不通读大文件。检索键：类名（`UCatChumFieldSubsystem`、`FCatFishingGroupModel`、`ACatFishingRodActor`、`UCatChumPlacementService`、`FCatFishingBiteTimingModel`）、字段名（`HelperStrengthMultiplier`、`EffectiveChumVector`、`FightParticipantIds`、`SlackStaminaRegenPerSecond`、`MaximumRodOperatorSlots`）、日志事件名（`fishing_bite_scheduled`、`chum_throw`、`chum_inventory_consumed`、`fishing_rod_operator_joined/left`、`fishing_group_stamina_settled`、`social_giant_prompt`）、Gameplay Tag（`Cat.Input.Fishing.*`、`Cat.State.Fishing.RodOperating`、`Cat.Ability.Event.Body.RequestManualHelp`）、中文注释（打窝、蓄力、聚鱼、渔网、死水、换人、合力、贡献、图鉴）、ini 段名。

**跳过的东西 / 查不了的东西**

- `Content/` 下约 1838 个 `.uasset`/`.umap` 未做文本扫描（资产，属可检查程度 ③④）。因此以下三类事实本轮**查不了**、不作为「未实现」依据：
  - 数值资产的实际落值：`DA_FishingFightBalance_Default`（HelperStrengthMultiplier、SlackStaminaRegenPerSecond 等）、窝料 `ChumInfluence`（半径、时长、两条衰减曲线）、鱼种 `ChumPreference`/`BaitMultiplier`。本报告只在有生成脚本（`Scripts/create_fishing_fight_balance_asset.py`）可读时才引用落值。
  - InputAction / IMC 资产里的**实际按键绑定**（F、R、E、Q 到底绑到哪个 Tag）。#38、#41 的判定依据是 C++ 侧的意图 Tag 结构、服务端半径常量与代码注释，不是按键资产本身。
  - 蓝图 Widget、Montage、Niagara、GameplayAbility 资产的图逻辑与表现（#10、#54 判 ❓ 即出于此）。
- 引擎入口验证脚本（`Scripts/verify_*`）本轮未运行，无 PIE / 自动化测试执行结果。
- `Source/Catfishing/**/Tests/` 只用于交叉印证接口形状（如 `CatFishingGroupMovementTests.cpp`、`CatFishingGroupModelTests.cpp` 的存在说明 GroupModel 已接运行链），未把测试内的行为当作正式链路证据。
- `Knowledge/Feishu/` 全目录只读，未做任何修改；「鱼/鱼的行为.md」按页头声明不作为评分对象。
- 评审范围外的设计条目（钓鱼规则 §1、§3～§7 与附录）未逐条列入，仅在多人附篇引用它们时作为上下文读取。

### 复核记录

复核人独立按行重搜 Source/ Config/ Scripts/，抽验 34 行（❌/🔄/⚠️ 全部 32 行 + ❓ 2 行），另抽 8 行 ✅ 读 file:line 核语义。原判 71 行中 68 行维持、3 行改 Notes/Code Reference（判级不变），新增 3 行漏项。

| # | 原判 | 改判 | 依据 file:line 或搜过的关键词 |
|---|------|------|------|
| 2 | 🔄 Divergent | 🔄 Divergent（Notes 补台账冲突） | Config/DefaultGame.ini:267 `InfluenceAreaMultiplier=8.0` 对 D-31 登记的 2.0；工程自补决策记录.md:48 |
| 10 | ❓ Unverifiable | ❓ 维持（Code Reference 改指蓝图钩子） | CatChumFieldPresentationActor.cpp:31-43 占位网格、46-60 `ApplyPublicState/BP_OnFieldAdded`；搜 `涟漪 Ripple FishShadow 鱼影 Niagara` 无 C++ 承载 |
| 17 | 🔄 Divergent | 🔄 维持（订正 ini 键名笔误） | Config/DefaultGame.ini:234 键名是 `MaximumBiteDelaySeconds` 而非 `MaximumWaitSeconds`（后者是 `FCatFishingBiteTimingParameters` 字段名） |
| 19 | 🔄 Divergent | 🔄 维持（订正「无两段抽样」的说法） | CatFishCatalogSettings.cpp:246-281 确有第一段抽样，抽的是挑战档不是 X/P/J 类别；:282-335 第二段抽鱼种 |
| 41 | ❌ Missing | ❌ 维持（补万能交互链证据） | CatInteractionTargetingComponent.cpp:66-85 准星取焦存在；grep `ICatInteractable` 实现者只有 CampInventory/FishGuard/FishTank/ShopKiosk/FishPickup，Rod 与 FishEncounter 都不在内 |
| 57 | ⚠️ Partial | ⚠️ 维持（补证） | CatHUDModel.cpp 全文 grep `IsPrimaryOperator|Primary|Helper` 零命中 |
| 64 | 🔄 Divergent | 🔄 维持（补行号） | CatFishingFightWorkModel.cpp:3-15 `ComputeCatWorkDrain` |
| 72 | （原稿无此行） | ✅ Implemented（新增） | CatFishingService.cpp:1618-1643；CatFishingSession.cpp:993-999；CatFishSelectionTypes.cpp:19-23 |
| 73 | （原稿无此行） | ✅ Implemented（新增） | CatFishDefinition.h:84-86,100；CatFishCatalogSettings.cpp:305-311；grep `Rarity 稀有` 在选鱼链零命中 |
| 74 | （原稿无此行） | ✅ Implemented（新增） | CatFishPickupActor.cpp:710-729；CatRunImprintService.cpp:166-186；CatProfileContracts.h:8-19 |

**维持原判的抽验明细（换关键词重搜后代码里确实没有／确实如此）**

- #1 #2 #3 #4 #5 #11 #25：读 `CatChumFieldTypes.h:45-96,199-210`（`FCatChumInfluenceSpec` 无目标鱼种字段、无整数存量）、`CatChumFieldSubsystem.cpp:189-275`（Prepare/Activate 一律新建 field，无并窝分支）、`:337-384`（采样端按 GUID 排序累加全部覆盖 field，无归属唯一判定）、`:388-400`（到期整体下线）。另搜 `Deduct Decrement ConsumeChum 存量 扣存量` 在 Environment/ 零命中 → #22 #24 #31 一并维持。
- #7 #8 #9 #12：`CatFishingCommandComponent.cpp:780-796`（`ChumPressed` 只记时间戳）、`:1180-1192`（`ChargeAlphaFromHeldSeconds` + `PredictChumThrow`，非水面即 `InvalidWaterTarget`）、`CatChumPlacementService.cpp:133`（只拒 `bDowned`）、`:162-166`（落岸拒绝、不生成掉落物）、`:174`（`MaxPlacementRangeCentimeters` + `MaxAimDeviationDegrees`）。搜 `搏斗 Fight gate` 在打窝链上无闸门。
- #13 #14 #16 #17 #18：`CatFishingBiteTimingModel.cpp:36-74` 全文读完（三锚点分段线性 + 二分求截顶指数速率），搜 `T_base K= 1 + Total` 全库零命中；`CatFishingSession.cpp:837-880` 一次采样一次排定时器，`:908-914` 到点只发 `ProbeTriggered`，`:944-1007` 鱼种在 `TrueBiteWindow` 才 `ResolveHookSelectionFromAuthority`。
- #26 #27 #28：搜 `Aggregation 聚鱼 School Frenzy 门槛 触发概率 窗口` —— 只命中 `CatfishingGameModeBase.cpp:1646-1708` 的自然事件写口与 `CatEnvironmentSettings.h:30,59,63`，无门槛/roll/窗口/咬钩倍率任何字段。#28 补充口径：代码那条第二路径写入的是**窝料 field**（等价于加一份窝料），不是设计里的「聚鱼窗口」；因为窗口本身不存在（#26），这条判 🔄 表达的是「环境事件仍有向 ChumField 的写口，与设计『环境事件触发归完整版』相反」。
- #29 #30：搜 `渔网 CastNet ThrowNet FishingNet 死水 DeadWater Stagnant` —— 全库只在 `Docs/Architecture/商店库存与营地公共仓库子技术方案.md:170` 出现；`ECatEquipmentKind`（CatEquipmentTypes.h:11-31）确无渔网项。
- #35 #37 #44 #65 #67：`CatFishingFightRunner.cpp:349-350`（主 ×1.0／辅 ×`HelperStrengthMultiplier`）、`:384`（`RecoveryPerParticipant` 只在 `bSlackRecoveryActive` 非零）、`CatFishingFightBalanceDefinition.h:49` 默认 0.5、`Scripts/create_fishing_fight_balance_asset.py:28` `helper_strength_multiplier: 0.5`、`:43` `slack_stamina_regen_per_second: 3.0`。另 grep `FightStamina` 排除 Fishing/ 与 ASC 后**零命中**，证实全库不存在搏斗外逐秒恢复 → #37 #65 #67 维持。
- #36 #58 #59 #60 #61 #62 #63 #66 #68：`CatFishingGroupModel.cpp` 全文读完（1-210）。`SettleStamina:157-198` 是等额注水均摊、与位置无关（无 K_pos）；`ComputeForces:44-77` 只产出 `MovementStrengthWorld` 与加权期望速度，无标量 S、无 ρ；搜 `0.8 0.2 Rho Alignment Stalemate Deadlock 顶牛 打转 拔河 甩飞` 全库零命中。
- #38 #39 #43 #46 #48：`CatFishingService.cpp:686-692`（`FMath::Square(250.0)`）、`CatFishingCommandComponent.cpp:735-768`（注释「R 的鱼竿三态」、`FindNearestOperableRod(..., 250.0)`）、`CatFishingService.cpp:1126`（「零体力只影响出力而不影响操竿资格」）、`CatFishingService.cpp:1105-1158`（按加入顺序晋升，无 50% 门槛）、`CatFishingSession.cpp:1635-1656`（判据 `GetOperatorSlotIndex != INDEX_NONE`，辅助手同样触发）。`ECatFishingCommandType`（CatFishingCommandTypes.h:9-19）确无换人请求命令。另搜「走出半径自动退出」：离位路径只有 `LeaveRod`、`ReleaseFishingOperatorForCharacter`（落水/倒地），无距离轮询 → #38 后半维持。
- #47 #70：`CatHUDWidget.h:36-69` 与 `CatFishingViewTypes.h:25-60` 逐字段读完；搜 `濒死 LowStamina Critical NearDeath` 在 UI/ 零命中，无逐队友方向/状态/体力条字段。
- #50 #52 #53：`CatFishPickupActor.cpp:544-552`（`ArchiveCommittedCapture(Result.Committed, StableNetId)`，`StableNetId` 来自入护的持有者）、`:695-729`；`CatRunImprintService.cpp:30-56`（`FishRecorded` Grant 发给传入的 Recipient）；`CatFishingSession.cpp:2371-2408`（每步按当前操作位重建 `FightParticipantIds`）、`:1711-1719`／`:1820-1830`（非巨物只带钓手＋抄/拾者）。
- #71：`CatFishingAbilityTags.cpp` 全文读完，Cosmetic 标签确为 5 个。

**✅ 行抽验（读 file:line 核语义，全部相符）**

- #6 `CatChumFieldSubsystem.cpp:185-192` 预算夹返回 `FieldCapacityExceeded`；Config:268-269 行号正确。
- #23 `CatFishingSession.cpp:1089` 在选鱼成功后才 `CommitFishingBaitDeferred`；`CatEquipmentComponent.cpp:1506` 函数体注释与语义相符。
- #32 `CatfishingGameModeBase.h:24` `MaxCampSpawnPlayers = 4`；`CatOnlineSubsystem.cpp:874` 写入 `NumPublicConnections`、`:2296` 校验同一常量。
- #45 `CatFishingCommandComponent.cpp:620-644` 非主位提交线杯边沿被拒并打 `AuxiliaryMovementOnly`；`CatFishingFightRunner.cpp:480-488` `RefreshCatAction` 只读 `FindPrimaryParticipant()`。
- #49 `CatFishingService.cpp:1437-1446` `TransferSessionFisher`；`CatFishingSession.cpp:398-420` 可迁移阶段含 `HookedFight/ExhaustedReel`，鱼竿状态不重置。
- #51 `CatFishingSession.cpp:502-519` `RequestScoop` 只查参战能力谓词，不查竿主身份。
- #55 `CatSocialService.cpp:364-405` 手动求助全链（重放缓存 → 冷却 → 半径信号）；Config:303-304 行号正确。
- #56 `CatSocialService.cpp:435-443` `GiantFishSystem` 信号 `bGlobal = true`，与手动求助的按半径传播确实分开。
- #59 `CatFishingGroupModel.cpp:73-77` `if (Result.TotalActiveStrength > 0.0)` 才做除法。
- #68 `CatFishingGroupModel.cpp:51-53,66,129-132` 与 `CatFishingFightRunner.cpp:322-329` 被动位移不计做功。

**复核未能改判的边界（仍属 ③④）**

- `DA_FishingFightBalance_Default` 的运行时落值（#35 的 0.75 vs 0.5、#37 的 5 vs 3）只能读生成脚本，资产本体本轮不开引擎。
- InputAction/IMC 的实际按键绑定（#38 的 F vs R、#41、#46 的 E）本轮仍不可读；#38 的判据是 C++ 注释与半径常量，不是按键资产。
- #8 的「够不到」玩家提示、#10 的涟漪鱼影、#54 的合影慢镜头、#57 的力量条画法都在蓝图 Widget/Montage/Niagara 里，维持 ⚠️/❓ 不改判为「未实现」。
