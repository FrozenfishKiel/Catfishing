## 钓鱼系统-打窝聚鱼与多人

> 本轮增量重跑：代码 HEAD bcc7df9（基线 87ba975，其间 83 个提交），文档导出 2026-09-11T17:57（钓鱼规则 rev 333／v1.8、多人钓鱼附篇 rev 82／v1.3，两页正文与基线同）。要求行沿用基线措辞与顺序，状态与 code_ref 全部重核——多人一侧的实现基座已整体改写（合力操作位与 GroupModel 删除，改为单主控 ＋ 持续物理抓握），基线的 code_ref 大半已失效。

### Features

| # | Design Requirement | Status | Code Reference | Notes |
|---|---|---|---|---|
| 1 | 窝点维护 `{X腥, P香, J酵}` 三个存量，每格是窝点里对应偏好鱼的条数；打窝＝逐分量加算，存量恒为整条鱼（钓鱼规则.md:35） | 🔄 Divergent | Source/Catfishing/Environment/CatWaterTypes.h:142-164；Source/Catfishing/Environment/CatChumFieldTypes.h:41-92 | 代码仍是 `FCatChumVector{Fishy, Fragrant, Fermented}` 连续 double「浓度贡献」＋两条衰减曲线，无条数语义、无整数约束。D-30（08-28）已标「挂起」；09-09 李前臻裁「窝点模型整套挂起，两套都不动，等窝料与聚鱼内容验收」（设计修改记录 09-09 第⑤问；需求对齐差距清单 §4 同挂）——本行按现行设计页评分，处置待内容验收 |
| 2 | 窝点是以首次窝料落点为圆心、半径 5 米的圆，初始存量＝首次投料三属性增量（钓鱼规则.md:37） | 🔄 Divergent | Source/Catfishing/Environment/CatChumFieldSubsystem.cpp:193-212；Config/DefaultGame.ini:255-256；Source/Catfishing/Environment/CatChumFieldSettings.cpp:6-10 | 每次投放各自新建 field，半径＝窝料资产 `ChumInfluence.RadiusCentimeters` × √`InfluenceAreaMultiplier`，ini 现值 8.0（注释算出直径约 16.97 m），不存在「首次落点定圆」这一身份。D-31 登记的暂定值仍是 2.0，取值漂移未回写（台账引的行号 267 已随本轮 ini 改动移到 256） |
| 3 | 服务器维护共享存量，同一窝点的所有浮漂读同一份数据（钓鱼规则.md:37） | ⚠️ Partial | Source/Catfishing/Environment/CatChumFieldSubsystem.cpp:335-384 | 服务器权威、所有浮漂读同一个 field 集合 ✅；但读法仍是「按采样点把覆盖该点的 field 按距离/时间衰减加权求和」，不是一份可增可减的共享存量，没有「同一窝点」这个共享账户 |
| 4 | 新投料落在已有窝点圆内→向该窝点加算存量并沿用原圆心半径；否则新建窝点（钓鱼规则.md:39） | ❌ Missing | — | `PrepareField`（CatChumFieldSubsystem.cpp:97-229）与 `ActivatePreparedFieldDeferred`（:230-275）一律新建 field，无并窝分支；重叠只在采样时相加 |
| 5 | 几何归属唯一规则：近者优先（比圆心距离）、等距早者优先（创建序号小者）、边界算内（距离 ≤ 半径）（钓鱼规则.md:39） | ❌ Missing | — | 采样端按 GUID 排序累加全部覆盖 field（CatChumFieldSubsystem.cpp:358-380），没有「选一个窝点」的归属判定，三条优先级都无对应 |
| 6 | 叠加没有设计上限，代码只留正常游玩碰不到的工程安全夹（钓鱼规则.md:41） | ✅ Implemented | Config/DefaultGame.ini:257-258；Source/Catfishing/Environment/CatChumFieldSubsystem.cpp:184-191 | `MaxActiveFieldsPerRegion=32`、`MaxRawContributionPerRegion=100000.0` 是按水域记的工程夹，超限返回 `FieldCapacityExceeded`；无产品级容量上限 |
| 7 | 投窝料和抛竿是同一套操作：点击定点、无蓄力、无散布，点哪落哪（钓鱼规则.md:43） | 🔄 Divergent | Source/Catfishing/Fishing/Integration/CatFishingCommandComponent.cpp:978-994,1406-1413 | 仍是 Q 蓄力弹道：按下只记服务器时间戳，松开按 `ChargeAlphaFromHeldSeconds(Held)` 换蓄力比再 `PredictChumThrow` 算落点。设计 09-08 已裁「窝料投掷与抛竿同构：点击定点、无蓄力、无散布」；09-09「操作模式方向」又把抛投入口整体挂到装备栏重构（使用键键位待定），两条都指向这段要改。无 D 条目登记 |
| 8 | 射程限制，超程提示够不到（钓鱼规则.md:43） | ⚠️ Partial | Config/DefaultGame.ini:259-260；Source/Catfishing/Environment/CatChumPlacementService.cpp:159-177 | 服务端射程 `MaxPlacementRangeCentimeters=2000`（20 m）＋视角偏离 60° 限制，超程返回 `PlacementOutOfRange` ✅；本轮新增视线遮挡拒绝 `PlacementOccluded`（:170-177，见代码超前项）。「够不到」的玩家提示仍是蓝图 Widget（④），本轮查不了 |
| 9 | 投到岸上的窝料变成掉落物，可以捡回（钓鱼规则.md:43） | ❌ Missing | Source/Catfishing/Fishing/Integration/CatFishingCommandComponent.cpp:1414-1418；Source/Catfishing/Environment/CatChumPlacementService.cpp:153-158 | 非水面落点直接以 `InvalidWaterTarget` 拒绝、库存原样保留，不生成任何掉落物；grep `ChumPickup/DropChum/ChumDrop` 全库零命中，`Items/` 重构后也没有窝料世界物 |
| 10 | 投料落水泛涟漪、出现鱼影（钓鱼规则.md:43） | ❓ Unverifiable | Source/Catfishing/Environment/Presentation/CatChumFieldPresentationActor.cpp:31-43,45-59 | C++ 只有占位网格（引擎自带 Cube/Sphere ＋ BasicShapeMaterial）与 `ApplyPublicState → BP_OnFieldAdded/BP_OnFieldChanged/BP_OnFieldRemoved` 三个蓝图钩子；涟漪/鱼影落在 `PresentationClass` 指向的蓝图与材质里（④），文本搜索证明不了 |
| 11 | 叼起窝料再次抛入同一个窝执行叠加，不重置衰减计时（钓鱼规则.md:45） | ❌ Missing | — | 每次投放生成独立 field，各带自己的 `StartServerTime/ExpireServerTime`（CatChumFieldTypes.h:157-169；Subsystem:205-206），后投的料就是一条新曲线；且窝料落地不成掉落物（见 #9），叼回这条路本身不存在 |
| 12 | 补窝受道具总闸门约束：正在搏斗的猫不能掏窝料；其他处于可用道具状态的玩家可以为同一个窝补料（钓鱼规则.md:45；多人钓鱼附篇.md:75） | 🔄 Divergent | Source/Catfishing/Fishing/Integration/CatFishingCommandComponent.cpp:978；Source/Catfishing/Environment/CatChumPlacementService.cpp:82-86,135-143 | 注释仍写明「Q 打窝蓄力：与是否有会话无关，等口/遛鱼中都可以补窝」，`PlaceChum` 只拒 `bDowned` 与 `CanAcceptFishingCommand` 白天 gate，没有搏斗态闸门。队友替补料这半是通的（任一玩家都能投）。无 D 条目登记 |
| 13 | `Total = X+P+J`（条数）；Total>0 → T_base 15 秒，Total=0（含落点不在任何窝点内）→ T_base 120 秒（钓鱼规则.md:51-56） | 🔄 Divergent | Config/DefaultGame.ini:204-209；Source/Catfishing/Fishing/Simulation/CatFishingBiteTimingModel.cpp:37-44 | 仍是「无窝/一份/五份」三个均值锚点 20/14/6 秒，按连续贡献量 `SingleChumContribution=1.7`／`FullChumContribution=8.5` 分段线性插值出目标均值；没有 15/120 两档，无窝均值 20 秒而不是 120 秒 |
| 14 | `T_actual = T_base / (1 + Total/K)`，`K = 100`（钓鱼规则.md:58） | ❌ Missing | — | 全库无该公式与 K 常量；代码是「目标均值 → 二分求解截顶指数分布速率 → 采样等待秒数」（CatFishingBiteTimingModel.cpp:45-74、:76-89） |
| 15 | 计时器挂在浮漂上，每漂独立（钓鱼规则.md:60） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:734-745,763-790 | 每个 `ACatFishingSession`（＝一根竿一次抛投）各自持有 `ProbeTimerHandle`/`BiteWarningTimerHandle`，互不干涉 |
| 16 | 存量因补窝、衰减或钓走改变后，剩余时间按 `新剩余 = 旧剩余 × 新间隔 / 旧间隔` 折算，不重开完整计时（钓鱼规则.md:60） | ❌ Missing | Source/Catfishing/Fishing/CatFishingSession.cpp:702-745 | 窝料只在抛竿落水那一刻 `SampleChumAtPoint` 采样一次并一次性排好定时器，等待期间补窝、衰减都不重算剩余时间；代码里没有任何折算路径 |
| 17 | 窝空时间隔从约 15 秒跳到 120 秒，不加平滑、最低间隔或保底（钓鱼规则.md:62） | 🔄 Divergent | Config/DefaultGame.ini:210-212；Source/Catfishing/Fishing/Simulation/CatFishingBiteTimingModel.cpp:37-44,62-70 | 明确设了下限 `MinimumBiteDelaySeconds=3.0`、上限 `MaximumBiteDelaySeconds=40.0`、`BiteWarningSeconds=1.5`，并对贡献量做分段线性平滑——正是设计写明「不加」的三样 |
| 18 | 某个浮漂计时到点，服务器执行一次原子事务：读该漂主人当前的饵 → 选类别 → 选鱼种 → 当场扣 1 条库存（钓鱼规则.md:68） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:779-790,833-901 | 到点只发 `ProbeTriggered` 进试探；鱼种仍要等玩家在 `TrueBiteWindow` 内提竿成立才 `ResolveHookSelectionFromAuthority` 选出（选鱼前还会二次采样窝料），且没有任何库存扣减动作。09-08「抽中即出鱼影」也依赖抽鱼提前到到点这一刻 |
| 19 | 两步选鱼：先按 `X/Total、P/Total、J/Total` 选偏好类别，再在该类候选里按漂主当前饵的偏好权重选鱼种（钓鱼规则.md:70） | 🔄 Divergent | Source/Catfishing/Data/CatFishCatalogSettings.cpp:245-281,282-335；Config/DefaultGame.ini:91-98 | 仍是两段抽样，但第一段抽的是**挑战档**（Comfort/Matched/Risky 按 ini 权重 0.25/0.60/0.15 roll），不是 X/P/J 偏好类别；第二段才在选中带里按 `SpawnWeight × ChumModifier(三轴亲和度经半饱和＋曲线) × BaitModifier × ChallengeModifier` 归一化 roll 鱼种。三轴占比只以连续亲和度进乘法权重。09-09 晚「删代码七条」首条即「挑战档三带」，至 HEAD 未执行 |
| 20 | 窝料表「目标鱼种加权」的窝料，在第二步对该鱼种权重再乘表里倍率（钓鱼规则.md:70） | ❌ Missing | Source/Catfishing/Equipment/Fragments/CatEquipmentFragment_Chum.h:10-21；Source/Catfishing/Environment/CatChumFieldTypes.h:41-73 | 窝料 fragment 只有一个 `FCatChumInfluenceSpec`（三轴基础贡献、半径、时长、两条衰减曲线、单次份数上限），没有目标鱼种字段；选鱼端也无对应消费。该列是 09-08 晚⑤号裁决新加的 |
| 21 | Total=0 从基础池选、按当前饵权重、但不扣存量；候选为空或总权重为零时也落基础池（钓鱼规则.md:70） | ⚠️ Partial | Source/Catfishing/Data/CatFishCatalogSettings.cpp:290-319；Source/Catfishing/Fishing/CatFishingSession.cpp:718-719 | 无窝时 `TotalChum=0` 仍走同一条选鱼链、仍读饵权重，语义上接近「基础池」✅；但没有独立的基础池概念，`TotalCandidateWeight<=0` 时直接返回未选中而不是兜底出鱼——「兜底体验」这半没有。参数页记的「基础池」子表 09-10 才建、仍是占位 |
| 22 | 库存在抽中那一刻就扣，抽扣同一原子事务，抽中即受惊离窝，之后任何结局都不回滚（钓鱼规则.md:74） | ❌ Missing | — | 无窝料存量模型，因此没有「扣 1 条」这个动作，也无从谈原子性与不回滚；grep `Deduct/Decrement/ConsumeChum/扣存量` 在 Environment/ 零命中 |
| 23 | 饵的数量在真咬成立时扣当时挂着的那 1 份，试探期提竿不损饵（钓鱼规则.md:74） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:977-986 | 抛竿时只预约（Deferred），真咬窗内选出鱼、鱼 Actor 校验通过后才 `CommitFishingBaitDeferred` 落扣，失败即回滚整场；试探期提竿走空竿分支不提交。D-13 登记的普通饵一局消耗品口径 |
| 24 | 同窝多漂同刻到点按服务器时间戳判序，时间戳并列按席位序；A 漂抽走最后一条后 B 漂自然落基础池（钓鱼规则.md:76） | ❌ Missing | — | 依赖 #1/#22 的共享存量与扣减；当前每漂各自采样、各自出鱼，彼此没有可争抢的资源，这条判序规则在代码里没有对应实现 |
| 25 | 每 30 秒统一执行 `新存量 = floor(旧存量 × 0.9)`，逐分量向下取整，不为负（钓鱼规则.md:82） | 🔄 Divergent | Source/Catfishing/Environment/CatChumFieldTypes.h:75-92；Source/Catfishing/Environment/CatChumFieldSubsystem.cpp:365-379,385-400 | 每个 field 一条 `TimeFalloffCurve`（烘焙成 65 点采样表）连续衰减，到 `ExpireServerTime` 整体下线；没有 30 秒节拍、没有 0.9 系数、没有 floor 取整。曲线数值在 Content 资产里（③），但「30 秒 × 0.9 整数衰减」这套机制在 C++ 里确定不存在 |
| 26 | 三类存量都达到门槛后，每次投料独立判定一次触发；触发后咬钩加速、鱼群密度演出增强；期间再投料不重复触发；没有冷却（钓鱼规则.md:92） | ❌ Missing | — | 全库仍无「聚鱼窗口」状态：搜 `聚鱼/Aggregation/Frenzy/School/门槛/窗口` 只命中自然事件写口与窝料注释，没有门槛判定、触发 roll、窗口计时或咬钩倍率。D-25 已在复核段标「待裁」，登记的两个触发源前提已被 09-07 单源化裁决改变 |
| 27 | 聚鱼建议快照：每类门槛 30 条、每次合格投料触发概率 8%、持续 45 秒、咬钩速度 2.5 倍（钓鱼规则.md:96-99） | ❌ Missing | — | `CatChumFieldSettings`（Config:253-261）与 `CatFishingSettings`（Config:194-224）两段全查过，没有这四个量的任何承载字段 |
| 28 | 聚鱼只有投料一条触发路径，范围＝触发它的那个窝点；环境事件触发归完整版、不在本版（钓鱼规则.md:103） | 🔄 Divergent | Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1681-1700；Source/Catfishing/Environment/CatEnvironmentSettings.h:31,59,63 | 代码仍留第二条路径：公共自然事件按 `NaturalChumDefinitionId + NaturalChumAnchorId` 向 ChumField 写口提交一次，去重键 RunId+DayIndex+EventId+AnchorId。它写入的是窝料 field（等价于加一份窝料），不是设计里的聚鱼窗口。09-09 晚讨论处置「自然聚鱼写口先不动」，D-25 需按 09-07 单源化重登记 |
| 29 | 渔网是一次性道具：按落点窝点当前概率抽 5～8 条、稀有不加权、湖心巨影永不入网，产出落地先到先得（钓鱼规则.md:109） | ❌ Missing | Source/Catfishing/Equipment/Fragments/ | 装备已改 fragment 制，现存五种：Bait/Chum/Float/Rod/Scoop，没有渔网；grep `渔网/CastNet/ThrowNet/FishingNet` 在 Source/ 与 Config/ 零命中 |
| 30 | 渔网使用后该窝点进入 30 秒死水，所有浮漂咬钩计时挂起，结束后按折算规则恢复；死水期内再撒网正常结算、死水重置 30 秒不叠加（钓鱼规则.md:109,111） | ❌ Missing | — | 无死水状态、无按窝点挂起的路径，随 #29 一并缺失。复核更正：`ACatFishingSession` 确实存在一条清/重排定时器的通道——`RefreshBiteAvailabilityFromAuthority`（CatFishingSession.cpp:599-621）在 `CanGenerateNewFishingBites()` 转假时 Clear 掉 Probe/Warning，恢复时再走 `ScheduleWaitingProbeFromStateTree` 整条重排；但它是入夜闸门、不按窝点划范围、也不做剩余时间折算（见 #16），不构成死水挂起 |
| 31 | 渔网逐条按抽中鱼的偏好类别扣存量，实际条数 `min(5～8 随机数, Total)`；无窝点或空池按基础池给 1～2 条低保、不扣存量；渔网产出不扣竿的基础磨损（钓鱼规则.md:111） | ❌ Missing | — | 随 #29 一并缺失；且没有存量可扣（见 #22） |
| 32 | 单局上限 4 人（多人钓鱼附篇.md:27） | ✅ Implemented | Source/Catfishing/Framework/Game/CatfishingGameModeBase.h:24；Source/Catfishing/Online/CatOnlineSubsystem.cpp:1407；Source/Catfishing/Camp/CatCampHubActor.cpp:97-130 | `CatGameplayPlayerLimits::MaxCampSpawnPlayers = 4` 同时作为房间 `NumPublicConnections` 与营地出生位上限，超过容量拒绝而不是循环复用 |
| 33 | 游戏机制层没有房主：钓鱼、移动、献祭都没有身份特权（多人钓鱼附篇.md:27） | ✅ Implemented | Source/Catfishing/Environment/CatChumPlacementService.cpp:40-86；Source/Catfishing/Fishing/CatFishingSession.cpp:330-345；Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:1046-1069 | 打窝、抄网、拾取的服务端裁决只看身份合法性/距离/阶段，不查 host 身份；复核补查设计点名的献祭一侧，`Camp/`、`Run/`、`Framework/` 三处 grep `IsHost`／`bIsHost`／`HostPlayerState`／`房主` 只命中调试面板文案与房主退出 teardown，祭坛与翻天入口无身份 gate。本轮新增的「只有原物品主人能上竿」（CatFishingService.cpp:643,677）是物品归属规则，不是房主特权；D-05 登记的房主争议在房间层 |
| 34 | 参与角色有主钓手、辅助手和岸上替补；合力发生在搏斗拉竿阶段（多人钓鱼附篇.md:35-41） | 🔄 Divergent | Source/Catfishing/Fishing/Actors/CatFishingRodActor.cpp:112-121,190-221；Source/Catfishing/Fishing/CatFishingSession.cpp:181-196 | 本轮基座改写：操作位数组只写得进 0 或 1 人（`SetPrimaryOperatorFromAuthority` 先 Reset 再至多 Add 一个），`MaximumRodOperatorSlots` 已删；`SubmitFightAssist` 变成恒拒绝桩，日志 `Reason=PhysicalAssistanceOnly`。队友帮忙的唯一路径是物理抓握（见代码超前项），辅助手与岸上替补都不是代码里的角色 |
| 35 | 力量计入：钓鱼位 100%、辅助位 75%（多人钓鱼附篇.md:39-40,53） | ❌ Missing | Source/Catfishing/Fishing/Config/CatFishingFightBalanceDefinition.h:33-138 | `HelperStrengthMultiplier` 已从平衡资产与 Runner 中删除（基线时 C++ 默认 0.5），全库零命中；生成脚本 `Scripts/create_fishing_fight_balance_asset.py:28` 仍留着 `helper_strength_multiplier: 0.5` 这把过期钥匙，属脚本未跟改。物理协助者按自身满力量经约束传力，没有任何位置折扣 |
| 36 | 消耗倍率 K_pos：钓鱼位 1.0、辅助位 1.5（多人钓鱼附篇.md:39-40,53） | ❌ Missing | Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:321-370 | 结算只对唯一主控 `OperatorState` 计费（移动缺失位移 ＋ 收线/转竿做功），全库无 K_pos 字段；物理协助者走自己的 `UCatPhysicalEffortComponent`，也没有位置倍率 |
| 37 | 岸上替补搏斗外恢复 5 点/秒（多人钓鱼附篇.md:41） | ⚠️ Partial | Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h:18-21；Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:76-91 | 本轮新增个人体力线：`RecoveryPerSecond = 5.0` 已是代码事实（ini 无该段，取类默认）✅；但要满足「出过力（`bRecoveryPending`）＋ 本步不出力 ＋ 无负载 ＋ 静置满 `RecoveryDelaySeconds = 2.0` 秒」才开始回，且代码里没有「岸上替补」这个角色，恢复对所有猫一视同仁 |
| 38 | 入位：靠近搏斗中的主钓手 1.5 米内按 F；退出：再按 F 或走出半径，自由来去无惩罚（多人钓鱼附篇.md:45） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingService.cpp:643,668-684,728-768；Source/Catfishing/Fishing/Integration/CatFishingCommandComponent.cpp:961 | 上竿键（代码注释「R 只允许原物品主人显式取得自己的竿；其它抓握只产生物理约束」）现在**只对竿的主人开放**，半径 250 cm 而不是 150 cm；队友想搭手只能用物理抓握，既没有 1.5 米资格圈也没有「走出半径自动退出」。实际按键绑定在 InputAction 资产里（④），此处以 C++ 注释与半径常量为据。09-09「操作模式方向」已裁当前上竿/离竿操作位是临时的、键位待定 |
| 39 | 辅助手体力归零就弹出辅助位、2 秒喘气演出、不落水，恢复到 20% 以上才能重入（多人钓鱼附篇.md:45） | ⚠️ Partial | Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:61,89-90,99-108；Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h:22-23 | 物理协助线上「归零即弹出、20% 才能重入」已成立：体力见底 `SetExhausted(true)` → `Grab->ReleaseAllFromAuthority("PhysicalEffortExhausted")` 松开双手、`CanGripFromAuthority()` 转假，回到 `ExhaustionResumeRatio = 0.2` 以上才解除。缺的是「辅助位」这个身份本身与 2 秒喘气演出（`RecoveryDelaySeconds=2.0` 是回体延迟，不是演出）。基线那句反向口径「零体力只影响出力不影响操竿资格」在主控一侧仍在（见 #48） |
| 40 | 辅助人数不另设上限，受 4 人上限自然约束，主 1 加辅最多 3（多人钓鱼附篇.md:45） | ⚠️ Partial | Source/Catfishing/Interaction/Grab/CatPhysicsGrabComponent.h:47-72；Source/Catfishing/Framework/Game/CatfishingGameModeBase.h:24 | 抓握不设人数配额，房间 4 人上限是唯一自然约束，结果与设计一致；但代码里已没有「辅助位」这个配额对象（`MaximumRodOperatorSlots` 随本轮重构删除），这条只能算顺带成立 |
| 41 | F 目标选择：看着鱼按 F 是抓鱼、看着主钓手按 F 是入位，两者难分时先算抓鱼；准星态提示当前 F 目标（多人钓鱼附篇.md:47） | ❌ Missing | Source/Catfishing/AbilitySystem/Tags/CatFishingAbilityTags.cpp:5,8；Source/Catfishing/Interaction/CatInteractionTargetingComponent.cpp | 抄网与上竿仍是两个独立输入意图（`Cat.Input.Fishing.Scoop` / `Cat.Input.Fishing.RodInteract`），没有「同一键的目标优先级」裁决；万能交互链的 `ICatInteractable` 实现者只有祭坛/营地箱/鱼护/鱼缸/商店/落地鱼与 `CatItem`，鱼竿与水中 `ACatFishEncounterActor` 都不在内。设计要的「入位」目标现在更是没有对象（见 #34） |
| 42 | `总力量 = 主钓手当前力量 × 100% + Σ 辅助手当前力量 × 75%`，辅助力量不设上限；合力变化触发强度检查，提交 F_total 与各参与者 K_pos（多人钓鱼附篇.md:53） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:1454；Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:244-268；Source/Catfishing/Interaction/Grab/CatPhysicsGrabComponent.cpp:581-637 | `ActiveCombinedFishingStrength` 现在等于主控一人的出力（`Step.OperatorCatStrength`），加权求和公式随 GroupModel 一并删除；协助者的力量改以物理牵引进入系统（注释：`Force on target; the holder receives the equal and opposite force. No fishing membership or stamina sum.`），是满力量、不是 75%，也不提交 K_pos |
| 43 | 位置落水只判持竿者；辅助手归零弹出辅助位、不落水（多人钓鱼附篇.md:57） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:1522-1532 | 本轮改判：`HandleCatEnteredDangerousWaterFromAuthority` 现在要求 `Snapshot.RodActor->IsPrimaryOperator(Character->GetPlayerState())`，非主控入水不进这条出口；物理协助者根本不在会话里，归零只松手不落水（见 #39）。落水本身的三条件判据在 §4.6，不在本轮评分范围 |
| 44 | 主钓手放线时全员零消耗，辅助手不必退出（多人钓鱼附篇.md:57） | ⚠️ Partial | Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:330-352；Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:36-83 | `bFreeEffort = Step.bSlackRecoveryActive \|\| State.bFishExhausted` 时主控的移动与竿账单一并置零 ✅；但「全员」没有对象——物理协助者的体力由自己的 `SettleMovementFromAuthority` 结算，不读会话的放线状态，主控放线并不免掉他们的费用 |
| 45 | 辅助手没有放线权；只有主钓手能触发主动放弃（多人钓鱼附篇.md:57） | ✅ Implemented | Source/Catfishing/Fishing/Integration/CatFishingCommandComponent.cpp:794-818；Source/Catfishing/Fishing/CatFishingSession.cpp:1885-1902 | 非主位提交线杯边沿直接被拒（`NotFisher`）；`CutLineFromAuthority` 的准入是「本人＝当前 Fisher」，或「竿已落地、`GetOperatorCount()==0` ＋ 本人是竿主或 `LastSuspendedFisherPlayerState` ＋ 250 cm 内」——后者正是 09-09 裁决③的「主位主动离竿后走回竿边一定距离内可切线」，不对队友开放。由于操作位只剩主控一人（#34），这条结构性成立。复核更正：原引的 CommandComponent:1280 是 `BeginCastFromViewOnAuthority` 的主位校验，不是切线闸门 |
| 46 | 主钓手任意时刻可按 E 发起换人请求、再按 E 取消；请求无时限挂起、本竿结束自然失效、没有超时出口（多人钓鱼附篇.md:63） | ❌ Missing | Source/Catfishing/Fishing/Integration/CatFishingCommandTypes.h:8-19 | `ECatFishingCommandType` 仍无「换人请求」命令，`ACatFishingRodActor` 的复制状态里也没有挂起请求字段；本轮把接手路径收窄成「只有物品主人能取回」（#48）之后，换人握手更无处可挂 |
| 47 | 体力不高于 5% 时 UI 给濒死强提示（多人钓鱼附篇.md:63） | ❌ Missing | Source/Catfishing/UI/HUD/CatHUDWidget.h:30-160 | HUD 数据模型仍只有体力值/上限/归一化比例与 `bShowPersonalStamina`，没有濒死阈值标志或告警字段；grep `濒死/LowStamina/Critical/NearDeath` 在 UI/ 零命中 |
| 48 | 岸上替补自愿按 E 接手，先按先得、同刻并列按席位序；体力恢复到 50% 以上的替补才能接手（多人钓鱼附篇.md:65） | ❌ Missing | Source/Catfishing/Fishing/CatFishingService.cpp:677-684；Source/Catfishing/Fishing/CatFishingSession.cpp:211-236 | 本轮从基线的「无握手自动晋升」倒退为不可能：`OperateRod` 要求 `PlayerState == OwnerPlayerState`，`ResumeOwnerControlFromAuthority` 注释写明「仅允许原竿拥有者明确取回控制；物理抓握本身不会调用此入口或转让会话」。先按先得、席位序、50% 体力门槛都无对应；注释仍保留「控制资格与出力分离：原主控零体力仍能持有线杯控制权」（Session:220） |
| 49 | 接手瞬间完成：鱼与竿的状态完全继承，猫体力各是各的，接手者用自己的（多人钓鱼附篇.md:65） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:254-270；Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.h:97-100 | 状态继承机制还在：`ResumeOwnerFromAuthority` 重绑 ASC/力量/体力/输入序号，注释「恢复只读取本人当前属性，不补满体力或重建本场 Runner」，鱼与竿状态不重置 ✅；但入口只对竿的主人开放，不是设计要的替补接手（#48） |
| 50 | 图鉴归属不变，原始上钩者保留（多人钓鱼附篇.md:65；出处更正：09-07 晚 N8「图鉴两层制·收集层成功收鱼时写入、归上钩者」，09-08 补缺口轮换人条「原始上钩者归属不变」重申） | 🔄 Divergent | Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:1077-1089 | `ArchiveCommittedCapture` 仍把 `FishRecorded` 授给 `PickerStableNetId`（入护/叼走的人），不是上钩者；抢拾之后图鉴记到拾取者名下。文件随本轮 Items 重构从 `Items/World/` 移到 `Items/Fish/`，逻辑未变 |
| 51 | 多人搏斗到收鱼仍是两条路：可抄鱼贴岸抢抄，或翻肚落岸后抢拾；资格与并列判序见主文（多人钓鱼附篇.md:73） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:330-345,427-451；Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:1046-1069 | `RequestScoop` 对任何满足参战能力谓词的 Controller 开放（不限竿主），且 `HookedFight` 与 `NearShore` 两阶段都开放；落地鱼拾取同样对任一未倒地、在交互距离内的角色开放，服务端按到达顺序裁决并缓存终态 |
| 52 | 操作贡献者＝上钩钓手＋合力中按过左键至少一次的猫＋抄中或拾取的猫；打窝与围观不算（多人钓鱼附篇.md:81） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:1597-1603,1702-1707；Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:1103-1106 | 参与者名单现在恒等于「钓手 ＋ 抄/拾者」：基线时的「只有巨物才带参战名单」已去掉（这半向设计靠拢），但「合力中按过左键至少一次」无处可取——协助者不在会话里，按键事实也不落在会话上。打窝/围观确实不计入 ✅ |
| 53 | 贡献只用于演出（合影名单、头衔类），不进图鉴、不刷新个人最佳重量（多人钓鱼附篇.md:81） | ⚠️ Partial | Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:1103-1112；Source/Catfishing/Collection/CatRunImprintService.cpp:141 | 参与者名单唯一下游仍是 `CreateCapturePlansForParticipants`（印记成像计划，演出向）✅；但图鉴 Grant 走的是拾取者而非上钩者（见 #50），「不进图鉴」这条在结果上仍被破坏 |
| 54 | 举鱼展示、围观互动、巨物合力成功的慢镜头合影（多人钓鱼附篇.md:83） | ❓ Unverifiable | Source/Catfishing/Collection/CatRunImprintService.cpp:141-186 | C++ 只有成像候选/投递的协调骨架；慢镜头、合影构图、举鱼动作都是蓝图与动画资产（④），文本搜索证明不了 |
| 55 | 求助由玩家手动发出：急促猫叫加头顶图标（多人钓鱼附篇.md:87） | ✅ Implemented | Source/Catfishing/Social/CatSocialService.cpp:373-434；Source/Catfishing/AbilitySystem/BodyAction/Social/CatSocialBodyActionAbilities.cpp:53-110；Config/DefaultGame.ini:291-292 | `ECatHelpSignalKind::ManualFishing` 手动求助链完整（Ability → Social 服务 → 半径信号 ＋ 重放缓存 ＋ 冷却），`ManualHelpRadiusCentimeters=500`、`ManualHelpCooldownSeconds=6`；猫叫与图标的具体资源在表现层（④） |
| 56 | 只有巨物保留全体提示（多人钓鱼附篇.md:87） | ⚠️ Partial | Source/Catfishing/Social/CatSocialService.cpp:436-449；Source/Catfishing/Social/CatSocialTypes.h:20,45-47 | 类型层区分仍在（只有 `GiantFishSystem` 发 `bGlobal = true`，手动求助按半径传播）✅；但 `BroadcastGiantFishingPrompt` 在本轮重构后**没有任何玩法调用方**（全库只剩声明、定义与调试子系统的读取），巨物全体提示实际发不出来 |
| 57 | 主辅位的反馈与角色区分不做力量百分比条（多人钓鱼附篇.md:87） | ✅ Implemented | Source/Catfishing/UI/HUD/CatHUDWidget.h:79-86；Source/Catfishing/UI/HUD/CatHUDModel.cpp:152-158 | 本轮改判：HUD 新增 `bPrimaryRodOperator` 与左右手抓握状态，本人是不是主控在数据层可读（基线时零命中）；同时 `CombinedFishingStrength`／`ActiveHelperCount` 已从视图模型删除，没有力量百分比字段（`LineLoadPercent` 是线负载不是力量）。最终画法仍在蓝图 Widget（④） |
| 58 | WASD 控制移动；`F_net = Σ(猫力量 × 方向单位向量)`、`S = Σ猫力量`（按键者）、`ρ = \|F_net\| / S`；任意方向输入直接进合力求和（多人钓鱼附篇.md:93） | 🔄 Divergent | Source/Catfishing/Character/Physics/CatPhysicalBodyComponent.cpp:391-398,402-428；Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:19-29 | `FCatFishingGroupModel` 已整文件删除。现在没有任何标量合力：每只猫是一个物理身体，连接状态下的驱动力上限＝`力量 × ForcePerStrengthNewtons × 100`，多只猫的力在约束/接触处按物理叠加，斜向输入天然求和 ✅；但 `S`、`ρ` 与随后的三档判定完全没有输入。D-02 只登记了 WASD 键位 |
| 59 | 无人按键（S = 0）不进 ρ 计算，直接按静止处理，不存在除零（多人钓鱼附篇.md:93） | ⚠️ Partial | Source/Catfishing/Character/Physics/CatPhysicalBodyComponent.cpp:405-423 | 观感上的「按静止处理」成立：零意图时协作/钓鱼态改走有限刚度的原地站桩弹簧（`ElasticRangeCm = 10`），被压过力就让位、不会弹回旧位置；除零也不存在（根本没有 ρ 这条除法）。但这条要求本身是 ρ 管线上的守卫，管线已随 GroupModel 一并消失 |
| 60 | ρ 三档：齐步走 ρ≥高阈（0.8）正常移动、拖行居中走龟速、僵局 ρ≤低阈（0.2）（多人钓鱼附篇.md:95-99） | ❌ Missing | — | 全库无阈值常量、无档位枚举，`Config/DefaultGame.ini` 亦无对应段；速度是物理连续量，不存在「龟速档」。参数页 09-08 待办里 ρ 阈值 0.8/0.2 仍未挂号 |
| 61 | 僵局细分：两组共线反向就对顶——恰 2 人顶牛、4 人 2v2 拔河，其余组合自然推拉、无特定演出；方向散乱就打转（多人钓鱼附篇.md:99） | ❌ Missing | — | 没有对顶/打转状态机，也没有按人数分支的判定；对向输入在物理里只是力相互抵消（互推确实会发生，但没有任何状态识别与演出触发） |
| 62 | 打转持续超过 5 秒：恰 3 人随机甩飞一只，4 人连环摔倒，两者互斥（多人钓鱼附篇.md:101；09-08 裁：连环摔倒恰 4 人、不产生搏斗结局） | ❌ Missing | — | 无打转状态即无计时，无甩飞/连环摔倒的任何触发或效果 |
| 63 | 移动合力是全额力量相加，钓鱼合力才有辅助位 75% 折扣，两处口径独立（多人钓鱼附篇.md:101） | ⚠️ Partial | Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:19-29；Source/Catfishing/Fishing/Config/CatFishingFightBalanceDefinition.h:33-44 | 移动一侧确实是全额力量（`GetMaximumForceKgCmS2` 直接读本人 `FishingStrength`，无折扣）✅；但钓鱼一侧的 75% 折扣整体不存在（#35），所以现在是一套口径覆盖两处，而不是设计说的两套独立口径 |
| 64 | 移动消耗表：正常移动 1、龟速被拖者 8／顺行者 2、原地打转 5、顶牛拔河 6（点/秒/人）（多人钓鱼附篇.md:111-114） | 🔄 Divergent | Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h:13-17；Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:62-83；Source/Catfishing/Physics/Simulation/CatIntentMotionModel.cpp:17-30；Source/Catfishing/Character/CatCharacterMovementComponent.cpp:54-62 | 改成统一的「意图缺失位移」计价：`StaminaPerUnfulfilledMeter = 2.0` 点/米，把实际位移投影到意图方向、按缺口米数收费（CatIntentMotionModel.cpp:22-30），站桩由 `SupportReferenceSpeedCmS = 100` 把反抗载荷的出力比折算成等效意图位移（CatCharacterMovementComponent.cpp:54-62）；没有按档位取常数的表，且未连接、自由行走时完全不计费。复核更正：原引 `CatFishingOperatorWorkModel.cpp:29-44` 越界——该文件只有 18 行，它是搏斗侧入口（:3-17），真正的米价结算在 CatIntentMotionModel |
| 65 | 无人移动（静止）恢复 5 点/秒/人（多人钓鱼附篇.md:115） | ⚠️ Partial | Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:76-91；Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h:18-21 | 速率对上了（5 点/秒）✅，但要「出过力 ＋ 本步不出力 ＋ 无负载 ＋ 静置超过 2 秒」才开始回，且只在协作连接语境里计；不是设计说的静止即恒定回体 |
| 66 | 移动中体力归零的猫走不动：不能主动移动、没有拖/背交互、原地恢复并随队被拖行、不阻塞编组（多人钓鱼附篇.md:107,123） | ⚠️ Partial | Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:19-34,99-108；Source/Catfishing/Character/Physics/CatPhysicalBodyComponent.cpp:384-398,405-411 | 比基线靠近：力竭时 `GetMaximumForceKgCmS2()` 返回 0（连接状态下推不动任何东西）、双手自动松开、被动接触位移零计费也零阻力（`bPassiveBodyContact` 直接返回零驱动力），恢复见 #65，确实也没有拖/背交互 ✅；但「不能主动移动」只在协作连接时成立——未连接的力竭猫仍走普通移动分支（:426-428），可以自由走动 |
| 67 | 恢复速率由主文基础自然恢复提供：搏斗外一律 5 点/秒，静止、岸上替补、摸鱼与归零随队被拖都是这一个数、没有场景乘数（多人钓鱼附篇.md:123） | ⚠️ Partial | Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h:18-21；Source/Catfishing/AbilitySystem/Core/CatAbilitySystemComponent.cpp:309-330；Source/Catfishing/Fishing/CatFishingSession.cpp:125-135 | 「5 点/秒、无场景乘数」这半本轮成立（唯一速率，ini 未覆盖）✅；但仍保留两条与设计相斥的写法：进入 `HookedFight` 时 `InitializeFishingStaminaForSession` 把主控体力一次性补满（不是逐秒回），以及 2 秒静置延迟与「出过力才回」的门槛 |
| 68 | 不按键者不进计票：不贡献力量向量、不拖慢队伍，随队被拖、自己照常恢复（多人钓鱼附篇.md:125） | ⚠️ Partial | Source/Catfishing/Character/Physics/CatPhysicalBodyComponent.cpp:384-390,405-411；Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:62-83 | 零意图即不出力、不计费，被动位移明确不产生驱动力也不收费 ✅；但「不拖慢队伍」在物理模型下不再自动成立——不按键的猫仍是一具有质量与摩擦的身体，被拖时要消耗队友的力，这属运行时行为，文本不能量化（④） |
| 69 | 编组就是同房间全员一队、没有子编组，加入退出即入局离局（多人钓鱼附篇.md:125） | 🔄 Divergent | Source/Catfishing/Character/Physics/CatPhysicalBodyComponent.cpp:370-390；Source/Catfishing/Interaction/Grab/CatPhysicsGrabComponent.cpp:581-637 | 「组」这个对象已彻底不存在（`FCatFishingGroupMotionState` 随 GroupModel 删除）：谁和谁共同运动完全由当帧的物理接触/抓握连通性决定，同房间没抓在一起的人互不影响 |
| 70 | UI：屏幕上方按 W/A/S/D 图标显示每个队友当前方向；移动状态文字「齐步走／被拖行／原地打转／顶牛中」；每个角色头顶体力条、力竭变灰（多人钓鱼附篇.md:129） | ❌ Missing | Source/Catfishing/UI/HUD/CatHUDWidget.h:30-160；Source/Catfishing/UI/CatFishingViewTypes.h:20-52 | HUD 数据模型只有本人属性与本场鱼的状态，没有逐队友的方向、状态或体力条数据；基线时还有的 `ActiveHelperCount` 也已删除，Widget 再怎么画也拿不到数据 |
| 71 | 六个群体动作演出：顶牛、被拖着走、原地打转、连环摔倒、被甩飞、拔河拉长（多人钓鱼附篇.md:132-138） | ❌ Missing | Source/Catfishing/AbilitySystem/Tags/CatFishingAbilityTags.cpp:34-47 | 依赖 #60-#62 的档位判定；Cosmetic 标签仍只有 ScoopSwing/HookPull/LineCut/CatInWater/LineBroken 五个，没有任何群体动作事件标签 |
| 72 | 多人不绕过当前可用协作能力的鱼候选过滤；加入、退出、换人与在局人数变化是能力变化的来源（多人钓鱼附篇.md:27,49） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingService.cpp:1514-1573；Source/Catfishing/Fishing/CatFishingSession.cpp:871-887；Source/Catfishing/Data/CatFishCatalogSettings.cpp:219 | 选鱼前 `BuildFightCapabilitySnapshot` 实时遍历全部 PlayerController，按统一谓词（未倒地、力量与体力均为正）统计人数/合力/合体力并冻进 `FrozenSelectionContext`；候选管线用 `PassesActivePlayerCount` 过滤，人数是实时事实、不走单人旁路 |
| 73 | 稀有度本身不进抽鱼概率，鱼种分布与出现条件由鱼册提供（钓鱼规则.md:70） | ✅ Implemented | Source/Catfishing/Data/CatFishDefinition.h:111；Source/Catfishing/Data/CatFishCatalogSettings.cpp:282-335 | `RarityTierId` 只作稳定内容 ID，grep `Rarity` 在选鱼链零命中；最终权重只由 `SpawnWeight × ChumModifier × BaitModifier × ChallengeModifier` 构成，稀有度经由内容侧 SpawnWeight 表达而不在代码里硬编码档位概率 |
| 74 | 连携激励只做演出（慢镜头、合影、头衔类），没有任何数值加成（多人钓鱼附篇.md:83） | ✅ Implemented | Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:1103-1112；Source/Catfishing/Framework/Core/CatProfileContracts.h:9-19 | 参与者名单唯一下游仍是印记成像计划，授予物只有 `ECatProfileGrantKind` 四种（FishRecorded/FishSilhouette/Imprint/Unlock），没有按参与人数发的力量/体力/经济修正；唯一受人数影响的是候选过滤（#72），那是过滤不是奖励 |
| 75 | 抽鱼的「主人」由到点的那个浮漂确定：不为窝点先抽一条公共鱼再比各人的饵，也不在多个饵命中的玩家之间随机分钩（钓鱼规则.md:68） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:863,884,896 | 复核补行（原报告漏列）。选鱼是 `ACatFishingSession` 的实例方法，饵只读本场 `AttemptSnapshot.BaitDefinitionId`（:884）、装备只读抛竿者预留的 `CastEquipment`（:863），再逐会话调 `SelectRuntimeDefinition`（:896）；全库无按窝点/水域聚合抽取的公共鱼池，也无跨玩家分钩仲裁。这条反模式护栏代码是满足的 |
| 76 | 随机区间的端点算在区间内（钓鱼规则.md:70） | ✅ Implemented | Source/Catfishing/Data/CatFishCatalogSettings.cpp:276,331；Source/Catfishing/Fishing/Simulation/CatFishingBiteTimingModel.cpp:80,86 | 复核补行（原报告漏列；09-08 补缺口轮与「候选为空落基础池」同批拍的一条）。两处轮盘游标都用 `Cursor <= 0.0` 收尾并显式保留「最后一个候选」的确定性回退（:329 注释），等待采样对 `UnitRandom` 取 `[0,1]` 闭区间、`== 1.0` 单独映射到截顶值，端点都算在内 |

### Summary
- Total features: 76
- ✅ Implemented: 15
- ⚠️ Partial: 15
- ❌ Missing: 25
- 🔄 Divergent: 19
- ❓ Unverifiable: 2
- Inspectable: (76 - 2) / 76 * 100% = 97.4%
- Coverage of inspected: (15 + 0.5 * 15) / (76 - 2) * 100% = 30.4%

### Scan Scope

**工程根**：`D:/LocalGameProject/Unreal/Catfishing`（engine=unreal 5.8，tech_stack=C++/Blueprint，模块 Catfishing / CatfishingEditor）。代码以 HEAD `bcc7df9` 工作区文件为准（基线 `87ba975`，其间 83 个提交）。设计镜像 `C:/Users/lzcm7/OneDrive/GameProject/CatFishing/Feishu`（导出 2026-09-11T17:57），只读、未作任何修改。

**本轮必须重扫的原因**：`Source/Catfishing/Fishing/Simulation/CatFishingGroupModel.*` 与 `Source/Catfishing/Items/CatItemsService.*` 等整文件删除，`CatFishPickupActor` 从 `Items/World/` 移到 `Items/Fish/`，`CatFishingFightRunner`／`CatFishingService`／`CatFishingRodActor` 大幅重写并新增 `Interaction/Grab/` 与 `AbilitySystem/Physics/`，`Config/DefaultGame.ini` 改动 82 行——基线报告的 code_ref 与行号大半失效，本轮逐行重新定位。

**扫过的目录与文件类型**

- `Source/Catfishing/Environment/**`（.h/.cpp）：ChumFieldTypes/Subsystem/Settings/PlacementService/Anchor/ReplicationComponent/Presentation，`CatWaterTypes.h` 的 `FCatChumVector`，`CatEnvironmentSettings`。
- `Source/Catfishing/Fishing/**`（.h/.cpp，排除 `Tests/`）：`CatFishingSession`、`CatFishingService`、`CatFishingSettings`、`Actors/`（RodActor、FishEncounterActor、ActorTypes）、`Simulation/`（FightRunner、FightSimulator、BiteTimingModel、FightWorkModel、OperatorWorkModel、RodResistanceModel）、`Integration/`（CommandComponent、CommandTypes、AimLibrary、PhysicalRodComponent）、`Config/`（FightBalanceDefinition）。
- `Source/Catfishing/Interaction/Grab/`（本轮新增的物理抓握）、`Source/Catfishing/Character/Physics/CatPhysicalBodyComponent.*`、`Source/Catfishing/Character/CatCharacterMovementComponent.cpp`。
- `Source/Catfishing/AbilitySystem/`：`Config/CatPhysicalEffortSettings`、`Physics/CatPhysicalEffortComponent`、`Core/CatAbilitySystemComponent`、`Tags/CatFishingAbilityTags`、`BodyAction/Social/`。
- `Source/Catfishing/Social/`、`Source/Catfishing/Items/Fish/CatFishPickupActor.*`、`Source/Catfishing/Collection/`、`Source/Catfishing/Data/`（CatFishCatalogSettings、CatFishDefinition、CatFishSelectionTypes）、`Source/Catfishing/Equipment/`（Fragments 五件）、`Source/Catfishing/UI/HUD/`、`Source/Catfishing/UI/CatFishingViewTypes.h`、`Source/Catfishing/Online/`、`Source/Catfishing/Framework/Game/`、`Source/Catfishing/Camp/`、`Source/Catfishing/Condition/`。
- `Config/DefaultGame.ini` 全段（重点 `CatFishingSettings`、`CatChumFieldSettings`、`CatFishCatalogSettings`、`CatSocialSettings`、`CatInteractionSettings`）；确认无 `CatPhysicalEffortSettings` 段，故该组数值取类默认。
- `Scripts/create_fishing_fight_balance_asset.py`（读取资产落值，发现 `helper_strength_multiplier` 已是过期键）。
- 代码侧台账：`Docs/Development/工程自补决策记录.md`（D-01…D-32 与 09-09 复核段通读）、`Docs/Development/需求对齐差距清单.md`（§4 与 09-09 拍定表，只作线索、未作为 ✅ 依据）。
- 设计侧：`GDD 系统分册/钓鱼系统/钓鱼规则.md` §2 全文（v1.8，rev 333）、`多人钓鱼附篇.md` 全文（v1.3，rev 82）、`裁决同步/设计修改记录.md`（09-06 至 09-10 全段）、`数值模拟与参数记录.md`（只作参考、不评分）。

**检查方法**

Grep 定位 ＋ Read 片段核语义，改写重的文件（RodActor.h、FightRunner.h、PhysicalEffortComponent、GrabComponent.h、BiteTimingModel）整节通读。检索键：类名（`UCatChumFieldSubsystem`、`UCatChumPlacementService`、`FCatFishingBiteTimingModel`、`ACatFishingRodActor`、`UCatFishingFightRunner`、`UCatPhysicsGrabComponent`、`UCatPhysicalEffortComponent`）、字段名（`HelperStrengthMultiplier`、`MaximumRodOperatorSlots`、`OperatorPlayerStates`、`EffectiveChumVector`、`SlackStaminaRegenPerSecond`、`ExhaustionResumeRatio`、`bCooperative`）、日志事件名（`chum_field_prepared`、`chum_throw`、`chum_inventory_consumed`、`fishing_bite_scheduled`、`fishing_legacy_assist_rejected`、`physical_effort_settled`、`physics_grip_traction`）、Gameplay Tag（`Cat.Input.Fishing.*`、`Cat.Cosmetic.Fishing.*`）、中文注释（打窝、蓄力、聚鱼、渔网、死水、换人、合力、贡献、图鉴、零体力）、ini 段名。

**跳过的东西 / 查不了的东西**

- `Content/` 下 `.uasset`/`.umap` 未做文本扫描（资产，属可检查程度 ③④）。因此以下事实本轮**查不了**、不作为「未实现」依据：
  - 数值资产落值：`DA_FishingFightBalance_Default`、窝料 `ChumInfluence`（半径、时长、两条衰减曲线）、鱼种 `ChumPreference`/`BaitMultiplier`/可捞圈半径、`Bite_*` 性格资产。仅在有生成脚本可读时才引用落值。
  - InputAction / IMC 资产里的实际按键绑定（#38 的 R vs F、#41、#46 的 E）；判据是 C++ 侧意图 Tag、服务端半径常量与代码注释。
  - 蓝图 Widget、Montage、Niagara、GameplayAbility 的图逻辑与表现（#10、#54 判 ❓ 即出于此；#8、#57 的画法同理不改判为未实现）。
- 运行时行为（#68 的「被拖是否拖慢队伍」、物理求解的实际手感）需 PIE 或专项测量，本轮未跑；`Scripts/verify_*` 未执行。
- `Source/Catfishing/**/Tests/`（本轮增删极多）只用于交叉印证接口形状，未把测试内行为当作正式链路证据。
- 评分范围外的设计条目（钓鱼规则 §1、§3～§7 与附录）未逐条列入，仅在多人附篇引用它们时作为上下文读取；`数值模拟与参数记录.md` 只作参考、不产生要求行。

**Phase 2b 复核补扫**（原扫描清单之外、由复核者另开的文件与检索）

- `Source/Catfishing/Physics/Simulation/CatIntentMotionModel.*`（原清单未列的目录，#64 的米价结算实体）、`Source/Catfishing/Character/CatCharacterMovementComponent.cpp:48-78`（站桩折算）、`Source/Catfishing/Fishing/Simulation/CatFishingOperatorWorkModel.cpp` 全文（18 行）。
- `ACatFishingSession::CutLineFromAuthority`（CatFishingSession.cpp:1850-1920）与 `RefreshBiteAvailabilityFromAuthority`／`ScheduleWaitingProbeFromStateTree`（:599-680），用于核 #45、#16、#30。
- `ICatInteractable` 的全部实现者枚举（grep `public ICatInteractable`，8 个：Altar／CampInventory／FishGuard／FishTank／CatItem／FishPickup／FishBuyer／ShopKiosk），用于核 #41。
- `Source/Catfishing/Camp/`、`Source/Catfishing/Run/`、`Source/Catfishing/Framework/` 的 `IsHost`／`HostPlayerState`／`房主` 检索，用于核 #33 的献祭一侧。
- `Docs/Development/工程自补决策记录.md` 决策台账全表（D-01…D-32 与 09-09 复核段）与 `需求对齐差距清单.md` §4 及「09-09 下午拍定」表；`裁决同步/设计修改记录.md` 09-07／09-08／09-09／09-10 四段逐条读，核对报告引用的每条裁决日期与内容。
- `git rev-list --count 87ba975..HEAD` = 83，与报告表头一致。

### Code-only mechanics

| 机制 | code_ref | 文档现状与出处 | 待确认问题 |
|---|---|---|---|
| 持续物理抓握协作：任何猫可用左右手抓住鱼竿或队友身体，服务端建约束并施加等量反向牵引力（注释：`Force on target; the holder receives the equal and opposite force. No fishing membership or stamina sum.`），协助者按自身满力量出力、按「意图缺失位移」单独扣体，力竭即自动松手 | Source/Catfishing/Interaction/Grab/CatPhysicsGrabComponent.cpp:581-637；Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:19-108；Source/Catfishing/Fishing/CatFishingSession.cpp:181-196 | 未写。多人附篇 §2.1～§2.3（多人钓鱼附篇.md:35-57）只有「辅助位入位、力量 75%、K_pos 1.5」这一套；设计修改记录 09-09「操作模式方向」只说了上竿/离竿操作位是临时的，没有裁过物理抓握这条协作路径 | 合力钓鱼改成物理抓握是正式方向（那 §2.1～§2.3 整节要重写、75%／K_pos 退役）？还是回到辅助位模型？ |
| 抽鱼第一步按「挑战档三带」（Comfort/Matched/Risky）roll，档内再算窝料/饵权重；档位阈值与权重在 ini | Source/Catfishing/Data/CatFishCatalogSettings.cpp:245-281；Config/DefaultGame.ini:91-98 | 设计已裁删但代码未删：设计修改记录 09-09 晚「代码超前项讨论处置」的「删代码七条」首条即挑战档三带；钓鱼规则 §2.3（钓鱼规则.md:70）只有「三类占比 → 饵权重」两步 | 已裁的删除动作何时执行？删挑战档之后 D-31 登记的「保留完整筛选顺序」是否一并销账？ |
| 鱼饵直接改咬钩等待：`BiteRateMultiplier` 乘采样速率、`MinimumBiteDelayMultiplier` 乘最小静默时长 | Source/Catfishing/Fishing/CatFishingSession.cpp:694-701；Source/Catfishing/Equipment/Fragments/CatEquipmentFragment_Bait.h:21-27 | 未写。钓鱼规则 §2.3（钓鱼规则.md:74）明确「饵的两个作用分开」＝种类权重在抽鱼时消费、数量在真咬时扣，没有第三个「饵改咬钩间隔」的作用；鱼饵表也无此列 | 饵影响咬钩速度是要补进 §2.2／道具册鱼饵表，还是该删掉这两个倍率？ |
| 打窝落点的视角偏离拒绝：服务端要求视线方向与「视点→落点」夹角不超过 `MaxAimDeviationDegrees`（现值 60°），超出与超程共用 `PlacementOutOfRange` 错误码（复核补行） | Source/Catfishing/Environment/CatChumPlacementService.cpp:164-169；Config/DefaultGame.ini:260 | 未写。钓鱼规则 §2.1（钓鱼规则.md:43）投料一节只有射程与超程提示，§3.1 抛竿（钓鱼规则.md:121）也只有射程与遮挡两项，全库设计页无「瞄准夹角」这个量；它与遮挡拒绝同批加在同一段代码里，但 09-09 只裁了遮挡那条 | 视角偏离 60° 是正式设计约束（要和射程、遮挡并列写进 §2.1／§3.1 并给单独提示），还是纯工程护栏？若保留，它和超程共用一个错误码导致玩家看到的提示相同，要不要拆开？ |
| 打窝落点的视线遮挡拒绝 `PlacementOccluded`（服务端从视点到落点打一条线） | Source/Catfishing/Environment/CatChumPlacementService.cpp:170-177 | 半写。09-09 已裁「抛竿加视线遮挡拒绝并提示『够不到那边』」并落进钓鱼规则 §3.1（钓鱼规则.md:121），但 §2.1 投料一节（钓鱼规则.md:43）只写了射程与超程提示 | 投料的遮挡拒绝与抛竿同口径吗？要不要在 §2.1 补一句（提示文案是否也沿用「够不到那边」）？ |
| 单次投放份数：`ChumThrowQuantity` 与窝料资产 `MaximumQuantityPerPlacement` 两道闸 | Source/Catfishing/Fishing/CatFishingSettings.h:122；Source/Catfishing/Environment/CatChumFieldTypes.h:61-62；Source/Catfishing/Environment/CatChumPlacementService.cpp:135-143 | 未写。钓鱼规则 §2.1（钓鱼规则.md:35-41）只讲「按配方加存量、叠加无上限」，没有「一次投几份」这个量；参数页携带上限行只给了窝料 5 份 | 一次投放份数是正式设计量（要写进 §2.1 与窝料表），还是纯工程参数？ |
| `ECatFishingCommandType::TailRescue` 与 `FCatTailRescueCommand` 残留：只有枚举与空结构，无任何服务端处理链 | Source/Catfishing/Fishing/Integration/CatFishingCommandTypes.h:12,141 | 设计已裁不做：多人钓鱼附篇 §3.2（多人钓鱼附篇.md:81）写「救援不是玩法，不列救援资格」；钓鱼规则 §7.1（钓鱼规则.md:357）写「不承诺未实现的拽尾巴救援」 | 这两个死声明可直接删（不需要设计判断），确认一下有没有蓝图在引用该枚举值？ |

### 复核记录

Phase 2b 独立复核（HEAD `bcc7df9`，工作区文件；`git rev-list --count 87ba975..HEAD` = 83，与表头一致）。全部 74 条原始要求行逐行重核，未抄基线、未抄被复核报告的任何状态或 code_ref。

**逐行驳斥范围（全量，非抽样）**：59 条 ❌／🔄／⚠️ 全部独立找实现——❌ 25 条（#4、5、9、11、14、16、20、22、24、26、27、29、30、31、35、36、41、46、47、48、60、61、62、70、71）、🔄 19 条（#1、2、7、12、13、17、18、19、25、28、34、38、42、49、50、52、58、64、69）、⚠️ 15 条（#3、8、21、37、39、40、44、53、56、59、63、65、66、67、68）。每条至少换两组检索键重搜（类名／字段名／日志事件名／Gameplay Tag／中文注释／ini 段名），命中处 Read 原文核语义。结论：**59 条全部维持原判，零改判**。关键的负向结论都用零命中检索坐实：`MergeField`／`ExistingField`／`并窝`、`Deduct`／`ConsumeChum`／`扣存量`、`渔网`／`CastNet`／`DeadWater`／`死水`、`聚鱼`／`Frenzy`／`School`、`HelperStrength`／`K_pos`／`MaximumRodOperatorSlots`、`GroupModel`／`龟速`／`打转`／`顶牛`／`拔河`／`甩飞`／`摔倒`、UI 侧 `濒死`／`LowStamina`／`队友`／`CombinedFishingStrength`／`ActiveHelperCount`，均在 `Source/` 与 `Config/` 零命中（`helper_strength_multiplier` 只剩 `Scripts/create_fishing_fight_balance_asset.py:28` 一处过期键，与原报告一致）。

**✅ 抽验：13 行，即全部 ✅（超出五行下限）**。#6、15、23、32、33、43、45、51、55、57、72、73、74 逐个 Read 到行核语义，不只核文件名。其中 #45 的第二个 code_ref 被驳倒（见下表）；#33 的设计要求点名「钓鱼、移动、献祭」三处，原报告只给了打窝／抄网／拾取三条证据，复核另查献祭一侧补齐；其余 11 行位置与语义都对得上，多条行号精确到个位（#32 的 `CatOnlineSubsystem.cpp:1407`、#42 的 `CatFishingSession.cpp:1454`、#43 的 `:1522-1532`、#74 的 `CatProfileContracts.h:9-19`）。

**❓ 抽验：2 行，即全部 ❓（该状态只有两行，不足五行，按实际数量记，不虚构抽样）**。#10 与 #54 都复查是否把可文本检查的东西推给了「查不了」：#10 的 `CatChumFieldPresentationActor.cpp` 全文读过，C++ 侧确实只有引擎自带 Cube/Sphere 占位网格与三个 `BP_OnField*` 钩子，`Environment/` 下 `涟漪`／`Ripple`／`鱼影`／`FishShadow` 零命中，涟漪与鱼影只能落在 `PresentationClass` 指向的蓝图与材质里（可检查程度 ④），维持 ❓；#54 的 `CatRunImprintService::CreateCapturePlansForParticipants`（:141-186）读过，只有候选校验与投递编排，慢镜头／构图／举鱼动作无 C++ 承载，维持 ❓。两行都没有把静态可查的字段或接线推给 ❓。同时反查了另一个方向：#8 与 #57 把「玩家提示画法」「HUD 最终画法」留在蓝图但仍按已查到的 C++ 事实评分，没有因为含不可读部分就整行推 ❓，这个处理是对的。

**裁决账本与工程台账复核**：`裁决同步/设计修改记录.md` 09-07／09-08／09-09／09-10 四段逐条读过。09-09 下午八问⑤「窝点模型挂起，两套都不动，等窝料与聚鱼内容验收」属于**挂起实施、未改设计文本**，因此 #1～#5、#11、#13、#16、#22、#24、#25 仍按现行设计页评分，原报告的处理正确；09-09 晚「删代码七条」首条＝挑战档三带、「操作模式方向」＝上竿／离竿操作位临时且抛投入口挂装备栏重构、09-09「抛竿加遮挡拒绝并提示够不到那边」已落进钓鱼规则 §3.1（钓鱼规则.md:121 原文核对无误）、09-08 晚⑤「窝料表目标鱼种加权列」、09-08 一致性检查③「连环摔倒恰 4 人且不产生搏斗结局」——报告引用的这五条日期与内容全部属实。09-09 八问⑥把搏斗计费对象改按代码，但同条明写「移动按秒常数、搏斗外 5 点/秒且搏斗中不恢复、放线回体基础 0 三条不变」（需求对齐差距清单 §4「09-09 下午拍定」表同记），因此 #64／#65／#67 的 🔄／⚠️ 不因该裁决翻转。`工程自补决策记录.md` 通读 D-01…D-32 与 09-09 复核段：D-30「挂起」、D-31「暂定，取值已漂移未回写」、D-25「待裁」、D-13、D-05、D-02 的编号与状态引用无误；确认**没有任何 D 条目登记 Q 键蓄力打窝（#7）、搏斗态补窝闸门缺失（#12）或持续物理抓握协作（代码超前项首行）**，原报告「无 D 条目登记」的断言成立。`需求对齐差距清单.md` 只作线索，未拿其完成记录替代任何 ✅ 的 code_ref。

**漏项补行**：对照钓鱼规则 §2.1～§2.6 与多人钓鱼附篇全篇逐句回扫，发现两条可独立判定、原报告未列的要求，按 #75／#76 追加（原行号不重排），两条都实测为 ✅；Code-only 表补一条打窝视角偏离 60° 拒绝——它与已登记的遮挡拒绝写在同一段代码里，但 09-09 只裁了遮挡那条，且它与超程共用 `PlacementOutOfRange` 错误码，属需要设计回答的代码超前项。

**未能完成的检查**：Blueprint／Widget／Montage／Niagara 图逻辑、InputAction 与 IMC 的实际键位绑定、`Content/` 下数值资产落值（窝料 `ChumInfluence` 两条衰减曲线、`DA_FishingFightBalance_Default`、鱼种 `ChumPreference`／`BaitMultiplier`）仍属可检查程度 ③④，复核同样未走引擎入口，一律不作为「未实现」依据；PIE 与 `Scripts/verify_*` 未执行，#68 的「被拖是否拖慢队伍」等运行时量仍无结论。这些限制与原报告一致，本轮未新增可检查项。

| # | 原判 | 改判 | 依据 |
|---|---|---|---|
| 30 | ❌ Missing | ❌ Missing | 状态维持。Notes 证据更正：原文「Probe/Warning 定时器只有 Set/Clear，没有 pause/resume 接口」不成立——`RefreshBiteAvailabilityFromAuthority`（CatFishingSession.cpp:599-621）在 `CanGenerateNewFishingBites()` 转假时 Clear 双定时器、恢复时 `ScheduleWaitingProbeFromStateTree` 整条重排，是一条真实的清/重排通道。但它按昼夜而非窝点划范围，且重排是重新采样而非折算剩余时间，构不成死水挂起；渔网与死水本身（`渔网`／`CastNet`／`ThrowNet`／`DeadWater`／`死水` 在 `Source/`＋`Config/`＋`Scripts/` 零命中）仍缺失，❌ 成立 |
| 33 | ✅ Implemented | ✅ Implemented | 状态维持，证据补齐。设计点名的三处特权里「献祭」原未取证：复核对 `Source/Catfishing/Camp/`、`Run/`、`Framework/` grep `IsHost`\|`bIsHost`\|`HostPlayerState`\|`房主`，只命中 `Run/Debug/` 的调试面板中文文案与 `CatRunContracts.h:37,57` 的房主退出 teardown，祭坛与翻天入口无身份 gate，与打窝（CatChumPlacementService.cpp:40-86）、抄网（CatFishingSession.cpp:330-345）、拾取（CatFishPickupActor.cpp:1046-1069）同口径 |
| 45 | ✅ Implemented | ✅ Implemented | 状态维持，**code_ref 被驳倒并更正**。原引 `CatFishingCommandComponent.cpp:1280` 说「`CutLine` 同样要求 `Rod->IsPrimaryOperator`」：该行实际位于 `BeginCastFromViewOnAuthority`（函数起点 :1260）的抛竿主位校验，与切线无关；CutLine 在命令层（:1037-1071、:1227-1248）根本不查主位。真正的闸门在 `ACatFishingSession::CutLineFromAuthority`（CatFishingSession.cpp:1885-1902）：放行「本人＝当前 `FisherPlayerState`」，或「竿 `Grounded` 且 `GetOperatorCount()==0` ＋ 本人是 `OwnerPlayerState` 或 `LastSuspendedFisherPlayerState` ＋ 250 cm 内」。后者不是队友通道，正是 09-09 八问③裁的「主位主动离竿才进无人值守放线，走回竿边一定距离内可切线」，故 ✅ 仍成立 |
| 50 | 🔄 Divergent | 🔄 Divergent | 状态维持（`ArchiveCommittedCapture` 把 `FishRecorded` 授给 `PickerStableNetId`，CatFishPickupActor.cpp:1088，实测无误）。Design Requirement 出处更正：「收集层归上钩者」是 09-07 晚 N8「图鉴两层制」所裁（设计修改记录 09-07 段），不是 09-08 晚；09-08 只在补缺口轮的换人条里重申「原始上钩者归属不变」 |
| 64 | 🔄 Divergent | 🔄 Divergent | 状态维持，**code_ref 被驳倒并更正**。原引 `Source/Catfishing/Fishing/Simulation/CatFishingOperatorWorkModel.cpp:29-44` 指向不存在的行——该文件全长 18 行。实际链路：`FCatFishingOperatorWorkModel::ComputeMovementStaminaDrain`（:3-17）只做入参校验与单位换算，米价结算在 `Source/Catfishing/Physics/Simulation/CatIntentMotionModel.cpp:22-30`（把实际位移投影到意图方向、缺口 cm ÷ 100 × 米价），站桩的等效意图折算在 `Source/Catfishing/Character/CatCharacterMovementComponent.cpp:54-62`（`SupportReferenceSpeedCmS`）。原 Notes 对机制的描述本身是对的，只是引错了落点 |
| 75 | 新增 | ✅ Implemented | 漏项补行。钓鱼规则.md:68 的反模式护栏「主人由到点的浮漂确定，不为窝点先抽一条公共鱼再比各人的饵，也不在多个饵命中的玩家之间随机分钩」原报告未列。选鱼是 `ACatFishingSession` 的实例方法，饵只读本场 `AttemptSnapshot.BaitDefinitionId`（CatFishingSession.cpp:884）、装备只读抛竿者预留的 `CastEquipment`（:863），逐会话调 `SelectRuntimeDefinition`（:896）；全库无按窝点/水域聚合的公共抽取，也无跨玩家分钩仲裁 |
| 76 | 新增 | ✅ Implemented | 漏项补行。钓鱼规则.md:70 句末「随机区间的端点算在区间内」（09-08 补缺口轮与「候选为空落基础池」同批所裁）原报告未列。挑战档轮盘 `BandCursor <= 0.0`（CatFishCatalogSettings.cpp:276）与鱼种轮盘 `Cursor <= 0.0`（:331）都取闭区间，并保留「同带最后一个候选」的确定性尾端回退（:329 注释）；等待采样 `FCatFishingBiteTimingDistribution::TrySample` 接受 `UnitRandom ∈ [0,1]`（BiteTimingModel.cpp:80）且 `== 1.0` 单独映射到截顶值（:86） |
| Code-only 新增行 | 新增 | — | 代码超前项补行：打窝落点的视角偏离拒绝。`MinimumAimDot = cos(MaxAimDeviationDegrees)`，服务端要求视线方向与「视点→落点」夹角 ≤ 60°，否则返回 `PlacementOutOfRange`（CatChumPlacementService.cpp:164-169；Config/DefaultGame.ini:260）。它和已登记的 `PlacementOccluded` 写在同一段、同一批加入，但 09-09「保留并补文档三条」只裁了遮挡那条；钓鱼规则 §2.1（:43）与 §3.1（:121）都没有「瞄准夹角」这个量，且它与超程共用同一个错误码，玩家侧提示无法区分 |
