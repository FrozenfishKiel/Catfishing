## 钓鱼系统-核心

### Features

| # | Design Requirement | Status | Code Reference | Notes |
|---|-------------------|--------|---------------|-------|
| 1 | 一竿流程七阶段：备装→可选打窝→抛竿→等待/试探→真咬/提竿→搏斗→收鱼/失败（钓鱼规则.md:23） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingTypes.h:9-31；Source/Catfishing/Fishing/CatFishingSession.cpp:90-130 | 本轮重核：`ECatFishingPhase` 仍是 11 值（Created/Probe/TrueBiteWindow/HookedFight/NearShore/Resolved/Terminated/CastFlight/Waiting/AutoHauling/ExhaustedReel），`EnterPhaseFromStateTree` 是唯一写口并对 NearShore/HookedFight 做前置校验；转移拓扑在 ST_FishingSession 资产（④，未逐边核对） |
| 2 | 装备即状态：手持鱼竿就是钓鱼待机，不设模式键（钓鱼规则.md:25） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingService.cpp:189-192,627-700；Source/Catfishing/Fishing/CatFishingService.cpp:728-767 | 重核维持。抛竿硬前置仍是「未部署、已断竿、或操作位不是本人」三取一即拒 `RodOccupied`；`OperateRod`（R 键，要求本人是竿主且距握把 250cm 内）与 `LeaveRod` 构成显式上竿/离竿模式键。设计 §1 自己标注本段为临时口径，09-09 晚裁决也把代码的操作位定为临时（方向是装备栏每槽一道具＋使用键），两边都待装备栏重构后重写 |
| 3 | 准备态可走动/切饵；搏斗中不能直接退，只能走主动放弃（钓鱼规则.md:25） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingService.cpp:728-767；Source/Catfishing/Fishing/CatFishingService.cpp:1104-1118；Source/Catfishing/Fishing/CatFishingSession.cpp:1636-1689 | 重核维持。`LeaveRod` 在搏斗中只在固定步边界内临时拒（`fishing_membership_busy`，Retryable），否则照常离竿并走 `SuspendOperatorFromAuthority`→`BeginUnattendedSlackFromAuthority`；准备态走动/切饵 ✅。「主位离竿」这一半 09-09 已进设计，单列第 100 行 |
| 4 | 抄网与鱼护都不是必带，准备由各玩家独立进行（钓鱼规则.md:25） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:346-349,436-449；Source/Catfishing/FishContainers/CatFishGuardActor.cpp:120-170 | 无抄网只让 `bScoopReachReady=false` 在抢抄处被拒，不进抛竿前置；鱼护已重构为独立库存物品＋世界容器 Actor，同样不入抛竿前置 |
| 5 | 左键点水面抛竿，无力度条、无蓄力，任意合法水边可抛（钓鱼规则.md:119） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingService.cpp:243-250；Source/Catfishing/UI/CatFishingViewTypes.h:37 | `BeginCast` 消费客户端候选点并由 `ResolveCandidatePointToWater` 吸附水面；`PrimaryPowerAlpha` 仍标注 "charging was removed"；站位不设合法区，只查落点、射程与视线 |
| 6 | 超射程与遮挡都拒绝抛竿并提示「够不到那边」（钓鱼规则.md:121；v1.7 补遮挡） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingService.cpp:255-277 | 两条拒绝都在：射程走 `CastOutOfRange`＋`cast_range_rejected` 日志（含 DistanceCm/MaximumCm），遮挡走视点到落点的 `LineTraceSingleByChannel` 后返回 `InvalidWaterTarget`。差的是「提示相同」：两条用的是不同错误码，且玩家可见文案在 WBP（④ 查不了） |
| 7 | 射程由漂提供（点击点到猫站立点的距离 D_click 不超过浮漂射程）（钓鱼规则.md:121） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingService.cpp:255-258 | 重核维持：`MaxRange = min(竿 MaximumLineLengthCentimeters, 漂 MaximumCastDistanceCentimeters)`，量的是 `ToLandingFromRod`（竿尖→落点）而不是猫站立点，与第 34 行同一口径问题。注意本轮 `Scripts/update_fishing_cast_ranges.py` 已从工作区删除，三档射程现值只在漂资产里（③ 查不了），09-09 裁决的 10/15/20 米无法再按 ② 证据核对 |
| 8 | 实际落点按漂精准度在点击点周围均匀圆盘随机偏移（高/中/低 = 0.5/1/1.5 米）（钓鱼规则.md:121） | ❌ Missing | — | 重核维持。搜过 `CastErrorStandardDeviationCentimeters`/`MaximumCastErrorRadiusCentimeters`/`Accuracy`/`FloatAccuracyOffsetRadiusMeters`（Source+Config+Scripts）：两个字段只在 `CatEquipmentFragment_Float.cpp:8-12` 的就绪校验里出现，零运行消费者；落点＝客户端点位吸附水面，点哪落哪。D-23 的落盘脚本与本轮的 `update_fishing_cast_ranges.py` 都已不在工作区 |
| 9 | 偏出水面（落岸）判空竿收回、不损饵、可重抛；超射程后偏移越界收敛到射程圆边界（钓鱼规则.md:121） | ❌ Missing | — | 重核维持。依赖第 8 行：无散布即无落岸分支。`ResolveCandidatePointToWater` 判定 Outside 直接以 `InvalidWaterTarget` 在 BeginCast 拒绝（CatFishingService.cpp:246-251），根本不建会话，也就没有「空竿收回」这一步 |
| 10 | 以实际浮漂落点按窝点归属选窝点，不在窝内按空池间隔（钓鱼规则.md:125） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:702-716 | `SampleChumAtPoint(AttemptSnapshot.ServerCorrectedLandingWorldPoint, …)`，采样失败按无窝调度并打 `fishing_bite_chum_sample_failed`；窝点数学本身归 §2 报告 |
| 11 | 不设离开判定、不自动收竿（钓鱼规则.md:127） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:623-762 | 等待/试探链路只有 BiteWarning/Probe 两个计时器与夜间门，无距离或离开触发的收竿分支 |
| 12 | 计时期间可切饵、不重置计时；真咬扣的是当时挂着的那 1 份（钓鱼规则.md:127） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingService.cpp:289-296；Source/Catfishing/Equipment/CatEquipmentComponent.cpp:1063-1112 | 重核维持：饵在 `BeginFishingUse` 抛竿时按实例 ID 冻结并从库存预扣一份，等待期换饵不改本竿用饵；设计要求到点消费「当时挂着的」饵 |
| 13 | 浮漂归属在落水瞬间定死、不随漂移重算（钓鱼规则.md:127） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:704-707 | 窝料采样固定用冻结的 `ServerCorrectedLandingWorldPoint`，不随 Hook Actor 位置重算 |
| 14 | 无饵不能抛竿，鱼饵随抛竿挂出（钓鱼规则.md:133） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingService.cpp:232-241；Source/Catfishing/Equipment/CatEquipmentComponent.cpp:1094-1097 | 缺 Bait 定义、`CanServeFishingBait()` 不成立或 `bRunConsumable=false` 一律拒绝预留 |
| 15 | 咬钩前主动收竿零损失；进入咬钩后无论上鱼/超时/放弃/断竿/落水都消耗 1 份饵（钓鱼规则.md:133） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:975-983,2041；Source/Catfishing/Fishing/CatFishingSession.cpp:1004-1016 | 重核维持：零损失收竿 ✅（`FinalizeSession` 统一 `ReleaseFishingUse` 归还预留）；扣饵时点仍是「玩家在真咬窗内成功提竿并选出鱼」时的 `CommitFishingBaitDeferred`，响应窗超时回 Waiting 且不扣饵（见第 25 行）。D-10 已登记响应窗结局缺口 |
| 16 | 携带 8 份饵＝8 次咬钩机会（钓鱼规则.md:133，快照值） | 🔄 Divergent | Config/DefaultGame.ini:63 `DefaultQuantityStackCapacity=5`；Source/Catfishing/Inventory/CatInventorySettings.h:37,69 | 本轮字段随库存重构改名（原 `CatEquipmentSettings.InventoryQuantityStackCapacity`→`CatInventorySettings.DefaultQuantityStackCapacity`），值仍是 5 且仍是单一全局上限，表达不了「鱼饵 8／窝料 5」两档。D-13 登记的取值与位置已过期，需按新字段重登记。复核更正行号：该键在 ini 第 63 行（第 62 行是 `PlayerInventorySlotCapacity=24`） |
| 17 | 试探期提竿必空竿、不损饵，且空竿揭图鉴剪影（钓鱼规则.md:133,141） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:1763-1771,2041 | Waiting/Probe 提竿发 `EarlyHook` 并 `FinalizeSession(Terminated, EmptyHook)`，饵按预留归还 ✅；选鱼事务在 1772 行之后的 TrueBiteWindow 分支才跑，此刻 `FishDefinition == nullptr`，无从揭剪影——而且本轮剪影 Grant 已彻底没有生产者（见第 73 行） |
| 18 | 从咬钩成立到本竿结局落定禁止主动掏用道具，抄网除外（钓鱼规则.md:135） | ❌ Missing | — | 重核维持。搜过 `CanUseItemAtSlot`/`CanUseFromInventory`/`ActivationBlockedTags`/`BlockAbilitiesWithTag`/`IsFishingBusy`：`CanUseItemAtSlot`(CatInventoryComponent.cpp:2538-2562) 只查槽位与实例，转交的 `UCatEquipmentInventoryItemInstance::CanUseFromInventory`(CatEquipmentInventoryItemInstance.cpp:134-152) 只查定义能力与角色组件；全库 `ActivationBlockedTags` 零命中，GAS 侧无任何屏蔽关系 |
| 19 | 竿耐久不能回补、竿不可维修（钓鱼规则.md:135,204） | ✅ Implemented | Source/Catfishing/Equipment/CatEquipmentInventoryItemInstance.cpp:47-67,275；Source/Catfishing/Fishing/CatFishingSession.cpp:1125-1135 | 本轮改判（原 🔄）：营地修竿链已随本轮重构删除——全库 `repair`/`RestoreRod`/`DurabilityRestore` 零命中（唯一 "repairs" 命中在物理原型 Controller 注释里），`Camp/` 下也不再有 `CatEquipmentCommandCoordinator`。现在 `SetRodRuntimeStateFromAuthority(MaximumRodDurability,false)` 只在实例首次初始化时调用；开场读的是同一实例的剩余耐久，注释写明「定义上限仅用于新购和维修，开场不能补耐久」。D-21/D-22 的跨场累计仍成立 |
| 20 | 演出时序：抽中瞬间鱼影出现（按真鱼体型）→浮漂轻点进入试探期→漂猛沉即真咬（钓鱼规则.md:141） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingTypes.h:16；Source/Catfishing/Fishing/CatFishingSession.cpp:833-1002,1772-1790 | 重核维持：`ECatFishingPhase::TrueBiteWindow` 的注释仍写「此时只存在浮漂信号，鱼种与鱼 Actor 要等合法左键到达服务器后才创建」，选鱼＋生成 Encounter 整体在 `RequestHookFromAuthority` 之后，因此试探期与真咬期都没有鱼实例，没有「抽中即出鱼影」。浮漂三态 Calm/BiteWarning/Sink 在 Presentation/CatFishingPresentationSettings.h |
| 21 | 试探期时长 2～4 秒随机（钓鱼规则.md:141，占位快照） | ⚠️ Partial | Source/Catfishing/Data/CatFishPersonalityDefinition.h:15；Source/Catfishing/Data/CatFishPersonalityDefinition.cpp:5；Config/DefaultGame.ini:212 `BiteWarningSeconds=1.5` | 重核维持：`ProbeDurationSeconds` 全库两处命中都是就绪校验，零运行消费者；Probe 停留时长由 ST_FishingSession 的 Wait Task 决定（④ 查不了）。同资产上的 `PerfectHookWindowSeconds` 确被消费（第 23 行），可见是单独这一项没接 |
| 22 | 真咬后的响应时长按鱼种为 8～15 秒（钓鱼规则.md:141） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:800,820,829；Config/DefaultGame.ini:199 `TrueBiteWindowSeconds=3.0` | 重核维持：用全局 3 秒；鱼种级 `UCatBitePersonalityDefinition::TrueBiteWindowSeconds` 仍只在就绪校验里出现（CatFishPersonalityDefinition.cpp:8），零运行消费者。D-10 登记的 10 秒占位仍未回写 |
| 23 | 前 1 秒为完美窗，用服务器时间戳判定，恰好 1 秒算完美（闭区间）（钓鱼规则.md:141,149） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:1775-1797 | `SinceBite = 服务器时间 - PhaseStartedServerTime`，先冻结再停表（选鱼耗时不影响判定），`SinceBite <= PerfectHookWindowSeconds` 闭区间，注释显式拒收客户端上报反应时间；1 秒这个值本身在 Bite 资产里（③） |
| 24 | 完美削减系数：普通鱼力量×0.8 体力×0.85，稀有鱼×0.85/×0.9，按鱼册稀有度最高档取稀有系数（钓鱼规则.md:145,149,295） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:1049-1053；Source/Catfishing/Data/CatFishPersonalityDefinition.h:18-20；Source/Catfishing/Data/CatFishDefinition.cpp:36 | 重核维持：三个倍率全按 `BitePersonalityId`（Cautious/Steady/Aggressive/Giant 四个模板）取，`RarityTierId` 只参与 `IsRuntimeDefinitionReady` 校验、不进任何取值。09-09 晚另裁「四套性格模板是测试用、正式口径逐鱼配」，该条也未落地 |
| 25 | 超时：响应窗结束仍未提竿则鱼吐钩逃跑、饵已消耗（钓鱼规则.md:147） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:1004-1016,623-640 | 重核维持：`HandleTrueBiteWindowExpired` 只结束「本轮咬钩机会」，发 `WindowExpired` 让 StateTree 回 Waiting 重新派生种子与计时；不扣饵、不结束会话、没有鱼逃终局。函数注释明写「漏按只结束当前咬钩机会，不结束整次架杆会话」。D-10 需改记为待改代码 |
| 26 | 完美削减在入场时直接乘到本场实际值，此后全部计算用削后值（钓鱼规则.md:149） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:1050-1055,1092,1144,1157 | `Config.FishStrength = BaseFishStrength × FishStrengthScale`(1092)、`InitialState.FishStamina = 快照体力 × FishStaminaScale`(1144)、初始线长 × `LineLengthScale` 后再 Clamp(1157)，三项都在 Runner 启动前冻结，之后只读。复核逐行读过，原 code_ref 的 1090 偏 2 行已更正 |
| 27 | 猫体力基础上限 100 点（钓鱼规则.md:157） | ✅ Implemented | Config/DefaultGame.ini:146 `InitialFightStamina=100.0`；Source/Catfishing/Fishing/CatFishingSession.cpp:1055-1063 | 上限从 ASC `MaxFightStamina` 属性读，Fishing 不另建一份 |
| 28 | 猫力品种基准：普通/橘/狸花/缅因＝50/70/90/130（钓鱼规则.md:158） | ⚠️ Partial | Config/DefaultGame.ini:145,149-150 `InitialFishingStrength=50.0`、`+CharacterDefinitions=Cat_Default`；Source/Catfishing/Character/CatCharacterDefinition.h:33-39；Content/Catfishing/Data/Character/ | 重核维持但证据面更新（基线引的 `Scripts/create_default_cat_character_asset.py` 已从工作区删除）：ini 段（②）直接给出全局初值 50，普通档对得上；`UCatCharacterDefinition` 有 `FishingStrength` 字段可分品种，但注册进 `CharacterDefinitions` 的只有 `Cat_Default` 一份，资产目录（①）下也只有 `Cat_Default.uasset`，橘/狸花/缅因三档在数据层不存在 |
| 29 | 鱼力量 F_fish 由鱼表按实际重量给出（实例力量＝重量×K）（钓鱼规则.md:159,295） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:1094；Source/Catfishing/Fishing/Config/CatFishingFightBalanceDefinition.h；Scripts/create_fishing_fight_balance_asset.py:25 | 本轮改判（原 ✅）：「重量×K」这条成立且逐条个体重量确实改变力量；但 K 是平衡资产上的全局 `strength_per_kilogram = 10.0`（落盘脚本 ② 可查），而 09-09 下午李前臻已裁「按设计恢复逐鱼 K（巨影 K5）、全局 StrengthPerKilogram 退出主链」，且 `UCatFishDefinition` 本轮已把弃用的逐鱼 `FishStrength` 字段整体删除，逐鱼 K 连承载列都没有了。D-20 需改记为待改代码 |
| 30 | 鱼体力初始值＝鱼表「体力系数」× 实际重量（钓鱼规则.md:160；09-08 v1.5 裁决） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:995；Source/Catfishing/Data/CatFishDefinition.h:150-152 | 重核维持：`Snapshot.FishFightStaminaRemaining = SelectedDefinition->FishFightStamina`，仍是鱼定义上的静态定额，与重量无关；搜过 `StaminaPerKilogram`/`FightStaminaCoefficient` 零命中。无对应 D 条目，需新登记 |
| 31 | 鱼体力由鱼行为模型按鱼自己的规则维护，钓鱼只读，≤0 即翻肚（钓鱼规则.md:160,316） | 🔄 Divergent | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.cpp:566-577,640-652,686-695 | 重核维持：扣减在钓鱼侧固定步模拟器里算——`FishIntent`（意图位移 − 实际进展）× `FishStaminaPerUnfulfilledMeter`，再由 Simulator clamp 并判 `FishExhausted`；鱼侧 StateTree 只出行为与方向，不写自己的体力。「≤阈值即力竭→FishExhausted/ExhaustedReel」这一半 ✅ |
| 32 | 竿耐久是动态资源，三档上限 40/70/120，归零断竿（钓鱼规则.md:161） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:1112-1135；Source/Catfishing/Equipment/Fragments/CatEquipmentFragment_Rod.h:17-19；Content/Catfishing/Data/Equipment/ | 机制齐（跨场累计、实例回写、`RodBroken` 终局，D-21/D-22）。字段随重构搬到 `UCatEquipmentFragment_Rod::MaximumRodDurability`。数值面本轮退回 ③：`Scripts/configure_starter_rod_tuning.py` 已删除，150 这个值不再有 ② 证据；装备目录（①）仍只有 `Equip_Rod_StarterT1` 与 `Equip_Rod_ShopT2` 两根竿，「三档」在数据层仍不成立 |
| 33 | 竿强度是静态配置，三档 25/60/210；强度不超过总力量与鱼力较小者即当场瞬断，是巨影装备门槛（钓鱼规则.md:162,176） | 🔄 Divergent | Source/Catfishing/Equipment/Fragments/CatEquipmentFragment_Rod.h:17-47；Source/Catfishing/Fishing/CatFishingTypes.h:44-46 | 重核维持且比基线更远：装备定义重构后 `FishingStrength`（旧承载阈值）字段已从鱼竿片段整体删除——`grep FishingStrength Equipment/` 零命中，竿强度连承载列都没有了；`ECatFishingOutcome::LineBroken` 仍标 `UMETA(Hidden)`「现行搏斗不再生成强度过载断线」，全库无瞬断产生点。09-09 下午裁决已要求恢复三档竿强与瞬断并取消弃用（D-21 待改代码） |
| 34 | D 初始值＝浮漂实际落点到猫的距离，L₀＝D₀（钓鱼规则.md:163,209） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:944-945,1145-1147,1189-1194 | 重核维持：`InitialLineLength = FVector::Distance(RodTip, FishLocation)`、`ResolvedInitialDistance = FVector::Distance(RodTipWorldPosition, Exact.WaterSurfaceWorldPoint)`，起算点是竿尖不是猫站立点，与第 7 行同一口径问题。「现量并冻结、L₀＝D₀、不用全局常量」三点成立（D-19） |
| 35 | L_max 三档 60/80/100 米（钓鱼规则.md:166,209） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:1111；Source/Catfishing/Equipment/Fragments/CatEquipmentFragment_Rod.h:21-23；Content/Catfishing/Data/Equipment/ | 字段与消费链齐备（`MaximumLineLengthCentimeters` 进 `Config.MaximumLineLengthCentimeters`，并在第 7 行抛竿射程里同时作上限）。三档具体值是 ③；资产目录（①）只有两根竿，「三档」结构在数据层仍不成立 |
| 36 | F_total 单人即持竿猫当前力量，K_pos 单人恒为 1.0（钓鱼规则.md:168） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:1082-1083；Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:267-268 | 主位不打折（`Config.PrimaryOperatorCatStrength` 直取 `FishingStrength` 属性），每固定步按主控实际属性刷新；本轮多人改走物理抓握，辅助力量不再进这条合计（多人部分归多人附篇）。复核更正 code_ref：原 1069-1071 指的是体力初始化失败日志，赋值在 1082-1083 |
| 37 | 当前力量不随体力衰减，体力低不降力量（钓鱼规则.md:170,305） | ✅ Implemented | Source/Catfishing/AbilitySystem/Attributes/CatSurvivalAttributeSet.cpp:40-100；Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:267 | `FishingStrength` 与 `FightStamina/MaxFightStamina` 是彼此独立属性，Clamp 互不影响；Runner 的 `ActiveFishingStrength` 在体力为正时恒等于属性值，只有归零时整体置 0 进力竭分支（那是 §4.6 的出口语义，不是衰减） |
| 38 | 猫力达到鱼力 2 倍即碾压，直接把鱼甩上岸并进待拾取；瞬时判定，检查序 ①竿强瞬断→②碾压→③常规搏斗（钓鱼规则.md:178） | ❌ Missing | — | 重核维持。搜过 `碾压`/`Overwhelm`/`Crush`/`Overpower`/`Fling`/`Ashore`/`StrengthRatio`：`StrengthRatio` 只在 `CatFishCatalogSettings.cpp:61-67` 的选鱼挑战度里出现（D-29 已裁删代码），与搏斗内碾压无关；`AutoHauling` 是力竭拖岸不是碾压甩岸。`TryEnterHookedFightFromAuthority`(CatFishingSession.cpp:1018) 直接建 Runner，开场无任何强度检查序，也无合力变动重查入口 |
| 39 | 鱼行为骨架：上钩先向外游，外冲与向岸低强度游动按鱼种时长交替；鱼体力低于 50% 后休息期变长（钓鱼规则.md:184） | ⚠️ Partial | Source/Catfishing/Fishing/Behavior/CatFishBehaviorTypes.h:6-26；Source/Catfishing/Fishing/CatFishingSession.cpp:1194（初始 `OutwardRush`） | 三行为 OutwardRush/LateralArc/EaseOff 与 `LowStamina`/`NeedsRecovery` 条件已建且注释写明「StateTree 资产组合这些只读条件选择真实边；模型不内置下一行为」；具体转移边、时长区间与 50% 阈值全在 ST_FishFight 资产（④），文本查不了 |
| 40 | 最近冲岸距离按鱼种/食性配置（食肉中鱼不进离岸 1.5 米等）（钓鱼规则.md:186） | ❌ Missing | — | 重核维持。搜过 `ShoreDistance`/`MinimumShore`/`冲岸`/`离岸`：`UCatFishDefinition`（本轮已重构为库存物品定义基类的子类）从头到尾没有冲岸距离列；命中的全是钓鱼侧岸线关系查询。09-10 已裁「鱼表格新列『最近冲岸距离』按参数页贴岸阀快照填」，代码侧仍无承载字段 |
| 41 | 搏斗消耗按猫的出力计：收线按线长每米单价、转竿按弧度单价、顶住按负载平方每秒，转竿支撑与顶住只收较高一项；负载项＝空载系数＋当前负载×负载系数，放线时三样都不计（钓鱼规则.md:192-197；09-09 裁决⑥） | ✅ Implemented | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.cpp:586-627；Source/Catfishing/Fishing/Simulation/CatFishingFightWorkModel.cpp:3-15；Scripts/create_fishing_fight_balance_asset.py:33-42 | 本轮改判（原 🔄，因设计已于 09-09 按代码改写）：`ComputeCatChannel(ActualReelDistance, CatStaminaCostPerStrengthCentimeter,…)` 与 `ComputeCatChannel(CatRodPositiveWorkRadians, CatRodStaminaCostPerStrengthRadian,…)` 逐项计价，`CatHoldStaminaDrain = CatSupportStaminaPerSecond × dt × 负载² × 倍率` 且只在 `bLoadedConstraint && !bFreeSpool` 时收，`CatRodSupportStaminaDrain = max(0, 转竿支撑 − 顶住)` 实现「只收较高一项」，`WorkModel` 的 `UnloadedWorkMultiplier + NormalizedLoad × LoadStaminaMultiplier` 就是负载项；`bSlackRecovery` 时整段跳过。现值（0.002/弧度 0.03/空载 0.15/支撑 2.0）在落盘脚本（②）可查。旧公式的 `K_dir`/`K_over`/`K_pos`/`R_base`/`ΔD_blocked` 全库仍零命中 |
| 42 | 消耗随固定步运动求解走，求解抖动会抖体力条，需要一层平滑（钓鱼规则.md:199） | ⚠️ Partial | Config/DefaultGame.ini:217 `HeldRodFishPullSmoothingSeconds=0.15`；Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:303-316 | 有竿向负载平滑与强对抗确认时长；体力扣减本身仍无平滑层，本轮新增的移动采样只做时间切片累积（`PendingMovementSamples` 按固定步切片），不是对扣减做平滑 |
| 43 | 移动附加按秒计费的常数：W 前移 1.5 点/秒、S 后退 3 点/秒；力量差不进腿部消耗；W/S 在钓鱼中不翻转（钓鱼规则.md:201） | 🔄 Divergent | Source/Catfishing/Fishing/Simulation/CatFishingOperatorWorkModel.cpp:3-18；Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:330-343；Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h:13-14 | 重核维持，但机制本轮又换了一次（09-11「以意图位移结算合力钓鱼的个人出力与体力」）：现在按「意图位移 − 实际完成位移」的缺口计价（`StaminaPerUnfulfilledMeter = 2.0` 点/米 × `CatMovementStaminaMultiplier`），不区分方向、没有前移/后退两档常数、也不按秒——而且这恰恰把力量差算进了腿部消耗（被鱼拖住走不动就多扣），与设计「力量差不进腿部消耗」相反 |
| 44 | 线绷紧的每一秒竿耐久另扣「鱼力 × 0.05」（钓鱼规则.md:203） | 🔄 Divergent | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.cpp:665-678；Scripts/create_fishing_fight_balance_asset.py:47 | 重核维持：实际是 `(max(鱼线力, 猫线力) × StalemateRodWearPerFishStrength + FishFullEffortRodWearPerSecond × 出力²) × WearLoad × dt × TautRodWearMultiplier`——既按「线力」不按「鱼力」，又叠了出力平方与方向负载缩放；系数现值 0.1 不是 0.05（落盘脚本 ②）。绷紧才扣这一点 ✅（`bLineRestraining` 门），力竭段与拖落水段不扣 |
| 45 | 搏斗以渔获结束时，竿另扣基础磨损 1 点（钓鱼规则.md:203） | ❌ Missing | — | 重核维持。抽读 `CommitCatchEquipmentFromAuthority`(CatFishingSession.cpp:1545-1555)：函数体只有一句 `CommitFishingBaitDeferred`，注释写死「每步磨损已经写回同一鱼竿实例；捕获仅收口饵料，不能再重复扣耐久」；`ApplyFishingRodWear` 的调用点只有 Runner 的逐步磨损链 |
| 46 | 真咬成立当刻已超 L_max 直接判脱钩鱼逃，饵已扣、不丢漂（钓鱼规则.md:209） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:1150-1156,1195-1200,1811-1819 | 超 L_max 时 `TryEnterHookedFightFromAuthority` 返回 false，上层把整场判为 `Invalidated`（系统性异常终止），不是「脱钩鱼逃」这一玩法终局。注：搏斗中另有 `Escaped` 终局（Simulator.cpp:448，超 `L_max + EscapeSlackCentimeters` 才判），那是入场之后的路径 |
| 47 | 放线是解除线轮锁定而非主动出线，无张力零消耗、没有「放线速率」参数；线出尽后解锁失效被迫全额对抗（钓鱼规则.md:211） | ✅ Implemented | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.cpp:67,181,498；Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.h:16-22 | `ECatFightCatAction` 注释写明「不按=锁线，左键=收线，右键在未放尽时自由出线」，`IsLineAtMaximum` 一到上限就取消 free spool 恢复张力对抗；全库无任何放线速率配置（D-17） |
| 48 | 放线本身不恢复体力，搏斗中一律不恢复；放线回体基础为 0，只作猫册三选一成长项（钓鱼规则.md:213；v1.3 裁决） | 🔄 Divergent | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.cpp:629-635；Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:346-350；Scripts/create_fishing_fight_balance_asset.py:43 | 重核维持：落盘脚本（②）仍写死 `slack_stamina_regen_per_second = 3.0`，Simulator 在 `bSlackRecovery` 时把 `CatStaminaDrain` 直接置为负数（回体），Runner 再按「未受载」条件二次结算。这是正式通道，不是成长项开关 |
| 49 | 放线期间不判位置落水（钓鱼规则.md:213） | ⚠️ Partial | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.cpp:181,498；Source/Catfishing/Fishing/CatFishingSession.cpp:1522-1544 | 放线＝free spool、张力不作用于猫，物理上不会产生拖拽；但没有显式的「放线时跳过落水判定」分支，落水仍由 Condition 侧真实浸没深度触发（见第 51 行） |
| 50 | 三个资源归零出口：鱼体力≤0 翻肚、猫体力≤0 被拖下水鱼逃、竿耐久≤0 断竿鱼逃（钓鱼规则.md:221-223） | 🔄 Divergent | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.h:7-14；Source/Catfishing/Fishing/CatFishingSession.cpp:1410-1425,1465-1478,1522-1544 | 重核维持并加固：`ECatFightStepOutcome` 只有 None/FishExhausted/RodBroken/Escaped 四值，**没有猫力竭终局**；头文件注释直说「猫力竭先进入持续拖拽，由真实水深确认落水，不直接结束本场」。全库搜 `CatInWater` 仍只有枚举定义、`TerminateSession` 白名单 case、表现 Tag 查表三处，没有任何 `TerminateSession(CatInWater)` 调用。09-09 裁决③已把「落水或猫体力归零即结场」写进设计 §4.6，代码未跟 |
| 51 | 位置落水三条件同时满足（猫岸距归零＋鱼向外游＋总力量小于鱼力），只判持竿者，体力落水优先（钓鱼规则.md:226） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:1522-1544 | 重核改述：「只判持竿者」本轮已修正（`Snapshot.RodActor->IsPrimaryOperator(Character->GetPlayerState())`，不再是任意操作位）；但仍没有三条件表达式，落水由 Condition 侧真实浸没深度触发，且「两出口同刻体力优先」的裁序无从谈起——按第 50 行，落水这一路压根不产生终局 |
| 52 | 落水后果：鱼逃、饵已扣、嘴里原有的鱼保留，无其他损失（钓鱼规则.md:228） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:1536-1544；Source/Catfishing/Character/CatCharacter.cpp:392-396,466-470 | 重核维持：嘴叼鱼保留 ✅（`ReleaseMouthCarryFromAuthority` 只在 UnPossessed/EndPlay/倒地三条路径触发，落水不在其中）、饵已扣 ✅；「鱼逃」这条不是打折而是相反——落水只播一次表现＋`ReleaseFishingOperatorForCharacter`，会话转「无人值守放线」继续跑，鱼还挂在竿上 |
| 53 | 主动放弃＝搏斗中长按收竿键 1.5 秒，带进度条 UI，松手即取消（钓鱼规则.md:234） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:1850-1940；Source/Catfishing/AbilitySystem/Fishing/InputAbilities/CatFishingCancelAbility.cpp:12-28 | 重核维持：命令与 `LineCut` 终局完整，可切线阶段含 HookedFight/NearShore/ExhaustedReel/AutoHauling。C++ 侧确是一次性离散输入（`FinishOneShot` + `SubmitCancel`，无按住时长参数），1.5 秒长按只可能配在 IA 资产的 Hold Trigger、进度条只可能在 WBP（④ 查不了） |
| 54 | 放弃按鱼逃同口径：已揭剪影保留、收集层不写（钓鱼规则.md:234） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:1912-1918 | 收集层不写 ✅（`LineCut` 不经 `ArchiveCommittedCapture`）；剪影这一半本轮变成「无从保留」——剪影 Grant 已无生产者，见第 73 行 |
| 55 | 可抄资格：对象是钩上的鱼或翻肚鱼，抄手朝向射线碰到挂在鱼身上的可捞圆即够着、够着即必中；自由游动的鱼不可捞（钓鱼规则.md:244；v1.6 按代码转正） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:352-354,392-398,420-428,436-449 | 本轮改判（原 🔄，因设计已于 09-09 按代码改写）：`DoesScoopRayReachFish(抄手位置, 水平朝向, 抄网射程, 鱼位置, 逐鱼可捞圆半径, 高差上限)` 即唯一范围判定，代码注释显式说明「不再要求鱼处于近岸带内…再叠一层离岸距离等于两套口径」；阶段限制（HookedFight/NearShore/ExhaustedReel）等价于「钩上或翻肚」，更早阶段拒绝。可捞圆半径逐鱼配在 `UCatFishDefinition::ScoopTargetRadiusCentimeters`（CatFishDefinition.h:137-144），具体值属 ③ |
| 56 | 抄网非必带、单一款式，没有长度/体型上限/成功率属性，不按竿长、鱼重量或抄网等级判断（钓鱼规则.md:244） | ✅ Implemented | Source/Catfishing/Equipment/Fragments/CatEquipmentFragment_Scoop.h:17-19；Source/Catfishing/Fishing/Integration/CatFishingAimLibrary.cpp（`TryResolveScoopReach`） | 抄网片段只有 `ScoopReachCentimeters` 一个参数；有效距离＝min(全局设置, 抄网资产)，判定不读竿长/重量/等级 |
| 57 | 湖心巨影的可捞圆为 0、不可抄，只能翻肚拖岸或碾压落岸后拾取（钓鱼规则.md:244） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:392,436-441；Source/Catfishing/Data/CatFishDefinition.h:137-144 | 本轮改判（原 ❌，因设计 v1.6 把口径改成「可捞圆为 0」）：通用闸门在——`FishRadius > 0.0` 与 `FishDefinition->ScoopTargetRadiusCentimeters <= 0.0` 两处都会拒抄，字段注释也写明「0 表示未裁，服务器一律拒绝抢抄」。差的是语义与数据两头：字段声明的语义是「未裁」而不是「巨影不可抄」，且 `Fish_LakeGiantShadow.uasset` 上的实际半径属 ③ 查不了；`bGiant`/`BodyClass` 仍不参与抢抄判定（CatFishingTypes.h:175 的 bGiant 只用于协作与表现） |
| 58 | 按 F 挥网，命中由服务器在判定瞬间验证；命中后鱼由挥网的猫叼在嘴里，实物归抄网者（钓鱼规则.md:248） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:330-345,490-517,1690-1742 | 身份由服务器 Controller 重建（注释「客户端提交的任何身份字段都不可信」），`SpawnScoopedFishPickupFromAuthority` 直接挂到抄手嘴部 Socket；F 这个键位本身在 IA 资产（④） |
| 59 | 挥空的猫进入 3 秒硬直、无法操作（钓鱼规则.md:250,266） | ⚠️ Partial | Config/DefaultGame.ini:202 `ScoopCooldownSeconds=3.0`；Source/Catfishing/AbilitySystem/Fishing/InputAbilities/CatFishingScoopAbility.cpp:21-25,37-52 | 重核维持：3 秒值对得上，但 `CommitAbility` 在 ActivateAbility 首行无条件施加冷却——成功抄到的猫也吃 3 秒；而且是 GAS 冷却（只锁抄网技能），不是「无法操作」的全身硬直 |
| 60 | 嘴里只能叼一条鱼；叼着鱼不能抛竿、不能再抄或拾第二条（钓鱼规则.md:252） | ⚠️ Partial | Source/Catfishing/Character/CatCharacter.cpp:256-270；Source/Catfishing/Fishing/CatFishingSession.cpp:392-394,1697；Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:1054-1057 | 本轮加固：09-11「统一单嘴占用」把鱼与鱼护并入同一个 `MouthCarriedActor` 单槽，抢抄前置 `bMouthFree`、拾取前置 `FindCarriedFish(...) \|\| FindCarriedGuard(...)` 都在 ✅；缺的仍是抛竿——`BeginCast`（CatFishingService.cpp:165-300）全程没有叼鱼拒绝分支 |
| 61 | 落水时嘴里的鱼保留（钓鱼规则.md:252,228） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:1536-1544 | 落水只播表现并释放操作位，不触碰嘴叼 Actor |
| 62 | 翻肚后不放线时线长固定，猫往后走鱼就被拉得离岸更近，拖过岸线即按力竭处理、侧翻落在碰岸处；这一段无对抗即无消耗（钓鱼规则.md:258；v1.8 按代码改写） | ⚠️ Partial | Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:686-692,803-808；Source/Catfishing/Fishing/CatFishingSession.cpp:1490-1520；Config/DefaultGame.ini:273 `LandingCompletionDistanceToRodCentimeters=75.0` | 本轮改判（原 🔄，因设计 v1.8 已按代码改写）：越岸即 `bFishBeached`→`FishExhausted`→`ExhaustedReel` 并侧翻落地 ✅；力竭段 `bFreeEffort` 让双方都不计消耗 ✅（Simulator.cpp:587、Runner.cpp:331）。仍差一步：Pickup 不在「碰岸处」生成，而要玩家继续把鱼收到竿尖 75cm 内才交接（`IsFishBeachedForAuthority()` + `Dist2D(Encounter, RodTip) <= ReachTolerance`），落地点因此不是设计说的碰岸点 |
| 63 | 落岸不等于入袋，仍要按 F 拾取、拾取必中；岸上的鱼谁都能拾、先到先得（钓鱼规则.md:258） | 🔄 Divergent | Source/Catfishing/Items/Fish/CatFishPickupActor.h:116；Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:977-996,1019-1076 | 重核维持：拾取走通用交互接口（`ICatInteractable::Interact`，头文件注释仍写「对死鱼按 E」），不是收鱼专用的 F，交互半径也走 `CatInteractionSettings` 而非抄网口径；谁都能拾＋先到先得 ✅（Actor 无原钓手所有权，`PickupTerminalByRequester` 只做重放幂等）。提示文案本轮已进 C++：「叼起 {鱼种} {n} kg」 |
| 64 | 水里翻肚的鱼超过苏醒时限（默认 30 秒）未上岸就苏醒逃跑，拖动中计时照走（钓鱼规则.md:260） | ❌ Missing | — | 重核维持。搜过 `苏醒`/`Revive`/`Awaken`/`Wake`/`RevivalSeconds`/`ExhaustedTimeout`（Source+Config）：与鱼无关的两处 `WakeAllRigidBodies` 之外零命中。`ExhaustedReel` 分支（CatFishingSession.cpp:1490-1520）只有「鱼已上岸且到竿尖距离内 → 生成 Pickup」一条出口，不设任何计时器 |
| 65 | 地上未拾取的鱼翻天消失，不能靠「离水安全」跨天囤鱼（钓鱼规则.md:260） | ❌ Missing | — | 重核维持。搜过 GameMode 的翻天路径（`BeginAltarDayTransition`/`CommitAltarDayTransition`/`FinishAltarDayTransition`，CatfishingGameModeBase.cpp:1205-1350）与阶段切换段（1077-1170）：均不触碰任何 Pickup Actor，也无天数/阶段驱动的世界清理；`ReleaseMouthCarryFromAuthority` 只由角色生命周期与倒地触发 |
| 66 | 所有猫都能抢同一条可抄鱼/岸上鱼，以服务器收到指令的时间戳判先到先得，不能用客户端帧数（钓鱼规则.md:266） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:330-345,398-418,450-470 | 服务器按到达顺序串行处理并用 `bCaptureResolved` 一次性收口，重放走 `ScoopTerminalCache`（键＝身份+RequestId）；命令不接受任何客户端位置/时间载荷 |
| 67 | 时间戳完全并列时按席位序（加入房间先后）打破（钓鱼规则.md:268） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:398-418 | 服务器串行处理使「并列」在工程上不成立，事实上确定可复现；但没有席位序这一显式 tiebreak 字段，若将来改成同帧批处理就没有依据 |
| 68 | 同一 tick 的事件结算序固定为：收鱼 → 苏醒 → 落水（钓鱼规则.md:268） | ❌ Missing | — | 重核维持：苏醒不存在（第 64 行）；收鱼与落水的先后也无显式裁序——不过按第 50 行落水不写终态，所以「到手的鱼被同刻落水吞掉」在当前工程里不会发生，这条要求是被架空而不是被违反 |
| 69 | 抄网射程 2 米；落岸鱼可拾距离 1.5 米，是全游戏统一交互半径，与抄网射程是两个参数（钓鱼规则.md:272） | ⚠️ Partial | Config/DefaultGame.ini:201 `ScoopReachCentimeters=200.0`；Config/DefaultGame.ini:265,268 `MaximumTargetingDistanceCentimeters=300.0`、`MaximumServerInteractionDistanceCentimeters=350.0`；Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:989-993 | 本轮改判（原 🔄）：抄网射程 2 米这一半现在对得上（全局 200cm，再与抄网资产 reach 取 min）；「两个参数」的结构也成立。落岸鱼这一半仍不对——拾取走 `CatInteractionSettings` 的 300cm 准星 / 350cm 服务器容差，既不是 1.5 米也不是单一口径。D-03 登记的 100cm 仍未回写 |
| 70 | 够不够着看抄手的身体朝向：从抄手位置沿面朝方向水平发射线，转镜头不改变朝向（钓鱼规则.md:272；v1.6 按代码转正） | ✅ Implemented | Source/Catfishing/Fishing/Integration/CatFishingAimLibrary.cpp（`ResolveScoopFacingHorizontal`）；Source/Catfishing/Fishing/CatFishingSession.cpp:366-369 | 本轮改判（原 🔄「不加朝向判定」，设计已按代码改写）：取 `Character->GetActorForwardVector()` 的水平分量，代码注释显式声明「这是身体动作而非镜头瞄准动作；自由转动 Camera/Controller 不得改变抄网判定方向」，日志字段也标 `ScoopFacingSource=CharacterActorForward` |
| 71 | 鱼的位置以服务器物理碰撞箱中心为准（钓鱼规则.md:272） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:392-393,70-88 | 用 `Encounter->GetActorLocation()` 权威位置（D-12：已删除客户端位置输入与「钓手面前水面点」替身） |
| 72 | 多个合法目标同时够着时 F 作用于玩家视角中心所指的那个，UI 以准星态提示当前 F 目标（钓鱼规则.md:278） | ⚠️ Partial | Source/Catfishing/Fishing/Integration/CatFishingAimLibrary.cpp（`ResolveScoopFacingHorizontal`）；Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:967-975；Config/DefaultGame.ini:270 `FocusStencilValue=1` | 拾取侧有准星焦点/描边通道（`BeginLocalFocus`＋Stencil，走 `CatInteractionSettings` 的准星射线）；抄网侧明确用角色正前方而非视角中心，与本条相反；每场会话只有一条鱼，多目标择一在抄网侧不成立 |
| 73 | 剪影层在上钩成立时揭开、永不撤销，试探期空竿同揭（钓鱼规则.md:284） | ❌ Missing | — | 本轮改判（原 🔄）：`ECatProfileGrantKind::FishSilhouette` 现在**没有任何生产者**——全库 `Kind = ECatProfileGrantKind::` 的三处赋值只有 FishRecorded / Unlock / Imprint（CatRunImprintService.cpp:43,75,299），枚举与 Profile 消费端（CatProfileSubsystem.cpp:237,256,280）还在但收不到输入。09-09 晚裁「删代码：重试预算」，基线里唯一那条「重试预算耗尽逃鱼→发剪影」的路径随之删除（提交 6358679），剪影因此从「时点错」退化为「完全不揭」 |
| 74 | 收集层在成功收鱼时写入，归上钩者；实物被队友抢走不取消登记（钓鱼规则.md:284；09-08 裁决「收集层归上钩者」） | 🔄 Divergent | Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:818,929,1078-1090；Source/Catfishing/Collection/CatRunImprintService.cpp:29-44 | 重核维持：`ArchiveCommittedCapture(…, StableNetId)` 的接收者是「把鱼塞进容器/消费掉的那个人」，写入时点是入鱼护或售出/吃掉，不是成功收鱼。09-11 新增的「从鱼护鱼缸叼取原鱼」让同一条鱼可以反复进出容器，`bCaptureRecorded` 保证只记一次，但归属仍是首个入库者 |
| 75 | 重量没有组队加成（钓鱼规则.md:284） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:989-991 | 重量在选鱼事务里按冻结上下文抽样并写入快照，与参与者数量无关 |
| 76 | 鱼册输入：每鱼×每饵权重、出现条件（区域/时段/天气）、在场协作能力条件（钓鱼规则.md:294） | ✅ Implemented | Source/Catfishing/Data/CatFishDefinition.h:109-167；Source/Catfishing/Fishing/CatFishingSession.cpp:869-891 | `RegionIds`/`TimeOfDay`/`Weather`/`SpawnWeight`/`MinimumFightParticipants`/`BaitWeightMultipliers` 字段齐备并进 `FrozenSelectionContext`（D-31：时段/天气门默认旁路，ini 两开关为 False） |
| 77 | 「巨影档」是重量档名、湖心巨影是鱼种名，不能互代来确定可抄或可食用性（钓鱼规则.md:298） | ⚠️ Partial | Source/Catfishing/Data/CatFishDefinition.h:12-22,101-103 | `ECatFishBodyClass` 仍只有 Unknown/Standard/Giant 三值，注释声明「只表达协作档位，不携带任何力量、体力或几何公式」，方向对；但四档体重轴（09-06 重拍为 小<1／中 1≤w<5／大 5≤w<15／巨影 w≥15）在数据模型里仍不存在（D-09 待按新阈值重登记） |
| 78 | 道具册输入：漂射程与精准度、竿强度与耐久、最大线长、抄网与鱼护装备状态（钓鱼规则.md:304） | ⚠️ Partial | Source/Catfishing/Equipment/Fragments/CatEquipmentFragment_Rod.h:17-35；Source/Catfishing/Equipment/Fragments/CatEquipmentFragment_Float.h:17-31；Source/Catfishing/Equipment/Fragments/CatEquipmentFragment_Scoop.h:17-19 | 本轮装备定义重构为「物品定义＋片段」：射程/耐久/磨损/线长/抄网 reach 五项字段与消费链齐备；精准度两字段仍零运行消费者（第 8 行）；**竿强度字段整体消失**（第 33 行）；鱼护已改为独立库存物品＋容器 Actor，不再走装备快照 |
| 79 | 猫册输入：当前力量、当前/上限体力、倒地/可行动状态（钓鱼规则.md:305） | ✅ Implemented | Source/Catfishing/AbilitySystem/Attributes/CatSurvivalAttributeSet.h:26-49；Source/Catfishing/Fishing/CatFishingService.cpp:1023-1028；Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:789-796 | 钓鱼命令 gate 与 `CanControllerStartFishingAction` 都显式检查 `Conditions->GetSnapshot().bDowned`；力量与体力全走 ASC 属性 |
| 80 | 搏斗外自然恢复 5 点/秒，站立/岸上替补/摸鱼一律同速率、无场景乘数；搏斗中不恢复（钓鱼规则.md:310；v1.3 裁决） | ⚠️ Partial | Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h:18-23；Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:43,76-85 | 本轮改判（原 🔄）：09-11 的身体出力重构补上了这条链——`RecoveryPerSecond = 5.0`、无场景乘数 ✅，且 `Drive.bFishing` 时整段跳过（搏斗中不恢复 ✅）。差两处：① 设计没有的门槛——必须连续静止超过 `RecoveryDelaySeconds = 2.0` 且不受载（`bCanRest = !bExerting && !Drive.bUnderLoad && Drive.bLocomotion`）才开始回；② 每次进搏斗仍在 `EnterPhaseFromStateTree`/`TryEnterHookedFightFromAuthority` 调 `InitializeFishingStaminaForSession()` 把体力一次性补满（CatAbilitySystemComponent.cpp:309-330，会话入口在 CatFishingSession.cpp:1067），等于开场满血，与「靠 5 点/秒慢慢回」是两套。复核补第三处：回体另有 `bRecoveryPending` 闸，只由「协作出力」或「协作中体力见底」置位（CatPhysicalEffortComponent.cpp:61,70,81），而搏斗扣体走 Runner、第 43 行整段被 `Drive.bFishing` 跳过（同文件:43），所以被钓鱼扣干的体力在搏斗外根本不进入这条 5 点/秒通道——「站立／岸上替补／摸鱼同速率」这半句在钓鱼场景下没有生效路径。另有搏斗内放线回体 3/秒，见第 48 行 |
| 81 | 鱼行为→钓鱼运动接口：每 tick 交付意图方向、意图速度、相对水位移，翻肚前意图非零（钓鱼规则.md:316） | ✅ Implemented | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.h:157-158,250,265,292；Source/Catfishing/Fishing/Simulation/CatFishSteeringModel.h | `IntendedSwimSpeedCentimetersPerSecond`(250)、`FishIntendedDistanceCentimeters`(265)、`FishEffortDirection`(292) 逐步输出；`FishEffortRatio`(158) 注释声明「零出力仍保留活鱼惯性」(157)。复核更正 code_ref：原 294-300/320-327 指的是 `NormalizedLineLoad`～`Outcome` 与类声明，不含这四个字段 |
| 82 | 钓鱼→鱼行为接口：提竿结果对应的入场削减系数在提竿判定落定时交付一次；线的绷紧/松弛状态每 tick 可读（钓鱼规则.md:317） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:1050-1055,1144,1157；Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.h:280-281,302 | 张力/绷线状态每步可读 ✅（`NormalizedTension`(280)、`bLineTaut`(281)、`LineTensionNewtons`(302)；复核更正 code_ref，原 290-291/305 指的是位置纠偏与 `EffectiveCatStrength`）；完美系数仍由 Session 直接乘进模拟配置，不是交给鱼侧的一次性输入，鱼侧无从按自己的规则消费它 |
| 83 | 渔获交接链：抄取或拾取→嘴里→可选鱼护暂存→带回营地共用大鱼缸（钓鱼规则.md:324） | ✅ Implemented | Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:750-830；Source/Catfishing/FishContainers/CatFishGuardActor.cpp:120-200；Source/Catfishing/FishContainers/CatFishTankActor.h | 三段接力齐全，嘴叼鱼始终是世界 Actor；09-11 新增反向链路（从鱼护/鱼缸重新叼起原鱼，Actor 作为「静态保管载体」保留），容量配置在 Config/DefaultGame.ini:117-118（鱼缸 20、鱼护 8） |
| 84 | 当天扔下等于赠送路径（谁都能捡），物品不能扔进湖里（钓鱼规则.md:324） | ⚠️ Partial | Source/Catfishing/Framework/Game/CatfishingPlayerController.cpp:1092-1127；Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:579-600,704-742 | 本轮改判（原 ❌）：09-11 补上了玩家主动丢弃——`ServerDropCarriedItem` 按当前嘴叼对象分派（鱼走 `DropFromAuthority`，鱼护走库存 `ReleaseItemToWorldFromAuthority`），落地后恢复 `InteractionSphere` 交互、任何人都能再捡 ✅。差的是「不能扔进湖里」：`ReleaseMouthCarryFromAuthority` 只用 `ResolveHighestBlockingSurface` 找最高阻挡面，没有任何水域拒绝分支 |
| 85 | 转存接口要区分成功与拒收，满容器不得当作转存成功（钓鱼规则.md:328） | ✅ Implemented | Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:783-810 | 先 `CanFullyAcceptInventoryBatch` 选容器，`TryAddInventoryBatchInternal` 失败即回滚 `bConsumptionCommitted` 并返回 `CapacityExceeded`，鱼继续叼着 |
| 86 | 钓鱼对同一条鱼输出鱼种、实际重量、上钩归属与实物持有人；上钩事件与成功收鱼事件分别对应两个捕获时点（钓鱼规则.md:326） | ⚠️ Partial | Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:810-818,1078-1090；Source/Catfishing/Collection/CatRunImprintService.cpp:29-44 | 鱼种、实际重量、实物持有人都随 Grant 输出 ✅；「上钩归属」不在输出里（见第 74 行），两个捕获时点也塌成一个（剪影侧已彻底无产出，见第 73 行） |
| 87 | 入夜：不再产生新咬钩，还没咬钩的浮漂剩余计时清掉，进行中的搏斗允许打完（钓鱼规则.md:336） | ✅ Implemented | Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:788-808,1077-1085；Source/Catfishing/Fishing/CatFishingSession.cpp:599-622,644-660 | 本轮改判（原 🔄）：09-11「入夜仅停止新咬钩并保留钓鱼操作与搏斗」把两条门彻底拆开——`CanAcceptFishingCommand` 在 NormalNight/结算夜仍放行抛收竿、放线、抄网、打窝；`CanGenerateNewFishingBites` 只在 DayActive 且未过截止时为真；`SuspendFishingAndReleaseOperators()` 现在只在 NotStarted/Ending/Ended 三个相位调用（注释「只有局未启动或真正结束才终止会话；夜晚保留操作位和正在进行的搏斗」）。`RefreshBiteAvailabilityFromAuthority` 对 Waiting 会话清掉 BiteWarning/Probe 两个计时器并把浮漂压回 Calm ✅ |
| 88 | 入夜瞬间处于试探期的竿按空竿收回、不损饵；真咬待响应的算进行中，允许提竿并打完，超时鱼逃；与入夜同刻到点的浮漂不再抽鱼（钓鱼规则.md:336） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:763-792,805-812,1772-1790 | 本轮改判（原 ❌）：「与入夜同刻到点的浮漂不再抽鱼」✅（`HandleBiteWarningTimer`/`HandleProbeTimer`/`OpenTrueBiteWindowFromStateTree` 三处都查 `CanGenerateNewFishingBites`）；「真咬待响应允许提竿打完」✅（`RequestHookFromAuthority` 不查夜间门）。差两处：① 试探期撞上入夜走的是 `WindowExpired` 回 Waiting，竿留在水里、不走「空竿收回」（不损饵这一半成立）；② 响应窗超时仍不判鱼逃（第 25 行） |
| 89 | 强制中断（献祭互动等）打断进行中的互动并按抽扣时点结账（钓鱼规则.md:337） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingSession.cpp:1828-1848；Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:782-786,1205-1258；Source/Catfishing/Framework/Game/CatfishingPlayerController.cpp:83,107 | `CancelFromAuthority` 与 `LineCut` 两条主动收口路径在；09-11 新增的祭坛全员确认＋翻天过场只做「输入锁」——`CanAcceptGameplayCommand` 在 `DayTransition.bActive` 期间拒绝新命令、Controller 侧 `SetDayTransitionLocked` 清按住输入，但不打断也不结账任何进行中的钓鱼会话（会话跨过整个过场继续存在）。剪影保留这一半不成立（第 73 行），与收鱼/断竿/换人同刻的关系仍无代码表达 |
| 90 | 局结束停止钓鱼，释放窝、漂、搏斗等本系统瞬态；已输出的捕获事实不因失败撤销（钓鱼规则.md:340） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingService.cpp:1071-1097；Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1852 | `CloseCommandsAndTerminateAll` 关命令门、终止全部会话、销毁资源托管者并清 `PreservedRodEquipment`；捕获 Grant 走 Profile 持久层，不随会话回滚 |
| 91 | 必须可感知的操作反馈 12 项（抛竿/鱼休息/鱼发力/鱼可抄/体力偏低/落水危险/完美/断竿/鱼逃/挥空/主动放弃/碾压）（钓鱼规则.md:351-363） | ⚠️ Partial | Source/Catfishing/AbilitySystem/Tags/CatFishingAbilityTags.cpp:33-47；Source/Catfishing/UI/CatFishingViewTypes.h:20-52；Source/Catfishing/Fishing/Debug/CatFishingDebugSubsystem.cpp:686-712 | 重核维持：事件与状态位齐备（Cosmetic tag、`FishMotionIntent`、`bPerfectHook`、`Outcome`、`NormalizedLineLoad` 全部投影进 UI DTO）；带中文文案的仍只有 Debug HUD（「咬钩了！按左键提竿！」「鱼在发力！…」「鱼到近岸了！快抄！（按 F）」），正式 WBP/Montage 属 ③④ 查不了；碾压一项因机制缺失（第 38 行）无事件来源 |
| 92 | 基础咬钩信号要让漂主远距离感知，含声音、身体语言与震动通道；铃铛漂增强为全场可闻（钓鱼规则.md:365） | ❓ Unverifiable | Source/Catfishing/Fishing/Presentation/CatFishingPresentationSettings.h；Source/Catfishing/Equipment/Fragments/CatEquipmentFragment_Float.h:29-31；Source/Catfishing/Settings/CatGameUserSettings.cpp:118-129 | 维持 ❓，可查的部分已查：C++ 提供浮漂三态表现参数与 `BiteSignalStability` 字段（后者只在就绪校验里出现，无运行消费者）；震动只有一个全局 `bForceFeedbackEnabled` 总开关，没有咬钩专用触发；声音与铃铛漂差异只可能在 Cue、Montage 与 WBP（③④） |
| 93 | 鱼影按已抽定那条真鱼的体型演出，附近玩家都能看见（钓鱼规则.md:369） | 🔄 Divergent | Source/Catfishing/Fishing/CatFishingSession.cpp:932-936,1802-1807；Source/Catfishing/Fishing/CatFishingTypes.h:16 | 上钩后 Encounter 按冻结 `VisualScale`（`ComputeUniformVisualScale(WeightKilograms)`）复制给所有客户端并由 `PublishInitialPresentationFromAuthority` 统一放行 ✅；但上钩之前根本没有鱼实例（第 20 行），设计要的「抽中即出鱼影、只看体型不认鱼种」那一段不成立 |
| 94 | 线的松紧、竿身弯曲表达张力（钓鱼规则.md:371,375） | ✅ Implemented | Source/Catfishing/Fishing/Presentation/CatRodBendComponent.cpp；Source/Catfishing/Fishing/Presentation/CatFishingLineCurveComponent.cpp；Source/Catfishing/Fishing/CatFishingSession.cpp:1434-1440 | 竿弯按复制的实际张力 N 驱动，鱼线曲线按 `LineLengthCentimeters`/`SlackLineLengthCentimeters`/`NormalizedTension` 与端点插值；纯表现、不改玩法 |
| 95 | 猫体力低于 30% 给偏低提示（钓鱼规则.md:375，快照值） | ❓ Unverifiable | — | 维持 ❓：把 `Source/Catfishing/UI/` 全目录搜了 `0.3`/`LowStamina`/`Threshold`，零命中；Config 与 Scripts 同样没有 30% 常量。`FightStamina` 与上限都已投影进 HUD（CatHUDModel.cpp:60-65,178），阈值与变色只可能在 WBP_CatHUD（④）。鱼侧的 `LowStamina` 条件另在 Behavior 里（第 39 行），与本条无关 |
| 96 | 线长余量给世界内表现，力量差不给数字（钓鱼规则.md:375） | ⚠️ Partial | Source/Catfishing/Fishing/CatFishingTypes.h（`ActiveCombinedFishingStrength`/`NormalizedLineLoad`）；Source/Catfishing/UI/ItemTooltip/CatItemTooltipModel.cpp:36-46 | 快照仍复制了力量与负载标量，是否显示成数字由 WBP 决定（④）；本轮新增的物品悬停提示已经在 C++ 里把鱼竿耐久写成「耐久：{当前} / {上限}」的明文数字（另见 Code-only mechanics），方向上与「不给数字」有张力 |
| 97 | 增效道具按「进入咬钩」计一场、咬钩前收竿不计场；备装预穿戴的被动效果正常结算；鱼体力不接受道具直接改动，猫体力道具只在搏斗外使用（钓鱼规则.md:135） | ❌ Missing | — | 重核维持。搜过 `增效`/`Buff`/`PassiveEffect`/`bSpecialBait`：全库没有任何增效道具或被动效果通道；09-09 晚裁「删代码：重试预算」后连 `LoseSpecialBait` 失败预算链也已移除，`bSpecialBait` 现在只是鱼饵片段上的身份位。后两句（鱼/猫体力不被道具改动）当前是「因为不存在此类道具而空成立」，不算实现 |
| 98 | 下一轮钓鱼开放：从新的准备与打窝流程开始，不恢复昨天的咬钩倒计时（钓鱼规则.md:339） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:599-611,668-690；Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1168-1172,1340-1344 | 重核维持但成因改写：入夜不再终止会话，而是清掉两个计时器；天亮后 `RefreshBiteAvailabilityFromAuthority` 让每个 Waiting 会话重走 `ScheduleWaitingProbeFromStateTree`，`++BiteOpportunitySequence` 派生全新随机种子并重新采样等待时长，旧剩余时间没有任何恢复路径 ✅。注：竿与漂会原样跨夜留在水里，「从新的准备流程开始」这一层只对计时成立 |
| 99 | 完美削减是三项削减：鱼力、鱼体力，再加初始线长按完美线长系数缩短（钓鱼规则.md:149,209；v1.7 按代码转正） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:1050-1053,1145-1157；Source/Catfishing/Data/CatFishPersonalityDefinition.h:20 | 新增行（09-09 代码超前项裁决「保留并补文档」后设计才写上）：`PerfectInitialLineLengthMultiplier` 乘进 `RequestedInitialLineLength`，并被钳到「竿尖到水面的最短物理长度」与 `MaximumLineLengthCentimeters` 之间，注释写明「尽可能保留完美提竿的缩线收益」。系数值属 ③ |
| 100 | 主位在搏斗中主动走开且无人接手时竿落地、会话不结场（无人值守放线）；原持竿者或竿主人走到竿边 2.5 米内可按切线，鱼逃（钓鱼规则.md:230；09-09 裁决③进册） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingService.cpp:1104-1118；Source/Catfishing/Fishing/CatFishingSession.cpp:1636-1689,1884-1904 | 新增行（原先只在代码里，09-09 裁决③写进设计）：`RemoveOperatorAndReconcileSession`→`SuspendOperatorFromAuthority`→`BeginUnattendedSlackFromAuthority`，会话继续跑并打 `Mode=UnattendedSlack`；`CutLineFromAuthority` 对无主落地竿放行的三条件是「竿已落地且无操作者＋请求者是竿主或最近一次被挂起的钓手＋距竿 250cm 内」——2.5 米与设计逐字对上 |
| 101 | 抄网三条拒绝条件：抄手与鱼之间有遮挡、抄手脚下坡度超过 45 度、抄手与鱼高差超过 2.5 米（钓鱼规则.md:272；v1.6 按代码转正） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:370-385,436-449；Config/DefaultGame.ini:220 `MaximumScoopGroundSlopeDegrees=45.0`；Source/Catfishing/Fishing/CatFishingSettings.h:137,145 | 新增行：三项都在且数值逐字对上（坡度 45°、`MaximumScoopVerticalDeltaCentimeters=250.0`；复核更正 ini 行号 214→220）；`scoop_rejected` 日志已按 09-09 裁决要求拆开逐项谓词（FightCapable/MouthFree/ScoopReachReady/FishRadiusSet/ScooperOnLand/RayReachesFish/LineOfSight/ValidGround）。给玩家的提示文案属 ④ |
| 102 | 抄手必须站在岸上，嘴里不能叼着鱼（钓鱼规则.md:272） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:356-362,392-394,436-441 | 新增行：`ScooperSpatial.Containment == ECatWaterContainment::Outside`（站在岸上）与 `bMouthFree`（单嘴空闲，鱼与鱼护共用同一槽）都是抢抄硬前置 |
| 103 | 翻天完成时窝点清空，聚鱼窗口与死水随窝点一并清空（钓鱼规则.md:338） | ❌ Missing | — | 新增行（§6.5 生命周期表未覆盖行）：搜过 `BeginAltarDayTransition`/`CommitAltarDayTransition`/`FinishAltarDayTransition`（CatfishingGameModeBase.cpp:1205-1350）与相位切换段（1077-1170），全程不触碰 `UCatChumFieldSubsystem`；`Environment/CatChumField*` 下也没有任何按天/按局清空的入口（只有 `ExpiredCleanupIntervalSeconds` 的自然过期）。窝点机制本身归 §2 报告，此处只判「翻天要清空」这条生命周期契约 |
| 104 | 等待期移动不受线约束（钓鱼规则.md:209） | ✅ Implemented | Source/Catfishing/Fishing/CatFishingSession.cpp:1233；Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:267-279 | 复核补行（§4.5 首段第二句，原报告 §4.5 只取了 L₀＝D₀、超 L_max、L_max 三档三条）：线长约束与竿端受力只存在于 `FightRunner`，而 Runner 只在 `TryEnterHookedFightFromAuthority` 内 `NewObject` 创建（1233）；`SetFishingMotorBudget`（把线上的力接到猫身上的唯一入口）也只在 `CatFishingPhysicalRodComponent.cpp:363` 由已建立的搏斗链调用。Waiting/Probe/TrueBiteWindow 三阶段没有任何把 D 钳回 L_max 的代码，猫可以一路走到超程——第 46 行「真咬成立当刻已超 L_max」的分支能触发本身就是这条要求成立的反证 |
| 105 | 队友的装备只靠世界表现与凑近闻来了解，不新增查看装备的菜单（钓鱼规则.md:371） | ✅ Implemented | Source/Catfishing/UI/ItemTooltip/CatItemTooltipController.h:23-25,40；Source/Catfishing/UI/WorldInfo/CatWorldInfoTypes.h:10-22 | 复核补行（§7.2 第二段，本轮新增两套信息面板后这条才真正有被动的风险）：物品悬停提示的唯一来源是 `UCatInventorySlotWidget`（本人背包格），拿不到别人的实例；本轮新增的 `WorldInfo` 信息牌只挂在 `ACatAltarActor`(CatAltarActor.cpp:28) 与 `ACatFishTankActor`(CatFishTankActor.cpp:28) 两个世界物件上，没有角色/装备版本；全库无按玩家查装备的 UI 入口。注意这条与第 96 行、Code-only「悬停提示出明文数字」是两件事：那条问的是自己的数字要不要出，这条问的是能不能查别人 |

### Summary
- Total features: 105
- ✅ Implemented: 38
- ⚠️ Partial: 30
- ❌ Missing: 12
- 🔄 Divergent: 23
- ❓ Unverifiable: 2
- Inspectable: (105 - 2) / 105 * 100% = 98.1%
- Coverage of inspected: (38 + 0.5 * 30) / (105 - 2) * 100% = 51.5%

### Scan Scope

**扫过的目录（相对 D:/LocalGameProject/Unreal/Catfishing/，代码 HEAD bcc7df9）**
- `Source/Catfishing/Fishing/**`（Actors、Behavior、Config、Debug、Integration、Presentation、Simulation、StateTree 全部 .h/.cpp，含本轮新增的 `Simulation/CatFishingOperatorWorkModel.*`）
- `Source/Catfishing/Equipment/**`（CatEquipmentComponent、CatEquipmentDefinition、CatEquipmentInventoryItemInstance、CatFishingResourceCustodian，以及本轮新增的 `Fragments/CatEquipmentFragment_{Rod,Float,Bait,Chum,Scoop}.*`）
- `Source/Catfishing/Items/**`（本轮 `Items/World/CatFishPickupActor` 已移到 `Items/Fish/`）、`Source/Catfishing/FishContainers/**`（本轮新增目录：CatFishGuardActor、CatFishTankActor、CatFishContainerService、CatFishPickupSettings）
- `Source/Catfishing/AbilitySystem/**`（Fishing/InputAbilities、Effects、Tags、Attributes、Core、Config、以及本轮新增的 `Physics/CatPhysicalEffortComponent.*` 与 `Config/CatPhysicalEffortSettings.*`）
- `Source/Catfishing/Data/**`（CatFishDefinition、CatFishPersonalityDefinition、CatFishCatalogSettings、CatFishSelectionTypes）
- `Source/Catfishing/Inventory/`（CatInventoryComponent、CatInventorySettings、CatInventoryItemInstance、CatFishInventoryItemInstance、CatFishGuardInventoryItemInstance）、`Source/Catfishing/Collection/`、`Source/Catfishing/Profile/`、`Source/Catfishing/Interaction/`、`Source/Catfishing/Character/`、`Source/Catfishing/Camp/`、`Source/Catfishing/Environment/`、`Source/Catfishing/UI/`（HUD、ViewTypes、ItemTooltip、WorldInfo）、`Source/Catfishing/Framework/Game|Core/`、`Source/Catfishing/Run/`、`Source/Catfishing/Settings/`
- `Config/DefaultGame.ini` 全部 21 个 `[/Script/Catfishing.*]` 段（重点 CatFishingSettings、CatInventorySettings、CatAbilitySettings、CatFishContainerSettings、CatInteractionSettings、CatFishPickupSettings、CatChumFieldSettings、CatRunSettings）
- `Scripts/`（本轮目录已大幅变动，逐个列过并读了 `create_fishing_fight_balance_asset.py` 全文）
- `Docs/Development/工程自补决策记录.md`（D-01…D-32 全表＋末节「2026-09-09 设计对表复核」17 条通读）、`Docs/Development/需求对齐差距清单.md`（已完成的 Fishing 段作线索）
- 裁决账本 `C:/Users/lzcm7/OneDrive/GameProject/CatFishing/Feishu/裁决同步/设计修改记录.md`（读到 2026-09-10 末条；本轮判定按 09-09 下午八问、09-09 晚代码超前项处置与 09-10 三问的最新裁决执行）

**文件类型与方法**
- 只做静态文本检查（②）：Glob 定位 + Grep（类名、字段名、日志事件名 `fishing_*`/`scoop_*`/`cast_*`/`physical_effort_*`、Gameplay Tag 字符串、中文注释、ini 段名）+ 定点 Read 片段。约 200 个 Fishing 相关 .h/.cpp 在扫描面内。
- 设计侧读 `GDD 系统分册/钓鱼系统/钓鱼规则.md`（revision_id 333，fetched 2026-09-11T17:57；评分 §1、§3–§7）与 `钓鱼系统.md`（revision_id 388）总览，行号按镜像文件实际行号（含 front matter）。`数值模拟与参数记录.md` 只作参考、未进计数。
- 每一行的状态与 code_ref 都在本轮重新搜过并读过命中片段，未沿用基线 `2026-09-09-1321/钓鱼系统-核心.md` 的任何判定；基线只用于沿用要求措辞与顺序。共改判 14 行（19/29/41/55/57/62/69/70/73/80/84/87/88 与新增的 99–103）。

**Phase 2b 独立复核补记（同一 HEAD bcc7df9，同一镜像 2026-09-11T17:57）**
- 复核覆盖全部 65 条 ❌／🔄／⚠️（12＋23＋30），逐条独立重搜、重读命中片段；✅ 抽验 14 行、❓ 全查 2 行（该状态只有 2 行，不足 5 行按规则全查）。零条状态改判，六条 code_ref 更正（16/26/36/72/81/82/101 与 Code-only 的 NearShore 行），一条 Notes 补证（80），新增两条漏项（104/105）。
- 复核额外读过而原报告未点名的文件：`Source/Catfishing/Physics/Simulation/CatIntentMotionModel.cpp`（意图位移计价的唯一实现，零意图即零费用，直接决定第 43 行与 §4.4「被鱼拖走不另收费」的判法）、`Source/Catfishing/Character/CatCharacterMovementComponent.cpp:45-66`（静止支撑账的等效意图合成）、`Source/Catfishing/UI/WorldInfo/*`、`Source/Catfishing/UI/ItemTooltip/CatItemTooltipController.h`、`Source/Catfishing/Environment/CatChumFieldSubsystem.h`（确认该子系统根本没有清空全部窝点的公开入口，支撑第 103 行）。
- 裁决账本复核到同一末条（2026-09-10「鱼表格三问」＋本页压缩），未发现晚于 09-10 且会改变本报告任一行判定的裁决；原报告对 09-09 八问③④⑥⑦、09-09 晚「删代码七条／四套性格模板是测试用／力竭拖岸按代码／操作模式方向」与 09-10 冲岸距离列的引用逐条核对无误。
- 一处口径提示（不计入本报告行数）：09-09 裁决⑧写「鱼护容量 6」，代码 `Config/DefaultGame.ini:118 FishGuardCapacity=8`。钓鱼规则 §6.4（:328）把鱼护容量明写为「外部输入」，故不作为本册要求评分，第 83 行括注里的 8 只是证据引用；差异归道具册对表。

**跳过的东西**
- 不适用（结构上没有）：本报告不覆盖 §2 打窝/等待/抽鱼（另一份报告负责）与多人合力/换人/抢收（多人钓鱼附篇）；`鱼/鱼的行为.md` 页头声明「待整页重写，实现不读本页」，只作参考不评分。
- 查不了（有但当前手段验证不了，④/③）：`Content/` 下的 .uasset/.umap 一律不作文本扫描——包括 ST_FishingSession / ST_FishFight 的转移边与 Wait Task 时长、DA_FishingFightBalance_Default 与各 Bite/Fight 性格资产的实际数值、鱼与装备 DataAsset 的三档数值（竿耐久 40/70/120、L_max 60/80/100、猫力 50/70/90/130、漂射程 10/15/20 米、逐鱼可捞圆半径）、IMC/IA 键位与 Hold 触发时长、WBP_CatHUD 的提示文案与阈值配色、Montage 与 Cue。标 ❓ 的两行与多处 ⚠️ 的 Notes 都点明了这一点，**不得据本报告把它们当作未实现**。
- 本轮新增的「查不了」面：`Scripts/` 下 `update_fishing_cast_ranges.py`、`configure_starter_rod_tuning.py`、`create_default_cat_character_asset.py`、`create_formal_starter_scoop_asset.py` 已从工作区删除，基线据以给出 ② 证据的三档射程、初级竿耐久 150、抄网资产 reach 三个现行值本轮退回 ③（第 7/32/69 行已注明）。现存可查的落盘脚本只有 `create_fishing_fight_balance_asset.py`。
- 未知（没查）：引擎入口 `Scripts/verify_*.py` / `verify_*.ps1` 本轮未运行；`Content/` 资产的属性未走引擎导出。
- `Source/**/Tests/` 只作旁证阅读，未据测试断言给任何 ✅；本报告所有 ✅ 均给出正式运行链的 file:line。

### Code-only mechanics

| 机制 | code_ref | 文档现状与出处 | 待确认问题 |
|---|---|---|---|
| 猫体力归零后的「持续拖拽态」：不结场，鱼按 `ExhaustedCatTowAccelerationCentimetersPerSecondSquared` 继续把力竭猫往外拽，直到真实水深触发落水表现 | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.cpp（`ShouldEscapeExhaustedCat`）；Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.h:7-14,52-55 | 设计未写。钓鱼规则 §4.6（:222）只有「猫体力 ≤ 0 → 猫力竭 → 被拖下水，鱼逃」一个瞬时出口；09-09 裁决③重申「落水或猫体力归零即结场」。查阅范围：钓鱼规则全篇、设计修改记录 09-07～09-10 | 这个中间态是要写进 §4.6 作为落水前的过渡（那要定拖拽时长与可否自救），还是该删掉、按裁决③直接结场？ |
| 每人场上最多两根实体竿：一个人可以同时部署两根竿、各自跑独立会话 | Source/Catfishing/Fishing/CatFishingService.h:25-26（`MaximumDeployedRodsPerPlayer = 2`）；Source/Catfishing/Fishing/CatFishingService.cpp:452-481 | 设计未写。钓鱼规则 §1「一竿流程」（:23）与 §3.1 全篇按单竿叙述，道具册携带口径也没有「几根竿同时下水」这一条。查阅范围：钓鱼规则 §1/§3、钓鱼系统册 §3「选位与准备」 | 一人多竿是既定玩法还是工程占位？若保留，§1 的「一竿流程」与体力/饵消耗要不要按多竿重写？ |
| 强对抗与僵局判定：`StrongConfrontationAlignmentThreshold` + `StrongConfrontationConfirmationSeconds` 确认后置 `bStrongConfrontation`/`bStalemate` 并复制给 UI | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.cpp:650-662；Source/Catfishing/Fishing/CatFishingTypes.h（`bStrongConfrontation`） | 设计已删。09-07 决策点①的「僵持态」与 09-09 的位移驱动公式一并作废，现行钓鱼规则 §4.3/§4.4 没有「强对抗」这个概念；多人附篇的僵局只讲共线对顶演出。日志自标 `StrongConfrontationRole=PresentationOnly` | 它现在只驱动表现，是保留为 §7 的一个表现信号（那要进动作表现集清单），还是随僵持态一起删？ |
| 通用身体出力体力账：非钓鱼场合（推挤、抓握、攀爬）按「意图位移 − 实际位移」扣同一条搏斗体力，归零进 `bExhausted` 并强制松开所有抓握 | Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:36-108；Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h:13-23 | 设计未写。钓鱼规则 §6.2（:305）只声明「钓鱼输出扣体、力竭／落水结果」，猫体力在钓鱼之外被消耗没有出处；§4.1 也只把猫体力定义为搏斗资源 | 搏斗体力是否正式改成「全身出力池」（那 §4.1 的定义与 §6.2 的接口要改，恢复口径也要重算），还是该把非钓鱼消耗拆成另一条资源？ |
| 鱼距超 `L_max + EscapeSlackCentimeters`（现值 100cm）判 `Escaped` 终局 | Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.cpp:448-451；Scripts/create_fishing_fight_balance_asset.py:46 | 设计未写这条终局。钓鱼规则 §4.5（:211）只说「线出尽后解锁失效，张力全传给猫，被迫全额对抗」，没有「超出即鱼逃」；§4.6 的归零表也没有这一行 | 线出尽之后还能继续被拉开并直接鱼逃，是要补进 §4.5／§4.6 的出口表，还是该改成设计说的「强制对抗、不脱钩」？ |
| 物品悬停提示在 C++ 里把鱼重量与鱼竿耐久写成明文数字（「重量：{n} kg」「耐久：{当前} / {上限}（已断裂）」） | Source/Catfishing/UI/ItemTooltip/CatItemTooltipModel.cpp:24-46；Source/Catfishing/UI/ItemTooltip/CatItemTooltipWidget.h:41 | 设计未写。钓鱼规则 §7.3（:375）只规定「线长余量给世界内表现、力量差不给数字」，耐久与重量要不要出数字没有口径；交互册的提示文案子表 09-10 才新建 | 耐久/重量出数字是否符合「信息靠世界表现读」的红线？要写进交互册「提示文案」表还是收进世界内表现？ |
| `NearShore` 兼容阶段与 `NearShoreWidthCentimeters=300` 近岸带校验仍在运行链上（复核更正 ini 行号 213→219） | Source/Catfishing/Fishing/CatFishingTypes.h:20-21；Source/Catfishing/Fishing/CatFishingSession.cpp:70-88,114-124；Config/DefaultGame.ini:219 | 设计已删对应口径。09-09 裁决⑦作废「鱼离岸 1 米」，钓鱼规则 §5.1／§5.5 现在只认射线∩可捞圆，没有「近岸带」这个概念；枚举注释自称「兼容近岸阶段」 | 这个阶段与 300cm 近岸带是否该随 D-18 一起下线？留着会不会让 ST_FishingSession 再走出一条设计里不存在的阶段路径？ |

### 复核记录

**检查范围（Phase 2b 独立复核，同一 HEAD bcc7df9 / 同一镜像 revision_id 333）**

- 逐行驳的待驳行：全部 65 行 ❌／🔄／⚠️（❌ 12、🔄 23、⚠️ 30）。每行独立换关键词重搜（类名、字段名、日志事件名、中文注释、ini 段名、GAS Tag 串），命中即打开读调用链，确认没有才维持原判。零条状态改判。
- ✅ 抽验 14 行（要求最少 5）：1、5、10、13、23、26、36、41、55、66、70、75、100、101，全部按 file:line 实读片段并核语义，不只核文件名；抽样优先取 code_ref 单一、语义跨度大、本轮改判过的行。
- ❓ 抽验 2 行（该状态全部行数即 2，不足 5 行按规则全查，实际数量 2）：92、95。复查结论是没有把可文本检查的东西推给「查不了」——95 把 `Source/Catfishing/UI/` 全目录与 Config、Scripts 搜过 `0.3`／`LowStamina`／`Threshold`／`StaminaWarn`／`体力偏低`，零命中；92 可静态查的三块（浮漂三态参数、`BiteSignalStability` 零运行消费者、只有全局 `bForceFeedbackEnabled` 无咬钩专用震动）原报告已查完并写进 Notes。两行维持 ❓。
- 漏项复核：按六章标题、子规格与表格逐节点名，补第 104、105 行（编号接续，原行号不重排）。另四条判为已被现有行吸收、不另立行：§4.4「被鱼拖走不另收费」（零移动意图即零费用，`CatIntentMotionModel.cpp:19-20` 显式短路，归第 43 行）、§4.5「放线时 D 的增减方向」（第 47／49 行）、§5.4「其余各自 3 秒独立硬直」（第 59 行）、§1「岸边只能切换已经带出的物品」（属背包／营地边界，不在钓鱼册可判面）。
- Code-only mechanics 复核：七条 code_ref 逐条成立，另搜过 `MaximumDeployedRodsPerPlayer`／`StrongConfrontation*`／`EscapeSlackCentimeters`／`ExhaustedCatTow*`／`SupportReferenceSpeedCmS` 的消费链，未发现第八条漏登机制；仅一处 ini 行号更正。
- 未能完成的检查：`Content/` 下 .uasset／.umap 仍未走引擎入口（③④），`Scripts/verify_*.py` 未运行。与原报告声明一致，本复核没有扩大可检查面，也没有据此把任何 ③④ 项改判成实现或缺失。

| # | 原判 | 改判 | 依据 |
|---|---|---|---|
| 1 | ✅ Implemented | ✅ Implemented | 抽验：CatFishingTypes.h:9-31 实读，`ECatFishingPhase` 确为 11 值，注释「转移拓扑只由 ST_FishingSession 资产编排」；维持 ✅ 与「转移边未逐边核对」的限制说明 |
| 5 | ✅ Implemented | ✅ Implemented | 抽验：CatFishingViewTypes.h:37 逐字命中 `DeprecationMessage="Use bReeling; charging was removed"`；BeginCast（CatFishingService.cpp:243-250）只消费客户端候选点并吸附水面，全程无站位合法区判定 |
| 10 | ✅ Implemented | ✅ Implemented | 抽验：`SampleChumAtPoint(AttemptSnapshot.ServerCorrectedLandingWorldPoint, …)` 实读于 CatFishingSession.cpp:706，原 code_ref 区间 702-716 覆盖该行 |
| 13 | ✅ Implemented | ✅ Implemented | 抽验：同 706 行，采样点是冻结的落点而非 Hook Actor 当前位置，「不随漂移重算」成立 |
| 16 | 🔄 Divergent | 🔄 Divergent | 依据更正：`DefaultQuantityStackCapacity=5` 在 Config/DefaultGame.ini 第 63 行，原写的第 62 行是 `PlayerInventorySlotCapacity=24`。单一全局上限的结论不变 |
| 23 | ✅ Implemented | ✅ Implemented | 抽验：CatFishingSession.cpp:1776 冻结 `SinceBite`、1797 闭区间比较 `<= Bite->PerfectHookWindowSeconds`，注释「服务器完全按自己的时间戳判定…不接受客户端上报的反应时间」 |
| 26 | ✅ Implemented | ✅ Implemented | 依据更正：三项削减实际落在 1092（FishStrength）／1144（FishStamina）／1157（LineLength Clamp），原 code_ref 的 1090 偏 2 行；语义与 ✅ 不变 |
| 36 | ✅ Implemented | ✅ Implemented | 依据更正：`Config.PrimaryOperatorCatStrength = GetNumericAttribute(FishingStrength)` 在 CatFishingSession.cpp:1082-1083，原写的 1069-1071 是体力初始化失败日志；Runner 逐步刷新在 FightRunner.cpp:267-268 |
| 41 | ✅ Implemented | ✅ Implemented | 抽验（本轮改判行重点核）：Simulator.cpp:586-627 两次 `ComputeCatChannel` 逐项计价，`CatHoldStaminaDrain` 只在 `bLoadedConstraint && !bFreeSpool` 收，`CatRodSupportStaminaDrain = max(0, RodSupport - Hold)` 与末尾三项相加正好等于 `max(Hold, RodSupport)`，「只收较高一项」算式闭合；WorkModel.cpp:12-13 即「空载系数＋负载×负载系数」；`bSlackRecovery` 整段跳过（629-635） |
| 55 | ✅ Implemented | ✅ Implemented | 抽验（本轮改判行重点核）：CatFishingSession.cpp:392-398 的 `bRayReachesFish` 是唯一范围判定，注释逐字写「不再要求鱼处于近岸带内…再叠一层离岸距离等于两套口径」；阶段白名单 HookedFight/NearShore/ExhaustedReel 同址可见 |
| 66 | ✅ Implemented | ✅ Implemented | 抽验：`ScoopTerminalCache` 键＝身份＋RequestId、`bCaptureResolved` 一次性收口、命令载荷只有 RequestId 与服务器重建身份（注释「客户端提交的任何身份字段都不可信」），实读 CatFishingSession.cpp:330-345,398-418 |
| 70 | ✅ Implemented | ✅ Implemented | 抽验（本轮改判行重点核）：`ResolveScoopFacingHorizontal` 调用处注释逐字写「这是身体动作而非镜头瞄准动作；自由转动 Camera/Controller 不得改变抄网判定方向」，日志字段 `ScoopFacingSource=CharacterActorForward` 同址 |
| 72 | ⚠️ Partial | ⚠️ Partial | 依据更正：`FocusStencilValue=1` 在 Config/DefaultGame.ini 第 270 行（第 271 行是空行）；两套口径的结论不变 |
| 75 | ✅ Implemented | ✅ Implemented | 抽验：CatFishingSession.cpp:991 `Snapshot.FishWeightKilograms = FrozenSelectionResult.WeightKilograms`，重量来自冻结选鱼上下文，链上无参与者数量项 |
| 80 | ⚠️ Partial | ⚠️ Partial | 依据补强（状态不变）：除原列两处，复核发现第三道闸——回体还要 `bRecoveryPending`，它只由协作出力或协作中体力见底置位（CatPhysicalEffortComponent.cpp:61,70,81），而钓鱼扣体走 Runner、该组件在 `Drive.bFishing` 时整段 return（同文件:43），所以钓鱼扣掉的体力在搏斗外没有进入 5 点/秒通道的路径。三处都属「有速率、无覆盖」，仍判 ⚠️ 而非 🔄 |
| 81 | ✅ Implemented | ✅ Implemented | 依据更正：四个字段实际在 CatFishingFightSimulator.h:157-158（`FishEffortRatio` 与「零出力仍保留活鱼惯性」注释）、250、265、292；原 294-300／320-327 指的是 `NormalizedLineLoad`～`Outcome` 与类声明 |
| 82 | ⚠️ Partial | ⚠️ Partial | 依据更正：`NormalizedTension`＝280、`bLineTaut`＝281、`LineTensionNewtons`＝302；原 290-291／305 指的是位置纠偏与 `EffectiveCatStrength`。「完美系数由 Session 直接乘进模拟配置、不交鱼侧」经 1052-1055 复核成立，维持 ⚠️ |
| 100 | ✅ Implemented | ✅ Implemented | 抽验：CatFishingSession.cpp:1885-1902 的 lambda 三条件逐字命中——Grounded 且 `GetOperatorCount()==0`、竿主或 `LastSuspendedFisherPlayerState`、`DistSquared <= Square(250.0)`；2.5 米与设计逐字对上 |
| 101 | ✅ Implemented | ✅ Implemented | 依据更正：`MaximumScoopGroundSlopeDegrees=45.0` 在 Config/DefaultGame.ini 第 220 行（第 214 行是 `HeldRodMaximumAngularSpeedDegreesPerSecond=360.0`）；三项拒绝条件实读 CatFishingSession.cpp:370-385，逐项谓词日志同址 |
| 104 | 新增 | ✅ Implemented | 漏项补行（钓鱼规则.md:209「等待期移动不受线约束」）：线长约束与竿端受力只在 `FightRunner` 内，Runner 只在 `TryEnterHookedFightFromAuthority` 里 `NewObject`（CatFishingSession.cpp:1233），`SetFishingMotorBudget` 唯一调用点在 CatFishingPhysicalRodComponent.cpp:363；Waiting/Probe/TrueBiteWindow 无任何把 D 钳回 L_max 的代码 |
| 105 | 新增 | ✅ Implemented | 漏项补行（钓鱼规则.md:371「不新增查看装备的菜单」）：悬停提示唯一来源是本人背包格 `UCatInventorySlotWidget`（CatItemTooltipController.h:23-25,40）；新增的 `WorldInfo` 信息牌只挂在 CatAltarActor.cpp:28 与 CatFishTankActor.cpp:28 两个世界物件上，无角色/装备版本，全库无按玩家查装备的入口 |
| Code-only · NearShore 行 | 代码超前项 | 代码超前项 | 依据更正：`NearShoreWidthCentimeters=300.0` 在 Config/DefaultGame.ini 第 219 行（第 213 行是 `FixedFightStepSeconds=0.05`）；该机制仍在运行链上的结论不变 |
| 8、9、18、38、40、45、64、65、68、73、97、103 | ❌ Missing | ❌ Missing | 12 条 ❌ 逐条独立重搜后全部维持。关键复搜证据：散布两字段全库只有 `CatEquipmentFragment_Float.cpp:8-12` 的就绪校验、零运行消费者（8／9）；`ActivationBlockedTags` 全库零命中、`CanUseItemAtSlot` 只在 CatInventoryComponent.cpp:2538 查槽位（18）；`碾压`／`Overwhelm`／`Crush`／`Overpower`／`Fling`／`Ashore` 全库只命中物理原型注释，`StrengthRatio` 只在 CatFishCatalogSettings.cpp:61-67 的选鱼挑战度（38）；`冲岸`／`ShoreDistance`／`MinimumShore`／`离岸` 在 Data/ 与 Behavior/ 零命中（40）；`CommitCatchEquipmentFromAuthority`(CatFishingSession.cpp:1545-1554) 函数体只有一句 `CommitFishingBaitDeferred` 加「不能再重复扣耐久」注释（45）；`苏醒`／`Revive`／`Awaken`／`RevivalSeconds`／`ExhaustedTimeout` 全库零命中（64，并连带架空 68）；`Items/`、`FishContainers/` 下 `DayTransition`／`翻天`／`LifeSpan`／`Expire` 零命中（65）；`ECatProfileGrantKind::` 的三处赋值只有 FishRecorded／Unlock／Imprint（CatRunImprintService.cpp:43,75,299），`FishSilhouette` 确无生产者（73）；`增效`／`Buff`／`PassiveEffect` 零通道、`bSpecialBait` 只剩 `CatEquipmentFragment_Bait.h:19` 一个身份位（97）；`UCatChumFieldSubsystem` 公开 API（CatChumFieldSubsystem.h:61-71）根本没有清空全部窝点的入口，`ClearAllFields`／`ResetFields`／`ExpireAll` 全库零命中（103） |
| 2、3、7、12、15、16、20、22、24、25、30、31、33、34、43、44、48、50、51、52、63、74、93 | 🔄 Divergent | 🔄 Divergent | 23 条 🔄 逐条重核后全部维持，未发现「其实已实现」的漏判。逐项落点：抛竿硬前置与 `OperateRod`／`LeaveRod`（CatFishingService.cpp:189-192）；`MaxRange` 量的是 `ToLandingFromRod`（同文件:255-258，竿尖不是猫站立点，第 7／34 行同因）；饵在 `BeginFishingUse` 按实例 ID 冻结（同文件:232-241）而扣饵在选鱼事务内 `CommitFishingBaitDeferred`（CatFishingSession.cpp:977）；`HandleTrueBiteWindowExpired`(1002-1016) 注释逐字写「漏按只结束当前咬钩机会，不结束整次架杆会话」；真咬窗用全局 `Settings->TrueBiteWindowSeconds`(800,820,829)，鱼种级同名字段只在 CatFishPersonalityDefinition.cpp:6 的就绪校验里；三个完美倍率全走 `BitePersonalityId`(1052-1055)，`RarityTierId` 不进取值；`Snapshot.FishFightStaminaRemaining = SelectedDefinition->FishFightStamina`(995) 与重量无关；鱼体力扣减在钓鱼侧 Simulator.cpp:566-577；`ECatFightStepOutcome` 只有四值、无猫力竭终局（CatFishingFightSimulator.h:7-14），`HandleCatEnteredDangerousWaterFromAuthority`(1522-1544) 只播表现并释放操作位、不写终态；鱼竿片段（CatEquipmentFragment_Rod.h 全 48 行）确无竿强度承载列，`Equipment/` 下 `FishingStrength` 零命中；磨损式 `(max(鱼线力,猫线力)×StalemateRodWearPerFishStrength + FishFullEffortRodWearPerSecond×出力²)×WearLoad×dt×TautRodWearMultiplier`(Simulator.cpp:665-678) 与「鱼力×0.05」两处不同；移动费按意图缺口计价（OperatorWorkModel.cpp:11-17）无前移/后退两档常数 |
| 6、17、21、28、29、32、35、39、42、46、49、53、54、57、59、60、62、67、69、76…96 中的 30 条 | ⚠️ Partial | ⚠️ Partial | 30 条 ⚠️ 逐条重核后全部维持。重点复验：两条抛竿拒绝走不同错误码且全库无「错误码→文案」映射（6）；`CommitAbility` 在 ActivateAbility 首行无条件施冷却，成功抄到的猫同吃 3 秒（CatFishingScoopAbility.cpp:21-25）（59）；`BeginCast` 全函数（CatFishingService.cpp:165-300）`Mouth`／`Carried` 零命中，叼鱼不拒抛竿（60）；`ExhaustedReel` 出口要求 `Dist2D(Encounter, RodTip) <= ReachTolerance`（CatFishingSession.cpp:1511-1514），落地点确非碰岸点（62）；拾取半径走 `CatInteractionSettings`（CatFishPickupActor.cpp:988-992）而非 1.5 米（69）；`ServerDropCarriedItem`（CatfishingPlayerController.cpp:1093-1126）无水域拒绝分支（84）；`CanAcceptGameplayCommand` 在 `DayTransition.bActive` 期间只拒新命令、不打断会话（CatfishingGameModeBase.cpp:785）（89）；鱼力量＝`WeightKilograms × Context.StrengthPerKilogram`（CatFishCatalogSettings.cpp:202）确按个体重量缩放但 K 是全局（29） |
