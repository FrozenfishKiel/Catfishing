## 猫咪与状态

### Features

| # | Design Requirement | Status | Code Reference | Notes |
|---|---|---|---|---|
| 1 | 品种决定初始力量，普通 50／橘 70／狸花 90／缅因 130（猫咪与状态.md:15） | ⚠️ Partial | `Source/Catfishing/Character/CatCharacterDefinition.h:35`；`Source/Catfishing/AbilitySystem/Config/CatAbilitySettings.cpp:95`；`Config/DefaultGame.ini:145`、`Config/DefaultGame.ini:150` | 「按猫种给初始力量」的机制仍成立：`UCatCharacterDefinition::FishingStrength` 在属性播种时写进 ASC。正式清单仍只登记 `Cat_Default` 一条（`Content/Catfishing/Data/Character/` 下只有 `Cat_Default.uasset`），全局回退 `InitialFishingStrength=50.0`，四品种资产不存在。本轮新增的 `BP_CuteCatCharacter` 是换皮子类（共用 `BP_CatCharacterBase`），不带第二份 `CatDefinitionId` 数值模板。四个具体数值写在 .uasset 里，属 ③，本轮不跑引擎查不了。 |
| 2 | 选猫界面用非数值示意力气（鱼骨 icon ×1～×4 ＋性格文案），不显示数字（猫咪与状态.md:15） | ❌ Missing | — | `Source/Catfishing/UI/CatUISettings.h:85`–`113` 的八个正式 WBP 入口里仍没有选猫页；`Source/Catfishing/UI/Frontend/` 只有 Page／Room／Save／Settings／Root 五套 Model／Controller。全库 `CharacterSelect`／`CatSelect`／`品种`／`breed` 零命中。 |
| 3 | 吃鱼给经验并压入经验槽（猫咪与状态.md:80） | ✅ Implemented | `Source/Catfishing/Growth/CatGrowthComponent.cpp:56`、`Source/Catfishing/Growth/CatGrowthComponent.cpp:72`–`88`；`Source/Catfishing/FishContainers/CatFishContainerService.cpp:428` | 实物鱼提交后按 RequestId 幂等加经验，复制 `FCatGrowthSnapshot`。本轮吃鱼入口从旧 `CatFishConsumptionCoordinator` 迁到 `UCatFishContainerService::ConsumeFish`（`:653`）与 `UCatFishInventoryItemInstance`（`Source/Catfishing/Inventory/CatFishInventoryItemInstance.cpp:166`），成长写口不变。 |
| 4 | 偷来的鱼吃掉同样获得成长（猫咪与状态.md:41；数值成长.md:19） | ✅ Implemented | `Source/Catfishing/Social/CatSocialService.cpp:501` | 偷鱼链路复用同一个 `ConsumeCommittedFish`，成长入口一致；先过 `ValidateFishConsumption`（`:496`）。 |
| 5 | 经验只由吃鱼产生，钓鱼行为本身不给经验（数值成长.md:38） | ✅ Implemented | `Source/Catfishing/Growth/CatGrowthComponent.cpp:38`–`63`、`Source/Catfishing/Growth/CatGrowthComponent.cpp:72` | `AddExperienceFromCommittedFish` 全项目唯一调用点仍在 `ApplyCommittedFish`；搜过 `TotalExperience`／`ExperienceInCurrentSlot`，除 HUD 只读投影外无第二个写入者。 |
| 6 | 经验 ＝ 鱼种经验系数 × 实际重量（数值成长.md:35） | 🔄 Divergent | `Source/Catfishing/Data/CatFishDefinition.h:174`–`175`；`Source/Catfishing/Growth/CatGrowthComponent.cpp:56` | 代码仍用 `EatingExperience` 单值（字段注释逐字写着「值来自当前鱼表体重档」），`FloorToInt` 后直接入槽，不乘实际重量，鱼定义里也没有「经验系数」字段。D-09 登记的是旧四档体重轴口径，`工程自补决策记录.md:68` 已自认前提整体过期（经验 09-07 退出体重档链），**D-09 需复核**。 |
| 7 | 经验槽 250 点（数值成长.md:35；升级效果.md:13） | 🔄 Divergent | `Config/DefaultGame.ini:175`；`Source/Catfishing/Growth/CatGrowthSettings.h:21` | 配置仍是 `ExperiencePerChoiceSlot=10`，字段注释仍写「当前需求锁定为 10」。设计 09-07 校准为 250（数值成长.md:99 v1.9），无对应 D 条目登记，属未跟进而非有意分歧。 |
| 8 | 溢出经验滚入下一槽、连满时逐个弹出（数值成长.md:36） | ⚠️ Partial | `Source/Catfishing/Growth/CatGrowthComponent.cpp:81`–`85` | 溢出滚槽与连满累计已实现（while 循环 ＋ `PendingChoiceCount`）；「逐个弹出」依赖三选一面板，面板不存在（见第 10 行）。 |
| 9 | 槽满触发三选一：随机抽三项、同次互异、当场生效、本局永久、同项可跨次叠加、按出现次序解锁（数值成长.md:36；升级效果.md:19） | ❌ Missing | `Source/Catfishing/Growth/CatGrowthComponent.cpp:84` | 满槽只把 `PendingChoiceCount` 加一。全库搜过 `ChoiceOption`／`UpgradeOption`／`GrowthOption`／`OptionPool`／`PerkPool`／`三选一`，命中仅 `PendingChoiceCount` 一处与四条注释，无选项池、抽取、生效或叠加代码。`Docs/Development/需求对齐差距清单.md:388` 仍自述「Buff 选项、具体数值／文案、疲惫纯表现触发条件和黄色体力护盾表现边界仍未裁」。 |
| 10 | 三选一面板不冻结世界、个人选择不阻挡他人、可挂起、无超时（数值成长.md:36） | ❌ Missing | — | 无三选一面板（`Source/Catfishing/UI/CatUISettings.h:85`–`113` 无对应 WBP 类），面板行为规则无承载。 |
| 11 | 选项池 11 项的效果定义、每次数值、叠加上限、出现次序（升级效果.md:26–37） | ❌ Missing | — | 全库无选项池 DataAsset、无配置段、无任何「力量 +10／体力上限 +20／完美窗 +0.5s／背包 +1／后勤扩容 +2／咬钩 -5%／重量 +5%／磨损 -10%」的成长修饰器。底层被作用面（力量、体力上限、背包格数、完美窗、咬钩间隔、竿耐久）在钓鱼／道具侧已存在，缺的是成长项本身。 |
| 12 | 「buff 持续时长」升级项不作用于猫神的祝福（数值成长.md:46；升级效果.md:30） | ❌ Missing | — | 限时 buff 与祝福均未实现，豁免规则无对象。 |
| 13 | 吃鱼给限时 buff，按鱼种配置、时长 60～600 秒（猫咪与状态.md:80；数值成长.md:26、44） | ❌ Missing | `Source/Catfishing/Data/CatFishDefinition.h:170`–`183` | 鱼定义 `Use` 段仍只有 `FoodSafety`／`EatingExperience`／`PoisonIncrease`／`bTankDisplayEligible` 四项，没有 buff 与持续时间字段；`Source/Catfishing/AbilitySystem/Effects/` 下的 GE 全是 Run／Shop／Poison／Stamina 类，无吃鱼 buff。字段都不存在，不是「值在资产里查不了」。 |
| 14 | buff 叠加规则「类＝鱼种」：同种刷新时长、异种并存（数值成长.md:45） | ❌ Missing | — | 无 buff 实体，叠加规则无承载。 |
| 15 | 增益效果族：移速 +15%／+25%、省力 -25%／-50%、打窝增效 +50%（吃鱼效果.md:23–28） | ❌ Missing | — | 无对应 GE、无移速／消耗系数／窝料效果的成长修饰入口；`UCatPhysicalEffortSettings`（`Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h`）只有全局单价，无按鱼种的减免通道。 |
| 16 | 社交演出族 buff 四项：变声器、满嘴泥巴、头上长小花、请勿靠近（吃鱼效果.md:39–42） | ❌ Missing | `Source/Catfishing/AbilitySystem/Tags/CatFishingAbilityTags.cpp:26`–`37` | 无对应 GE 与 GameplayCue；Tag 表里只有六条 BodyAction 事件与四条 Fishing Cosmetic，无任何吃鱼演出 Tag。 |
| 17 | 「请勿靠近」屏蔽帮助与恶作剧（搬运被屏蔽、偷鱼被抓的扑倒反制不被屏蔽）（吃鱼效果.md:44） | ❌ Missing | `Source/Catfishing/Social/CatSocialService.cpp:295`–`300`；`Source/Catfishing/Camp/CatCampHubActor.cpp:183`–`225` | 恶作剧屏蔽仍只有防骚扰牌子一条通道（`ACatProtectionSignActor::ProtectsMischiefAgainst`）；搬运 `RescueToCamp` 只判救援者未倒地、目标已倒地与距离，无任何 buff 屏蔽判定。 |
| 18 | 猫神的祝福：献祭湖心巨影后全队 600 秒（省力 -50% ＋黄色体力 +50 ＋金光挡水花），不可被覆盖（吃鱼效果.md:29、58；数值成长.md:46） | ❌ Missing | `Source/Catfishing/AbilitySystem/Executions/CatRunSettleOfferingExecutionCalculation.cpp:85`–`130`；`Source/Catfishing/Camp/CatAltarActor.cpp:209`–`243` | 本轮献祭链已按 09-09 裁决②重写为「祭坛半径内实物鱼批量结算」，但结算只产出世界进度增减，不发任何全队 buff；献祭物按重量档计点，不识别巨影身份。**复核更正证据**：`Blessing` 与 `祝福` 在 `Source/`／`Config/`／`Scripts/` 全文各为 **零命中**（原文写的「仅命中 Fishing 调试配色」是把第 19 行的 `Yellow` 搜索结果串到了这一行，`Yellow` 才只命中 `CatFishingDebugSubsystem.cpp` 与 `CatPhysicsPrototypePawn.cpp` 的调试配色）；结论不变，证据更强。 |
| 19 | 黄色体力：体力条末端一次性护盾段，绿先扣、不自然回复、可叠加无上限、过夜清空，来源＝特定鱼种与祝福（数值成长.md:50–52） | ❌ Missing | `Source/Catfishing/AbilitySystem/Attributes/CatSurvivalAttributeSet.h:28`、`:33`、`:38`、`:43` | 属性集仍只有 `FishingStrength`／`FightStamina`／`MaxFightStamina`／`Poison` 四项；全库 `Yellow`／`Shield`／`护盾` 零业务命中。`需求对齐差距清单.md:388` 同样记为未裁未做。 |
| 20 | 搏斗外体力自然回复 5 点/秒、无场景乘数（数值成长.md:56） | ⚠️ Partial | `Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h:19`–`21`；`Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:76`–`91`；`Source/Catfishing/Character/CatCharacterMovementComponent.cpp:97`；`Source/Catfishing/Character/Physics/CatPhysicalBodyComponent.cpp:393`–`398` | **本轮改判（原 ❌ Missing）**：新增的个人出力组件带了搏斗外回体，速率 `RecoveryPerSecond = 5.0` 与设计一致、无场景乘数。但三处不齐：①只对本次付过出力费的猫开启（`bRecoveryPending` 仅由 `bExerting` 或零体力置位，`:61`、`:70`），刚打完一场搏斗、单纯站着的猫不回体——**复核补证**：闸门比原文写的更窄，`bExerting` 依赖 `bActive` 依赖 `Drive.bCooperative`，而 `bCooperative` 只在 `CaptureDriveSample` 里 `!bFishing && bConnected`（被抓握、受外力或有身体接触）时置真（`CatPhysicalBodyComponent.cpp:393`–`398`），所以整条回体通道只存在于合力／接触态，一只脱离接触独自站着或走路的猫既不付费也不回体；②要求连续静止满 `RecoveryDelaySeconds = 2.0` 才起算（`:80`），设计无此延迟；③搏斗入场另有一条「补满体力」通道与它并存（见 Code-only 第 3 条），设计的「空条约 20 秒回满」在钓鱼路径上因此读不到。9-09 裁决⑥重申「搏斗外 5 点/秒且搏斗中不恢复」不变。 |
| 21 | 搏斗内放线本身不回体，回体速率基础为 0、只靠三选一「放线回体速度」加（数值成长.md:56；升级效果.md:29） | 🔄 Divergent | `Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:350`–`351`；`Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.cpp:633`–`637`；`Source/Catfishing/Fishing/Simulation/CatFishingFightSimulator.h:77`；`Source/Catfishing/Fishing/Config/CatFishingFightBalanceDefinition.h:135`–`138` | 放线回体仍是常驻结算项（`bSlackRecoveryActive` 且未受载时按速率回体），结构上不是「基础 0 ＋成长项加成」；`SlackStaminaRegenPerSecond` 的字段注释本身就写着「正常右键且身体／杆完全卸载时主控每秒恢复的体力」。正式值在 FightBalance .uasset 里属 ③查不了；模拟器默认 1.5，与「基础 0」相反。09-09 裁决⑥重申该口径不变，仍无对应 D 条目。 |
| 22 | 咸鱼不可食用、湖心巨影无法食用（吃鱼效果.md:58、62） | ❌ Missing | `Source/Catfishing/Data/CatFishDefinition.cpp:33`–`36`；`Config/DefaultGame.ini:104`、`Config/DefaultGame.ini:107` | `ECatFishFoodSafety` 仍只有 `Unset`／`Safe`／`Toxic`，`IsRuntimeDefinitionReady` 的 `bFoodReady` 要求 FoodSafety ∈ {Safe, Toxic} 且 `EatingExperience > 0`；而 `Fish_Salted` 与 `Fish_LakeGiantShadow` 都在 16 条正式 `+Definitions=` 清单里，所以它们必然被标成可食用、且必须给正经验，否则整条鱼从鱼表消失、根本钓不上来。设计要的「可钓可卖、不可食用」在当前数据模型里仍无法表达。搜过：`FoodSafety`、`ECatFishFoodSafety`、`Inedible`、`不可食用`。 |
| 23 | 第 10 天结算生成「毕业照」印记（本局 build 快照 ＋猫味外号）（升级效果.md:44） | ❌ Missing | `Config/DefaultGame.ini:53`、`Config/DefaultGame.ini:123`、`Config/DefaultGame.ini:285`；`Source/Catfishing/Run/CatRunSettings.cpp:89`–`91` | 正式印记事件 ID 仍只有 `RainBloom`／`CampfireCover`／`TheftCaught`；无 build 快照或外号生成代码。**残留出处**：本行的「第 10 天」触发时点已被 09-09 裁决①推翻（推进轴改世界进度、代码的每日额度与第 10 天毕业作废），代码也已改成 `CanEnterSuccessSettlementNight(WorldProgress >= 100)`、过场文案「毕业之夜」（`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1301`）；升级效果.md:44 未同步。按裁决评分：末段结算生成毕业照这件事本身仍未实现。 |
| 24 | 负面效果族三项：重毒＝吃下即倒地且苏醒后力量 -50%、麻痹＝体力不再自然回复、中毒＝体力上限 -40%（吃鱼效果.md:52–54） | ⚠️ Partial | `Source/Catfishing/Condition/CatConditionComponent.cpp:346`–`350`；`Source/Catfishing/Data/CatFishDefinition.h:177`–`179` | 只有「Toxic 鱼 → `ApplyPoisonDelta(PoisonIncrease)`」一条通道。麻痹（禁自然回复）、体力上限 -40%、苏醒后力量 -50% 三项都没有字段也没有实现；`PoisonIncrease` 一个标量表达不了这三种不同效果。 |
| 25 | 中毒按鱼各配、无渐进升级，最重一档＝吃下即倒地（猫咪与状态.md:104） | 🔄 Divergent | `Source/Catfishing/Condition/CatConditionSettings.h:25`；`Config/DefaultGame.ini:163`；`Source/Catfishing/Condition/CatConditionComponent.cpp:520` | 代码仍是跨鱼累加的 Poison 标量 ＋ 阈值 100 倒地，正是被设计砍掉的「渐进加重」模型：连吃两条轻毒鱼也会倒地，而单条重毒鱼是否倒地取决于该鱼 `PoisonIncrease` 是否 ≥100（值在 .uasset 里属 ③查不了）。无对应 D 条目登记。 |
| 26 | 倒地来源全游戏唯一＝吃到重毒鱼（猫咪与状态.md:116；吃鱼效果.md:48） | ✅ Implemented | `Source/Catfishing/Condition/CatConditionComponent.cpp:511`–`533` | `Snapshot.bDowned` 的唯一写入点是 `EvaluateDownedFromAttributes`，只由 Poison 阈值裁决；全库无第二个写入点，也没有死亡分支。 |
| 27 | 认识一种鱼分两层：钓到＝记外观与名字，首次吃过＝图鉴补食用效果（猫咪与状态.md:104；速查表 :224） | ❌ Missing | `Source/Catfishing/Framework/Core/CatProfileContracts.h:12`–`31` | 图鉴仍只有 `Unknown → Silhouette → Recorded` 三态，Grant 只有 `FishRecorded`／`FishSilhouette`／`Imprint`／`Unlock`；没有「吃过＝解锁效果知识层」的第二层。毒鱼防盗陷阱（猫咪与状态.md:175）依赖这一层，同样不成立。归属在鱼册／图鉴册，本册消费。 |
| 28 | 吃掉的鱼不影响图鉴，钓到瞬间已记录（猫咪与状态.md:80；数值成长.md:19） | ✅ Implemented | `Source/Catfishing/Collection/CatRunImprintService.cpp:29`–`54` | `FishRecorded` Grant 在捕获提交时按 `CaptureRequestId` 授予一次；吃鱼链（Condition／Growth／FishContainers／Social）无任何图鉴写入。 |
| 29 | 倒地者可缓慢爬行（猫咪与状态.md:116） | 🔄 Divergent | `Source/Catfishing/Character/CatCharacter.cpp:461`–`471`；`Source/Catfishing/Character/Physics/CatPhysicalBodyComponent.cpp:552`、`Source/Catfishing/Character/Physics/CatPhysicalBodyComponent.cpp:689`–`692` | **本轮改判（原 ❌ Missing）**：本轮接线后倒地不再是「没做爬行」，而是明确做成了相反口径——`RefreshPhysicalCondition` 在倒地时调 `SetLocomotionEnabledFromAuthority(false)`，`bLocomotionEnabled` 为假时移动意图被清零、地面支撑关闭，身体只剩被推被拖。设计要的「缓慢爬行」在代码里是被主动关掉的。 |
| 30 | 倒地满一段时间（占位 60 秒）后自己站起来（猫咪与状态.md:116） | ❌ Missing | `Source/Catfishing/Condition/CatConditionComponent.cpp:47`；`Source/Catfishing/Condition/CatConditionSettings.h:21`–`54` | Condition 组件仍显式关闭 Tick、无任何计时器，配置里也没有自愈时长字段；`bDowned` 只在恢复命令后重算。设计 09-07 新增（猫咪与状态.md:271 v1.25），代码未跟进。 |
| 31 | 伙伴搬运回营地即解除倒地（猫咪与状态.md:116） | ⚠️ Partial | `Source/Catfishing/Camp/CatCampHubActor.cpp:213`–`225`；`Source/Catfishing/Condition/CatConditionComponent.cpp:430`–`462`；`Config/DefaultGame.ini:163`、`Config/DefaultGame.ini:169` | 搬运链路存在（范围校验 → `TeleportTo` 救援点 → `CompleteCarryToCamp`），但仍只写 `RecoveryMode=CarriedToCamp`，不减 Poison、不清 `bDowned`；解除仍要另做一次营地休息，而 `CampRestPoisonRelief=50` 对阈值 100 只能减一半。 |
| 32 | 翻天时仍在倒地的自动救起，清晨在营地醒来（猫咪与状态.md:116） | ❌ Missing | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1259`–`1310`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1312`–`1345` | 本轮翻天改由祭坛过场驱动（`CommitAltarDayTransition` ／ `FinishAltarDayTransition`），全程只处理供品结算、世界进度、阶段与截止时间，没有任何遍历倒地者清 Poison ／ 送回营地的逻辑。 |
| 33 | 倒地者不计入「全员到齐」的翻天判定（猫咪与状态.md:116） | ✅ Implemented | `Source/Catfishing/Camp/CatAltarActor.cpp:163`–`174`、`Source/Catfishing/Camp/CatAltarActor.cpp:188` | **本轮改判（原 ❌ Missing）**：`RefreshConfirmations` 遍历 PlayerController 时 `if (!GameMode->IsControllerActive(Player) \|\| (Condition && Condition->GetSnapshot().bDowned)) continue;`（`:168`），倒地者既不进分母 `EligibleCount` 也不进在场集合，`ConfirmedCount == EligibleCount` 因此不会被一个中毒的人卡住。与设计 09-05 裁决一致。 |
| 34 | 倒地时不能自己献祭，队友可代献他鱼护里的鱼（猫咪与状态.md:116） | ⚠️ Partial | `Source/Catfishing/Camp/CatAltarActor.cpp:76`；`Source/Catfishing/FishContainers/CatFishContainerService.cpp:469`；`Source/Catfishing/FishContainers/CatFishGuardActor.cpp:131` | **本轮改判（原 ❌ Missing）**：前半句成立——`CanInteract` 显式拒绝倒地者，倒地猫按不了祭坛的确认；取鱼与叼鱼护同样对倒地者 fail-closed。后半句无专门承载：没有「替他人献」的命令或容器授权，只能靠新献祭模型的副作用（祭坛半径内所有无附着的实物鱼一律计入）＋鱼护改成地面可拾容器来间接达成；若倒地者的鱼护还在他自己背包里，队友无从取出。 |
| 35 | 倒地者视角不黑屏，镜头贴地看得见围过来的猫脸（猫咪与状态.md:116） | ❌ Missing | `Source/Catfishing/Character/CatCharacter.cpp:89`–`96` | `CalcCamera` 仍只有「钓鱼持杆第一人称 ／ 其余交回 `Super`（角色蓝图相机）」两支，无倒地贴地镜头分支。 |
| 36 | 钓鱼中倒地＝中断钓鱼、交回剩余成员（猫咪与状态.md:118） | ✅ Implemented | `Source/Catfishing/Condition/CatConditionComponent.cpp:524`–`532`；`Source/Catfishing/Fishing/CatFishingService.cpp:1511`、`Source/Catfishing/Fishing/CatFishingService.cpp:1529`；`Source/Catfishing/Environment/CatChumPlacementService.cpp:135` | 首次倒地即 `ReleaseFishingOperatorForCharacter` 释放该身体的钓鱼操作位；倒地者被拒绝新的钓鱼与打窝命令。 |
| 37 | 旁观者互动：凑近嗅嗅、爪子戳肚皮、舔脸、旁边躺平陪倒（猫咪与状态.md:120） | ❌ Missing | `Source/Catfishing/AbilitySystem/Tags/CatFishingAbilityTags.cpp:26`–`31` | BodyAction 事件 Tag 仍只有营地休息、篝火回看、搬运救援、求助、恶作剧、放牌子六条，无任何针对倒地者的围观互动。 |
| 38 | 搬运演出：叼后颈拖着走、拖到一半掉了滚一圈、两猫协作抬运（猫咪与状态.md:122） | ⚠️ Partial | `Config/DefaultGame.ini:156`；`Source/Catfishing/Camp/CatCampHubActor.cpp:216`、`Source/Catfishing/Camp/CatCampHubActor.cpp:184` | 救援长动作的 Montage 已配置并接到播放挂点（`AM_BodyAction_RescueCharacterToCamp`，前摇 0.45s），但玩法实现仍是一次 `TeleportTo`，没有拖行过程、掉落重捡或双猫抬运；「拖运要短到是段子」的过程本身不存在。 |
| 39 | 单人可通过休息与爬行自救、全程不卡死（猫咪与状态.md:146；:210） | ⚠️ Partial | `Source/Catfishing/Condition/CatConditionComponent.cpp:373`–`387`；`Config/DefaultGame.ini:168`；`Source/Catfishing/Character/Tests/CatUprightMovementTests.cpp:128` | 野外自救命令存在且 `FieldRestPoisonRelief=15` 已配，但正式运行链仍没接：`RequestFieldSelfRecovery` 的调用点全在自动化测试里（`CatUprightMovementTests.cpp:128`、`CatfishingEditor/.../CatPhysicalCharacterAnimationNetworkTests.cpp:240`、`CatCuteCatNetworkTests.cpp:130`），没有 PlayerController RPC、BodyAction 或 UI 入口。叠加第 29、30 行（爬行被主动关闭、无自愈计时），单人脱困当前不成立。测试只作旁证，不充当产品链路证据。 |
| 40 | 营地是恢复落点，回营休息可恢复（猫咪与状态.md:164；:191） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingPlayerController.cpp:761`；`Source/Catfishing/Camp/CatCampHubActor.cpp:175`；`Source/Catfishing/Condition/CatConditionComponent.cpp:390`–`404`；`Config/DefaultGame.ini:169` | RPC → BodyAction Ability（含 CampRest Montage）→ Camp 范围校验 → `RequestCampRest` 全链贯通。 |
| 41 | 草药机制已删除，倒地解除只剩救援与休息（猫咪与状态.md:249 v1.3） | 🔄 Divergent | `Source/Catfishing/Framework/Game/CatfishingPlayerController.cpp:1152`–`1161`；`Source/Catfishing/Condition/CatConditionComponent.cpp:171`–`184`、`Source/Catfishing/Condition/CatConditionComponent.cpp:186`–`321`、`Source/Catfishing/Condition/CatConditionComponent.cpp:407`–`427`；`Source/Catfishing/Condition/CatHerbRecoveryItemFragment.h:9`；`Config/DefaultGame.ini:170`–`171` | 设计 08-13 已删该机制，09-09「代码超前项逐个过」又明确裁「删代码一条——草药恢复链」。本轮只是把 `CatHerbRecoveryCoordinator` 拆成库存 Fragment ＋ Condition 内的事务，链路反而更完整了：`ServerUseHerbOnCharacter` RPC 仍在、`HerbPoisonRelief=50`／`HerbUseRangeCentimeters=250` 仍开着、`ECatRecoveryMode::Herb` 仍在枚举里、`Content/Catfishing/Animation/BodyAction/AM_BodyAction_UseHerbOnCharacter.uasset` 仍在。无 D 条目登记。 |
| 42 | 中毒失态演出：走路画龙、瞳孔漩涡、打嗝冒泡、幻觉、翻肚皮晕倒、头顶转圈小鱼（猫咪与状态.md:104、106、190） | ⚠️ Partial | `Source/Catfishing/Condition/CatConditionPresentationComponent.cpp:18`–`23`、`Source/Catfishing/Condition/CatConditionPresentationComponent.cpp:40`–`67` | **本轮改判（原 ❓ Unverifiable）**：新增的表现组件把倒地表现收进了 C++，可以直接读：倒地播「站→坐」→「坐→趴」→趴伏循环，起身播「趴→坐」→「坐→站」，五段都用 `Animalia/Cat/Animations/InPlace/Trans_*` 与 `Lying_00-IP`。这不是设计写的「原地晃三晃、定格一拍、慢镜头翻成四脚朝天、舌头露一小截」——现行是普通趴卧，没有翻肚皮。另外五项（画龙走路、瞳孔漩涡、打嗝冒泡、幻觉、头顶转圈小鱼）在 C++／Config／Tag 全部零命中，只可能在 `Content/Animalia/Cat/ABP_Cat.uasset` 的图里（④），不据此判缺失。 |
| 43 | 淋湿：雨天渐湿、落水全湿（猫咪与状态.md:134） | ⚠️ Partial | `Source/Catfishing/Condition/CatConditionComponent.cpp:78`–`155`；`Source/Catfishing/Fishing/Simulation/CatFishingFightRunner.cpp:896`；`Config/DefaultGame.ini:164` | 涉水／落水侧成立：按脚点浸没深度（湿润阈值 1cm）维护 `bWet` 与危险水域滞回。但两处仍未接线——`UpdateWaterExposureFromAuthority` 的唯一产品调用点是搏斗固定步，搏斗外趟水不会变湿；`SetWetFromAuthority`（天气入口）全项目零调用者，Environment 的天气与 Condition 之间没有任何接缝。`bWet` 在 C++ 侧也没有消费者，只复制给蓝图。 |
| 44 | 湿毛贴身瘦一圈、上岸波浪式抖水、半干毛蓬（猫咪与状态.md:134、136） | ❓ Unverifiable | `Source/Catfishing/AbilitySystem/Tags/CatFishingAbilityTags.cpp:37`；`Source/Catfishing/Character/CatCharacter.cpp:142`、`Source/Catfishing/Character/CatCharacter.cpp:148`、`Source/Catfishing/Character/CatCharacter.cpp:220`–`240`；`Config/DefaultGame.ini:182` | 挂点是接好的：`Cat.Cosmetic.Fishing.CatInWater` Tag 存在，落水收场经 `PlayFishingOutcomeMontageFromPresentation` 播放 `CatInWaterMontage`（现指向 `AM_Death_01-IP_Montage`，占位）。这一格里到底有没有「波浪式抖水」、湿毛材质与半干毛蓬，都在 Montage／材质／Niagara 资产里，属 ④查不了。`FCatConditionSnapshot::bWet` 已复制并对蓝图只读暴露，供 ABP 消费。**复核补证（可文本检查的那一层已查完，结论仍是 ❓ 而非 ❌）**：①这条挂点对的是**落水**（`Cosmetic_Fishing_CatInWater`）不是**上岸**，C++／Config 里没有任何出水事件，`AM_Death_01-IP_Montage` 是第三方死亡动画占位；②`FCatConditionSnapshot` 只有 `bWet` 布尔与 `ECatWaterExposureState{Dry, Shallow, Dangerous}`（`Source/Catfishing/Condition/CatConditionTypes.h:23`–`29`、`:49`–`55`），没有「半干」这一档的数据载体；③`bWet` 的唯一产品驱动仍是搏斗固定步（见第 43 行）。三条都不足以证否 ABP 侧自带的本地计时与材质切换，故按 ④ 维持 ❓。 |
| 45 | 雨天猫下意识往树下、叶子下钻（猫咪与状态.md:134） | ❌ Missing | — | 全库无天气驱动的角色行为逻辑；`Character/`、`Condition/`、`Growth/` 三个目录 `Weather` 零命中，`shelter`／`躲雨`／`RainCover`／`TakeCover` 全库零命中，也没有躲雨点位或 AI 组件。 |
| 46 | 饥饿与疲惫退出数值状态，猫身上不再有这两条属性（猫咪与状态.md:15、45、90、144） | ✅ Implemented | `Source/Catfishing/AbilitySystem/Attributes/CatSurvivalAttributeSet.h:28`、`:33`、`:38`、`:43`；`Config/DefaultEngine.ini:114` | 属性集只剩三项 ＋ 上限；`Source/`／`Config/`／`Scripts/` 全文 `Hunger`／`Fatigue` 仅剩一条属性重定向（`CatFishDefinition.HungerRelief → EatingExperience`），运行时零命中。D-06「已执行」，与设计一致。**证据变更**：基线引用的静态断言脚本 `Scripts/verify_character_growth_condition.ps1` 本轮已不在工作区（`Scripts/` 只剩 12 个 .py／.ps1，无 verify_character*），改以属性集本体与全库搜索为据。 |
| 47 | 疲惫三档纯演出：轻→打哈欠、中→走路拖沓、重→坐下瞌睡点头，无数值条无惩罚（猫咪与状态.md:92） | ❌ Missing | `Source/Catfishing/AbilitySystem/Tags/CatFishingAbilityTags.cpp:26`–`31` | 搜过 `FatigueTier`／`Drowsy`／`Yawn`／`哈欠`／`瞌睡`／`拖沓`，C++／Config 零命中。D-06 自述「疲惫的轻→中→重纯演出档位本轮没有实现」，并把「按什么计档」列为策划需回答；`需求对齐差距清单.md:388` 同口径。 |
| 48 | 猫行为库素材：舔毛、甩尾、打哈欠、打滚、伸懒腰、休息、睡觉（猫咪与状态.md:68、188） | ⚠️ Partial | `Content/Animalia/Cat/Animations/`（`Sitting_00`–`02`、`Lying_00`–`02`、`Sleeping_V1_01`、`Sleeping_V2_01`、`Stand_03_LookAround` 与成套 `Trans_*`）；`Content/Animalia/Cat/ABP_Cat.uasset`；`Content/Characters/CuteCat/Meshes/SK_CuteCat_Anim_Armature_idle_A_yawn.uasset`、`Content/Characters/CuteCat/Meshes/SK_CuteCat_Anim_Armature_layingdown_A_yawn.uasset` | 资产是否存在属 ①，可直接查。逐条数：休息（Sitting／Lying 各三段＋过渡）与睡觉（Sleeping V1／V2＋四条进出过渡）**素材成立**；**打哈欠也成立**——`Content/Characters/CuteCat/Meshes/` 下有 `..._idle_A_yawn` 与 `..._layingdown_A_yawn` 两段（**本次复核更正**：原文写「按 `groom／lick／yawn／roll／stretch／tail` 六词过完 `Content/Characters/CuteCat/` 一个都没有」不成立，该 CuteCat 的动画序列落在 `Meshes/` 而不是 `Animation/`，漏扫了这一层）。仍无同名资产的是舔毛、甩尾、打滚、伸懒腰四项（`groom／lick／roll／stretch／tail` 全库零命中，`*tail*` 的命中全是 `Detail`／`Longtail` 之类的子串），第三方 Animalia 库只到 Scratching／Eating／Drinking／Attack／Hit／Death。**七项里三项有素材、四项无**。另有未纳入原扫描范围的第三套猫包 `Content/StylizedCat/Animations/`（`AS_StylizedCat_idle_01`–`07`、`idle_sleep` 等），idle 里演的是什么属 ③，不据此加减。剩下的 ④ 只是「已有的这三项进没进 ABP idle 图」。C++／Config 侧无这些行为的 Tag 或 Montage 配置。 |
| 49 | 行为与状态联动：累了瞌睡点头、看见别人的鱼护舔嘴、无聊时追尾巴拍水面（猫咪与状态.md:68） | ❌ Missing | `Source/Catfishing/Condition/CatConditionPresentationComponent.cpp:40`–`50`；`Source/Catfishing/UI/HUD/CatHUDModel.cpp:311` | 全项目唯一的「状态 → 表现」接线是本轮新增的倒地姿势组件，它只读 `bDowned`；本行三项（疲惫、看见鱼护、无聊）都没有档位、感知或触发器，Growth／Poison 两份事实的另一个消费者仍只是 HUD 的一行调试文本。联动这一层在 C++ 里可查，确实没有。 |
| 50 | 群体行为：打哈欠传染、营地睡觉叠罗汉（猫咪与状态.md:70） | ❌ Missing | — | 无多猫行为同步或邻近感知逻辑；本轮新增的多猫接触只有物理互推与抓握（`CatModelContactComponent`／`CatPhysicsGrabComponent`），不产生行为传染。 |
| 51 | 通用动画规范：爪抓握自动贴合、双脚随坡度、尾巴耳朵程序化摆动、紧张炸毛、入水毛塌（猫咪与状态.md:149） | ⚠️ Partial | `Source/Catfishing/Character/Animation/CatQuadrupedLocomotion.cpp:345`–`369`、`Source/Catfishing/Character/Animation/CatQuadrupedLocomotion.h:18`–`25`；`Source/Catfishing/Character/Animation/CatCharacterRigSettings.h:24`–`26`；`Source/Catfishing/Interaction/Grab/CatPhysicsGrabComponent.h:71` | **本轮改判（原 ❓ Unverifiable）**：前两项现在有可读的 C++ 实现——四足落脚 IK 按真实地面射线与坡面法线（`ImpactNormal.Z >= 0.65`）解算，带 `MaxFootOffsetCm`／`MaxPelvisOffsetCm`／`MaxPlantDriftCm` 边界与步幅匹配；爪抓握由物理手球＋约束提供世界握点并按目标贴合。后三项（尾巴耳朵程序化摆动、紧张炸毛变粗／耳朵压平、入水毛发变塌）在 C++／Config 零命中，只可能在 ABP 的控制骨骼节点与材质里（④），不据此判缺失。 |
| 52 | 多猫交互动作通用层：扑倒滚成一团、碰鼻子、舔脸、叼后颈搬运（猫咪与状态.md:149） | ⚠️ Partial | `Config/DefaultGame.ini:154`–`159`；`Content/Catfishing/Animation/BodyAction/`（`AM_BodyAction_RescueCharacterToCamp`、`AM_BodyAction_CatchTheft`、`AM_BodyAction_BeginTheft`） | 叼后颈搬运已有 Tag ＋ Montage ＋ ini 映射；扑倒反制的 Montage 资产（`AM_BodyAction_CatchTheft`／`AM_BodyAction_BeginTheft`）已生成但**仍不在** `ActionPresentationConfigs` 的六条映射里；碰鼻子与舔脸无资产、无 Tag。本轮新增的双猫物理互推／抓握（`Source/Catfishing/Character/CatCharacterMovementComponent.cpp:190`–`210`）是通用物理接触，不是「扑倒滚成一团」这条演出。 |
| 53 | 无常驻状态条，成长信息只在需要的时刻显示（猫咪与状态.md:193；数值成长.md:61） | 🔄 Divergent | `Source/Catfishing/UI/HUD/CatHUDModel.cpp:311`–`317` | HUD 仍常驻输出「猫状态：中毒 x｜钓鱼力量 x｜搏斗体力 x｜成长总经验 x，当前槽 x，待选 x」一整行，正是「按需显示」口径要去掉的常驻数值面。当前是开发期调试 HUD，无 D 条目登记这条偏差。 |
| 54 | 吃鱼瞬时浮层：体力快照（含黄段与本次增量）、经验槽小环与 +N、已认识的鱼显示效果一行、没吃过显示「？？？」（数值成长.md:63） | ❌ Missing | — | 无一次性浮层 Widget，也无「已认识／未认识」的知识层可读（见第 27 行）。 |
| 55 | 三选一卡面显示每项已叠次数（数值成长.md:64） | ❌ Missing | — | 三选一卡面不存在。 |
| 56 | 主动查看面板：黄色体力存量、已叠 build 构成、各 buff 剩余时间（数值成长.md:65） | ❌ Missing | — | 三项数据源（黄体、build、buff）本身都不存在，面板亦无。 |
| 57 | 渔获信息三值同屏：翻鱼护/鱼缸时显示每条鱼的经验、供奉、售价原始值（数值成长.md:66） | ⚠️ Partial | `Source/Catfishing/UI/Inventory/CatFishGuardInventoryWidget.cpp:111`–`113`；`Source/Catfishing/UI/WorldInfo/CatFishTankWorldInfoComponent.cpp:113`；`Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:984`–`986` | **本轮改判（原 ❌ Missing）**：三个值各自露了两个头——鱼护面板给「选中鱼售价／全部售价」，鱼缸信息牌给缸内供奉总点数，地面鱼的交互提示给重量（`叼起 {0} {1} kg`）。但设计要的是翻鱼护／鱼缸时**逐条**把经验／供奉／售价三个原始值放同屏；经验从未出现在任何 UI（`Source/Catfishing/UI/` 全目录 `EatingExperience` 零命中），供奉只有总量没有逐条。 |
| 58 | 状态与成长跟局走，跨局持久载体只有图鉴、印记、外观解锁（猫咪与状态.md:157；数值成长.md:19） | ✅ Implemented | `Source/Catfishing/Growth/CatGrowthTypes.h:16`–`26`；`Source/Catfishing/Framework/Core/CatProfileContracts.h:12`–`19` | Growth／Condition Snapshot 都挂在 Character 上、随之销毁；`Source/Catfishing/Save/` 与 `Source/Catfishing/Profile/` 里没有 Growth／Condition／Poison 任何字段，Profile Grant 只有图鉴／剪影／印记／解锁四类。 |
| 59 | 不做生命值系统、无任何状态死亡，失败可恢复（猫咪与状态.md:47、155） | ✅ Implemented | `Source/Catfishing/Condition/CatConditionTypes.h:41`–`63`；`Source/Catfishing/Condition/CatConditionComponent.cpp:511`–`533` | 只有可恢复的 `bDowned`，没有生命值属性，也没有死亡终态或永久失败分支。 |
| 60 | 吃鱼演出：叼住甩头吞下、吃完舔爪抹脸满足眯眼；经验满时伸大懒腰抖毛（猫咪与状态.md:82） | ❌ Missing | `Config/DefaultGame.ini:154`–`159`；`Source/Catfishing/AbilitySystem/Tags/CatFishingAbilityTags.cpp:26`–`31`；`Content/Catfishing/Animation/BodyAction/AM_BodyAction_ConsumeFish.uasset` | `AM_BodyAction_ConsumeFish` 资产已生成，但既没有 `Cat.Ability.Event.Body.ConsumeFish` 之类的事件 Tag，也不在六条 `ActionPresentationConfigs` 映射里，播放挂点收不到它；`ApplyCommittedFish` 与 `ConsumeCommittedFish` 全程只写数据、不发表现事件。「经验满时伸懒腰」同理无触发器（见第 49 行）。搜过：`ConsumeFish`、`AbilityEvent_Body_`、`ActionPresentationConfigs`、`吃鱼`。 |
| 61 | 咸鱼可卖但**不可献**，献祭时被猫神拨开（吃鱼效果.md:62） | ❌ Missing | `Source/Catfishing/Camp/CatAltarActor.cpp:218`–`243`；`Source/Catfishing/Run/CatRunSettings.cpp:83`–`86`；`Config/DefaultGame.ini:249` | 本轮献祭改成「祭坛半径内所有 Available 且无附着的实物鱼按重量档计点」，完全不问鱼种，鱼定义里也没有「可否献祭」的开关。唯一的按鱼种分支是 `IsStinkyOfferingFish`，配的是臭臭鱼（`+StinkyOfferingFishDefinitionIds=Stinky`），走的是增益折扣而不是拒收，与咸鱼无关。搜过：`Salted`、`不可献`、`bSacrificeEligible`、`IsStinkyOfferingFish`。 |

### Summary
- Total features: 61
- ✅ Implemented: 11
- ⚠️ Partial: 14
- ❌ Missing: 28
- 🔄 Divergent: 7
- ❓ Unverifiable: 1
- Inspectable: (61 - 1) / 61 * 100% = 98.4%
- Coverage of inspected: (11 + 0.5 * 14) / (61 - 1) * 100% = 30.0%

### Scan Scope

**项目声明（来自主流程转交的 `game-toolkit.yaml`，未自行探测）**：engine=unreal 5.8；project_root=`D:/LocalGameProject/Unreal/Catfishing`；tech_stack=C++／Blueprint（模块 Catfishing、CatfishingEditor）。`source_scope`：`Source/` 下 .cpp／.h 为源码，`Config/` 与 `Scripts/` 可文本检查，`Content/` 为资产不计入文本扫描，`Intermediate/Saved/Binaries` 下的 .cpp／.h 是生成物不算范围。`inspectability` 四档：①文件存在／路径命名可直接查；②C++／Config／Scripts 可静态文本检查；③资产属性、类与引用需引擎入口；④Blueprint 图逻辑与运行时行为文本证明不了，不得据此判「未实现」。

**代码基线**：工作区 HEAD `bcc7df9`（合并远端 09-11 的 59 个实现提交）；相对基线 `87ba975`，本系统相关目录净变动约 8.9k 增／10.3k 删（140 文件）。`git status` 只有 `game-toolkit.yaml`／`gap-analysis.config.yaml` 两处未提交修改，与本系统无关。状态与 code_ref 全部本轮重新取证，未沿用基线判定。

**扫过的目录与文件类型**
- `Source/Catfishing/` 全模块 `.h`／`.cpp`，重点读：`Condition/`（含新增 `CatConditionPresentationComponent`、`CatHerbRecoveryItemFragment`）、`Growth/`、`Character/`（含新增 `Animation/CatQuadrupedLocomotion`、`Animation/CatCharacterRigSettings`、`Physics/CatPhysicalBodyComponent`）、`AbilitySystem/`（Attributes、Config、Core、Effects、Executions、Tags、BodyAction、新增 `Physics/CatPhysicalEffortComponent`）、`Camp/`（含新增 `CatAltarActor`）、`FishContainers/`、`Collection/`、`Data/CatFishDefinition.*`、`Fishing/`（Session、Simulation、Service、Config、Presentation）、`Framework/Game/CatfishingGameModeBase.cpp`、`Framework/Core/CatProfileContracts.h`、`Run/`、`Social/`、`Items/Fish/`、`Inventory/`、`Interaction/Grab/`、`UI/`（HUD、Frontend、Inventory、WorldInfo、CatUISettings）、`Save/`、`Profile/`、`Environment/`。
- `Source/CatfishingEditor/` 只用于确认 `RequestFieldSelfRecovery` 的调用点分布（第 39 行）。
- `Config/DefaultGame.ini` 全文（`CatAbilitySettings`、`CatBodyActionPresentationSettings`、`CatConditionSettings`、`CatGrowthSettings`、`CatFishCatalogSettings` 的 16 条 `+Definitions=`、`CatRunSettings`、`CatCampSettings`、印记事件 ID 等段）；`Config/DefaultEngine.ini` 的属性重定向段。
- `Scripts/` 目录清单（12 个 .py／.ps1）。
- `Content/` 只做 ① 类的路径与文件名核对：`Animalia/Cat/Animations/`（含 `InPlace/`、`Comp/`）、`Characters/CuteCat/`、`Catfishing/Animation/BodyAction/`（12 个 `AM_BodyAction_*`）、`Catfishing/Data/Fish/`（16 条鱼定义）、`Catfishing/Data/Character/`（只有 `Cat_Default`）。**Phase 2b 补扫**：`Characters/CuteCat/Meshes/`（动画序列实际落在这里，含两段 `*_yawn`）与第三套猫包 `StylizedCat/Animations/`，首轮 Content 清点漏了这两层，第 48 行据此更正（详见复核记录）。
- 账本：`Docs/Development/工程自补决策记录.md`（D-01…D-32 全表＋文末「2026-09-09 设计对表复核」17 条，取 D-06、D-09）、`Docs/Development/需求对齐差距清单.md`（§1／§3 的 Character / Growth / Condition 段与 §4）、`裁决同步/设计修改记录.md`（09-05／09-07／09-08／09-09／09-10 五批，重点 09-09 的八问①⑥、代码超前项「删草药链」、账本六问⑥，以及 09-10 风铃鱼黄体占位）。
- 设计侧：`GDD 系统分册/猫咪与状态/` 四页全篇（猫咪与状态 rev 208、数值成长 rev 67、升级效果 rev 66、吃鱼效果 rev 64，均 2026-09-11T17:57 导出）；`数值模拟与参数记录.md` 只作参考、不评分；负责人取自 `GDD 系统分册/GDD 系统分册.md` 的分册一览（本册属主钱成澔）。

**方法**
先按类名、字段名、日志事件名（`character_downed`、`character_wet_changed`、`condition_pose_observed`、`physical_effort_settled`、`AltarConfirmed`）、Gameplay Tag、ini 段名与中文注释 Grep 定位，再 Read 片段核对语义与调用链；引用行号为文件实际行号（Markdown 含 front matter；本轮未涉及 CSV）。

**跳过与不作为证据的**
- `Content/` 下的 `.uasset`／`.umap` 未作文本扫描：只按路径／文件名列举其存在，其属性、类、图逻辑属 ③④，本轮不跑引擎，因此鱼的 `PoisonIncrease`／`EatingExperience` 具体取值、`Cat_Default` 的四项数值、FightBalance 的 `SlackStaminaRegenPerSecond` 正式值、`ABP_Cat` 的行为图一律记为「查不了」，不据此判缺失。
- 主流程给的 `verify_entry` 线索 `Scripts/verify_character_growth_condition.ps1` **在本轮工作区已不存在**（`Scripts/` 只剩 12 个脚本，无 `verify_character*`），所以基线用过的「静态断言层」证据本轮无法复用，第 46 行改用属性集本体与全库搜索取证；这是范围收窄，不是新增缺口。
- `Source/**/Tests/` 与 `Source/CatfishingEditor/**/Tests/` 的断言只用作「命令存在但正式链路未接」的判据（第 39 行），不单独充当实现证据。
- 各设计页「版本历史」段只用于溯源裁决日期，不作为要求来源。
- 已建专表处（鱼表格、道具四表）以表为准；`数值模拟与参数记录.md` 只作参考。
- 本轮 59 个提交里的合力钓鱼位移结算、抓握物理、库存重构、售鱼链路、悬停提示等，只在与本册要求相交处取证（第 20／21／29／33／34／51／52／57 行），其余归钓鱼／道具／联机册。

**Phase 2b 独立复核的实际扫描范围（在上述基础上重跑，不沿用首轮结论）**

- 逐行独立取证：49 行 ❌／🔄／⚠️ 全部按「换名再搜」重跑（`Buff`／`BuffDuration`／`TimedEffect`／`Overshield`／`Nickname`／`Graduation`／`毕业`／`BuildSnapshot`／`SelfRecovery`／`AutoRevive`／`Revive`／`MoveSpeedBonus`／`SpeedMultiplier`／`EffortDiscount`／`ChumBonus`／`Stumble`／`Hallucin`／`幻觉`／`画龙`／`Wobble`／`Shake`／`抖水`／`Groom`／`StandUp`／`GetUp`／`Crawl`／`爬行`／`Paralys`／`麻痹`／`Inedible`／`不可食用`／`shelter`／`躲雨`／`RainCover`／`TakeCover`／`CharacterSelect`／`CatSelect`／`breed`／`品种` 等 40 余词），命中项逐个打开读调用链。
- 全文通读：`Growth/`（4 文件全篇）、`Condition/`（组件、表现组件、Settings、Types、Fragment 全篇）、`Config/DefaultGame.ini` 全文 292 行、`AbilitySystem/Physics/CatPhysicalEffortComponent.cpp` 全篇、`AbilitySystem/Config/CatPhysicalEffortSettings.h` 全篇、`Condition/CatConditionPresentationComponent.cpp` 全篇。
- 片段核读：`Character/CatCharacter.cpp`（`CalcCamera`、`RefreshPhysicalCondition`、落水 Cosmetic 分支）、`Character/Physics/CatPhysicalBodyComponent.cpp`（`CaptureDriveSample`／`ComputeDriveForce`／`SetLocomotionEnabledFromAuthority`）、`Character/CatCharacterMovementComponent.cpp`（`AdvanceFromAuthority`、接触驱动）、`Character/Animation/`（四足 IK 与 Rig 设置）、`Camp/CatAltarActor.cpp`、`Camp/CatCampHubActor.cpp`、`FishContainers/CatFishContainerService.cpp`、`Social/CatSocialService.cpp`、`Collection/CatRunImprintService.cpp`、`Framework/Game/CatfishingGameModeBase.cpp`（翻天过场两段）、`Framework/Game/CatfishingPlayerController.cpp`（草药 RPC）、`AbilitySystem/Core/CatAbilitySystemComponent.cpp`（体力重置三入口）、`Fishing/Simulation/`（放线回体）、`UI/HUD`／`UI/Inventory`／`UI/WorldInfo`／`Items/Fish`。
- 反向核对：`bDowned` 全部 30 处引用、`SetWetFromAuthority`／`UpdateWaterExposureFromAuthority`／`bWet` 全部引用、`ConsumeCommittedFish`／`ApplyCommittedFish` 全部调用点、`RequestFishingStaminaReset`／`InitializeFishingStaminaForSession` 全部调用点、`RequestFieldSelfRecovery`／`RequestCampRest`／`CompleteCarryToCamp`／`ApplyCommittedHerbRecovery` 全部调用点、`Growth` 组件的全部消费者、`Save/`＋`Profile/` 目录对 `Growth`／`Condition`／`Poison` 的全部命中。
- 账本：`工程自补决策记录.md`（52 条 D 表＋文末 09-09 复核段，逐条核 D-06／D-08／D-09）、`需求对齐差距清单.md:388`、`裁决同步/设计修改记录.md` 09-06～09-10 五段全文。
- 设计侧：四页全篇重读（猫咪与状态 272 行、数值成长 104 行、升级效果 59 行、吃鱼效果 87 行，均含 front matter），逐行回核 61 条要求的出处行号，无一处引错。
- 仍未完成的检查：③④ 两档不变——`Cat_Default` 的四项初值、16 条鱼的 `EatingExperience`／`PoisonIncrease`、FightBalance 的 `SlackStaminaRegenPerSecond` 正式值、`ABP_Cat`／`ABP_CuteCat` 的图逻辑、Montage 与材质内容，本轮同样不跑引擎，不据此判缺失。

### Code-only mechanics

| 机制 | code_ref | 文档现状与出处 | 待确认问题 |
|---|---|---|---|
| 倒地时服务器强制释放嘴里叼着的鱼或鱼护，就地落地 | `Source/Catfishing/Character/CatCharacter.cpp:466`–`470` | 猫咪与状态 §3.1.5（:116–:118）只写了倒地可爬行、可被搬运、可被代献；落水一节（:118）反而明写「嘴里叼着的鱼保留」。倒地掉落这条在本册、钓鱼规则与联机册都查不到 | 倒地要不要掉嘴里的东西？若要，该补进 §3.1.5 并说明与落水口径的差别；若不要，删这段释放 |
| 倒地＝全面交互禁用：钓鱼、打窝、商店交易、库存使用、容器取鱼、鱼护拾取、社交求助／恶作剧、祭坛确认，再加通用命令门、地面鱼叼起与投放、以及一处 Controller 侧 gate——**复核重数共 12 处**（原文写「八处」漏了后四处） | `Source/Catfishing/Fishing/CatFishingService.cpp:1511`、`Source/Catfishing/Fishing/CatFishingService.cpp:1529`、`Source/Catfishing/Environment/CatChumPlacementService.cpp:135`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.cpp:201`、`Source/Catfishing/Inventory/CatInventoryComponent.cpp:2227`、`Source/Catfishing/FishContainers/CatFishContainerService.cpp:469`、`Source/Catfishing/FishContainers/CatFishGuardActor.cpp:131`、`Source/Catfishing/Social/CatSocialService.cpp:634`、`Source/Catfishing/Camp/CatAltarActor.cpp:76`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:798`、`Source/Catfishing/Framework/Game/CatfishingPlayerController.cpp:1097`、`Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:610`、`Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:1055` | 本册只写了两条禁令：钓鱼中倒地要中断钓鱼（:118）、倒地不能自己把鱼叼上祭坛（:116）。其余六处是代码自行推广的 | 倒地猫到底还能做什么？「倒地者视角不黑屏、被救的人也有戏看」要不要保留一部分可交互（比如被舔脸、看队友）？需要一条正面清单进 §3.1.5 |
| 每次进入搏斗把钓手体力补满，且多条钓鱼终局路径再次 `RequestFishingStaminaReset` | `Source/Catfishing/AbilitySystem/Core/CatAbilitySystemComponent.cpp:309`–`330`、`Source/Catfishing/AbilitySystem/Core/CatAbilitySystemComponent.cpp:332`–`345`；`Source/Catfishing/Fishing/CatFishingSession.cpp:130`、`Source/Catfishing/Fishing/CatFishingSession.cpp:1067`（入场补满两个入口）；`Source/Catfishing/Fishing/CatFishingSession.cpp:1154`、`:1197`、`:1238`、`:1255`、`:2052`、`:2134`（**复核补齐**：六条钓鱼终局路径再挂一次 `RequestFishingStaminaReset`，原文只写了「多条」没给行号） | 数值成长 §5（:56）只给了「搏斗外自然回复 5 点/秒、空条约 20 秒回满」一条回体口径；「搏斗入场补满」是第二条、且让前一条在钓鱼路径上几乎失效。三张数值子页与钓鱼规则都没有它 | 体力是「跨场持续、按 5 点/秒慢慢回」还是「每场开打即满」？两条并存时以哪条为准？这条直接决定连续硬仗的代价与小鱼干的价值窗 |
| 出力体力恢复的两个前置条件：必须先付过出力费（`bRecoveryPending`），且连续静止满 2 秒（`RecoveryDelaySeconds = 2.0`）才起算 | `Source/Catfishing/AbilitySystem/Config/CatPhysicalEffortSettings.h:18`–`21`；`Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:70`、`Source/Catfishing/AbilitySystem/Physics/CatPhysicalEffortComponent.cpp:76`–`82` | 数值成长 §5 写的是无条件的「站立、岸上替补、摸鱼随队一律同一速率」。2 秒静止延迟与「只有付过费的猫才回体」两条，参数页与三张子页均无 | 回体要不要设静止延迟？值多少？没参与合力、只是走累了的猫算不算「搏斗外」，该不该回体？ |
| 臭臭鱼作为供品每条给整批世界进度增益打 25% 折扣（上限归零） | `Source/Catfishing/AbilitySystem/Executions/CatRunSettleOfferingExecutionCalculation.cpp:109`–`110`；`Source/Catfishing/Camp/CatAltarActor.cpp:237`；`Config/DefaultGame.ini:249` | 吃鱼效果 §3（:42–:44）只给臭臭鱼定了「请勿靠近」这一条食用效果与屏蔽范围，没有献祭折扣；咸鱼一栏（:62）写的「不可献」反而没实现（第 61 行）。献祭侧的扣分规则在本册与局与进程册都查不到 | 献祭折扣该记在哪一册（吃鱼效果的臭臭鱼条目，还是局与进程的献祭结算）？25%／条与「咸鱼不可献」是同一个设计意图的两种写法，还是两条独立规则？ |

### 复核记录

**检查范围**：61 行全部独立复核，未沿用任何基线或首轮结论。逐行驳的 49 行 ❌／🔄／⚠️ 全部重搜重读（❌ 28、🔄 7、⚠️ 14）；✅ 抽验 **11 行（该状态全部）**，优先挑 Code Reference 单一或语义跨度大的第 4／26／28／33／36／40 行，逐个 Read 到 file:line 核语义而不是只核文件名；❓ 抽验 **1 行（该状态全部，不足五行故全查）**。

**结论：零改判。** 49 行待驳项没有一行能找到正式链上的实现，11 行 ✅ 的引用位置与语义全部成立，1 行 ❓ 在可文本检查的那一层查完后仍应留 ❓（三条子项都可能落在 ABP 的本地计时与材质里，按声明 ④ 不得据此判「未实现」）。Summary 八行与两个比例按本轮重数后与原值一致：Total 61＝✅11＋⚠️14＋❌28＋🔄7＋❓1；Inspectable (61-1)/61＝98.4%；Coverage (11+0.5×14)/(61-1)＝30.0%。

**改了什么**：四处事实性证据更正（第 18／20／44／48 行）、两处 Code-only 计数与行号补齐（第 2 条 gate 由八处更正为 12 处、第 3 条补六个终局调用点）、Scan Scope 补记 Phase 2b 实扫范围与首轮漏扫的两层 Content 目录；另修一处格式缺陷——第 33 行 Notes 里引用代码的两个字面竖线未转义、把该行撑成七列，已改为 `\|\|` 并顺手补上 `:168` 行号。其中第 48 行是唯一影响读数的一处——首轮判「七项里两项有素材」，实际是三项。

**漏项核查（未补行及理由）**：按四页标题与子规格重走一遍，未纳入 Features 表的只剩三类，均判为不值得占行：①**资源边界约束**（数值成长.md:31「成长效果不提供搏斗内体力回复、不回补竿耐久、出鱼概率类只改权重无保底」）——它约束的对象是三选一与 buff，而这两者整体不存在（第 9／11／13 行），此刻补行只会产出一条空判；②**准入三闸**（升级效果.md:20 池上限 16、纯被动、五类口味各 1～4 项）与**末段动机**（升级效果.md:45 最后两天把吃鱼动机从经验切到 buff）——前者是策划扩池纪律不落代码，后者的「最后两天」轴已被 09-09 裁决①连同第 10 天毕业一起作废（第 23 行已记残留出处）；③**落水嘴里的鱼保留**（猫咪与状态.md:118）——该行自己写明「口径在钓鱼规则 4.6，本册持湿毛与抖水演出」，归钓鱼册计分，本册不重复计数，但它与代码「倒地即释放单嘴物」的冲突已由 Code-only 第 1 条挂出。Code-only 五条反向核过，未发现第六条本册范围内的代码超前机制。

**未能完成的检查**：③④ 两档与首轮同界，本轮同样不跑引擎——`Cat_Default` 的品种四值、16 条鱼的 `EatingExperience`／`PoisonIncrease`、FightBalance 的 `SlackStaminaRegenPerSecond` 正式值、`ABP_Cat`／`ABP_CuteCat` 图逻辑、Montage 与材质内容一律记为查不了，不据此判缺失。`Scripts/` 下确认只有 12 个脚本＋`Art/` 目录，`verify_character_growth_condition.ps1` 已不存在，主流程给的 `verify_entry` 线索本轮无法复用（与首轮说明一致）。

| # | 原判 | 改判 | 依据 |
|---|---|---|---|
| 1 | ⚠️ Partial | ⚠️ Partial | `CatCharacterDefinition.h:35` 确为 `FishingStrength`，`CatAbilitySettings.cpp:95` 确把它播种进 ASC；`DefaultGame.ini:145` `InitialFishingStrength=50.0`、`:150` 只登记 `Cat_Default` 一条，`Content/Catfishing/Data/Character/` 下确实只有 `Cat_Default.uasset`。机制在、四品种清单不在，维持 |
| 2 | ❌ Missing | ❌ Missing | `CatUISettings.h` 八个 WBP 入口重数一遍确为 85／89／93／97／101／105／109／113，无选猫页；`UI/Frontend/` 确只有 Page／Room／Save／Settings／Root 五套。搜 `CharacterSelect`／`CatSelect`／`breed`／`Breed` 全零；`品种` 唯一命中是 `CatShopModel.cpp:142` 的「商品种类」跨词子串，不是猫种 |
| 3 | ✅ Implemented | ✅ Implemented | 抽验：`CatGrowthComponent.cpp:56` 确为 `AddExperienceFromCommittedFish(FloorToInt(EatingExperience))`，`:72`–`88` 确为入槽与溢出循环；`CatFishContainerService.cpp:428` 确调 `ConsumeCommittedFish`，`ConsumeFish` 确在 `:653` |
| 4 | ✅ Implemented | ✅ Implemented | 抽验：`CatSocialService.cpp:496` 确先过 `ValidateFishConsumption`、`:501` 确调同一个 `ConsumeCommittedFish`，与容器链、库存链（`CatFishInventoryItemInstance.cpp:166`）三入口合流到 `CatConditionComponent.cpp:353` 的 `Growth->ApplyCommittedFish` |
| 5 | ✅ Implemented | ✅ Implemented | 抽验：`AddExperienceFromCommittedFish` 全项目唯一调用点确在 `CatGrowthComponent.cpp:56`；`TotalExperience`／`ExperienceInCurrentSlot` 的写入者只有 `:79`–`80`，另一端只有 `CatHUDModel` 只读投影（`:53`、`:70`、`:105`、`:192`） |
| 6 | 🔄 Divergent | 🔄 Divergent | `CatFishDefinition.h:173`–`175` 确是单值 `EatingExperience` 且注释写「值来自当前鱼表体重档」，无「经验系数」字段，`:56` 也确实不乘重量；`工程自补决策记录.md:68` 确写 D-09 前提整体过期、经验已退出体重档链 |
| 7 | 🔄 Divergent | 🔄 Divergent | `Config/DefaultGame.ini:175` 确为 `ExperiencePerChoiceSlot=10`，`CatGrowthSettings.h:21` 注释确写「当前需求锁定为 10」；设计 250 见数值成长.md:35、升级效果.md:13。通搜 52 条 D 表无「经验槽」条目，「属未跟进而非有意分歧」成立 |
| 9 | ❌ Missing | ❌ Missing | `三选一` 全库四处命中全是 `Growth/` 的注释（`CatGrowthComponent.h:40`、`CatGrowthSettings.h:21`、`CatGrowthTypes.h:20`、`:24`）；`ChoiceOption`／`UpgradeOption`／`GrowthOption`／`OptionPool`／`PerkPool` 全零；`CatGrowthComponent.cpp:84` 确只 `++PendingChoiceCount` |
| 11 | ❌ Missing | ❌ Missing | 反查 `GetGrowthComponent`／`FCatGrowthSnapshot` 的全部消费者：只有 `CatConditionComponent`（写）与 `CatHUDModel`／`CatHUDWidget`（读）。没有任何代码把成长事实接到力量、体力上限、背包格数、完美窗、咬钩间隔或竿耐久上 |
| 13 | ❌ Missing | ❌ Missing | `CatFishDefinition.h:169`–`183` 的 `Use` 段确只有四项，无 buff／时长字段；`Buff` 一词在 `Source/`＋`Config/` 去掉 `*Buffer*` 后只剩 12 条注释、全在 `Growth/` 与 `CatCharacter.h:232`，字面写着「Buff 未裁时不生成临时效果」。`BuffDuration`／`TimedEffect` 全零 |
| 17 | ❌ Missing | ❌ Missing | `ProtectsMischiefAgainst` 全项目只有 `CatProtectionSignActor.cpp:37` 一个定义、`CatSocialService.cpp:297` 一个调用点；`CatCampHubActor.cpp:193`–`215` 的 `RescueToCamp` 确只判救援者未倒地、目标已倒地与 `InteractionRadiusCentimeters`，无 buff 屏蔽 |
| 18 | ❌ Missing | ❌ Missing | 状态维持，**证据更正**：`Blessing` 与 `祝福` 在 `Source/`／`Config/`／`Scripts/` 各为零命中，原文「仅命中 Fishing 调试配色」写错了（那是第 19 行 `Yellow` 的结果）。`CatRunSettleOfferingExecutionCalculation.cpp:105`–`131` 确只产出 `WorldProgressDelta`，`CatAltarActor.cpp:218`–`241` 确只按重量档计点 |
| 19 | ❌ Missing | ❌ Missing | `CatSurvivalAttributeSet.h` 的四条属性确在 `:28`／`:33`／`:38`／`:43`；`Yellow` 三处命中全是 `DrawDebugPoint`／`FLinearColor::Yellow`／调试 HUD 配色，`Shield`／`护盾`／`Overshield` 全零 |
| 20 | ⚠️ Partial | ⚠️ Partial | 状态维持，**证据加强**：`RecoveryPerSecond = 5.0`（`CatPhysicalEffortSettings.h:19`）与 `RecoveryDelaySeconds = 2.0`（`:21`）属实，`CatPhysicalEffortComponent.cpp:80`–`82` 的静止延迟属实。原文把「站着不回体」归因于 `bRecoveryPending`，实际闸门更窄：`bExerting`←`bActive`←`Drive.bCooperative`，而 `bCooperative` 只在 `CatPhysicalBodyComponent.cpp:393` 的 `!bFishing && bConnected` 下置真，整条回体通道只存在于合力／接触态。速率与「无场景乘数」两项仍成立，故仍判 ⚠️ 而非 🔄 |
| 21 | 🔄 Divergent | 🔄 Divergent | `CatFishingFightRunner.cpp:350`–`351` 与 `CatFishingFightSimulator.cpp:633`–`637` 确是常驻回体项；`CatFishingFightSimulator.h:77` 默认 1.5；`CatFishingFightBalanceDefinition.h:135`–`138` 注释确写「正常右键且身体/杆完全卸载时主控每秒恢复的体力」，且资产字段默认 -1.0（未配置哨兵），正式值仍属 ③ |
| 22 | ❌ Missing | ❌ Missing | `ECatFishFoodSafety` 确只有 `Unset／Safe／Toxic`（`CatFishDefinition.h:26`–`34`）；`CatFishDefinition.cpp:32`–`35` 的 `bFoodReady` 确要求 FoodSafety∈{Safe,Toxic} 且 `EatingExperience>0`。**补强**：`CatFishCatalogSettings.cpp:18` 用 `IsRuntimeDefinitionReady()` 过滤候选鱼，所以 `Fish_Salted`／`Fish_LakeGiantShadow` 只能二选一——要么可钓但被标可食用，要么不可食用但根本抽不到；设计要的「可钓可卖、不可食用」在当前数据模型里确实无法表达 |
| 23 | ❌ Missing | ❌ Missing | `Nickname`／`外号`／`BuildSnapshot` 全零；`毕业`／`Graduation` 的 10 处命中全是 Run 阶段文案与注释，最实处是 `CatfishingGameModeBase.cpp:1301` 的「毕业之夜」与 `CatRunSettings.cpp:89`–`91` 的 `WorldProgress >= 100`。正式印记事件 ID 确只有 `DefaultGame.ini:53`／`:123`／`:285` 三条 |
| 26 | ✅ Implemented | ✅ Implemented | 抽验：全项目 `bDowned` 30 处引用逐条看过，唯一写入点确是 `CatConditionComponent.cpp:520`，其余全是读；`:515` 的阈值 gate 与 `:520` 的 `IsPoisonAtLeast` 之外没有第二条倒地来源，也没有死亡分支 |
| 28 | ✅ Implemented | ✅ Implemented | 抽验：`CatRunImprintService.cpp:29`–`54` 确在捕获提交时按 `CaptureRequestId` 幂等授予 `FishRecorded`；吃鱼链（Condition／Growth／FishContainers／Social）四个文件里对 `Imprint`／`Profile`／`Collection` 零写入 |
| 29 | 🔄 Divergent | 🔄 Divergent | `CatCharacter.cpp:465` 确在倒地时 `SetLocomotionEnabledFromAuthority(false)`；`CatPhysicalBodyComponent.cpp:552` 确在 `!bLocomotionEnabled` 时把 `MoveInput` 清零，`:689`–`696` 确同时 `ClearControlIntent`。唯一的「爬行」字样是 `CatConditionTypes.h:13` 对 `FieldSelfRecovery` 的注释，是恢复方式命名不是位移实现 |
| 30 | ❌ Missing | ❌ Missing | `CatConditionComponent.cpp:47` 确关 Tick；`Condition/` 目录（除测试）`Timer`／`SetTimer` 零命中；`CatConditionSettings.h:19`–`53` 全篇读过，只有阈值、水深与三条清毒量，无自愈时长字段 |
| 31 | ⚠️ Partial | ⚠️ Partial | `CatCampHubActor.cpp:216` 的 `TeleportTo`→`:221` 的 `CompleteCarryToCamp` 链属实；`CatConditionComponent.cpp:454` 确只写 `RecoveryMode = CarriedToCamp`、不动 Poison 也不清 `bDowned`；`CampRestPoisonRelief=50` 对阈值 100 只减一半属实 |
| 32 | ❌ Missing | ❌ Missing | `CommitAltarDayTransition`（`:1259`–`1309`）与 `FinishAltarDayTransition`（`:1312`–`1348`）全篇读过，只处理供品结算、世界进度、阶段与截止时间。补查：整个 `CatfishingGameModeBase.cpp` 对 `CatConditionComponent` 只有 `:798` 一处引用（命令门），全项目 `ApplyPoisonDelta` 的调用点也只有 Condition 组件内部 |
| 33 | ✅ Implemented | ✅ Implemented | 抽验：`CatAltarActor.cpp:168` 确是 `if (!IsControllerActive \|\| (Condition && bDowned)) continue;`，倒地者既不进 `EligibleCount`（`:169`）也不进 `PresentPlayers`（`:172`）；`:188` 确是 `EligibleCount > 0 && ConfirmedCount == EligibleCount` |
| 34 | ⚠️ Partial | ⚠️ Partial | `CatAltarActor.cpp:76` 确在 `CanInteract` 里拒绝倒地者；`CatFishContainerService.cpp:469`、`CatFishGuardActor.cpp:131` 确同样 fail-closed。`CollectOffering`（`:209`–`243`）确按半径无差别收鱼，没有「替他人献」的命令或授权，后半句仍无专门承载 |
| 36 | ✅ Implemented | ✅ Implemented | 抽验：`CatConditionComponent.cpp:524`–`532` 确在首次倒地时调 `ReleaseFishingOperatorForCharacter`；该函数（`CatFishingService.cpp:914`–`939`）走 `RemoveOperatorAndReconcileSession`，会话按剩余操作位重整而不是整场作废，「交回剩余成员」成立；`:1511`／`:1529` 与 `CatChumPlacementService.cpp:135` 确拒绝倒地者的新命令 |
| 39 | ⚠️ Partial | ⚠️ Partial | `RequestFieldSelfRecovery` 全项目调用点复查为六处，全部在自动化测试（`CatUprightMovementTests.cpp:128`／`:134`、`CatPhysicalCharacterAnimationNetworkTests.cpp:240`／`:258`、`CatCuteCatNetworkTests.cpp:130`／`:137`），无 RPC／BodyAction／UI 入口；`FieldRestPoisonRelief=15` 已配（`DefaultGame.ini:168`） |
| 40 | ✅ Implemented | ✅ Implemented | 抽验：`CatfishingPlayerController.cpp:761` 的 `ServerRequestCampRest`→`CatCampHubActor.cpp:175` 的范围解析→`CatConditionComponent.cpp:390`–`404` 的 `RequestCampRest`→`:472` 的 `ApplyRecovery` 全链贯通；`CampRestPoisonRelief=50` 已配，`DefaultGame.ini:154` 的 CampRest Montage 也在 |
| 41 | 🔄 Divergent | 🔄 Divergent | 草药链比首轮描述的更完整：RPC 在 `CatfishingPlayerController.cpp:1152`–`1168`，事务在 `CatConditionComponent.cpp:186`–`321`，恢复口在 `:407`–`427`，Fragment 在 `CatHerbRecoveryItemFragment.h:9`，`ECatRecoveryMode::Herb` 在 `CatConditionTypes.h:18`，`HerbPoisonRelief=50`／`HerbUseRangeCentimeters=250` 在 ini `:170`–`171`，`AM_BodyAction_UseHerbOnCharacter.uasset` 仍在。52 条 D 表无草药条目，09-09「删代码一条——草药恢复链」未执行 |
| 43 | ⚠️ Partial | ⚠️ Partial | `SetWetFromAuthority`（`:64`）确为全项目零调用者（除自身声明）；`UpdateWaterExposureFromAuthority` 的产品调用点确只有 `CatFishingFightRunner.cpp:896`（另一处在测试）；`bWet` 除组件内部写入外无任何 C++ 消费者，只经 `FCatConditionSnapshot::bWet` 复制给蓝图 |
| 44 | ❓ Unverifiable | ❓ Unverifiable | 该状态仅此一行，按规则全查。可文本检查的三条已补进 Notes：落水挂点≠上岸、`ECatWaterExposureState` 无「半干」档、`bWet` 唯一驱动是搏斗固定步。三条都不能证否 ABP 本地计时与材质切换，按声明 ④ 维持 ❓，未推给 ❌ |
| 45 | ❌ Missing | ❌ Missing | `Character/`＋`Condition/`＋`Growth/` 三目录 `Weather` 零命中；`shelter`／`躲雨`／`RainCover`／`TakeCover` 全库零命中 |
| 46 | ✅ Implemented | ✅ Implemented | 抽验：`CatSurvivalAttributeSet.h` 确只剩 `:28`／`:33`／`:38`／`:43` 四项（三项＋上限）；`Hunger`／`Fatigue` 在 `Source/`＋`Config/`＋`Scripts/` 全文唯一命中是 `DefaultEngine.ini:114` 的属性重定向；`工程自补决策记录.md:25` 的 D-06 状态确为「已执行」。`Scripts/` 确无 `verify_character*`，首轮的范围收窄说明属实 |
| 48 | ⚠️ Partial | ⚠️ Partial | 状态维持，**证据更正且读数改变**：首轮「按六词过完 `Content/Characters/CuteCat/` 一个都没有」不成立——`Content/Characters/CuteCat/Meshes/SK_CuteCat_Anim_Armature_idle_A_yawn.uasset` 与 `..._layingdown_A_yawn.uasset` 两段打哈欠素材存在（该包动画序列落在 `Meshes/` 不在 `Animation/`，首轮漏扫这一层）。舔毛／甩尾／打滚／伸懒腰仍全零（`*tail*` 的命中全是 `Detail`／`Longtail` 子串）。七项里由「两项有素材」更正为**三项有素材、四项无** |
| 51 | ⚠️ Partial | ⚠️ Partial | `CatQuadrupedLocomotion.cpp:345`–`369` 确按地面射线与 `ImpactNormal.Z >= 0.65` 解算落脚，`:22`–`:24`（.h）确有三条位移边界；`CatCharacterRigSettings.h:24`／`:26` 确定义四条腿链与 pelvis；`CatPhysicsGrabComponent.h:71` 确提供世界握点。后三项（尾耳程序化、炸毛、入水毛塌）C++／Config 零命中，属 ④ 不判缺失 |
| 53 | 🔄 Divergent | 🔄 Divergent | `CatHUDModel.cpp:311`–`317` 确是常驻拼接的「猫状态：中毒 x｜钓鱼力量 x｜搏斗体力 x｜成长总经验 x，当前槽 x，待选 x」，与「按需显示、无常驻数值面」相反；52 条 D 表无此条登记 |
| 57 | ⚠️ Partial | ⚠️ Partial | `CatFishGuardInventoryWidget.cpp:112`–`113` 确只给「选中鱼售价／全部售价」；`CatFishTankWorldInfoComponent.cpp:113` 确只累计缸内供奉总点数；`CatFishPickupActor.cpp:984`–`986` 确只在交互提示里给重量。`Source/Catfishing/UI/` 全目录 `EatingExperience` 零命中，逐条三值同屏确实不存在，三值里只有售价做到了逐条 |
| 58 | ✅ Implemented | ✅ Implemented | 抽验：`CatGrowthTypes.h:16`–`26` 与 `CatConditionTypes.h:41`–`63` 的 Snapshot 都挂在 Character 上；`Save/`＋`Profile/` 两目录对 `Growth`／`Poison`／`Condition` 的全部命中只有 `CatProfileSubsystem.cpp:273` 的 `FirstCaptureCondition`（图鉴条件，不是身体状态）；`ECatProfileGrantKind` 确只有图鉴／剪影／印记／解锁四类 |
| 59 | ✅ Implemented | ✅ Implemented | 抽验：`CatConditionTypes.h:41`–`63` 全篇读过，只有 `bWet`／`WaterExposure`／`bDowned`／`RecoveryMode`，无生命值；`CatConditionComponent.cpp:511`–`533` 无死亡终态，注释亦写明「始终没有死亡分支」 |
| 61 | ❌ Missing | ❌ Missing | `CatAltarActor.cpp:218`–`241` 的 `CollectOffering` 确只按 `State==Available && !GetAttachParentActor() && 半径内` 收鱼并按重量档计点，不问鱼种；唯一鱼种分支 `IsStinkyOfferingFish`（`CatRunSettings.cpp:83`–`86`、`DefaultGame.ini:249`）走的是增益折扣不是拒收 |
