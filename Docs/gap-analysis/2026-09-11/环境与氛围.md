## 环境与氛围

### Features

| # | Design Requirement | Status | Code Reference | Notes |
|---|---|---|---|---|
| 1 | 时钟源＝局内时间流：开局启动、随局推进、局末即弃，任何机制不读现实时钟（环境与氛围.md rev106:65、:153、:46） | ✅ Implemented | `Source/Catfishing/Environment/CatEnvironmentSettings.cpp:11-29`；`Source/Catfishing/Environment/CatConfiguredEnvironmentProvider.cpp:22-23` | 时段只由 `FCatRunPhaseSnapshot` 的 `ServerTimeAnchorSeconds`/`DeadlineServerTimeSeconds` 与 `World->GetTimeSeconds()` 推算；「绝不读取现实时间」写在 `CatEnvironmentSettings.cpp:11` 的流程注释里。本轮重搜全库 `FDateTime::Now`/`UtcNow`/`FPlatformTime::SystemTime`，只命中 `Source/Catfishing/Save/CatSaveSubsystem.cpp:510`、`:981` 的存档时间戳，环境链零命中 |
| 2 | 白天时段轴＝清晨/白天/黄昏三段，夜晚不入时段轴（:65、:156 轴定义权归本册） | ✅ Implemented | `Source/Catfishing/Framework/Core/CatRunContracts.h:117-129`；`Source/Catfishing/Environment/CatEnvironmentSettings.cpp:22-28`；`Config/DefaultGame.ini:51-52` | `ECatEnvironmentTimeOfDay{Unknown,Morning,Day,Dusk}`，非 DayActive 或无 deadline 一律 Unknown（`:15-21`）。**本轮变化**：`MorningEndFraction` 由 0.001 改成 0（提交 dfb9509，ini:50 注释「测试时跳过清晨」），`Progress < 0` 恒不成立，现网配置下 Morning 永不返回——轴仍是三段，只是测试值把清晨关掉了 |
| 3 | 一局＝多天，白天限时推进、夜晚无时限停摆、全员同意后开始新的一天（:65，天结构归局与进程册） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1098`、`:1124-1134`、`:1137-1141`；`Source/Catfishing/Camp/CatAltarActor.h:34`、`:94`；`Source/Catfishing/Run/CatRunSettings.h:102`；`Config/DefaultGame.ini:233` | DayActive 递增 DayIndex 并只在此挂 `DayDeadlineTimerHandle`；`NormalNight` 分支不设任何计时器。**本轮变化**：`SubmitNextDayReady` 命令已删，翻天改由祭坛 `RefreshConfirmations()` 全员确认后请求（09-09 裁决②，设计修改记录.md:283）；`FinalDayIndex` 已删、天数无上限（09-09 裁决①，D-01 已闭）。`DayLengthSeconds=99999` 是工程测试值（提交 1f3ed0c），白天时长本就归数值阶段（§11-4） |
| 4 | 昼夜推进要真的改变语义：时段分界到点重新发布环境（事件出场时段的前提，:71） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1571-1601`、`:1604-1616` | Morning/Dusk 两个分界换算成绝对服务器秒后各安排一次 one-shot，到点 `++Revision` 并重发环境快照。注意现网 `MorningEndFraction=0` 使 Morning 刷新点等于锚点、不满足 `> ServerNowSeconds`（`:1586`），实际只有 Dusk 一个分界会触发 |
| 5 | 昼夜改光照（画面随一天走完一条弧线，:65、:185） | ✅ Implemented | `Source/Catfishing/Environment/Presentation/CatEnvironmentPresentationActor.cpp:497-525`、`:532-541`、`:200-215` | C++ 原生按表现阶段（Morning/Day/Dusk/Night/SettlementNight）与连续白天进度插值太阳、月光、天空光、高度雾与后处理，直接写本地场景对象；缺对象安全跳过，不伪造灯光 |
| 6 | 昼夜改环境音；夜晚虫鸣是「入夜」的听觉信号（:65、:186） | ❓ Unverifiable | — | `Source/Catfishing/Environment/**` 内 `Sound`/`Audio`/`Ambient` 零命中，唯一提及在 `Presentation/CatEnvironmentPresentationActor.h:54` 的注释「用于灯光、天空、雾和音频切换」。音频只可能挂在 `:389` 的 `BP_ApplyEnvironmentPresentation` 之后的蓝图与音频资产里，属可检查程度④，不据文本判未实现 |
| 7 | 天气按局内时钟在晴/雨/雾之间切换，每一局有不同的脸色（:77） | 🔄 Divergent | `Source/Catfishing/Environment/CatEnvironmentSettings.h:37-39`；`Source/Catfishing/Environment/CatConfiguredEnvironmentProvider.cpp:22`；`Config/DefaultGame.ini:49`（ConfiguredWeather=Clear） | D-24（待裁）：仍是「一局一个显式配置天气」，provider 每次求值直接复制 `ConfiguredWeather`，没有分布、切换粒度或调度种子。本轮重搜 `WeatherSchedule`/`WeatherWeight`/`ResolveWeatherForSlot`/`WeatherTransition` 在 Source/Config/Scripts 全部零命中；`Environment/**` 内 `FRandomStream` 只命中 `Presentation/CatChumFieldPresentationActor.cpp:98` 的装饰随机，`SetTimer` 只命中 `CatChumFieldSubsystem.cpp:51` 的到期清理。设计侧 09-09/09-10 无相关裁决，D-24 仍开着 |
| 8 | 天气轴枚举＝晴/雨/雾（轴定义权归本册，:77、:156） | ✅ Implemented | `Source/Catfishing/Framework/Core/CatRunContracts.h:103-115` | `ECatEnvironmentWeather{Unknown,Clear,Rain,Fog}`，Unknown 注释写明是 fail-closed 占位而非第四种天气 |
| 9 | 区域轴枚举由本册定义（有哪些区域），鱼落在轴上哪一格归鱼册（:156） | ⚠️ Partial | `Source/Catfishing/Environment/CatWaterTypes.h:58-79`；`Source/Catfishing/Environment/CatWaterQuerySubsystem.cpp:206-235`；`Source/Catfishing/Data/CatFishCatalogSettings.cpp:16-18`、`:194-195`；`Source/Catfishing/Data/CatFishDefinition.h:115` | 水域身份只有自由 `FName RegionId` ＋几何 Revision，没有区域枚举；机制上支持多水域注册与遍历（`QueryNearestShoreForPreview` 逐 RegionId 迭代，同 ID 多实例返回 `AmbiguousRegion` fail-closed），鱼册侧也确实按 `Definition->RegionIds.Contains(RegionId)` 消费这条轴。但「区域轴」没有落成可枚举类型，轴上有哪几格由资产自由填写。重搜 `ECatWaterRegionKind`/`RegionKind`/`WaterRegionType` 零命中 |
| 10 | 双区域探索结构：河流（日常水域）＋森林湖（要走一段路、鱼种与事件偏稀有）（:89） | ❓ Unverifiable | — | **本轮改判（基线为 ❌）**：基线的文本证据是 `Scripts/verify_showcase_formal_fish_region.py`、`verify_stage_a_map.py`、`create_stage_a_maps.py` 三个脚本里「水域恰好一个」的断言，这三个脚本已在提交 7e7c38e 连同另外三十余个脚本一起删除，Scripts/ 现存 13 项且无任何 Region/Water 断言。区域本体是 `.umap` 里的 `ACatWaterRegion` 摆放，属③，本轮不跑引擎无法确认。旁证只到线索级：`ForestLake`/`森林湖` 在 Source/Config/Scripts 零命中，`需求对齐差距清单.md:498` 写「由 Showcase2 `River` 水域使用正式资产」，鱼资产名里有 `Fish_ForestLongtail`/`Fish_LakeGiantShadow`——都不能证明第二个区域在不在正式地图里。可独立判定的那部分见第 31、32 行 |
| 11 | 钓点＝玩家概念而非机制点位，任意水边可钓，打窝即自发成为钓点（:89） | ✅ Implemented | `Source/Catfishing/Environment/CatWaterQuerySubsystem.cpp:112-141`、`:206-235`；`Source/Catfishing/Environment/CatChumPlacementService.h:17-19` | 无固定钓点表：落点进入水域几何即成立；窝点由投料落点现算，`ACatChumFieldAnchor` 只是自然事件的锚点，不是玩家钓点 |
| 12 | 时段与天气对鱼情做机会向修正（修正入口归本册，鱼表归鱼册；:65、:77、:213） | ⚠️ Partial | `Source/Catfishing/Fishing/CatFishingSession.cpp:818-819`、`:882-883`；`Source/Catfishing/Data/CatFishCatalogSettings.cpp:215-219`；`Source/Catfishing/Data/CatFishDefinition.h:119`、`:123`；`Config/DefaultGame.ini:89-90` | 接缝齐全：**本轮变化**是冻结点从抛竿移到真咬瞬间（`BiteTimeOfDay`/`BiteWeather` 在 `:818-819` 写入，`:882-883` 进 `FrozenSelectionContext`，跨夜真咬保留发生时的时段）；鱼定义也有 TimeOfDay/Weather 列。但 `bEnableTimeOfDayEligibilityFilter=False`、`bEnableWeatherEligibilityFilter=False`（D-31 默认旁路，开启节点仍待策划给），当前时段与天气对鱼情零影响 |
| 13 | 设计禁区：环境只给机会不做惩罚——无天气惩罚、无生存压力、无复杂生态模拟（:42-45、:154、:170） | ✅ Implemented | `Source/Catfishing/Environment/CatEnvironmentSettings.h:33-63`；`Source/Catfishing/Condition/CatConditionComponent.cpp:63-76`、`:103-105` | 反证式判定：环境设置里没有任何惩罚/扣减字段，Wet 只是可复制的离散事实、不挂数值（疲惫数值按 D-06 已删），也不存在生态/生存模拟模块。Condition 里唯一有后果的是危险水深→落水终局，其出处在猫册/钓鱼册（09-08 落水触发三条件），不是环境册的惩罚 |
| 14 | 雨天淋湿由本册触发（表现规范归猫册，本册只触发；:77） | ❌ Missing | `Source/Catfishing/Condition/CatConditionComponent.cpp:64-76`、`:103-105`；`Source/Catfishing/Condition/CatConditionComponent.h:33`；`Source/Catfishing/Framework/Core/CatRunContracts.h:111`（Rain 注释把 Wet 触发推给 Character） | `SetWetFromAuthority` 在整个 Source/ 里只有声明（`.h:33`）与定义（`.cpp:64`）两处，零调用者；`bWet` 的唯一实际写入在 `:132`，由浸没深度 ≥ `WetWaterDepthCentimeters` 驱动。`Environment.Weather` 的全部消费者只有真咬冻结、表现投影、调试面板三处，无一写 Condition。天气→淋湿这条触发链在 C++ 里不存在（可文本检查，非查不了） |
| 15 | 自然事件按局内时钟与天气条件出场，条件不成立就没有事件（不造占位事件；:101） | ⚠️ Partial | `Source/Catfishing/Environment/CatEnvironmentSettings.cpp:53-69`；`Source/Catfishing/Environment/CatEnvironmentSettings.h:49-55`；`Config/DefaultGame.ini:53-54` | **复核改判（原 ✅）**：后半截「条件不成立就没有事件」成立——事件要 runtime ready ＋ DayActive ＋已知时段＋天气匹配才输出，否则 None，不造占位（现网 ActiveEventId=RainBloom 要求 Rain 而 ConfiguredWeather=Clear，当前实际不成立，属配置状态）。但前半截两条轴只落了一条：`ActiveEventRequiredWeather` 是逐事件的天气条件，时段侧**没有对应字段**——`TryResolveActiveEvent` 里 `TimeOfDay != Unknown`（`:59`）只是「现在是可钓白天」的有效性门，不是「本事件只在黄昏/夜里出」的出场条件。重搜 `RequiredTimeOfDay`/`EventTimeOfDay` 全库零命中，`UCatEnvironmentSettings` 八个 Config 字段里无时段条件位（`.h:33-63`）。与第 16 行是两件事：那行说事件表没有，这行说时段这条出场轴没有入口 |
| 16 | 六类自然事件及各自出场条件成表：彩虹（雨后放晴）/萤火虫（夜·草木边）/晚霞（黄昏）/月光湖面（夜·晴）/鸟群蝴蝶（白天）/森林湖鱼群（:103-110） | 🔄 Divergent | `Source/Catfishing/Environment/CatEnvironmentSettings.h:49-55`；`Source/Catfishing/Framework/Core/CatRunContracts.h:263-269`；`Config/DefaultGame.ini:53` | D-26（待裁）：快照只装得下一个 `ActiveEventId`，配置只有单个事件位而不是事件表，事件名 RainBloom 也不在设计六类内；且 `TryResolveActiveEvent` 要求 `Phase == DayActive`（`CatEnvironmentSettings.cpp:59`），萤火虫/月光两个夜间事件结构上不可能成立。本轮重搜 `Rainbow`/`Fireflies`/`Firefly`/`SunsetGlow`/`MoonlitLake`/`彩虹`/`萤火`/`晚霞`/`鸟群`/`蝴蝶`，Source/Config/Scripts 全部零命中，六类事件 ID 一个都不存在 |
| 17 | 每个自然事件显式标注是否抛印记，环境印记供给印记相册「风景页」（:99、:103-110、:169） | ❌ Missing | `Source/Catfishing/Collection/CatRunImprintService.cpp:117`；`Source/Catfishing/Camp/CatCampHubActor.cpp:321`；`Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:1108-1109`；`Source/Catfishing/Social/CatSocialService.cpp:590` | 以 `SubmitImprintCandidate`／`FCatImprintCandidate` 反查全部调用点（排除 Tests）：唯一写口只被营地篝火、鱼获拾取与偷鱼被抓三处调用，没有任何环境事件入口，`Environment/**` 内 `ImprintCandidate` 零命中。D-07（状态已从「已执行」退回待裁）明写正式触发清单仍是策划待答。可文本检查，非查不了 |
| 18 | 事件必须隔着距离也能察觉（声音＋光＋猫身体语言），否则聚不起人（:101、:187） | ⚠️ Partial | `Source/Catfishing/Environment/Presentation/CatEnvironmentPresentationActor.cpp:359-362`、`:385-389` | 事件事实（`bHasActiveEvent`/`ActiveEventId`）已复制到客户端并投影给表现层，但 C++ 侧没有任何远距离感知信号：Social 与 UI 目录里无事件广播/提示/聚拢标记符号。声光与猫身体语言的本体只可能在 BP 与资产，那部分查不了 |
| 19 | 系统按局内时钟排程周期事件，随机只做小抖动（:145） | ❌ Missing | `Source/Catfishing/Environment/CatEnvironmentSettings.cpp:53-69`；`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1571-1601`（唯一的时钟排程只服务时段刷新） | 没有任何事件调度器或随机流：事件成立与否完全由静态配置＋当前时段/天气判定，既无周期排程也无抖动。`Environment/**` 内 `Schedule`/`Cooldown`/`Frequency`/`Interval` 只命中 ChumField 的过期清理间隔（`CatChumFieldSubsystem.cpp:49-52`）。**复核订正证据**：GameMode 的 `SetTimer` 不止两处，共七处（`:251` 存档检查点、`:1131`/`:1335`/`:2494` 白天截止、`:1245`/`:1246` 祭坛翻天淡入淡出、`:1588`/`:1594` Morning/Dusk 刷新，另有 `:2158` 的 NextTick），但逐个打开后没有一处产生事件——结论不变。D-26 收口时明确撤回过 `ResolveNaturalEventId` 一类实现名，属未做而非查不了 |
| 20 | 聚鱼时刻的「自然触发」源：森林湖按局内时钟/天气自发涌现鱼群（本册 §5，:114-123） | 🔄 Divergent | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1681-1751`；`Source/Catfishing/Environment/CatEnvironmentSettings.cpp:72-84`；`Config/DefaultGame.ini:55-56` | 代码仍保留自然源写口：事件成立后按 `RunId\|DayIndex\|EventId\|AnchorId` 去重向 ChumField 提交一次 `NaturalEvent` 聚鱼。**按裁决评**：09-07 聚鱼单源化（只投料触发，设计修改记录.md:257 ④），09-08 一致性检查②「环境册的自然触发暂不动、等下一轮过稿」（:273），09-09 晚代码超前项处置「自然聚鱼写口先不动」（:289）——自然源当前不在本版范围内，代码超前且已登记 D-25（待裁），故记 🔄 而非 ✅。基线用的「正式地图锚点数被断言为 0」这条旁证已随 `verify_showcase_formal_fish_region.py` 在 7e7c38e 删除，本轮无法复用 |
| 21 | 两种触发进入同一套演出与钓鱼收益，不写两套逻辑（:123、:129） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1725-1744`；`Source/Catfishing/Environment/CatChumFieldTypes.h:14-18` | 自然源不另建子系统：走玩家投窝同一个 `UCatChumFieldSubsystem::PrepareField / ActivatePreparedFieldDeferred` 写口，只用 `ECatChumFieldSource::NaturalEvent` 区分来源。「不写两套逻辑」这条本身成立；其前提（自然源是否留在本版）见第 20 行 |
| 22 | 所有环境状态（时间、天气、事件进度）属于这一局，局末即弃、无跨局存档（:55、:147、:155） | ✅ Implemented | `Source/Catfishing/Framework/Core/CatRunContracts.h:249-274`、`:309-311`；`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1660-1671` | 环境只以 `FCatEnvironmentSnapshot` 活在 `RunPublicState` 里，求值失败即发同 Revision 空环境；`Source/Catfishing/Save/**` 内 `Weather`/`TimeOfDay`/`Environment` 零命中。**新情况**：09-09 账本六问③加了「局中断点」存档，`CatRunSaveGame.h:152`、`:172` 存 DayIndex 与 WorldProgress——那是局与进程册的推进事实，环境三字段仍零持久化，本行不受影响 |
| 23 | 多人同步按局内广播，事件对同局全员同时可见、不存在离线错过（:147、:168） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1672-1677`；`Source/Catfishing/Environment/Presentation/CatEnvironmentPresentationActor.cpp:347-367` | 服务器 GameMode 是唯一写者，经 GameState 复制同一份公开快照；客户端表现层只读缓存，不自行推进阶段或猜天气 |
| 24 | 种花：固定花坛点位、当局开花、局末随局清空、不存档不成长（:135） | ❌ Missing | — | 全库（Source/Config/Scripts）`Flower`/`FlowerBed`/`种花`/`花坛`/`Butterfly`/`蝴蝶` 零命中。设计侧 §11-3 本身仍是「去留待裁、砍掉也可接受」，09-09/09-10 两轮裁决均未提及，因此这是未立项而非实现欠账 |
| 25 | 森林湖沿途与湖区的特殊看点、风景点、隐藏观察点分布（:89、:181） | ❓ Unverifiable | — | 看点分布是纯关卡摆放（`.umap`／资产，属③），本轮不跑引擎无法确认；同时设计侧 §11-6「自然事件完整清单与出场条件表」与看点落图都还没定稿，两侧都缺 |
| 26 | 猫味演出：入夜瞳孔放大、清晨伸懒腰、正午找树荫、雨天炸毛甩水、雨停集体抖毛、看鸟咔咔、扑萤等（:67、:79、:91、:106-110、:125） | ❓ Unverifiable | `Config/DefaultGame.ini:152-159`；`Source/Catfishing/Environment/Presentation/CatEnvironmentPresentationActor.h:54-72` | **本轮换证据**：基线引的 `Scripts/create_body_action_montages.py` 已随 7e7c38e 删除；现在的 Montage 清单在 `CatBodyActionPresentationSettings` 段，六条全是 Camp/Social 动作（CampRest、CampfirePlayback、RescueCharacterToCamp、RequestManualHelp、RequestMischief、PlaceProtectionSign），无任何环境演出。但表现层已经拿到 Weather/TimeOfDay/事件投影，AnimBP 与表现蓝图里是否按这些字段分支属④，文本证明不了，不判未实现 |
| 27 | §7 视觉反馈通道：湖面波纹、鱼跃、鸟群聚集、植物随时段的细节变化（:185） | ❓ Unverifiable | — | 除昼夜灯光/雾/后处理（第 5 行）外，这些都由材质、粒子与关卡资产承载（③④），本轮不跑引擎无法确认 |
| 28 | 天气与区域各自的抛印记事件④：全员挤在一处躲雨的团子同框（:81）、首次抵达森林湖穿出树林的瞬间（:93） | ❌ Missing | `Source/Catfishing/Collection/CatRunImprintService.cpp:117`；`Source/Catfishing/Camp/CatCampHubActor.cpp:321`；`Source/Catfishing/Items/Fish/CatFishPickupActor.cpp:1108-1109`；`Source/Catfishing/Social/CatSocialService.cpp:590` | 与第 17 行同一条唯一写口，三个调用者里没有天气、没有区域、也没有首达判定；`FirstArrival`/`Shelter`/`RainShelter` 类符号全库零命中。同属 D-07 未给正式触发清单 |
| 29 | 两种触发共用一个稀缺预算，按一个机制记一本账（:123） | ⚠️ Partial | `Source/Catfishing/Environment/CatChumFieldSubsystem.cpp:184-191`、`:211-212`；`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1734-1738` | 自然源与玩家投窝确实共用同一本账——`PrepareField` 对两种来源一视同仁地按 RegionId 扣 `MaxActiveFieldsPerRegion` 与 `MaxRawContributionPerRegion`，`ECatChumFieldSource` 只做来源标记不改配额。但设计说的「稀缺预算」是事件频率/冷却口径，那一层（自然涌现频率、共享 cd）D-25 明写仍待裁、当前源码没有；现有账本是窝料浓度容量账，不是稀缺预算账 |
| 30 | 夜晚不钓鱼、不产鱼情；入夜边界＝不再有新咬钩，进行中的搏斗允许打完（:65、:71；裁决同步/设计修改记录.md:58） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1634-1638`；`Source/Catfishing/Framework/Core/CatRunContracts.h:240-242`；`Source/Catfishing/Environment/CatEnvironmentSettings.cpp:15-21` | 本轮新增行（基线未评）。白天截止只把 `bNewFishingBitesAllowed` 置 false 并让 `UCatFishingService::RefreshBiteAvailabilityFromAuthority()` 清等待，契约注释明写「不是鱼竿操作门禁，夜晚仍可抛收竿和完成已有搏斗」（提交 e51e5a0）；同时 `ResolveTimeOfDay` 在非 DayActive 返回 Unknown，夜晚不产时段鱼情。与 09-07「入夜边界」裁决逐条对上 |
| 31 | 两区域同属「湖畔」主场景（不是两张分开的关卡）（:89） | ✅ Implemented | `Source/Catfishing/Online/CatOnlineSettings.h:40`；`Source/Catfishing/Online/CatOnlineSubsystem.cpp:679-691`、`:1996`；`Config/DefaultGame.ini:12` | 本轮新增行。玩法只有一张 `GameplayMap`（Showcase2），Frontend → GameplayMap 一次 ServerTravel（`:1996`）之后再无关卡切换入口。**复核补证**：全库 `OpenLevel` 零命中，`ServerTravel`/`ClientTravel` 只有四处——两处去 GameplayMap（`:1996`、`:2015`）、两处回 Frontend（`:2126`、`:2138`），没有任何区域间 travel。区域之间靠走路而非读图这条结构成立 |
| 32 | 两区域的差异要落在「能遇到什么」上，森林湖的事件偏稀有（:89、:95） | ❌ Missing | `Source/Catfishing/Environment/CatEnvironmentSettings.h:49-63`；`Source/Catfishing/Framework/Core/CatRunContracts.h:249-274` | 本轮新增行，是第 10 行里唯一能用文本判定的那一半。事件配置与事件快照都没有区域维度：`ActiveEventId`/`ActiveEventRequiredWeather` 是全局单值，`FCatEnvironmentSnapshot` 不带 RegionId，事件无法「只在森林湖出」。鱼种那一半有区域门（第 9 行），事件这一半整条不存在 |
| 33 | 每种天气至少绑一个专属演出或专属事件，天气不能只剩装饰（:83） | ⚠️ Partial | `Source/Catfishing/Environment/CatEnvironmentSettings.h:53-55`；`Config/DefaultGame.ini:49`、`:53-54` | 本轮新增行。机制上支持给事件挂天气条件（`ActiveEventRequiredWeather`），但配置只有一个事件位：Rain 挂了 RainBloom，Clear 与 Fog 没有任何专属事件；而现网 `ConfiguredWeather=Clear` 与该事件要求的 Rain 不匹配，三种天气目前一个专属事件都出不来。天气专属演出在 `BP_ApplyEnvironmentPresentation` 之后，属④ |
| 34 | 天气变化要同时改画面和声音（晴/雨/雾切换时画面与声音跟着变，:77、:185-186） | ⚠️ Partial | `Source/Catfishing/Environment/Presentation/CatEnvironmentPresentationActor.cpp:359`、`:497-525`、`:385-389` | 复核补漏行：原报告把昼夜画面（第 5 行）、昼夜环境音（第 6 行）、天气专属演出（第 33 行）都列了，唯独漏了「天气本身改画面与声音」这条。可文本判定的那半是负结论：天气已复制并投影到表现层（`:359`），但真正驱动灯光/天空光/雾/后处理的 `BuildScenePresentationValues` 只按 `PresentationPhase` 与 `DayProgress` 分支，**整个函数不读 `State.Weather`**，原生画面链对雨/雾零反应；音频侧更彻底——`AudioComponent`/`SoundBase`/`SoundCue`/`PlaySound`/`AmbientSound` 在整个 `Source/**` 零命中，C++ 不碰任何声音。剩下那半（BP 与材质/粒子/音频资产里是否按 Weather 分支）属③④，不据文本判未实现 |

### Summary
- Total features: 34
- ✅ Implemented: 13
- ⚠️ Partial: 7
- ❌ Missing: 6
- 🔄 Divergent: 3
- ❓ Unverifiable: 5
- Inspectable: (34 - 5) / 34 * 100% = 85.3%
- Coverage of inspected: (13 + 0.5 * 7) / (34 - 5) * 100% = 56.9%

### Scan Scope

**代码基线**：工程根 `D:/LocalGameProject/Unreal/Catfishing/`，HEAD `bcc7df9`（基线 `87ba975` 之后 59 个实现提交）。文档镜像 `C:/Users/lzcm7/OneDrive/GameProject/CatFishing/Feishu/GDD 系统分册/环境与氛围.md`（revision_id 106，fetched_at 2026-09-11T17:57，只读；正文相对基线未变，仍是 v1.8）。**状态与 code_ref 全部重核，无一行抄基线**；沿用基线的只有未变要求的措辞与顺序，新增 4 行（#30～#33）追加在末尾。

**扫过的目录与文件类型**（只看 `.cpp/.h`＋`Config/*.ini`＋`Scripts/*.py|*.ps1`）：

- `Source/Catfishing/Environment/**`（含 `Presentation/`、`Tests/`）、`Source/Catfishing/Run/**`（含 `Debug/`）、`Source/Catfishing/Framework/Core/CatRunContracts.h`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp`、`Source/Catfishing/Data/`（选鱼时段/天气/区域门与鱼定义）、`Source/Catfishing/Fishing/CatFishingSession.cpp|.h`（真咬环境冻结）、`Source/Catfishing/Condition/`（Wet 与水域暴露）、`Source/Catfishing/Collection/`、`Source/Catfishing/Camp/`（篝火、祭坛翻天）、`Source/Catfishing/Items/Fish/`、`Source/Catfishing/Social/`（印记候选提交点反查）、`Source/Catfishing/Save/**`（持久化反证）、`Source/Catfishing/Online/`（GameplayMap 与关卡切换反证）、`Source/Catfishing/AbilitySystem/BodyAction/`（演出反证）。
- `Config/DefaultGame.ini`：`CatOnlineSettings`、`CatEnvironmentSettings`、`CatFishCatalogSettings`、`CatRunSettings`、`CatBodyActionPresentationSettings`、`ProjectPackagingSettings` 各段逐行读过。
- `Scripts/` 全量（现存 13 项）。**本轮重大变化**：提交 `7e7c38e` 删除了三十余个脚本，其中 `verify_showcase_formal_fish_region.py`、`verify_stage_a_map.py`、`create_stage_a_maps.py`、`create_body_action_montages.py`、`verify_run_environment_social.ps1|_runtime.py`、`configure_run_environment_social_map.py` 正是基线第 10、20、26 行的证据来源——主流程给的源码线索里那两个 `verify_run_environment_social*` 已不存在。第 10 行因此从 ❌ 改判 ❓（证据没了、区域摆放本属③），第 20、26 行换证据后维持原判。
- 台账：`Docs/Development/工程自补决策记录.md`（D-01…D-31 通读＋末节 09-09 复核段，本册相关 D-01/D-06/D-07/D-24/D-25/D-26/D-30/D-31）、`Docs/Development/需求对齐差距清单.md`（§4 问题表、§3 Run/Environment/Social 段、09-09 下午拍定表）。台账的完成记录只作线索，每个 ✅ 均另给了实际 code_ref。
- 裁决：`裁决同步/设计修改记录.md`（09-10 压缩后的现行行号）——:57-58 天结构与入夜边界、:257 ④聚鱼单源化、:273 ②环境册自然触发暂不动、:283 设计对表八问①②、:287 账本六问③、:289 自然聚鱼写口先不动。§5 按裁决评（本版单源、自然触发归完整版），不按该册字面评；残留出处在环境与氛围.md:114-123。

**方法**：先按类名、字段名、日志事件名（`environment_day_segment_refreshed`、`environment_day_refresh_scheduled`、`natural_chum_terminal`、`character_wet_changed`）、配置段名与中文注释 Grep 定位，再 Read 命中片段核语义；对「有没有」类要求用反向 Grep 取反证（`SetWetFromAuthority` 零调用、Save 模块无环境字段、Montage 清单无环境演出、六类事件 ID 零命中、`Flower`/`森林湖` 零命中）。全部 file:line 在本轮实际 Read 过，行号按当前 HEAD 重取（GameMode 与 Contracts 相对基线整体位移）。

**跳过 / 无法覆盖的东西**：

- `Content/**` 的 `.uasset/.umap`（含 13 张 `.umap`）不计入文本扫描：水域区域摆放、看点分布、光照/材质/粒子、AnimBP 与表现蓝图逻辑属可检查程度③④，本轮不跑引擎入口，因此第 6、10、25、26、27 行判 ❓ 而非 ❌——把它们报成未实现会引发重复实现。
- `Intermediate/Build/**` 的 UHT 生成代码不作为实现证据；`Source/**/Tests/` 只作旁证，本册无「只在测试里存在」而被判 ✅ 的条目（`CatEnvironmentSettingsTests.cpp`、`CatFishingBiteTimingWorldTests.cpp` 只用来佐证第 12、30 行的语义，不替代产品链路）。
- 设计侧本身未定的项（§6 体验锚定数字五条、§11 七条待办的具体数值与清单）不作为可判定要求列入表格。**复核补注**：唯一的例外是第 24 行种花——§11-3 只挂着「去留待裁」，但 §3.1-6 正文已给出完整机制口径（固定花坛点位、当局开花、局末随局清空、不存档不成长），所以按可判定要求入表，Notes 里注明它是未立项、不是实现欠账；这一行的 ❌ 不应读成工程欠债。
- 鱼在区域/时段/天气三轴上的具体落格归鱼册，本册只持轴定义与修正入口，不在此评分。

**复核阶段补扫（Phase 2b）**：逐行重走了全部 14 条 ❌/🔄/⚠️ 与全部 5 条 ❓，并把 14 条 ✅ 的 file:line 全部 Read 过（不止抽五条）。补扫到的三处与原报告不同：① 事件的时段条件在 `UCatEnvironmentSettings` 八个 Config 字段里根本没有入口（第 15 行改判 ⚠️）；② GameMode 的 `SetTimer` 实际有七处而非两处，逐个打开后仍无事件调度（第 19 行证据订正，状态不变）；③ 设计 :77「天气改画面和声音」在原表中无对应行，补为第 34 行。另核：`Scripts/` 现存 13 项里只有 `verify_run_day_deadline.py` 仍碰环境链（加载 Showcase2、跑白天截止 PIE，内含已不存在的 `cat.RunEnvironmentSocial.DayLength` 旧指令），它是测试脚本，只作第 3、4、30 行旁证，不替代产品链路证据；`Source/Catfishing/Run/Debug/CatRunEnvironmentDebugCommands.cpp` 只注册 SkipToNight / SkipToNextDay / ForceNextDay 三条非 Shipping 调试指令，属局与进程调试面，不计入本册要求也不列为代码超前项。

### Code-only mechanics

| 机制 | code_ref | 文档现状与出处 | 待确认问题 |
|---|---|---|---|
| 画面表现阶段比设计多出「结算夜」与「局末」两档：`ECatEnvironmentPresentationPhase` 有 SettlementNight／Ended，各自有独立的灯光雾色目标 | `Source/Catfishing/Environment/Presentation/CatEnvironmentPresentationActor.h:18-34`；`.cpp:505-524`、`:244-248` | 环境册只写清晨/白天/黄昏/夜晚四段（:65、:185），没有结算夜与局末的画面口径；查阅范围＝环境与氛围.md 全篇、局与进程册未查 | 结算夜与局末的画面该补进环境册 §3.1-1，还是整块归局与进程册？ |
| 环境可整体关闭并 fail-closed：`bEnableEnvironmentRuntime` 默认 false，天气未配置或晨昏分界非法时 provider 拒绝出快照，Run 发同 Revision 的空环境而不是造一个中性晴天 | `Source/Catfishing/Environment/CatEnvironmentSettings.cpp:4-9`；`Source/Catfishing/Environment/CatConfiguredEnvironmentProvider.cpp:17-21`、`:32-39`；`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1660-1671`；`Config/DefaultGame.ini:48` | 设计未写「环境有总开关」「求值失败时天气是 Unknown 而不是晴」；查阅范围＝环境与氛围.md §3.1-1/§3.1-2/§4，裁决账本 09-07～09-10 无相关条目 | 这是纯工程护栏（可只留在 D-24 里），还是「天气可能是未知」这件事要进本册的边界节？ |
| 水域是显式几何对象，同 ID 多实例或跨水域重叠一律拒绝（`AmbiguousRegion`），不做「就近归一个」的兜底 | `Source/Catfishing/Environment/CatWaterQuerySubsystem.cpp:62-84`、`:112-141`、`:217-220`；`Source/Catfishing/Environment/CatWaterTypes.h:58-79` | 环境册只说「任意水边可钓」（:89），没写水域是显式几何区域、也没写重叠怎么办；窝点几何归属在钓鱼规则册，水域本身无人认领 | 「水域＝显式几何区域、重叠即拒」该补进本册 §3.1-3（区域轴的承载物），还是归钓鱼规则册一起写？ |

### 复核记录

独立复核（Phase 2b）覆盖：**全部 14 条 ❌/🔄/⚠️ 逐行重找实现**（#7/9/12/14/16/17/18/19/20/24/28/29/32/33），**14 条 ✅ 全部 Read 过 file:line**（超出五条下限，#1/2/3/4/5/8/11/13/15/21/22/23/30/31），**5 条 ❓ 全查**（#6/10/25/26/27，本册 ❓ 恰好五条，无抽样余量）。另按设计标题、子规格与事件表逐节比对找漏，补 1 行（#34）。结果：33 行原判里 32 行维持、1 行改判（#15 ✅→⚠️），另有 2 行证据订正但不改状态（#19、#31），新增 1 行。Summary 八行与两个比例按 34 行重算。

未能完成的检查：`Content/**` 的 13 张 `.umap` 与全部 `.uasset`（水域区域摆放、看点分布、材质/粒子、AnimBP 与表现蓝图）本轮仍无引擎入口，第 6、10、25、26、27 行的 ❓ 无法在本轮消解；要落地需跑一次编辑器导出（列出 `ACatWaterRegion` 实例的 RegionId、`BP_ApplyEnvironmentPresentation` 的分支、AnimBP 是否读 Weather/TimeOfDay），这不在文本手段内。

| # | 原判 | 改判 | 依据 |
|---|---|---|---|
| 1 | ✅ Implemented | ✅ Implemented | 核 `CatEnvironmentSettings.cpp:11` 的「绝不读取现实时间」注释与 `:12-29` 的时段计算，确认只吃 `RunSnapshot.ServerTimeAnchorSeconds`/`DeadlineServerTimeSeconds` ＋ `World->GetTimeSeconds()`；`CatConfiguredEnvironmentProvider.cpp:22-23` 属实。独立重搜 `FDateTime::Now`/`UtcNow`/`FPlatformTime::SystemTime`，环境链零命中 |
| 2 | ✅ Implemented | ✅ Implemented | `CatRunContracts.h:117-129` 确为 `ECatEnvironmentTimeOfDay{Unknown,Morning,Day,Dusk}`；`CatEnvironmentSettings.cpp:15-21` 非 DayActive／无 deadline 返回 Unknown；`Config/DefaultGame.ini:50-52` 注释「测试时跳过清晨」＋`MorningEndFraction=0`、`DuskStartFraction=0.8` 与 Notes 一致，`.h:43` 的默认值也已是 0 |
| 3 | ✅ Implemented | ✅ Implemented | `CatfishingGameModeBase.cpp:1098` DayIndex++、`:1124-1134` 只在 DayActive 挂 `DayDeadlineTimerHandle`、`:1137-1141` NormalNight 分支无计时器，均属实；`FinalDayIndex` 与 `SubmitNextDayReady` 全库零命中，删除属实；`Config/DefaultGame.ini:233` DayLengthSeconds=99999 属实。翻天链补核到 `CatAltarActor.cpp:134-139`（`RefreshConfirmations()` 全员确认后调 `BeginAltarDayTransition`）→ `CatfishingGameModeBase.cpp:1205`，比原 Code Reference 的两行头文件声明更实 |
| 4 | ✅ Implemented | ✅ Implemented | `CatfishingGameModeBase.cpp:1571-1601` 两个 one-shot、`:1604-1616` 到点 `++Revision` 并重发环境，均属实；`:1586` 的 `> ServerNowSeconds` 使 MorningEndFraction=0 时清晨刷新点不成立，Notes 的现网限制说明正确 |
| 5 | ✅ Implemented | ✅ Implemented | `CatEnvironmentPresentationActor.cpp:497-525` 按表现阶段与白天进度插值、`:532-541` 起写太阳/月光/天空光/雾/后处理且缺对象安全跳过、`:200-215` 为各段目标值，三处均属实 |
| 6 | ❓ Unverifiable | ❓ Unverifiable | 复核把搜索从 `Environment/**` 扩到整个 `Source/**`：`AudioComponent`/`SoundBase`/`SoundCue`/`USoundWave`/`PlaySound`/`AmbientSound`/`MetaSound` **零命中**，C++ 完全不碰音频；`Config/DefaultEngine.ini:97-104` 与 `DefaultGame.ini:38` 说明音频只存在于资产与 SoundClass/SoundMix 设置。按声明 ④ 仍不得据文本判未实现，维持 ❓ |
| 7 | 🔄 Divergent | 🔄 Divergent | 独立重搜 `WeatherSchedule`/`WeatherWeight`/`WeatherTransition`/`ResolveWeather`/`WeatherRoll`/`WeatherTable`/`WeatherDistribution` 在 Source/Config/Scripts **全部零命中**；`CatConfiguredEnvironmentProvider.cpp:22` 确为直接复制 `ConfiguredWeather`，无调度。D-24 台账原文「撤回旧暂定实现名……当前源码没有 `ResolveWeatherForSlot`、天气权重或天气调度种子」、状态「待裁」核对无误 |
| 8 | ✅ Implemented | ✅ Implemented | `CatRunContracts.h:103-115` 确为 `{Unknown,Clear,Rain,Fog}`，`:107` 注释写明 Unknown 是 fail-closed 占位而非第四种天气 |
| 9 | ⚠️ Partial | ⚠️ Partial | `CatWaterTypes.h:58-79` 确为自由 `FName RegionId` ＋ `GeometryRevision`，无区域枚举；`CatWaterQuerySubsystem.cpp:206-235` 逐 RegionId 迭代、同 ID 多实例返回 `AmbiguousRegion` 属实；鱼侧 `CatFishCatalogSettings.cpp:16-18` ＋ `:194-195` 的 `RegionIds.Contains(RegionId)` 门属实。重搜 `ECatWaterRegionKind`/`RegionKind`/`WaterRegionType` 零命中 |
| 10 | ❓ Unverifiable | ❓ Unverifiable | 复核确认 `7e7c38e` 确实删除了 `verify_showcase_formal_fish_region.py`／`verify_stage_a_map.py`／`create_stage_a_maps.py`／`create_body_action_montages.py`／`verify_run_environment_social.ps1`／`verify_run_environment_social_runtime.py`／`configure_run_environment_social_map.py`（`git show --name-status 7e7c38e` 七条 D 记录），基线证据确已不存在。另独立搜 `ForestLake`/`森林湖`/`River`/`LakeA`：Source/Config/Scripts 里只剩 `CatFishSelectionPolicyTests.cpp:192` 的测试用 "River" 与 `Config/DefaultGame.ini:56` 的 `NaturalChumAnchorId=LakeAAnchor`，都不能证明第二个区域在正式地图里。区域摆放属③，维持 ❓ |
| 11 | ✅ Implemented | ✅ Implemented | `CatWaterQuerySubsystem.cpp:112-141`／`:206-235` 无固定钓点表，落点入水域几何即成立；`CatChumPlacementService.h:17-19` 为玩家打窝入口；补核 `CatChumFieldAnchor.h:9-25` 注释明写「自然事件聚鱼的关卡锚点……不参与玩家交互或复制」，Notes 对锚点性质的判断属实 |
| 12 | ⚠️ Partial | ⚠️ Partial | `CatFishingSession.cpp:818-819` 真咬瞬间写 `BiteTimeOfDay`/`BiteWeather`、`:882-883` 进 `FrozenSelectionContext` 属实；`CatFishCatalogSettings.cpp:214-219` 的 `PassesTimeOfDay`/`PassesWeather` 门与 `Config/DefaultGame.ini:88-90` 两个 False 开关属实；`CatFishDefinition.h:119`（TimeOfDay 数组）、`:123`（Weather 数组）行号核对无误。D-31 台账「两门默认旁路、开启节点待策划」仍成立（台账里写的 ini:111-112 是旧行号，现为 89-90） |
| 13 | ✅ Implemented | ✅ Implemented | `CatEnvironmentSettings.h:33-63` 八个 Config 字段逐个看过，无任何惩罚／扣减项；`CatConditionComponent.cpp:63-76` 的 Wet 是纯离散事实（注释明写不触碰 Poison、成长、搏斗体力、移动能力），`:103-105` 由浸没深度驱动；反向查确认天气的全部消费者不含任何属性或 GE 写口 |
| 14 | ❌ Missing | ❌ Missing | 独立反查 `SetWetFromAuthority`：全库只有 `CatConditionComponent.h:33` 声明与 `.cpp:64` 定义，**零调用者**；`bWet` 的唯一实际写入在 `.cpp:132`，来源是 `UpdateWaterExposureFromAuthority` 的浸没深度。天气→淋湿链在 C++ 中确不存在，可文本判定 |
| 15 | ✅ Implemented | ⚠️ Partial | 改判依据：设计 :101 给的是**两条**出场条件轴（局内时钟＋天气），代码只落了天气一条。`CatEnvironmentSettings.h:49-55` 里全部事件相关 Config 只有 `ActiveEventId` 与 `ActiveEventRequiredWeather`；`.cpp:57-66` 的 `TimeOfDay != ECatEnvironmentTimeOfDay::Unknown` 只是「当前是可钓白天」的有效性门，不是逐事件的出场时段条件（设计 :65、:103-110 要的是萤火虫只在夜里、晚霞只在黄昏）。重搜 `RequiredTimeOfDay`/`EventTimeOfDay` 全库零命中。后半截「条件不成立就没有事件、不造占位」仍成立（`.cpp:56` 先清 OutEventId、失败返回 None），故记 ⚠️ 而非 🔄／❌ |
| 16 | 🔄 Divergent | 🔄 Divergent | `CatEnvironmentSettings.h:49-55` 单事件位、`CatRunContracts.h:263-269` 快照单 `ActiveEventId`、`.cpp:59` 的 `Phase == DayActive` 使夜间事件结构上不成立，三条均属实。独立重搜 `Rainbow`/`Firefl`/`SunsetGlow`/`Moonlit`/`彩虹`/`萤火`/`晚霞`/`鸟群`/`鱼群` 零命中。D-26 台账原文与「待裁」状态核对无误 |
| 17 | ❌ Missing | ❌ Missing | 独立反查 `SubmitImprintCandidate`/`FCatImprintCandidate`：写口在 `CatRunImprintService.cpp:117`，调用者恰好三处——`CatCampHubActor.cpp:321`、`CatFishPickupActor.cpp:1109`、`CatSocialService.cpp:590`（无测试调用），`Environment/**` 内 `Imprint` 零命中。旁证：`Config/DefaultGame.ini:123`、`:285` 只有 `CampfireCoverEventId`／`TheftCaughtImprintEventId` 两个事件 ID，没有环境事件。D-07 在台账末节确已从「已执行」退回「待裁」 |
| 18 | ⚠️ Partial | ⚠️ Partial | 独立反查 `ActiveEventId`/`bHasActiveEvent` 的全部消费者：只有 provider 写入、GameMode 自然聚鱼、表现层投影（`CatEnvironmentPresentationActor.cpp:361-362`）、调试面板四处，Social/UI 侧零命中，远距离感知信号在 C++ 确不存在；声光与猫身体语言本体属④。⚠️ 的切法（事实已复制＋感知未做）成立 |
| 19 | ❌ Missing | ❌ Missing | 结论维持，**证据订正**：原 Notes 写「GameMode 侧 `SetTimer` 只有白天截止与 Morning/Dusk 两个一次性时段刷新」，实际有七处（`:251`、`:1131`、`:1245`、`:1246`、`:1335`、`:1588`、`:1594`，另有 `:2158` 的 NextTick），逐个打开后确认分别是存档检查点、白天截止（三处）、祭坛翻天淡入淡出，均不产生事件；`Environment/**` 内唯一 `SetTimer` 是 `CatChumFieldSubsystem.cpp:51` 的过期清理。已在 Notes 里改写 |
| 20 | 🔄 Divergent | 🔄 Divergent | `CatfishingGameModeBase.cpp:1681-1751` 自然聚鱼写口与 `:1693-1695` 的 `RunId\|DayIndex\|EventId\|AnchorId` 去重键属实；`CatEnvironmentSettings.cpp:72-84`、`Config/DefaultGame.ini:55-56` 属实。裁决侧逐条核到原文：设计修改记录.md:257「聚鱼单源化」、:273「②环境册的自然触发暂不动、等下一轮过稿」、:289「自然聚鱼写口、印记媒体传输、公开图鉴发布链先不动」，按裁决记 🔄 成立；D-25 台账状态确为「待裁」 |
| 21 | ✅ Implemented | ✅ Implemented | `CatfishingGameModeBase.cpp:1725-1744` 确实构造同一个 `FCatPrepareChumFieldRequest` 走 `PrepareField`／`ActivatePreparedFieldDeferred`，只用 `:1734` 的 `Request.Source = ECatChumFieldSource::NaturalEvent` 区分来源；`CatChumFieldTypes.h:14-18` 枚举只有 Player/NaturalEvent 两值，无第二套子系统 |
| 22 | ✅ Implemented | ✅ Implemented | `CatRunContracts.h:249-274`、`:309-311` 属实；`CatfishingGameModeBase.cpp:1660-1671` 求值失败发同 Revision 空环境属实。独立反证：`Source/Catfishing/Save/**` 内 `Weather`/`TimeOfDay`/`Environment` **零命中**；`CatRunSaveGame.h:152`（DayIndex）、`:172`（WorldProgress）确为局与进程事实，不含环境三字段 |
| 23 | ✅ Implemented | ✅ Implemented | `CatfishingGameModeBase.cpp:1672-1677` 服务器唯一写者经 GameState 复制属实；`CatEnvironmentPresentationActor.cpp:347-367` 客户端只从同一份 `RunPublicState` 拷贝、不自行推进阶段或猜天气，属实 |
| 24 | ❌ Missing | ❌ Missing | 独立重搜 `flower`/`butterfly`/`blossom`/`garden`/`种花`/`花坛`/`蝴蝶` 在 Source/Config/Scripts：只命中 `CatQuadrupedLocomotion.cpp` 足部 IK 的 `bPlanted`/`PlantOffsetWorld` 等同形词，无任何种花实现。设计侧 §11-3（环境与氛围.md:229）确为「去留待裁」，09-09／09-10 裁决账本无相关条目，Notes 的「未立项而非实现欠账」定性成立；已在 Scan Scope 补注它为何仍入表 |
| 25 | ❓ Unverifiable | ❓ Unverifiable | 看点／风景点／隐藏观察点分布是纯关卡摆放（③），无任何文本承载；设计侧 §11-6（:232）也仍未定稿，两侧都缺，维持 ❓ |
| 26 | ❓ Unverifiable | ❓ Unverifiable | 独立核 `Config/DefaultGame.ini:152-159`：六条 Montage 配置确实全是 Camp/Social 动作；再反查 GameplayTag 源头 `CatFishingAbilityTags.cpp:26-31`，六个 `Cat.Ability.Event.Body.*` 标签逐字对应，**没有任何环境演出标签**。但 `CatEnvironmentPresentationActor.h:54-72` 的投影确已把 Weather/TimeOfDay/事件交给蓝图，AnimBP 与表现蓝图分支属④，维持 ❓ |
| 27 | ❓ Unverifiable | ❓ Unverifiable | 湖面波纹／鱼跃／鸟群／植物细节由材质、粒子与关卡资产承载（③④），C++ 侧只有第 5 行的灯光雾与后处理；`CatWaterRegionPresentationActor.h:48-60` 只是水域几何预览（开发用可见性开关），不是这些反馈通道。维持 ❓ |
| 28 | ❌ Missing | ❌ Missing | 与第 17 行同一条唯一写口，三个调用者逐个打开确认没有天气、没有区域、也没有首达判定；独立重搜 `FirstArrival`/`Shelter`/`RainShelter` 全库零命中。同属 D-07 未给正式触发清单 |
| 29 | ⚠️ Partial | ⚠️ Partial | `CatChumFieldSubsystem.cpp:184-191` 按 RegionId 取 `FCatChumBudgetState` 并同时校验 `MaxActiveFieldsPerRegion` 与 `MaxRawContributionPerRegion`、`:211-212` 两种来源一视同仁地占额，属实；`ECatChumFieldSource` 只作来源标记、不改配额，属实。设计要的事件频率／共享 cd 那层在 D-25 里仍待裁、源码没有，⚠️ 的切法成立 |
| 30 | ✅ Implemented | ✅ Implemented | `CatfishingGameModeBase.cpp:1634-1638` 白天截止只置 `bNewFishingBitesAllowed=false` 并调 `RefreshBiteAvailabilityFromAuthority()`；`CatRunContracts.h:240-242` 注释逐字为「不是鱼竿操作门禁，夜晚仍可抛收竿和完成已有搏斗」；`CatEnvironmentSettings.cpp:15-21` 非 DayActive 返回 Unknown。裁决原文核到 设计修改记录.md:58「入夜边界：进夜不再有鱼咬钩，进行中的搏斗允许打完」，逐条对上 |
| 31 | ✅ Implemented | ✅ Implemented | 结论维持，**证据补强**：原 Code Reference 只给了预载链 `:679-691`，真正的一次性 ServerTravel 在 `CatOnlineSubsystem.cpp:1996`。复核反查全库 `OpenLevel` 零命中，`ServerTravel`／`ClientTravel` 只有四处（`:1996`、`:2015` 去 GameplayMap；`:2126`、`:2138` 回 Frontend），无区域间 travel。已补进 Code Reference 与 Notes |
| 32 | ❌ Missing | ❌ Missing | `CatEnvironmentSettings.h:49-63` 与 `CatRunContracts.h:249-274` 逐字段看过：事件配置与事件快照都没有区域维度，`FCatEnvironmentSnapshot` 不带 RegionId，事件无法「只在森林湖出」。与第 9 行（鱼种那半有区域门）对照后，「事件这一半整条不存在」成立 |
| 33 | ⚠️ Partial | ⚠️ Partial | `CatEnvironmentSettings.h:53-55` 的 `ActiveEventRequiredWeather` 确是天气条件接缝；`Config/DefaultGame.ini:49`（Clear）与 `:53-54`（RainBloom 要 Rain）确实互不匹配，三种天气目前一个专属事件都出不来；单事件位使 Clear 与 Fog 结构上无专属事件。⚠️ 成立 |
| 34 | 新增 | ⚠️ Partial | 补漏行：设计 :77「画面和声音跟着变」与 :185-186 的天气侧反馈通道在原表中无对应行（原第 5 行只覆盖昼夜画面、第 6 行只覆盖昼夜环境音、第 33 行只覆盖天气专属演出／事件）。可文本判定的负结论：`CatEnvironmentPresentationActor.cpp:497-525` 的 `BuildScenePresentationValues` 只按 `PresentationPhase` 与 `DayProgress` 分支，整个函数不读 `State.Weather`，原生画面链对雨／雾零反应；音频侧 `Source/**` 内 `AudioComponent`/`SoundBase`/`SoundCue`/`PlaySound`/`AmbientSound` 零命中。天气事实确已投影到表现层（`:359`、`:385-389`），剩余部分属③④ |

