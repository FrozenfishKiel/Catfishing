## 商店

### Features

| # | Design Requirement | Status | Code Reference | Notes |
|---|-------------------|--------|---------------|-------|
| 1 | 设计禁区：不做真钱入口（金币只在单局内流转）、不做消费审批流程、不设生人局限花钱开关（商店.md:43-46） | ✅ Implemented | `Source/Catfishing/ShopEconomy/CatShopEconomyService.h:207`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.h:74` | 三条都是否定要求，本轮逐条重查。全库无 IAP／充值／商城入口；玩家侧购买写口只有 `ServerSubmitShopCartAtKiosk`→`SubmitCartFromKiosk` 一条（`Framework/Game/CatfishingPlayerController.h:219-221`），链上没有审批、投票或房主批准分支；公款是 WorldSubsystem 的单一 `FCatShopWalletSnapshot Wallet`，`Save/CatRunSaveGame.h` 全文无 Wallet/Balance 字段，天然只在单局内流转 |
| 2 | 金币为全队共用的单一钱包，无个人分账，收入与支出对全队可见（商店.md:25、81） | ✅ Implemented | `Source/Catfishing/ShopEconomy/CatShopEconomyService.h:207`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:658-685`、`Source/Catfishing/Framework/Game/CatfishingGameState.h:104-105` | 服务里只有一份 `Wallet`，没有 per-player 余额字段。`BuildPublicSnapshot` 把余额、`WalletRevision`、货架库存与**本局全部**公开流水（含 `ActorPlayerState`、`WalletDelta`）不裁剪地复制进 `ShopEconomySnapshot`（`ReplicatedUsing=OnRep_ShopEconomySnapshot`），所有客户端都读得到。是否被界面显示是另一条（第 38、41 行） |
| 3 | 钓到的鱼有卖／吃／献三条互相竞争的处置出口（商店.md:23、50） | ✅ Implemented | `Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.cpp:113`、`Source/Catfishing/Items/CatFishConsumptionCoordinator.h:22`、`Source/Catfishing/Run/CatSacrificeCoordinator.cpp:42` | 三条出口的服务器写口都存在（`SubmitFishSaleFromPlayer`／`ConsumeReachableFish`／`RequestSacrifice`）。**更正基线的行号**：售鱼入口在 `.cpp:113` 而非 `RequestSacrifice` 引的 `:144`（后者落在 `PrepareForRunTeardown` 里）。卖这条当前因收鱼价未裁而 fail-closed（见第 20 行），献这条当前只在白天开（见第 5 行），运行期实际可用的取舍不足三路 |
| 4 | 任何玩家出售任何来源的鱼——自己的鱼、缸里的鱼和偷来的鱼——所得统一进入公款（商店.md:52、67） | ⚠️ Partial | `Source/Catfishing/Items/CatItemsService.cpp:317-318`、`Source/Catfishing/Items/CatItemsService.cpp:341`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:494` | 「所得进公款」✅：`ApplyFishSale` 只加同一份 `Wallet.Balance`，与卖家身份无关。缺的是来源覆盖面：`TryPrepareFishForSaleFromContainer` 的可售容器白名单只有 `FishGuard` 与 `SharedFishTank`，鱼护还额外要求 `Fish.OwnerStableNetId == SellerStableNetId`。偷来的鱼被 `BeginFishTheft` 从源容器整体移入 escrow（`Items/CatItemsService.h:148-163`，鱼从源数组移除、只留返还槽），既不在这两类容器里、`OwnerStableNetId` 也仍是受害者；`Social/CatSocialService.h:76、79` 的偷鱼协议只有「窗口到期吃掉」和「原位返还」两个出口，没有卖 |
| 5 | 白天不可献祭，夜晚由全队统一开启献祭（商店.md:67） | 🔄 Divergent | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1183`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1234` | 预演与提交两处 gate 逐字相同：`Phase != DayActive \|\| !bQuotaOpen` 即返回 `InvalidPhase`——只有白天能献、夜晚一律拒，与设计正好相反。设计已更新于 09-04（`裁决同步/设计修改记录.md:450-456`：「仪式由夜晚开场移到夜晚收尾，并与翻天合成一个动作」，连带二改写毕业收尾、:456 落地行记局与进程 v1.25 与商店 v1.5 同批；**复核更正基线出处**：原引 :452-456 只覆盖三条连带与落地行，漏掉 :450 这句正题），代码未跟进；`工程自补决策记录.md` D-01…D-32 全表无对应条目，09-09 复核节也未收录本条。该口径已被 `需求对齐差距清单.md:434`（09-09 新增行）登记为待确认问题 |
| 6 | 用公款购买新竿、鱼饵、鱼漂、道具（商店.md:52、71） | ⚠️ Partial | `Config/DefaultGame.ini:150`、`Config/DefaultGame.ini:56`、`Scripts/verify_equipment_shop_runtime.py:136-143`、`Source/Catfishing/ShopEconomy/CatShopInventoryComponent.cpp:110` | 目录机制对品类通用（任意 `DefinitionId` 都能上架），正式目录指向 `DT_ShopCatalog_Default`（资产存在：`Content/Catfishing/Data/Shop/DT_ShopCatalog_Default.uasset`）。脚本里的 `expected_rows` 是「必须包含」的子集断言（`missing_rows = expected_rows - row_names`），列出 2 竿＋8 饵＋4 窝料共 14 行，**不锁定行数**，所以据它断定「鱼漂／道具没有商店行」是过度解读——那属资产 ③，本轮不跑引擎读不到。立得住的是抄网：`Config/DefaultGame.ini:56` 的临时开关 `bAutoGrantStarterScoopNet=True` 仍在，`需求对齐差距清单.md:287` 明写它的删除条件正是「商店的抄网商品接通」；竿这一路 `Rod_ShopT2` 已以 240 售价接通（同文件 :279） |
| 7 | 鱼缸容量升级＝唯一设施类商品：不进团队装备库、直接作用在营地那口缸上、按档位买、随局清空；初始 10 条，两档 20／30，价 300／700 金币（商店.md:71） | ❌ Missing | `Config/DefaultGame.ini:139`、`Source/Catfishing/Items/CatItemsSettings.h:19`、`Source/Catfishing/Items/CatFishTankActor.cpp:50` | 缸容量是单一固定配置 `SharedFishTankCapacity=20`，由 `GetContainerCapacity` 在 `ACatFishTankActor::BeginPlay` 注册容器时一次读死，既没有档位也没有升级写口，连「初始 10」这个已拍值都对不上。目录项 `FCatShopCatalogEntry` 只有 `DefinitionId`＋`PurchaseQuantity` 一种交付形态（`Catalog/CatShopCatalogTypes.h:19-33`），`RunCartOrder` 的唯一收货方是营地公共仓库（`Trading/CatShopTradeController.cpp:19-31`）。搜过 `TankCapacity`／`CapacityTier`／`UpgradeTier`／`Facility`，Source 与 Config 里零命中。设计侧数值已定稿（`数值模拟与参数记录.md:52`、`设计修改记录.md:613、617`），属工程未建位 |
| 8 | 使用公款支付，商品明码标价且不可讲价（商店.md:71） | ✅ Implemented | `Source/Catfishing/ShopEconomy/Trading/CatShopTradingTypes.h:223-234`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:259`、`Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.cpp:8-17` | 客户端提交的 `FCatShopCartLineCommand` 只有 `EntryId`＋`CartCount`，价格提交不上来；`ResolveCatalogCartForAuthority`（`.cpp:150`）与 `PurchaseCatalogCart` 都从服务器当前货架目录重算小计与总价，注释明写「客户端传来的价格或数量倍率一律不用」。`UnitPrice` 默认哨兵 -1＝未填价，`IsRuntimeReady` 用 `UnitPrice < 0` 把漏价条目挡在运行目录外，0 价仍合法通过 |
| 9 | 每笔购买向全队广播，并记录购买者、商品、价格和购买时刻，供 Playtest 回看（商店.md:52、97） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:349`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradingTypes.h:57-131`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:147-157` | 事件与账本都在：首次提交时 `OnPublicTransactionCommitted.Broadcast` 发一次（重放和拒绝不发），GameMode 在任何玩家订单可达之前订阅并重建整份复制快照。购买者（`StableNetId`→`ActorPlayerState`）、商品（`EntryId`／`DefinitionId`）、价格（`WalletDelta`）齐全，但**没有购买时刻**——逐字段核过 `FCatShopTransactionRecord`（TransactionId/RequestId/StableNetId/两个 bool/交付三项/EntryId/ShopInventoryId/DefinitionId/PurchaseQuantity/FishInstanceId/WalletDelta/WalletRevision/StockRevision），无时间戳也无天序号；`Event=shop_cart_submitted` 日志（`Trading/CatShopTradeController.cpp:105-109`）只有 RequestId、行数和两个错误码，不含买家与金额。表现层缺失另见第 41 行 |
| 10 | 购买物进入团队装备库：竿和漂放入公共架，其他备装自取，消耗品从公库领取（商店.md:53、71） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.cpp:19-31`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.cpp:326`、`Source/Catfishing/Camp/CatCampInventoryActor.cpp:682` | 「先入库、玩家再自取」这条链已按 D-04 建成：扣款前用 `ValidateAddItemsFromAuthority` 预检整批容量，支付成立后 `AddItemsFromAuthority`（`.cpp:389`）统一入库，玩家用 `WithdrawToEquipmentFromAuthority` 取进随身 Equipment。但设计的「公共架（竿／漂）／公库（消耗品）」二分在代码里不存在：`ACatCampHubActor` 只有一个 `PublicInventory`（`Camp/CatCampHubActor.h:76`），`ResolvePublicInventoryForShopOrder` 也只返回它，所有品类进同一容器 |
| 11 | 消耗品单次携带上限以「钓鱼系统」2.7 为准（商店.md:71） | 🔄 Divergent | `Config/DefaultGame.ini:64`、`Source/Catfishing/Equipment/CatEquipmentSettings.h:45-49` | 现行口径是**鱼饵 8 份／窝料 5 份**（`数值模拟与参数记录.md:51`「携带上限」行，2026-08-19 由 5 改；`GDD 系统分册/道具/道具.md:85` 与 `钓鱼系统/钓鱼规则.md` 同值），而代码是单一的 `InventoryQuantityStackCapacity=5`——既对不上 8，结构上也表达不了「饵和窝料两个不同上限」。D-13（08-20）登记为暂定值，且设计改动（08-19）早于该条目，故记 D 编号而非「设计已更新」；`工程自补决策记录.md:70`（09-09 复核节）已把 D-13 拟改为「暂定，待改代码」并记了同一组依据。另：商店册引用的「钓鱼系统 2.7」指针本身也失效——现行〈钓鱼规则〉§2 只到 2.6（`钓鱼规则.md:105`） |
| 12 | 特殊饵与特殊道具每日限量进货（商店.md:73） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:60-73`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:609-625`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1060-1064` | 机制齐全并已接线：`bDailyRestock`＋`DailyRestockQuantity` 字段、`AdvanceShopDay` 只对标了每日进货的有限库存重置（同一天重复调用不补第二次），Run 进入 `DayActive` 递增天序号后按正式天号调用一次并重发快照。哪些条目真的标了每日进货、进货量多少写在 `DT_ShopCatalog_Default.uasset` 里，属可检查程度 ③，本轮不跑引擎读不到；`IsRuntimeReady` 会拦住「标了进货却没给数量」和「无限库存＋每日进货」两种错配，所以配置错误不会静默通过 |
| 13 | 装备每日刷新（货架每天换一轮）（商店.md:73、127） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:632`、`Source/Catfishing/ShopEconomy/CatShopInventoryComponent.cpp:77`、`Source/Catfishing/ShopEconomy/CatShopInventoryComponent.h:47` | 随机换货架的整条实现都在（`bAlwaysStocked`／`RefreshWeight`／`RandomEntryCount`／带种子的抽取／RequestId 幂等），但**没有任何调用方**：全库 grep `RefreshShopInventoryFromCatalog` 只有服务层自己的 `.h` 声明和它转调组件层这两处，`BeginPlay` 只调 `RebuildInitialInventoryFromCatalog` 建一次开局货架。两处都不是 `UFUNCTION`（该组件全文 3 个 UFUNCTION 均为 BlueprintPure 查询），蓝图也调不到，可排除「刷新时机藏在 BP 里」。跨天只补货不换架，货架整局不变 |
| 14 | 基础竿和基础补给永不缺货；1 级竿可免费自取，普通饵提供免费保底（商店.md:73） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:36-50`、`Source/Catfishing/UI/Shop/CatShopModel.cpp:359-366` | 表达手段齐备：`bUnlimitedStock` 表达永不缺货，`UnitPrice=0` 是显式免费（注释点名「免费普通饵就靠它」），UI 把 0 价渲染成「免费」、无限库存渲染成「库存：不限」。哪几行真配成 0 价／无限写在 DataTable 资产里（③），本轮读不到。设计侧的「营地免费自取」（`道具.md:85`：普通饵营地免费自取、保底件之一；`设计修改记录.md:76`：保底三件树枝竿＋羽毛漂＋普通饵）在代码里没有独立写口——搜过 `免费`／`Free`／`Grant`，全库唯一的无偿发放是临时抄网开关 `GrantStarterScoopNetIfConfigured`（`Equipment/CatEquipmentComponent.cpp:836`）与默认关闭的 `bAutoConfigureStarterLoadout`，正式路径只能走商店 0 价购买 |
| 15 | 鱼竿断裂后该鱼竿直接消失，系统不自动返还、不自动替换、不回退为 1 级竿（商店.md:75；DeiSFL.csv 第7行 首轮通过条件） | ⚠️ Partial | `Source/Catfishing/Equipment/CatEquipmentComponent.cpp:1607-1614`、`Source/Catfishing/Save/CatRunSaveGame.h:88-89`、`Source/Catfishing/UI/Inventory/CatInventoryModel.cpp:159` | 「不返还、不替换、不回退」✅——商店无退款写口（`RunCartOrder` 注释明写「扣钱不可逆而商店根本没有退款写口」），断竿不触发任何补发；`Equipment/Tests/CatRodReplacementTests.cpp:95-145` 那条链是玩家自己去公共仓库取 `ShopRodT2` 换竿，不是系统自动替换。「直接消失」没有：耐久归零只把实例置 `RodItem->bRodBroken = true` 并留在库存格，存档 DTO 保留 `bRodBroken`，库存 UI 还专门给它渲染后缀「，已断」——废竿被设计成继续挂在背包里。D-21（09-04 用户确认）只裁到「耐久归零仍为 RodBroken 并同步真实损坏」，未裁实例是否移除 |
| 16 | 赠送与丢弃统一为「扔下」行为（商店.md:77） | ❌ Missing | — | 搜过 `DropToWorld`／`DropItem`／`ThrowItem`／中文「扔下」「赠送」，全 Source 零命中；`Discard`／`Abandon` 命中的中文只有「丢弃事件负载／丢弃草稿／丢弃旧视图」这类无关语义。装备与道具没有落地写口，世界里唯一能落地的是鱼（`Items/World/CatFishPickupActor` 的嘴叼释放）。`需求对齐差距清单.md:271` 也自陈「当前未新增拾取／扔出玩家操作」 |
| 17 | 水面设空气墙，物品不能丢入湖中（商店.md:77） | ❓ Unverifiable | — | 阻挡体属关卡资产（`.umap` 里的 BlockingVolume／碰撞设置），C++／Config／Scripts 里没有也不该有对应文本，属可检查程度 ③④，本轮不跑引擎，不得据文本判缺失。注：即使有空气墙，第 16 行没有丢弃动作，这条当前无从触发 |
| 18 | 落地后至次日清晨仍无人拾取的物品，自动回收到营地（商店.md:77） | ❌ Missing | — | 搜过 `Reclaim`／`DayRollover`／中文「自动回收」「回收」，无任何掉落物回收或翻天清理逻辑（唯一中文命中是 HUD 的「钓鱼状态：自动回收」文案，语义无关）；前置的「扔下」本身也不存在（第 16 行）。另需注意本条设计已过期：`设计修改记录.md:477`（09-05 李前臻拍）把「次日清晨回营地」改为**翻天时消失**，道具册 v1.30 已落地，商店册 §3.1.2 此句是残留出处。按最新裁决评分同样是未实现 |
| 19 | 除结算夜收摊后，商人猫全天营业持续收鱼（含夜晚）（商店.md:63、87） | ✅ Implemented | `Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.cpp:126`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:768-772` | 商店链路用的是宽 gate `CanAcceptGameplayCommand`（只看 authority＋`bRunCommandsOpen`＋Controller 在 Active 身份记录里），不是 `CanAcceptFishingCommand` 那条追加了 `Phase == DayActive` 的白天 gate；`NormalNight` 相位入口把 `bRunCommandsOpen` 置真，所以夜晚照常可买可卖。只有 `CloseCommands` 会关（第 27 行） |
| 20 | 收购价以鱼体重为主要锚点（商店.md:63、128） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomySettings.h:29-30`、`Source/Catfishing/ShopEconomy/CatShopEconomySettings.h:57-65`、`Config/DefaultGame.ini:153` | 体重档位机制实现完整（`TryEvaluateFishPurchasePrice` 整表校验：非空、每档合法、重量严格递增、价格不递减；不外推、不插值、不给兜底价），但 `FishPurchasePricePolicy=Unset` 且 `FishPurchasePriceAnchors` 在 ini 里一行都没有，`bFishPurchasePriceDecided` 恒 false，`TryAppraiseFishSale` 恒返回 false，整条售鱼链 fail-closed；`Scripts/verify_equipment_shop_runtime.py:261-263` 还把「保持 Unset」写成运行期断言。档位**阈值**其实已拍（`数值模拟与参数记录.md:54`，09-06 重拍：小 w<1／中 1≤w<5／大 5≤w<15／巨影 w≥15），代码的档位表结构正好能表达它；缺的只是每档价格——参数页拍定表 43-61 行逐行核过，至今没有「收购价」这一行 |
| 21 | 窝料轴可作为收购价修正系数（商店.md:63） | ❌ Missing | `Source/Catfishing/ShopEconomy/CatShopEconomyService.h:90` | 估价签名 `TryAppraiseFishSale(double WeightKilograms, int32& OutSaleValue)` 只有重量一个入参；`FCatShopFishSaleCommand`（`Trading/CatShopTradingTypes.h:298-325`）也只带重量、鱼实例与 Items 提交证据，没有窝料或来源维度的修正位。搜过 `PriceModifier`／`Chum` 与售鱼命令载荷，无第二条估价路径。设计原文写作「可在数值阶段作为修正系数」，属可选项而非硬要求，不构成当前欠账 |
| 22 | 合力捞起的巨物形成全队可感知的大额进账（商店.md:63） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:206-221`、`Source/Catfishing/Data/CatFishDefinition.h:12-15` | 档位表能表达「越重越贵」，理论上可把巨影档配成大额；但没有任何「合力／巨物」专门加成，且档位表为空（第 20 行），当前产不出任何进账。鱼侧存在的 `ECatFishBodyClass`（Unknown／Standard／Giant）是**协作档位**枚举——注释明写「只表达协作档位，不携带任何力量、体力或几何公式」，而且它根本不进估价入参，估价只按连续重量取档。设计侧的四档（小／中／大／巨影）在 `数值模拟与参数记录.md:50、54` 已与供奉、游速共用同一套阈值。「播报」那一半归第 41 行：数据已复制给全队，缺的是表现层 |
| 23 | 收购价、吃鱼成长收益与献祭分量使用一套可对照的等价换算表（商店.md:65、125） | ❌ Missing | `Source/Catfishing/Data/CatFishDefinition.h:78`、`Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:206-221` | 搜过 `ExchangeRate`／`Equivalent`／中文「等价换算」：三条链在代码里完全独立——收购价走 `FCatShopFishWeightPrice` 档位表（按实际重量），献祭走 `CatFishDefinition::SacrificeContribution` 这个**按鱼种写死的整数**（`Run/CatSacrificeCoordinator.cpp:82、87` 直接取它做额度贡献），吃鱼走 `CatFishConsumptionCoordinator`＋FishDefinition 效果，没有共享标尺、换算函数或对照配置。设计侧其实已给了标尺（`数值模拟与参数记录.md:50`「供奉值挂体重档，与经验／抄网／收购价同阈值」、`:54` 给阈值），所以这条是工程未建位，不是产品未给表；顺带暴露一处待查：按鱼种写死的 `SacrificeContribution` 与「按每条鱼实际重量分档」对不上，该口径归局与进程册 |
| 24 | 每局设启动金，具体金额在数值阶段确定（商店.md:83、129） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomySettings.h:36-43`、`Config/DefaultGame.ini:151` | 字段与「未裁哨兵」机制设计得当：默认 -1 表示没人拍过，此时 `IsRuntimeEnabled` 为假、整个 ShopEconomy fail-closed，`StartingTeamWalletBalance=0` 才是「裁定白手起家」。但当前配置是 `999999`，是开发期放行值而不是产品裁定；D-14（08-20）登记的暂定起始公款是 **10**，改成 999999 未回写台账。`工程自补决策记录.md:71`（09-09 复核节）已把这处漂移登记为 D-14 待复核，并确认参数页至今没有「启动金」行 |
| 25 | 金币跟局走，局末通过结算流程归零（商店.md:81） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:23-40`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:709-727`、`Source/Catfishing/Save/CatRunSaveGame.h:213-263` | 跨局不带钱 ✅：公款是 WorldSubsystem 状态，`Deinitialize` 关命令门并清账本与幂等缓存，新 World 的 `Initialize`→`LoadRuntimeEconomyFromSettings` 重新从 Settings 冻结余额。但没有「结算流程归零」这个显式写口——`CloseCommands` 注释明写「不清公款、库存、账本」，靠 World teardown 兜底；另外逐字段核过 `UCatRunSaveGame`：存了 DayIndex、献祭进度、Players（含装备）与 CampInventory，**唯独没有公款**，局内存档读档后余额会回落到启动金 |
| 26 | 商人猫是世界里第一个 NPC，猫猫在他这里进行买卖（商店.md:87） | ❓ Unverifiable | `Source/Catfishing/ShopEconomy/CatShopKioskActor.h:15` | 交易角色由 `ACatShopKioskActor`（`SceneRoot`＋`InteractionCollision`＋交互组件＋货架组件）承担，C++ 里没有任何猫形 NPC 类、AI 或对话（搜过 `AIController`／`BehaviorTree`／`NPC`／`商人猫`，零命中）。摊位是不是被做成一只猫，写在 `Content/UI/Shop/BP_CatShopKiosk.uasset` 与其网格资产里（③④），本轮不跑引擎读不了；同目录 `WBP_CatShop*` 四件与字体资产说明表现层已有承载 |
| 27 | 商人猫收摊＝买卖冻结：购物车支付、售鱼入账、交付确认与每日进货全部停止受理新命令（商店.md:54、87） | ✅ Implemented | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:690-693`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:2552-2559`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1108`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1115` | 四个写口＋`AdvanceShopDay`（`.cpp:611` 首行即查 `bCommandsOpen`）统一受同一 gate 管；关闭后查询照读、既有 RequestId 重放仍返回首次终态，语义正确。两种结算夜相位切换与 World teardown 的 `Deinitialize` 都调用 `CloseShopForSettlementNight`／`CloseCommands`，重复调用无第二次副作用 |
| 28 | 成功局＝把世界进度顶到 100% 的那次上供当场转成结算夜（不翻天），商人猫在此收摊（商店.md:54） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1110-1116`、`Source/Catfishing/Run/CatRunSettings.h:48` | `SuccessSettlementNight` 相位入口直接 `CloseShopForSettlementNight()`，且该 case 不递增 `DayIndex`（只有 `DayActive` 那支 `++RunPublicState.Phase.DayIndex`），即不翻天。触发点判定（是否由 100% 那次上供触发，还是 `CanEnterSuccessSettlementNight` 的 FinalDay 口径）归局与进程册并已在 `需求对齐差距清单.md:433` 挂号，本册只核对「商人猫在此收摊」这一半 |
| 29 | 收摊后把剩余公款／装备换成小鱼干，给猫猫们在篝火旁娱乐（商店.md:54、87） | ❌ Missing | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:690-693` | 搜过 `小鱼干`／`DriedFish`／`Snack`／`Treat`，全 Source 零命中；篝火侧只有 `ACatCampHubActor::RequestCampfirePlayback` 那条表演链，与公款、库存无任何连接。`CloseCommands` 只把命令 gate 置否，注释明写「不清公款、库存、账本」，没有余额兑换、装备折算或篝火发放逻辑 |
| 30 | 失败局不进入结算夜，直接执行失败结算（商店.md:55） | 🔄 Divergent | `Source/Catfishing/Framework/Core/CatRunContracts.h:18`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1103-1109` | 代码里有独立的 `ECatRunPhase::FailureSettlementNight` 相位（注释：「截止时额度未达成后的失败结算夜；无计时，必须等待 SettlementComplete」），进入时同样走 `CloseShopForSettlementNight()`。设计已更新于 09-07（`设计修改记录.md:429`：「失败改为进度归零，且不进结算夜，采商店册第 47 行的口径」，局与进程 §3.1.3 与营地册两处已改），代码未跟进；`工程自补决策记录.md` 无对应 D 条目，同一根因已由 `需求对齐差距清单.md:433`（09-09 新增行）登记为最优先待确认问题 |
| 31 | 失败局无论剩余多少，本局金币与装备全部清零；已解锁的图鉴保持不变（商店.md:55） | ⚠️ Partial | `Source/Catfishing/Collection/CatRunImprintService.cpp:30-50`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:23-40`、`Source/Catfishing/Save/CatRunSaveGame.h:255-259` | 图鉴不受影响 ✅：`FishRecorded` Grant 在捕获提交时就建立并投递给 durable Profile，与售／吃／献无关。金币与装备的「清零」没有任何显式写口——失败结算相位只关商店命令门，余额与营地仓库靠 World teardown 一起消失；而 `UCatRunSaveGame` 会把 `Players`（含 `EquipmentSnapshot`）和 `CampInventory` 落盘，存档路径下「失败即清零」不成立 |
| 32 | 金币只买功能性补给（竿、饵、漂、道具），表达类内容（外观、庆祝动作、图鉴皮肤）走解锁不进商店（商店.md:91） | ✅ Implemented | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:19-21`、`Source/Catfishing/Framework/Core/CatProfileContracts.h:8-19` | 目录项的 `DefinitionId` 只交付给营地公共仓库的装备／消耗品库存，整个 `FCatShopCatalogEntry` 没有外观、动作或皮肤类交付通道（其余字段全是价格、库存、刷新与展示覆盖）；表达类内容走 Profile Grant 的四种 `ECatProfileGrantKind`（FishRecorded／FishSilhouette／Imprint／Unlock），与商店交付链完全分离 |
| 33 | 解锁＝商店上新货：解锁的品类进入货架，具体件仍需使用公款购买（商店.md:91） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:56-58`、`Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.cpp:8-17` | 「解锁后仍要花钱买」这一半 ✅（解锁不发实物，只影响上架）。但 `RequiredShopUnlockId` 目前是**只挡不放行**的字段：字段注释与 `IsRuntimeReady` 的 `!RequiredShopUnlockId.IsNone()` 分支都写明「当前没有商店解锁事实源，非空即 fail-closed」，即配了解锁条件的商品永远上不了架，等于这条链没接通 |
| 34 | 解锁清单跟玩家走，使下一局初始货架包含已解锁品类；具体装备和公款只跟当前局走（商店.md:93） | ⚠️ Partial | `Source/Catfishing/Profile/CatProfileSubsystem.h:46`、`Source/Catfishing/Framework/Game/CatfishingPlayerController.h:77-78`、`Source/Catfishing/Framework/Game/CatfishingPlayerState.cpp:77-98`、`Source/Catfishing/ShopEconomy/CatShopInventoryComponent.cpp:110-126` | 「解锁跟人走」✅（durable Profile 的 `GetEquipmentUnlockSnapshot`，D-04 已让 owning client 经 `ServerPublishEquipmentUnlocks` 上报、由 `ACatfishingPlayerState::SetAuthorizedEquipmentUnlocksFromAuthority` 校验后复制 `AuthorizedEquipmentUnlockIds`；**复核更正基线 code_ref**：原引的 `CatfishingPlayerController.h:219-226` 是 `ServerSubmitShopCartAtKiosk`／`ServerSellFish` 两个商店 RPC，与解锁上报无关）、「装备与公款跟局走」✅（第 25、31 行）。缺的是把两者接起来的那一步：`RebuildInitialInventoryFromCatalog` 只用 `FMath::Rand()` 种子从本摊位 DataTable 生成一次运行目录，不读任何玩家解锁快照，开局货架与已解锁品类无关 |
| 35 | 钓到瞬间即计入图鉴，记录不受后续出售、吃掉或献祭影响（商店.md:107-108） | ✅ Implemented | `Source/Catfishing/Collection/CatRunImprintService.cpp:30-50`、`Source/Catfishing/Items/World/CatFishPickupActor.cpp:705` | 捕获提交时按 `CaptureRequestId` 一次性建立不可变 `FishRecorded` Grant（同 RequestId 重放直接返回既有 GrantId）并投递给 Profile；售鱼、吃鱼、献祭三条链都不回写它。登记时点与 09-08「上钩揭剪影→上岸登记收集层」分层裁决一致（`设计修改记录.md:523`） |
| 36 | 偷卖后的追逐、追回与抓捕规则归联机社交，商店只负责收购端（商店.md:67、109） | ✅ Implemented | `Source/Catfishing/Social/CatSocialService.h:27-49`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.h:64-85` | 职责边界成立：偷鱼协议、追回窗口、抓捕与被抓印记全在 `UCatSocialService`，商店侧三个公开入口（整车、售鱼、售鱼命令）一行都不碰 Social。注意这条边界成立不代表第 4 行成立——偷来的鱼当前根本卖不掉 |
| 37 | 确定商店位置，并可验证钓点—鱼缸—商店之间的追逐距离（商店.md:99；GaHeFr.csv 第5行 待对齐内容列） | ❓ Unverifiable | `Source/Catfishing/ShopEconomy/CatShopKioskActor.h:69-70` | 摊位在关卡里摆在哪、离钓点和鱼缸多远，是 `.umap` 里的 Actor 摆放（③），本轮不跑引擎读不到；`Scripts/verify_equipment_shop_runtime.py` 只校验 PlayerStart／WaterRegion／资产，未断言摊位摆位。代码侧只有 `InteractionRadiusCentimeters=300` 这个交互半径，不表达世界布局 |
| 38 | 公款余额对全队常时可见（商店.md:100、133；GaHeFr.csv 第9行 待对齐内容列） | ⚠️ Partial | `Source/Catfishing/UI/Shop/CatShopModel.cpp:253-255`、`Source/Catfishing/Framework/Game/CatfishingGameState.h:52`、`Source/Catfishing/UI/HUD/` | 「对全队可见」这一半在正式链上成立：余额随 `ShopEconomySnapshot` 复制给每个客户端，商店页 Model 把它渲染成 `WalletText`＝「商店：团队公款 %d」（`bEconomyAvailable` 为假时退化成「公款数据未同步」），任何玩家打开摊位都读得到同一份公款。缺的是「常时」：`Source/Catfishing/UI/HUD/` 只有 `CatHUDModel`／`CatHUDWidget` 两组文件，全目录 grep `Wallet`／`Coin`／`Balance`／「公款」零命中；`GetShopEconomySnapshot()` 不是 UFUNCTION，`UCatShopModel` 全类零 UFUNCTION，HUD 蓝图拿不到余额。蓝图并非完全无路——`UCatShopWidget::GetLastShopViewState()` 是 `UFUNCTION(BlueprintPure)` 且 `FCatShopViewState::Economy` 为 BlueprintReadOnly（`UI/Shop/CatShopTypes.h:184`）——但那个 Widget 只在商店页打开时存在，正是「常时可见」要排除的场景。**复核改判**：基线判 ❌，但展示写口确实存在于正式链上且带 file:line，按本报告在第 15／31／39 行采用的同一尺度（复合要求成立一半即 ⚠️），此行应为部分实现而非未实现 |
| 39 | 缺货、每日刷新、免费保底与入库位置均需明确反馈（商店.md:100、135） | ⚠️ Partial | `Source/Catfishing/UI/Shop/CatShopModel.cpp:359-366`、`Source/Catfishing/UI/Shop/CatShopModel.cpp:113`、`Source/Catfishing/UI/Shop/CatShopTypes.h:184` | 四项里三项有：缺货（「库存：已售罄」并置 `bActionEnabled=false`）、免费保底（0 价渲染成「免费」）、入库位置（支付成功提示「支付成功，请在营地公共仓库查看新物品」）。每日刷新在 C++ 侧零反馈——全库 grep `ShopDayIndex`，除 `ShopEconomy/` 自身外零命中，UI 一个字都没用它（何况第 13 行本来就没换架）。留一个 ④ 口子：`ShopDayIndex` 位于 BlueprintReadOnly 的 `Economy` 里，商店页 WBP 理论上能自行显示天序号，本轮不跑引擎读不到 |
| 40 | 商人猫收摊需同时通过动作、音效与商店不可交互状态传达（商店.md:136） | ❓ Unverifiable | `Source/Catfishing/ShopEconomy/CatShopKioskActor.h:64-66`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:690-693` | `bInteractionEnabled` 是 `EditAnywhere, BlueprintReadWrite`，注释明写「蓝图写入后会同时影响提示、打开页面和服务端下单距离证明」（三处消费点已核：`CanInteract_Implementation`、`GetInteractionRadius_Implementation`、`CanServeOrderFromAuthority`），而 Run 相位（两种结算夜）本来就复制到客户端，BP 完全可以据此把摊位置为不可交互。动作与音效本就是 ④。C++ 侧能证的只有：`CloseCommands` 只关服务器写口、不碰摊位，全库无该字段的运行时写入点，公开经济快照里也没有「已收摊」位——手段具备、C++ 未接，按硬要求不能判「未实现」，留给引擎入口核对 `BP_CatShopKiosk` |
| 41 | 购买广播作为全场事件，至少显示购买者、商品和支出（商店.md:134） | ❌ Missing | `Source/Catfishing/Framework/Game/CatfishingGameState.h:52`、`Source/Catfishing/UI/Shop/CatShopModel.cpp:227`、`Source/Catfishing/UI/Shop/CatShopWidget.h:264-266` | 数据已复制到每个客户端（第 9 行），但 C++ 侧唯一消费者是 `UCatShopModel`，且只用余额与库存——全 `UI/` 目录 grep `Transactions` 零命中，HUD 与聊天侧无任何 toast 或流水。「商店页里有没有一条流水列表」属 ④（`GetLastShopViewState` 是 BlueprintPure、`Economy.Transactions` 蓝图可读）。能证伪的是「**全场**」那半：商店页只对打开它的那个玩家存在，而 HUD／GameState 路径上 `GetShopEconomySnapshot()` 非 UFUNCTION、`UCatShopModel` 无 UFUNCTION，全场事件在 C++ 侧确实无从产生 |
| 42 | Playtest 可采集的经济指标：每局购买广播次数、每日余额、局末金币与装备剩余率、偷卖次数（DeiSFL.csv 第3行、第5行、第6行 记录指标列） | ⚠️ Partial | `Source/Catfishing/ShopEconomy/CatShopEconomyService.h:210`、`Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:141-144` | `TransactionLedger` 逐笔留了操作者、商品、金额、公款版本和交付状态，`GetTransactionLedgerSnapshot` 可整本读出，理论上能数出购买次数和局末余额；但记录没有时间戳也没有天序号（第 9 行逐字段核过），「每日余额」这类按天切片的指标算不出来，也没有任何导出、统计或落盘入口，账本随 `Deinitialize` 一起清空 |
| 43 | 交易过程需要具备明确的猫味演出（商店.md:113） | ❓ Unverifiable | — | 设计原文标为软性边界、「具体动作、台词与视觉资产由内容／美术初版补齐」。演出是动画蒙太奇／音效／WBP 资产（③④），C++ 侧只有 `UCatShopInteractionComponent` 的开关页面逻辑，本轮不跑引擎无法判定有无 |
| 44 | 设计禁区：不让货币放大数值军备——购买提供选择与花样，不形成不可追赶的碾压（商店.md:44） | ❓ Unverifiable | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:9-93` | 目录结构本身不带属性词条、随机强化或叠加位（只有 DefinitionId＋数量＋价格＋库存＋刷新＋展示字段），商店不会自己制造军备；但「买到的竿会不会形成碾压」取决于装备资产的强度／耐久数值与鱼价曲线（③ 资产＋数值阶段未拍，且 D-20／D-21 的巨影门槛口径本身仍在待裁，见 `工程自补决策记录.md:74-75`），C++ 侧既无也不该有军备上限机制。本轮不跑引擎、参数页也未给收购价，无法判定 |
| 45 | 其余三项 Playtest 记录指标：卖／吃／献的鱼数与价值占比、高级竿连续占用时长与重复争抢次数、断竿后重新出发时间与免费 1 级竿使用率（DeiSFL.csv 第2行、第4行、第7行 记录指标列） | ❌ Missing | — | 复核补漏行：基线只把第 3、5、6 行的经济指标做成第 42 行，这三行「记录指标」列一条没进表，而它们正对着本册北极星（三路取舍）与 §6 两个锚定数字。全库无遥测／统计设施——grep `Telemetry`／`Analytics`／`Metric` 在 `Source/Catfishing/` 零命中（只命中 `IsometricEffortMultiplier` 与测试断言文案这类无关词）。逐路核过：卖能从 `TransactionLedger` 的 `bFishSale` 数出条数（`CatShopEconomyService.h:210`），吃这一路 `UCatFishConsumptionCoordinator` 全类无计数器、只有一条失败日志（`Items/CatFishConsumptionCoordinator.cpp:63`），献这一路只有 Run 侧聚合进度、无逐条鱼流水；「价值占比」还要先有第 23 行那张等价换算表。装备占用时长、争抢次数、重新出发时间三项都是时序量，而账本无时间戳（第 9 行），无处落脚 |

### Summary
- Total features: 45
- ✅ Implemented: 10
- ⚠️ Partial: 18
- ❌ Missing: 8
- 🔄 Divergent: 3
- ❓ Unverifiable: 6
- Inspectable: (45 - 6) / 45 * 100% = 86.7%
- Coverage of inspected: (10 + 0.5 * 18) / (45 - 6) * 100% = 48.7%

### Scan Scope

**项目声明（由主流程给定，未自行探测）**：engine=unreal 5.8；project_root=`D:/LocalGameProject/Unreal/Catfishing`；tech_stack=C++/Blueprint（模块 Catfishing、CatfishingEditor）。代码以工作区当前文件为准（分支 Develop-Debug，HEAD 87ba975；各系统 code 路径相对基线提交 674f4bd 的 git diff 为空，工作区无源码改动）。

**设计侧输入**
- `Knowledge/Feishu/GDD 系统分册/商店.md`（revision_id 347，fetched_at 2026-09-09T12:29:12）全篇＋两张嵌入 CSV：`商店.embedded/DeiSFL.csv`（7 行，逐行读）、`商店.embedded/GaHeFr.csv`（9 行，逐行读）。本次导出与基线（09-09 10:47）比对，商店册除 `fetched_at` 外零差异，故要求措辞与顺序沿用基线，状态与 code_ref 全部重新独立核对
- 参考不评分：`Knowledge/Feishu/裁决同步/裁决同步 · 道具与商店（王甜甜）.md`（revision 86）——只有窝料表待办与两批已代做，无商店侧现行需求
- 裁决账本：`Knowledge/Feishu/裁决同步/设计修改记录.md` 按日期核对与商店相关的 09-04（结算夜触发点 :452-456、鱼缸开口子 :442）、09-05（掉地物品改翻天消失 :477）、09-07（失败不进结算夜 :429、体重档消费链 :503-515）、09-08（鱼缸 10/20/30 :613、晚间九条含升级价 300/700 :617）
- 数值专表优先：`Knowledge/Feishu/数值模拟与参数记录.md` 拍定表 43-61 行逐行读，确认无「收购价」行、无「启动金」行；体重档阈值 :54、携带上限 :51、鱼缸容量 :52 已定稿
- 跨册指针核对：`GDD 系统分册/道具/道具.md:85`（鱼饵 8 份、普通饵营地免费自取）、`GDD 系统分册/钓鱼系统/钓鱼规则.md`（§2 只到 2.6，商店册引用的 2.7 已失效）

**扫过的目录与文件类型**
- `Source/Catfishing/ShopEconomy/`（含 `Catalog/`、`Trading/`）——全部 8 组 `.h`/`.cpp` 通读或逐段核对
- `Source/Catfishing/UI/Shop/`——`CatShopTypes.h`、`CatShopModel.cpp/h`、`CatShopWidget.h`、`CatShopInteractionComponent`、`CatShopPageController` 相关段落
- `Source/Catfishing/Camp/`——`CatCampHubActor.h/.cpp`、`CatCampInventoryActor.h/.cpp` 的入库/取用/解析段落
- 顺链扩展（初始线索之外）：`Source/Catfishing/Items/`（`CatItemsService.cpp` 售鱼容器白名单与 theft escrow、`CatItemsSettings`、`CatFishTankActor`、`CatFishPickupActor`）、`Source/Catfishing/Social/CatSocialService.h/.cpp`（偷鱼协议出口）、`Source/Catfishing/Framework/Game/`（`CatfishingGameModeBase.cpp` 相位切换／收摊／献祭 gate／快照发布、`CatfishingGameState.h`、`CatfishingPlayerController.h` 商店 RPC）、`Source/Catfishing/Framework/Core/`（`CatRunContracts.h`、`CatProfileContracts.h`）、`Source/Catfishing/Equipment/`（断竿写回、Starter 兜底）、`Source/Catfishing/Run/`（`CatRunSettings.h`、`CatSacrificeCoordinator`）、`Source/Catfishing/Save/CatRunSaveGame.h`、`Source/Catfishing/Profile/`、`Source/Catfishing/Collection/`、`Source/Catfishing/UI/HUD/`、`Source/Catfishing/UI/Inventory/`
- `Config/DefaultGame.ini`——`CatShopEconomySettings`(149-153)、`CatItemsSettings`(139-140)、`CatEquipmentSettings`(52-64) 三段
- `Scripts/verify_equipment_shop.ps1`、`Scripts/verify_equipment_shop_runtime.py`——只作**静态文本**读，未执行；其中的断言（商店 DataTable 行名子集、`PricePolicy=Unset`、临时抄网发放开关）用作资产内容的间接证据

**检查方法**
- Grep 定位后 Read 片段，未通读大文件。用过的检索词：类名 `UCatShopEconomyService`/`UCatShopTradeController`/`ACatShopKioskActor`/`ACatCampInventoryActor`/`UCatSocialService`；字段名 `StartingTeamWalletBalance`/`FishPurchasePricePolicy`/`FishPurchasePriceAnchors`/`bDailyRestock`/`RequiredShopUnlockId`/`bAlwaysStocked`/`bRodBroken`/`SharedFishTankCapacity`/`InventoryQuantityStackCapacity`/`ShopDayIndex`/`Transactions`；函数名 `RefreshShopInventoryFromCatalog`/`AdvanceShopDay`/`TryAppraiseFishSale`/`CanServeOrderFromAuthority`；日志事件名 `shop_cart_submitted`；中文注释「收摊」「公款」「免费」「设施」「自动回收」；否定式搜索 `DropToWorld`/`DropItem`/`ThrowItem`/「扔下」/「赠送」、`Reclaim`/`DayRollover`/「回收」、`小鱼干`/`DriedFish`/`Snack`/`Treat`、`ExchangeRate`/`Equivalent`/「等价换算」、`TankCapacity`/`CapacityTier`/`UpgradeTier`/`Facility`
- 台账对照：`Docs/Development/工程自补决策记录.md`（先读「怎么读」的状态定义，D-01…D-32 全表＋09-09 新增「设计对表复核」17 条通读；与商店相关的是 D-04、D-13、D-14、D-20/D-21、D-23）、`Docs/Development/需求对齐差距清单.md`（Equipment/Shop 模块段 :269-294 与 §4 全表，含 09-09 追加的八行）
- ✅ 行一律给 Source/Config 的 file:line；`需求对齐差距清单.md` 的「已完成」记录只作线索

**跳过与未做的事**
- `Content/` 下 `.uasset`/`.umap` 未作文本扫描，只用目录列表确认文件存在。因此 `DT_ShopCatalog_Default.uasset` 的逐行价格／库存／每日进货／刷新权重配置、`BP_CatShopKiosk` 与四件 `WBP_CatShop*` 的图逻辑、关卡里的摊位摆位与水面阻挡体**均未读取**——第 17、26、37、40、43、44 行判为「查不了」而非「未实现」，第 6、12、14 行的资产侧内容也在 Notes 里单独标出
- 未跑引擎、未编译、未执行 `Scripts/verify_equipment_shop*`（该脚本的 `Automation` 模式本身也已被主动阻断，见 `verify_equipment_shop.ps1:148`）
- `Source/**/Tests/` 下无任何引用 `UCatShopEconomyService` 或 `UCatShopTradeController` 的测试文件（三处命中只是把 `ShopRodT2` 当装备定义 ID 用），因此本轮没有可引用的自动化旁证，也没有「只在测试里成立」需要降为 ⚠️ 的条目
- 物品本体（清单、属性、解锁表达）归道具册、偷鱼追逐归联机社交册、结算夜触发点与世界进度轴归局与进程册，本报告只判交易、经济与商店侧反馈

**Phase 2b 独立复核补扫（在上述范围之外新增的检索）**

- 否定式再搜（换词重试，全部零命中）：`Gift`／`Toss`／`DropEquipment`／`ReleaseItem`／`SpawnPickup`／「抛出」「放下」「扔」（第 16 行）、`Despawn`／`ExpireDrop`／`DroppedItem`／`GroundItem`（第 18 行）、`Redeem`／`Exchange`／「兑换」「折算」（第 29 行）、`Telemetry`／`Analytics`／`Metric`（第 42、45 行）、`AIController`／`BehaviorTree`／`NPC`／`Merchant`／`Vendor`（第 26 行，仅命中两处中文注释「商人猫」）
- 接线再核：`bInteractionEnabled` 全库四处宿主逐个打开，确认商店摊位那份只被读、无任何 C++ 写入点（第 40 行）；`RefreshShopInventoryFromCatalog`／`RebuildInitialInventoryFromCatalog`／`AdvanceShopDay` 三个函数的全部命中逐条打开确认调用方（第 12、13 行）
- 出处复核：`数值模拟与参数记录.md` 拍定表按行重数（43=白天时长 … 51=携带上限、52=每日任务／世界进度、54=体重档阈值 …），确认第 7 行引的 :52 是该行「依据」列里的鱼缸容量占位（初始 10、两档 20／30、价 300／700），不是独立的鱼缸行；`裁决同步/设计修改记录.md` 的 :429、:442、:450-456、:477、:613、:617 六处逐段读；`工程自补决策记录.md` D-04／D-13／D-14／D-21 原始行与 09-09 复核节 :70、:71、:75 逐条比对；`需求对齐差距清单.md` :287（临时抄网删除条件）、:433、:434 三行核到实际行号
- 未能完成的检查（与基线相同）：未跑引擎，`DT_ShopCatalog_Default.uasset`、`BP_CatShopKiosk`、四件 `WBP_CatShop*` 与关卡摆位仍不可读，因此第 12、14 行的资产侧内容与第 17、26、37、40、43、44 行维持 ❓／带 ③④ 保留

### Code-only mechanics

| 机制 | code_ref | 文档现状与出处 | 待确认问题 |
|---|---|---|---|
| 购物车整车结算：多行选购、服务器重算总价、整车原子提交，任一行库存不足或总价溢出即整车拒绝 | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:150-258`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradingTypes.h:247-263` | 未写。商店册 §3.1.2 只写「明码标价、每笔购买广播」，通篇无「购物车」概念；`设计修改记录.md` 全库搜「购物车」零命中 | 一次买多件是既定交互形态该补进商店册／交互册，还是应改成逐件购买（会改变「每笔购买广播」的粒度）？ |
| 商店商品分类页：`DisplayCategoryId` ＋分类按钮，UI 本地按分类过滤 | `Source/Catfishing/ShopEconomy/Catalog/CatShopCatalogTypes.h:23-29`、`Source/Catfishing/UI/Shop/CatShopTypes.h:186-193` | 未写。商店册 §7 只列了缺货／刷新／免费保底／入库位置四项反馈，没有货架分类；交互册未在本轮评分范围内 | 分类维度（竿／饵／窝料／道具）该由策划在 GDD 定并写进商店册，还是留作纯 UI 实现细节？ |
| 服务端下单距离证明：提交购物车时服务器复核玩家 Pawn 仍在摊位 300cm 内，否则整单拒 | `Source/Catfishing/ShopEconomy/CatShopKioskActor.cpp:87-102`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.cpp:80` | 未写。商店册没有任何「必须站在摊位旁」的约束；§3.1.4 只写「全天营业」 | 打开页面后走开还能不能付款？半径 300cm 是产品口径还是工程占位，要不要进参数页？ |
| 两段式交付：购买账本先记 `bDeliveryPending`，营地公共仓库回执后才 `ConfirmTransactionDelivery` 置 `bDeliveryConfirmed` | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:516-607`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradingTypes.h:69-80` | 未写。商店册 §2 第 4 条只写「购买物进入团队装备库」，没有中间态；D-04 记的是入库顺序，没登记这个可观察的待交付状态 | 「已付款未入库」是否要对玩家可见（广播文案、商店页提示）？还是保持为纯内部状态？ |
| 售鱼最小金额门槛 `MinimumFishSaleValue=1`：估值低于该值的售鱼命令按「估价证据不足」拒绝 | `Source/Catfishing/ShopEconomy/CatShopEconomySettings.h:45-47`、`Config/DefaultGame.ini:152` | 未写。商店册 §3.1.1 只给「体重为主锚」的方向，没有下限概念；参数页拍定表无对应行 | 收购价档位表填好后，最低成交价还需要单独设吗？若需要，正式值该进参数页哪一行？ |
| 公款乐观并发：提交购物车或售鱼必须带 `ExpectedWalletRevision`，与服务器当前公款版本不符即整单拒 `RevisionConflict`（复核补录） | `Source/Catfishing/ShopEconomy/CatShopEconomyService.cpp:184-187`、`Source/Catfishing/ShopEconomy/Trading/CatShopTradeController.cpp:99`、`:142` | 未写。商店册 §3.1.2 只写「明码标价、不可讲价」；全册无「队友同时下单会互相顶掉」这类可观察行为，`设计修改记录.md` 搜「并发」「版本」零相关裁决 | 队友几乎同时下单导致我这单被拒，是产品要的行为（逼迫当面协商）还是应当自动重读余额重试？若保留，拒单文案要不要单列进 §7 的反馈清单？ |

### 复核记录

Phase 2b 独立复核，范围为全篇 45 行（补漏后）。**待驳行 28 条（❌ 8／🔄 3／⚠️ 17）全部逐条重查**：每条先换词重搜（类名、字段名、函数名、日志事件名、中文注释、Config 段名，否定式要求另换 3～5 个同义词），再打开命中片段与调用链核语义，未发现任何一条能被改判为 ✅ 的漏查实现。**✅ 抽验 6 行**（第 1、2、3、8、19、42 行，优先取 Code Reference 语义跨度大或跨模块的）：`CatShopEconomyService.h:207` Wallet 单例、`.cpp:658` BuildPublicSnapshot、`.cpp:259` PurchaseCatalogCart、`Trading/CatShopTradeController.cpp:113` SubmitFishSaleFromPlayer、`.h:74` SubmitCartFromKiosk、`CatfishingGameModeBase.cpp:768` CanAcceptGameplayCommand、`Run/CatSacrificeCoordinator.cpp:42` RequestSacrifice、`CatShopEconomyService.cpp:141` GetTransactionLedgerSnapshot 逐个 Read 到位，行号与语义均成立。**❓ 抽验全部 6 行**（第 17、26、37、40、43、44 行，不足五行故全查）：逐行确认它们依赖的是 `.umap` 摆位／阻挡体、`BP_CatShopKiosk` 与 `WBP_CatShop*` 图逻辑、`DT_ShopCatalog_Default` 行内容或装备强度资产，均属声明里的 ③④；其中第 40 行专门复查了 `bInteractionEnabled` 的全部四个宿主，确认商店摊位那份在 C++ 侧只被三处读取、无任何写入点，蓝图仍有接线余地，维持 ❓ 成立，没有把可文本检查的接线推给「查不了」。**账本复核**：`设计修改记录.md` 按日期核到最新（09-09 鱼表 fish_id），与本册相关的 09-04（:442 鱼缸开口子、:450-456 献祭移夜晚收尾）、09-05（:477 掉地物品改翻天消失）、09-07（:429 失败不进结算夜）、09-08 晚（:613 缸 10／20／30、:617 ⑥升级价 300／700）逐段读过，第 5、30 行按新裁决判 🔄 成立；`工程自补决策记录.md` 的 D-04／D-13／D-14／D-21 原始行与 09-09 复核节逐条比对，第 10、11、15、24 行引用的编号与状态词无误。**未能完成的检查**：本轮不跑引擎，`Scripts/verify_equipment_shop*` 只作静态文本读，资产内容与蓝图图逻辑仍不可验证（同基线）。

| # | 原判 | 改判 | 依据 |
|---|---|---|---|
| 5 | 🔄 Divergent | 🔄 Divergent | 状态维持。两处 gate 重读确认：`CatfishingGameModeBase.cpp:1183` 与 `:1234` 逐字同为 `Phase != DayActive \|\| !bQuotaOpen → InvalidPhase`。出处更正：设计侧原引 `设计修改记录.md:452-456` 只覆盖三条连带与落地行，正题「仪式由夜晚开场移到夜晚收尾」在 :450，已改引 :450-456 |
| 7 | ❌ Missing | ❌ Missing | 状态维持并复验出处。`Config/DefaultGame.ini:139` `SharedFishTankCapacity=20` 为单一固定值，`CatItemsSettings.cpp:11` 直接 `Max(0, …)` 返回，无档位入口；`TankCapacity`／`CapacityTier`／`UpgradeTier`／`Facility` 四词在 Source/Config/Scripts 全零命中。原引 `数值模拟与参数记录.md:52` 一度疑为错引（该行标题是「每日任务／世界进度」），实读后确认鱼缸占位数（初始 10、两档 20／30、价 300／700）就写在这一行的「依据」列里，引用成立 |
| 34 | ⚠️ Partial | ⚠️ Partial | 状态维持，code_ref 更正。原引 `CatfishingPlayerController.h:219-226` 实为 `ServerSubmitShopCartAtKiosk`／`ServerSellFish` 两个商店 RPC；解锁上报链在 `CatfishingPlayerController.h:77-78`→`.cpp:353-364`→`CatfishingPlayerState.cpp:77-98`，已替换。缺口结论不变：`CatShopInventoryComponent.cpp:110-126` 仍只用 `FMath::Rand()` 建开局货架，不读解锁快照 |
| 38 | ❌ Missing | ⚠️ Partial | 改判。基线自己给的 `CatShopModel.cpp:253-255` 就是正式链上的余额展示写口（`WalletText`＝「商店：团队公款 %d」，数据来自复制给全体客户端的 `ShopEconomySnapshot`），一行带可核 file:line 的实现不应判未实现。「常时」这一半的缺失重新核过并保留：`Source/Catfishing/UI/HUD/` 只有两组文件、grep `Wallet`／`Coin`／`Balance`／「公款」零命中，`GetShopEconomySnapshot()` 与 `UCatShopModel` 全类无 UFUNCTION。按本报告第 15／31／39 行的同一尺度定为 ⚠️ |
| 45 | 新增 | ❌ Missing | 补漏行。`DeiSFL.csv` 第2、4、7 行的「记录指标」列在基线整表中无对应行（第 42 行只取了第3、5、6 行）。全库无遥测设施（`Telemetry`／`Analytics`／`Metric` 在 `Source/Catfishing/` 零命中）；吃鱼侧 `UCatFishConsumptionCoordinator` 无计数器、只有 `Items/CatFishConsumptionCoordinator.cpp:63` 一条失败日志，献祭侧只有 Run 聚合进度，三项时序指标又受账本无时间戳（第 9 行）阻断 |
| — | 新增 | Code-only | 代码超前项补一条：公款乐观并发（`CatShopEconomyService.cpp:184-187` 的 `ExpectedRevision != Wallet.Revision → RevisionConflict`，客户端经 `CatShopTradeController.cpp:99`、`:142` 传入）。商店册通篇无此可观察行为，已随问题写进 Code-only mechanics 表；按模板不计入设计要求计数 |
