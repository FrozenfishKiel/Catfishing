## 环境与氛围
> 沿用基线 D:\LocalGameProject\Unreal\Catfishing\Docs\gap-analysis\2026-09-09，本轮未重跑：文档与代码均未变

### Features

| # | Design Requirement | Status | Code Reference | Notes |
|---|-------------------|--------|---------------|-------|
| 1 | 时钟源＝局内时间流：开局启动、随局推进、局末即弃，任何机制不读现实时钟（环境与氛围.md:65、:153、:46） | ✅ Implemented | `Source/Catfishing/Environment/CatEnvironmentSettings.cpp:11-29`；`Source/Catfishing/Environment/CatConfiguredEnvironmentProvider.cpp:22-23` | 时段只由 `FCatRunPhaseSnapshot` 的服务器锚点与截止点推算。**复核订正**：「绝不读取现实时间」这句注释在 `CatEnvironmentSettings.cpp:11`，不在 provider；provider 只做 `Weather=ConfiguredWeather` 与 `ResolveTimeOfDay(RunSnapshot, World->GetTimeSeconds())` 两次取值。全库 `FDateTime::Now/UtcNow` 只出现在 `Source/Catfishing/Save/CatSaveSubsystem.cpp`（存档时间戳），环境链无现实时钟读取 |
| 2 | 白天时段轴＝清晨/白天/黄昏三段，夜晚不入时段轴（:65、:156 轴定义权归本册） | ✅ Implemented | `Source/Catfishing/Framework/Core/CatRunContracts.h:121-131`；`Source/Catfishing/Environment/CatEnvironmentSettings.cpp:24-28`；`Config/DefaultGame.ini:45-46` | `ECatEnvironmentTimeOfDay{Unknown,Morning,Day,Dusk}`，夜晚阶段直接返回 Unknown；分界比例可配（MorningEndFraction=0.001、DuskStartFraction=0.8，复核订正原引的 :44-45 差一行） |
| 3 | 一局＝多天，白天限时推进、夜晚无时限停摆、全员同意后开始新的一天（:65，天结构归局与进程册） | ✅ Implemented | `Source/Catfishing/Run/CatRunSettings.h:61-86`；`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1343`（SubmitNextDayReady）、`:1591`（HandleDayDeadlineElapsed）；`Config/DefaultGame.ini:255-262` | 夜晚不设任何计时器。白天时长现值 `DayLengthSeconds=99999.0` 是工程测试值（白天实际不会到点），设计侧本就把时长留给数值阶段（§11-4）；最终天数见 D-01 |
| 4 | 昼夜推进要真的改变语义：时段分界到点重新发布环境（事件出场时段的前提，:71） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1543-1573`、`:1576-1588` | Morning/Dusk 两个分界换算成服务器绝对秒后各安排一次 one-shot，到点递增 Revision 并重发环境快照 |
| 5 | 昼夜改光照（画面随一天走完一条弧线，:65、:185） | ✅ Implemented | `Source/Catfishing/Environment/Presentation/CatEnvironmentPresentationActor.cpp:497-530`、`:532-600`、`:206-247` | C++ 按表现阶段（Morning/Day/Dusk/Night/SettlementNight）与白天进度插值太阳、月光、天空光、高度雾与后处理，直接写本地场景对象 |
| 6 | 昼夜改环境音；夜晚虫鸣是「入夜」的听觉信号（:65、:186） | ❓ Unverifiable | — | `Source/Catfishing/Environment/**` 内无任何 Sound/Audio/Ambient 符号；音频只可能挂在 `CatEnvironmentPresentationActor.cpp:389` 的 `BP_ApplyEnvironmentPresentation` 之后的蓝图与音频资产里，属查不了（④），不据此判未实现 |
| 7 | 天气按局内时钟在晴/雨/雾之间切换，每一局有不同的脸色（:77） | 🔄 Divergent | `Source/Catfishing/Environment/CatEnvironmentSettings.h:37-39`；`Source/Catfishing/Environment/CatConfiguredEnvironmentProvider.cpp:22`；`Config/DefaultGame.ini:44`（ConfiguredWeather=Clear） | D-24：当前是「一局一个显式配置天气」，provider 每次求值都直接复制 `ConfiguredWeather`，没有分布、切换粒度或调度种子；D-24 明写后续天气排期要作为 provider 内部调度实现、仍输出同一份快照。设计侧未在 09-07/09-08 裁决中改动，D-24 仍待裁。复核搜过 `WeatherSchedule`/`WeatherWeight`/`ResolveWeatherForSlot`/`FRandomStream`/`SetTimer`（Environment 目录内只有 ChumField 清理定时器），全部零命中；原引 `:43` 是 bEnableEnvironmentRuntime，已订正为 `:44` |
| 8 | 天气轴枚举＝晴/雨/雾（轴定义权归本册，:77、:156） | ✅ Implemented | `Source/Catfishing/Framework/Core/CatRunContracts.h:107-117` | `ECatEnvironmentWeather{Unknown,Clear,Rain,Fog}`，Unknown 为 fail-closed 占位而非第四种天气 |
| 9 | 区域轴枚举由本册定义（有哪些区域），鱼落在轴上哪一格归鱼册（:156） | ⚠️ Partial | `Source/Catfishing/Environment/CatWaterTypes.h:58-79`；`Source/Catfishing/Environment/CatWaterQuerySubsystem.cpp:112-126`、`:128-141`；`Source/Catfishing/Data/CatFishCatalogSettings.cpp:16-18`、`:194-196` | 水域身份只有自由 `FName RegionId`＋几何 Revision，没有区域枚举；机制上支持多水域并对多命中返回 `AmbiguousRegion` fail-closed，鱼册侧也确实按 `Definition->RegionIds.Contains(RegionId)` 消费这条轴（复核补证），但「区域轴」这条契约没有落成可枚举的类型，轴上有哪几格由数据自由填写。复核搜过 `ECatWaterRegion`/`RegionKind`/`WaterRegionType` 均零命中 |
| 10 | 双区域探索结构：河流（日常水域）＋森林湖（要走一段路、鱼种与事件偏稀有）（:89） | ❌ Missing | `Scripts/verify_showcase_formal_fish_region.py:22-31`；`Scripts/verify_stage_a_map.py:166-176`；`Scripts/create_stage_a_maps.py:73-119` | **复核换证据**：原引的两处脚本只断言「region_id=River 的水域恰好一个」，并不排斥第二个区域，撑不住结论。真正的写死点是——正式 GameplayMap（`CatOnlineSettings.GameplayMap=/Game/NaturePackage/Maps/Showcase2`）的验收脚本断言 `len(regions)==1` 且 RegionId 必须是 river；Lake 的建图与验收脚本同样断言 CatWaterRegion 总数 `!= 1` 即失败，且只生成一个 `region_id="River"`。全库（Source/Config/Scripts）搜 `ForestLake`/`Forest_Lake`/`森林湖` 零命中。地图摆放本身属③，但脚本文本已把「只有一个水域」写成验收条件 |
| 11 | 钓点＝玩家概念而非机制点位，任意水边可钓，打窝即自发成为钓点（:89） | ✅ Implemented | `Source/Catfishing/Environment/CatWaterQuerySubsystem.cpp:112-141`、`:173-205`；`Source/Catfishing/Environment/CatChumPlacementService.h` | 无固定钓点表：落点进入水域几何即可成立；窝点由投料落点现算，`ACatChumFieldAnchor` 只是自然事件的锚点，不是玩家钓点 |
| 12 | 时段与天气对鱼情做机会向修正（修正入口归本册，鱼表归鱼册；:65、:77、:213） | ⚠️ Partial | `Source/Catfishing/Fishing/CatFishingSession.cpp:993-994`；`Source/Catfishing/Data/CatFishCatalogSettings.cpp:215-218`；`Source/Catfishing/Data/CatFishDefinition.h:94-98`；`Config/DefaultGame.ini:111-112` | 接缝齐全：抛竿时把 `Environment.TimeOfDay/Weather` 冻结进选鱼上下文，鱼定义也有 TimeOfDay/Weather 列。但 `bEnableTimeOfDayEligibilityFilter=False`、`bEnableWeatherEligibilityFilter=False`（D-31 默认旁路），当前时段与天气对鱼情零影响 |
| 13 | 设计禁区：环境只给机会不做惩罚——无天气惩罚、无生存压力、无复杂生态模拟（:42-45、:154、:170） | ✅ Implemented | `Source/Catfishing/Condition/CatConditionComponent.cpp:41-52`；`Source/Catfishing/Environment/CatEnvironmentSettings.h:33-63` | 反证式判定：环境设置里没有任何惩罚/扣减字段，Wet 只是可复制的离散事实、不挂数值（疲惫数值已按 D-06 删除），也不存在生态/生存模拟模块 |
| 14 | 雨天淋湿由本册触发（表现规范归猫册，本册只触发；:77） | ❌ Missing | `Source/Catfishing/Condition/CatConditionComponent.cpp:41-52`、`:85-87`；`Source/Catfishing/Framework/Core/CatRunContracts.h:114`（Rain 注释把 Wet 触发推给 Character） | `SetWetFromAuthority` 在整个 Source/ 里只有定义（`.cpp:41`）与声明（`.h:33`）两处，零调用者；Wet 只由浸没深度 ≥ `WetWaterDepthCentimeters` 驱动。复核另搜 `Environment.Weather` 的全部消费者，只有选鱼冻结、表现投影和调试面板三处，无一写 Condition；天气→淋湿这条触发链在 C++ 里不存在（可文本检查，非查不了） |
| 15 | 自然事件按局内时钟与天气条件出场，条件不成立就没有事件（不造占位事件；:101） | ✅ Implemented | `Source/Catfishing/Environment/CatEnvironmentSettings.cpp:53-69`；`Config/DefaultGame.ini:47-48` | 事件要 DayActive＋已知时段＋天气匹配才成立（`ActiveEventRequiredWeather`），否则输出 None；现网配置 ActiveEventId=RainBloom 要求 Rain 而 ConfiguredWeather=Clear，故当前实际不会成立 |
| 16 | 六类自然事件及各自出场条件成表：彩虹（雨后放晴）/萤火虫（夜·草木边）/晚霞（黄昏）/月光湖面（夜·晴）/鸟群蝴蝶（白天）/森林湖鱼群（:103-110） | 🔄 Divergent | `Source/Catfishing/Environment/CatEnvironmentSettings.h:49-55`；`Config/DefaultGame.ini:47` | D-26：快照只装得下一个 `ActiveEventId`，配置只有单个事件位而不是事件表，事件名 RainBloom 也不在设计六类内；且 `TryResolveActiveEvent` 要求 DayActive，萤火虫/月光两个夜间事件结构上不可能成立。D-26 已登记「自然事件表、同刻多事件策略仍待裁，不能按旧记录当成已实现」。复核搜过 `Rainbow`/`Fireflies`/`SunsetGlow`/`MoonlitLake`/`彩虹`/`萤火`/`晚霞`/`鸟群`，Source/Config/Scripts 全部零命中，六类事件 ID 一个都不存在 |
| 17 | 每个自然事件显式标注是否抛印记，环境印记供给印记相册「风景页」（:99、:103-110、:169） | ❌ Missing | `Source/Catfishing/Camp/CatCampHubActor.cpp:306`；`Source/Catfishing/Items/World/CatFishPickupActor.cpp:710`；`Source/Catfishing/Social/CatSocialService.cpp:561-575` | 复核以 `SubmitImprintCandidate`／`FCatImprintCandidate` 反查全部调用点（排除 Tests）：唯一写口 `UCatRunImprintService::SubmitImprintCandidate`（`Collection/CatRunImprintService.cpp:142`）只被营地篝火、鱼获拾取与偷鱼被抓三处调用，没有任何环境事件入口，`Environment/**` 内零命中；D-26/D-07 已登记环境印记投递待裁。可文本检查，非查不了 |
| 18 | 事件必须隔着距离也能察觉（声音＋光＋猫身体语言），否则聚不起人（:101、:187） | ⚠️ Partial | `Source/Catfishing/Environment/Presentation/CatEnvironmentPresentationActor.cpp:356-365`、`:389` | 事件事实（bHasActiveEvent/ActiveEventId）已复制到客户端并投影给表现层，但 C++ 侧没有任何远距离感知信号（无音效、无提示、无聚拢广播）；声光与猫身体语言的本体只可能在 BP/资产，那部分查不了 |
| 19 | 系统按局内时钟排程周期事件，随机只做小抖动（:145） | ❌ Missing | `Source/Catfishing/Environment/CatEnvironmentSettings.cpp:53-69`；`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1543-1573`（唯一的时钟排程只服务时段刷新） | 没有任何事件调度器或随机流：事件成立与否完全由静态配置＋当前时段/天气判定，既无周期排程也无抖动。复核在 `Environment/**` 搜 `FRandomStream`/`Schedule`/`Timer`，只命中 ChumField 的过期清理定时器与表现层按 FieldId 播种的装饰随机；GameMode 侧的 `SetTimer` 只有 Morning/Dusk 两个一次性时段刷新，不产生事件。D-26 收口时明确撤回过 `ResolveNaturalEventId` 一类实现名，属未做而非查不了 |
| 20 | 聚鱼时刻的「自然触发」源：森林湖按局内时钟/天气自发涌现鱼群（本册 §5，:114-123） | 🔄 Divergent | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1647-1716`；`Source/Catfishing/Environment/CatEnvironmentSettings.cpp:72-84`；`Config/DefaultGame.ini:49-50` | 代码已实现自然源：事件成立后按 `RunId\|DayIndex\|EventId\|AnchorId` 去重向 ChumField 提交一次 `NaturalEvent` 聚鱼（D-25）。**设计已更新于 09-07（聚鱼单源化，只投料触发；自然触发归完整版），09-08 晚又裁「环境册 §5 自然触发暂不动，等下一轮过稿」（设计修改记录.md:554、:625），D-25 需复核**——本册字面仍写两源，按裁决评则该源当前不在 Demo 范围内。复核补一条事实：正式 GameplayMap Showcase2 的验收脚本断言 `len(anchors)==0`（`Scripts/verify_showcase_formal_fish_region.py:27`），锚点只存在于 Lake，所以这条自然源在当前上线地图里根本不会触发 |
| 21 | 两种触发进入同一套演出与钓鱼收益，不写两套逻辑（:123、:129） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1690-1710` | 自然源不另建子系统：走玩家投窝同一个 `UCatChumFieldSubsystem::PrepareField / ActivatePreparedFieldDeferred` 写口，只用 `ECatChumFieldSource::NaturalEvent` 区分来源。注意该条的前提（自然源存在）已被 09-07 单源裁决架空，见第 20 行 |
| 22 | 所有环境状态属于这一局，局末即弃、无跨局存档（:55、:147、:155） | ✅ Implemented | `Source/Catfishing/Framework/Core/CatRunContracts.h:174-197`、`:228-230`；`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1613-1631` | 环境只以 `FCatEnvironmentSnapshot` 活在 `RunPublicState` 里，求值失败即发同 Revision 空环境；`Source/Catfishing/Save/**` 无任何 Weather/TimeOfDay/Environment 字段 |
| 23 | 多人同步按局内广播，事件对同局全员同时可见、不存在离线错过（:147、:168） | ✅ Implemented | `Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1636-1642`；`Source/Catfishing/Environment/Presentation/CatEnvironmentPresentationActor.cpp:355-365` | 服务器 GameMode 是唯一写者，经 GameState 复制同一份公开快照；客户端表现层只读、不自行推进阶段或猜天气 |
| 24 | 种花：固定花坛点位、当局开花、局末随局清空、不存档不成长（:135） | ❌ Missing | — | 全库（Source/Config/Scripts）无 Flower/Plant/Seed 等相关符号。设计侧 §11-3 本身仍是「去留待裁、砍掉也可接受」，因此这是未立项而非实现欠账 |
| 25 | 森林湖沿途与湖区的特殊看点、风景点、隐藏观察点分布（:89、:181） | ❓ Unverifiable | — | 看点分布是纯关卡摆放（.umap／资产，属③），本轮不跑引擎无法确认；同时设计侧 §11-6「自然事件完整清单与出场条件表」与看点落图都还没定稿，两侧都缺 |
| 26 | 猫味演出：入夜瞳孔放大、清晨伸懒腰、正午找树荫、雨天炸毛甩水、雨停集体抖毛、看鸟咔咔、扑萤等（:67、:79、:91、:106-110、:125） | ❓ Unverifiable | `Scripts/create_body_action_montages.py:14-21`；`Source/Catfishing/Environment/Presentation/CatEnvironmentPresentationActor.h:58-72` | Montage 清单只有六个 Camp/Social 动作，无环境演出；但表现层已经拿到 Weather/TimeOfDay/事件投影，AnimBP 与表现蓝图里是否按这些字段分支属④，文本证明不了，不判未实现 |
| 27 | §7 视觉反馈通道：湖面波纹、鱼跃、鸟群聚集、植物随时段的细节变化（:185） | ❓ Unverifiable | — | 除昼夜灯光/雾/后处理（第 5 行）外，这些都由材质、粒子与关卡资产承载（③④），本轮不跑引擎无法确认 |
| 28 | 天气与区域各自的抛印记事件④：全员挤在一处躲雨的团子同框（:81）、首次抵达森林湖穿出树林的瞬间（:93） | ❌ Missing | `Source/Catfishing/Collection/CatRunImprintService.cpp:142`；`Source/Catfishing/Camp/CatCampHubActor.cpp:324`；`Source/Catfishing/Items/World/CatFishPickupActor.cpp:724-725`；`Source/Catfishing/Social/CatSocialService.cpp:575` | 复核补行：原报告第 17 行只覆盖了 §3.1-4 事件表的抛印记，本册另有两个同级「抛印记事件④」块（天气段的躲雨团子、双区域段的首达森林湖）同样没有入口。唯一印记写口的三个调用者里没有天气、没有区域、也没有首达判定；`FirstArrival`/`Shelter`/`RainShelter` 类符号全库零命中。与第 17 行同属 D-07 未给正式触发清单 |
| 29 | 两种触发共用一个稀缺预算，按一个机制记一本账（:123） | ⚠️ Partial | `Source/Catfishing/Environment/CatChumFieldSubsystem.cpp:184-191`；`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp:1702-1704` | 复核补行：自然源与玩家投窝确实共用同一本账——`PrepareField` 对两种来源一视同仁地按 RegionId 扣 `MaxActiveFieldsPerRegion` 与 `MaxRawContributionPerRegion`，`ECatChumFieldSource` 只做来源标记不改配额。但设计说的「稀缺预算」是事件频率/冷却口径，那一层（自然涌现频率、共享 cd）D-25 明写仍待裁、当前源码没有；现有账本是窝料浓度容量账，不是稀缺预算账 |

### Summary
- Total features: 29
- ✅ Implemented: 12
- ⚠️ Partial: 4
- ❌ Missing: 6
- 🔄 Divergent: 3
- ❓ Unverifiable: 4
- Inspectable: (29 - 4) / 29 * 100% = 86.2%
- Coverage of inspected: (12 + 0.5 * 4) / (29 - 4) * 100% = 56.0%

### Scan Scope

**扫过的目录与文件类型**（工程根 `D:/LocalGameProject/Unreal/Catfishing/`）：

- `Source/Catfishing/Environment/**`（含 `Presentation/`、`Tests/`）、`Source/Catfishing/Run/**`、`Source/Catfishing/Framework/Core/CatRunContracts.h`、`Source/Catfishing/Framework/Game/CatfishingGameModeBase.cpp/.h`、`Source/Catfishing/Data/`（选鱼时段/天气门）、`Source/Catfishing/Fishing/CatFishingSession.cpp`（选鱼上下文冻结）、`Source/Catfishing/Condition/`（Wet）、`Source/Catfishing/Collection/` 与 `Source/Catfishing/Camp/`、`Source/Catfishing/Items/World/`、`Source/Catfishing/Social/`（印记候选提交点）、`Source/Catfishing/Save/`（持久化反证）——只看 `.cpp/.h`。
- `Config/DefaultGame.ini`（`CatEnvironmentSettings`、`CatRunSettings`、`CatFishCatalogSettings`、`CatSocialSettings` 段）。
- `Scripts/` 下 `.ps1/.py`：`verify_run_environment_social.ps1`、`verify_run_environment_social_runtime.py`、`configure_run_environment_social_map.py`、`create_body_action_montages.py`、`verify_showcase_formal_fish_region.py`、`create_stage_a_maps.py`、`verify_stage_a_map.py`（后两项为复核补扫）。
- 台账：`Docs/Development/工程自补决策记录.md`（D-01…D-32 通读，本册相关 D-24/D-25/D-26/D-30/D-31）、`Docs/Development/需求对齐差距清单.md`（§1/§3 的 RunEnvironmentSocial 段落）。
- 裁决：`Knowledge/Feishu/裁决同步/设计修改记录.md`（09-07「聚鱼单源化」、09-08 晚「环境册 §5 自然触发暂不动，索引页先改单源」）。

**方法**：先按类名、字段名、日志事件名（`environment_day_segment_refreshed`、`natural_chum_terminal`）、配置段名与中文注释 Grep 定位，再 Read 片段；对「有没有」类要求用反向 Grep 取反证（如 `SetWetFromAuthority` 无调用者、Save 模块无环境字段、Montage 清单无环境演出）。

**跳过 / 无法覆盖的东西**：

- `Content/**` 的 `.uasset/.umap`（约 1838 个）不计入文本扫描：地图里的水域摆放、光照/材质/粒子、AnimBP 与表现蓝图逻辑属可检查程度③④，本轮不跑引擎入口（`Scripts/verify_*` 未执行），因此第 6、25、26、27 行判 ❓ 而非 ❌——把它们报成未实现会引发重复实现。
- `Intermediate/Build/**` 的 UHT 生成代码只作符号确认，不作为实现证据。
- `Source/**/Tests/` 只作辅助证据，不代替运行链；本册无「只在测试里存在」而被判 ✅ 的条目。
- 设计侧本身未定的项（§6 体验锚定数字、§11-1/2/4/5/6/7 七条待办的具体数值与清单）不作为可判定要求列入表格；§5 聚鱼两源按 09-07/09-08 裁决评（单源、自然触发归完整版），不按该册字面评。
- `Knowledge/Feishu/鱼/鱼的行为.md` 页头已声明整页重写、实现不读，本轮未作为评分依据。

### 复核记录

复核范围：全部 10 条待驳行（#7 #9 #10 #12 #14 #16 #17 #18 #19 #20）逐条重搜；✅ 行抽验 7 条（#1 #2 #3 #4 #5 #8 #21 #22 #23 中的 file:line 全部 Read 过位置）；❓ 行 4 条全查（本册 ❓ 只有 4 条，不足 5 条）。

| # | 原判 | 改判 | 依据 file:line 或搜过的关键词 |
|---|------|------|------------------------------|
| 1 | ✅ Implemented | ✅ Implemented（订正引用与 Notes） | 「绝不读取现实时间」在 `Environment/CatEnvironmentSettings.cpp:11`，不在 provider；`CatConfiguredEnvironmentProvider.cpp:22-23` 只做 ConfiguredWeather 与 ResolveTimeOfDay 两次取值 |
| 2 | ✅ Implemented | ✅ Implemented（订正 Config 行号） | `Config/DefaultGame.ini` 第 45-46 行才是 MorningEndFraction/DuskStartFraction；原引 :44-45 差一行 |
| 3 | ✅ Implemented | 维持 | `Run/CatRunSettings.h:61-63`（DayLengthSeconds）、`Config/DefaultGame.ini:254-262`、`CatfishingGameModeBase.cpp:1343`（SubmitNextDayReady）、`:1591`（HandleDayDeadlineElapsed）位置与语义均对 |
| 4 | ✅ Implemented | 维持 | `CatfishingGameModeBase.cpp:1543`（ScheduleDayEnvironmentRefreshes，两个 one-shot SetTimer）、`:1576`（HandleDayEnvironmentRefreshElapsed，++Revision 后 RefreshEnvironmentAndPublish） |
| 5 | ✅ Implemented | 维持 | `CatEnvironmentPresentationActor.cpp:497-525`（BuildScenePresentationValues 按 Morning/Day/Dusk/Night/SettlementNight 插值）、`:532`（ApplyBuiltInScenePresentation 写太阳/月亮/天空光/雾/后处理） |
| 7 | 🔄 Divergent | 维持（补证据、订正 Config 行号） | `WeatherSchedule`／`WeatherWeight`／`ResolveWeatherForSlot`／`FRandomStream`／`SetTimer` 在 Environment 内零命中；`CatConfiguredEnvironmentProvider.cpp:22` 直接复制 ConfiguredWeather；Config 天气行是 :44 |
| 8 | ✅ Implemented | 维持 | `Framework/Core/CatRunContracts.h:105-117`，Unknown 注释写明是 fail-closed 占位 |
| 9 | ⚠️ Partial | 维持（补消费端证据） | `CatFishCatalogSettings.cpp:16-18` `RegionIds.Contains(RegionId)`、`:194-196` 是这条轴的真实消费点；`ECatWaterRegion`／`RegionKind`／`WaterRegionType` 零命中 |
| 10 | ❌ Missing | 维持（换更强证据） | 原引两处只断言「River 区域恰好一个」，不排斥第二区域；真正写死点是 `verify_showcase_formal_fish_region.py:25`（正式 GameplayMap `len(regions)==1`）、`verify_stage_a_map.py:172`、`create_stage_a_maps.py:112`；`ForestLake`／`森林湖` 全库零命中 |
| 12 | ⚠️ Partial | 维持 | `CatFishingSession.cpp:993-994` 冻结 TimeOfDay/Weather；`CatFishCatalogSettings.cpp:214-219` 两道门被 `bEnableTimeOfDay/WeatherEligibilityFilter` 旁路；`Config/DefaultGame.ini:111-112` 确为 False（D-31） |
| 14 | ❌ Missing | 维持（补反证） | `SetWetFromAuthority` 全 Source 只有 `.h:33` 声明与 `.cpp:41` 定义，零调用；`Environment.Weather` 的消费者只有选鱼冻结、表现投影、调试面板三处 |
| 16 | 🔄 Divergent | 维持（补关键词） | `Rainbow`／`Fireflies`／`SunsetGlow`／`MoonlitLake`／`彩虹`／`萤火`／`晚霞`／`鸟群` 在 Source/Config/Scripts 零命中；`CatEnvironmentSettings.cpp:57-59` 的 DayActive 前置确认夜间事件不可能成立 |
| 17 | ❌ Missing | 维持（补唯一写口） | 唯一写口 `CatRunImprintService.cpp:142`，调用者只有 `CatCampHubActor.cpp:324`、`CatFishPickupActor.cpp:724-725`、`CatSocialService.cpp:575`；`Environment/**` 内 `ImprintCandidate` 零命中 |
| 18 | ⚠️ Partial | 维持 | `CatEnvironmentPresentationActor.cpp:359-362` 复制 Weather/TimeOfDay/bHasActiveEvent/ActiveEventId，`:389` 交给 `BP_ApplyEnvironmentPresentation`；C++ 侧无音效/广播符号，声光与身体语言属④ |
| 19 | ❌ Missing | 维持（补证据） | Environment 内 `Timer` 只命中 ChumField 清理，`FRandomStream` 只命中 `CatChumFieldPresentationActor.cpp:98` 的装饰随机；GameMode 的 SetTimer 只有时段刷新 |
| 20 | 🔄 Divergent | 维持（补上线地图事实） | `CatfishingGameModeBase.cpp:1647-1717` 自然源实现完整；`verify_showcase_formal_fish_region.py:27` 断言正式 GameplayMap 锚点数为 0，该源在上线地图不会触发；裁决出处 `设计修改记录.md:554`、`:625` |
| 21 | ✅ Implemented | 维持 | `CatfishingGameModeBase.cpp:1691-1706` 与玩家投窝同一 `PrepareField`／`ActivatePreparedFieldDeferred` 写口，仅 `Request.Source = ECatChumFieldSource::NaturalEvent` 区分 |
| 22 | ✅ Implemented | 维持 | `CatRunContracts.h:172-197`、`:228-230`；`Source/Catfishing/Save/` 内 `Weather`／`TimeOfDay`／`Environment` 零命中 |
| 23 | ✅ Implemented | 维持 | `CatfishingGameModeBase.cpp:1638-1641` 服务器唯一写者经 GameState 复制；表现层只读 |
| 6 | ❓ Unverifiable | 维持 | 复查确认 `Environment/**` 内 `sound`／`audio`／`ambient` 零命中（只在 `CatEnvironmentPresentationActor.h:56` 的注释里提到音频切换归表现层），推给④成立 |
| 25 | ❓ Unverifiable | 维持 | 看点分布无任何 C++/Config/Scripts 承载物可查，属③；本轮不跑引擎 |
| 26 | ❓ Unverifiable | 维持（订正行号） | Montage 清单实为 `create_body_action_montages.py:14-21`（六项，原写 :14-20 漏末行）；AnimBP 分支属④ |
| 27 | ❓ Unverifiable | 维持 | 材质/粒子/关卡资产属③④，无文本可查 |
| 28 | （原报告无此行） | ❌ Missing（新增） | 本册天气段 :81 与双区域段 :93 各有一个「抛印记事件④」块被漏评；证据同第 17 行的三个调用者，`FirstArrival`／`Shelter` 零命中 |
| 29 | （原报告无此行） | ⚠️ Partial（新增） | :123「共用一个稀缺预算、记一本账」被漏评；`CatChumFieldSubsystem.cpp:184-191` 两来源共用同一 RegionId 配额账本，但设计口径的事件频率/cd 预算 D-25 仍待裁 |

复核未改动的判定共 19 条，其中 6 条只补了证据或订正行号，Status 一律未变；新增 2 行使总数由 27 变 29。
