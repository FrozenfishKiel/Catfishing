# 蓝图表现接线参考

文档状态：2026-09-17 物品使用入口更新；下方历史记录不作为当前接线指令。

事实来源：`Source/Catfishing/AbilitySystem/Items/`、`AbilitySystem/Fishing/InputAbilities/CatFishingChumAbility.cpp`、`Equipment/CatEquippedInstance.cpp` 和正式物品资产。

## 1. 当前物品使用入口

程序实现行为；策划配置动作、GE、Cue、成本和条件。完整配置与七件新道具契约见 [物品使用与策划配置](Architecture/物品使用与策划配置.md)。

| 行为 | 正式能力入口 | 表现接线约束 |
| --- | --- | --- |
| 选中与使用 | 鱼竿选格即拿出、切走收回；其他物品选格只改焦点，使用激活精确来源 Spec | 不按物品名称寻找实例；使用中鱼竿仍锁定切换 |
| 鱼竿拿出/操作 | DeployFishingRod；装备实例授予 RodOperations | 世界竿物理与会话不依赖某次使用 GA 存活 |
| 窝料 | FishingChum 预测蓄力，标准 GAS 松开事件 | 预览读取活动能力；不再建立 ChumUse 临时授予或物品执行状态 |
| 抄网 | UseScoopNet，目标数据传输后权威排队捕获 | 当前挥网继续沿既有服务器 ScoopSwing 表现入口，不同时配置重复预测挥网 |
| 饵与漂 | SelectFishingLoadout | 装配不提前扣饵 |
| 吃鱼 | ConsumeFish，成本＋成长 GE | 不直接加经验，不使用已删除的 ConsumableEffectFragment |
| 鱼护 | 搬运/放置/容器交互 | 保留原容器及内鱼身份，不补虚构使用效果 |

正式 `/Game/Character/BP_CatCharacterBase` 的 ScoopSwing 仍通过 Character 的动作重定向播放。素材映射不由库存或目标数据负责。

输入仍为持竿时左键操作鱼竿，放竿后左键使用选中物品；Q 丢嘴叼物，E 交互，X 收竿。新资产迁移入口为 `Scripts/migrate_equipment_definitions.py`。

### 1.7 2026-09-16 左键抄网与打窝衔接记录

修改前：工作区存在房间聊天/UI 的并行改动，之后另有鱼竿/物理检查点和 FightSimulator 改动，本轮不接管这些内容。首次修改前未运行本轮检查；旧 F 挥网缩小来自用户反馈，旧蓝图直接向 Mesh 播原骨架动画的路径由编辑器审计确认。新路径保留抄取距离（厘米）、冷却（秒）、数量、嘴叼归属、扣饵与打窝数值，不改存档字段及持久化契约。

| 功能/环节 | 当前位置与引用证据 | 现有行为与目标差异 | 处理方式与目标位置 | 衔接依赖与顺序 | 回归风险与验证方式 | 处理结果与证据 |
| --- | --- | --- | --- | --- | --- | --- |
| 输入与调用方 | `CatAbilityInputBindingComponent::HandleAbilityInputTagPressed` → Controller Begin/End；正式 `/Game/Input/InputContext/IMC_InputContext` | 左键已由鱼竿迁移提供；抄网缺有效目标，打窝需核实按住/释放 | 保留按下时固定接收方，抄网/窝料进入选中实例 Use；修正路由日志 | 复用现有左键分流，不增加常驻 Ability | 主控鱼竿优先、模态拦截、取消、换槽不能串实例 | `Report4` 4 项 Quickbar 回归通过；`Report3` 正常/弱网工具链通过 |
| 目标与菜单消费者 | `CatEquipmentUseItemInstances::CaptureUseTarget`；`CatInventoryPageController::SubmitInventoryContextAction`；Controller 两个使用 RPC | 原 Use 只有物品身份，没有视线与 Actor；菜单也必须携带同一意图 | 新增 `FCatInventoryUseTarget`（默认无目标；原点厘米、方向单位向量、Actor），领域在服务器重验 | 先实例采样/上下文，再 RPC 与菜单调用 | 客户端目标不构成权威；同 RequestId 改目标须拒绝 | 库存目标/幂等用例、真实客户端采样通过；未单独人工点击菜单 |
| 异步状态与生命周期 | `InventoryComponent::ExecuteItemActionFromAuthority` → `ScoopFromInventoryUseOnAuthority` → `FishingResolutionSubsystem` Catch 队列 | 原同步等待误报失败，帧末丢失所选实例 | `bPending=false` 为默认；排队为 true，回调写一次终态，再发 owning-client 回执；清理时取消 | 先 Pending/完成口，再队列保留实例；取消旧排队任务 | 换 Pawn、旅行、重复请求和排队期间物品移走 | 取消/幂等/一次完成断言及两端同请求回执通过；`Report5` 丢网场景通过 |
| 捕获与资源权威 | `FishingCommandComponent::HandleAbilityCommandFromAuthority` → `FishingSession::RequestScoop` | 原 Session 可回退旧装备快照，和选中实例不一致 | 只按请求原实例、正数量及网定义裁决；移除快照射程重载 | 先校验原网，再表现/冷却，再原捕获事务 | 满体力鱼可捕获，嘴部单占用；不重复扣饵/结算 | 真实 HookedFight 会话 → Caught → 嘴叼、扣饵与重放用例通过 |
| 动画表现 | `/Game/Character/BP_CatCharacterBase` ScoopSwing → Character PlayAnimMontage；旧 BP_GA_Scoop → Get Mesh PlayMontage | 旧 Ability 绕过角色映射；Use 发起端还被多播跳过 | 删除无引用旧 Ability，ScoopSwing 纳入服务器确认事件，本地与旁观者各播放一次 | 沿现有角色重定向映射，不改动画资产 | 两套皮肤、播放/混合全过程比例、双端事件 | `Report4` 4 项 CharacterVariants 通过；CuteCat 比例 Min/Max=1；两套截图已检查 |
| 配置、资产与生成入口 | 3 个正式 `DA_CatAbilitySet_*`、正式 IMC、`CatInventoryQuickbarAuthoringLibrary`、迁移脚本 | F/Q 旧孤立资产误导后续接线 | 引用审计后删除 BP_GA_Scoop/Chum 与 IA_CatchFish/BaitSpot；正式左键映射和 ChumUse 原生 GA 保留 | 先硬/软/管理/搜索引用检查，后精确删除，再独立重载审计 | 二进制消费者不可仅凭源码排除；Cook 无新入口 | `InputMigrationBefore.json` 无包引用，正式能力/映射有效；旧输入 Tag 保留供作者器识别迁移，非运行入口 |
| 调试、日志与文档 | `CatFishingDebugSubsystem` → 选中网定义；`LogCatInventory` / `LogCatFishing` / `LogCatfishing` | 旧范围提示读取快照、G 提示失效；排队误作终态 | 调试读取同一快捷栏实例，提示左键；记录 Pending、完成及拒绝；修正本文与核心流程 | 消费者随权威入口同步更新 | 仅边沿日志，RequestId 关联双端；不增加 Tick 日志 | PIE 双端已有提交、完成、钓鱼终态与客户端回执；Development 构建通过 |
| 持久化、打包与验收边界 | 原库存/鱼嘴/窝料领域事务；现有 Cook 入口 | 不新增存档写入，不改 Cook 配置 | 保留原持久化权威；删除资产无消费者 | 编译、局部运行、表现证据分层 | 编译和 PIE 不替代打包双端真人操作 | 新 Cook/打包及 `<打包根目录>/Catfishing/Saved/Logs` 双端默认落盘未运行；整体模块未关闭 |

本轮证据位于 `Saved/ScoopChum/`：`Report3/index.json` 为 4/4 通过，`Report4/index.json` 为 8/8 通过；最终补测 `Report5/index.json` 为 4/4 通过（含帧末丢网拒绝）。首次工具网络夹具因第三人称相机尚未追上角色而瞄错，已改为等待实际 `CaptureUseTarget` 命中后按左键，未放宽生产校验；首次广域弱网用例在重新接杆阶段超时，最终 `Report4` 重跑通过。截图为 `Saved/CharacterVariantScreenshots/CuteCat_ScoopSwing.png` 与 `AnimaliaCat_ScoopSwing.png`。这些是 contract/runtime_behavior 与局部表现证据，正式湖岸真人手感、完整抄网模型演出和新打包双端仍需验收。

---

## 任务 2：Rod / Hook 表现蓝图与鱼种库表现

**目的**：给场景里的竿、钩（浮漂）、鱼挂上真实 Mesh。权威边界详见《FishingActorBlueprintHooks_zh-CN.md》，这里只列操作。鱼不再使用一个全局表现蓝图。

### 2.1 创建（目录 `/Game/Catfishing/Fishing/Actors/`）

| 蓝图 | 父类 | 只读状态里有什么 |
|---|---|---|
| `BP_FishingRod` | `CatFishingRodActor` | ItemInstanceId / bDeployed / OperatorPlayerStates / HolderPlayerState / bBroken / Skin ID |
| `BP_CatFishingHookActor`（`/Game/Blueprint/Actors/`） | `CatFishingHookActor` | Phase: CastFlight / Landed / Failed |
| 原生 `CatFishEncounterActor` | 不创建平行蓝图 | FishDefinitionId / MotionIntent / CurrentLineLength；Mesh/ABP 由鱼种库直连 |

### 2.2 每个里面做什么（共同套路）
1. Components 面板：在 **`VisualRoot`**（Hook 则是它下面的 HookVisualAnchor/BobberVisualAnchor/BaitVisualAnchor）下加 Static/Skeletal Mesh。
2. **每个 Mesh 的 Collision Preset 设为 `NoCollision`**——这是硬性要求：放竿改成 AlwaysSpawn 后，表现碰撞会把猫/竿顶飞或干扰移动。
3. Event Graph 实现对应事件切外观：
   - Rod：`BP_OnRodPresentationChanged` —— Current.OperatorPlayerStates 第 0 项有效→有人操作中的外观；bDeployed→架起；**R 的"架竿/离开"动作动画就播在这里**（比钩子准确）。
   - Rod 竿尖标记：在 VisualRoot 下加一个 Scene/Arrow 组件挪到你 Mesh 的视觉竿尖处，Details→**Component Tags 添加 `RodTipMarker`**——调试青色竿尖球和"竿尖→钩/鱼"的连线会优先跟随它（没有标记时回退权威锚点）。仅表现用途：服务器抛竿原点/线长判定不受它影响。
   - Hook：`BP_OnHookPresentationChanged` —— Phase==CastFlight 隐藏浮漂只留钩、Landed 显示浮漂漂浮、Failed 灰显。
   - Fish：基础 Mesh、Skeleton、AnimBP 和 Calm/Struggle/Exhausted 动画由原生 Actor 沿 `FishDefinition → PresentationDefinition` 应用；`BP_OnFishPresentationChanged` 只留给附加 VFX/SFX，不得按 ID 重选基础资源。
4. **每鱼表现参数**（打开对应 `FishPresentation_*`）：

   | 参数 | 作用 | 怎么调 |
   |---|---|---|
   | `Encounter Mesh Relative Transform` | 水中 Mesh 的轴向、深度、前后左右和基础比例 | **先调旋转**使鱼头对准 Actor +X，再调位置 |
   | `Landed Mesh Relative Transform` | 岸上鱼的局部摆放 | 调整地面高度和朝向，不改拾取 Sphere |
   | `Carried Mesh Relative Transform` | 嘴叼状态的局部对齐 | 与角色 Mouth Socket 联调 |
   | `Exhausted / Landed Actor Roll Degrees` | 水中力竭与岸上侧翻角 | 只调表现，不改抄网/线长判定 |
   | `Mesh Reference Weight / Min / Max Scale` | 本鱼重量到统一视觉比例的映射 | 不在蓝图里再次随机 Scale |

   **调参顺序**：先定 Encounter Transform 的旋转（把鱼头方向掰对），再调前后左右上下，最后分别调 Landed/Carried。

   注意：这些**全部只影响观感**。线长、近岸、抄网半圆等一切判定只用 Actor 的权威位置，与朝向和三个偏移无关。debug 球和鱼线会自动跟随 Mesh 的实际位置（读 `GetVisualWorldLocation`），所以调完能立刻看出对不对齐。
5. **禁止**：Set Actor Transform/Location/Rotation、Tick 模拟位置、从表现事件里调任何 Submit，以及在蓝图中维护 `FishDefinitionId → Mesh/ABP` 映射。

### 2.3 注册到设置
Project Settings → **Catfishing Fishing Presentation** 只注册 Hook/FishEncounter/Chum 等表现类，其中 `FishEncounterActorClass` 保持原生 `/Script/Catfishing.CatFishEncounterActor`。鱼竿 Actor 类从 `Equip_Rod_*` 的 `UseActorClass` 读取，给每个正式鱼竿 DataAsset 填 `BP_FishingRod`；鱼资源只在正式 `Fish_*` 的 `PresentationDefinition` 配置。保存资产并重启 Editor（DeveloperSettings 和 DataAsset 软引用都别靠热加载赌运气）。

### 2.4 验收
PIE 完整走一遍：R 放竿看到竿 Mesh；抛竿看到钩/漂飞出与落水切换；真咬提竿后看到鱼 Mesh 且红球（debug）与鱼 Mesh 同位置；挣扎/顺从时鱼动画切换。位置对不上以 debug 球为准（球=服务器事实）。

---

## 任务 3：窝点表现 BP（可选，debug 绿圈已可用）

1. `/Game/Catfishing/Fishing/Actors/BP_ChumFieldPresentation`，父类 `CatChumFieldPresentationActor`。
2. 里面按复制来的中心/半径放一个贴花或 Niagara 圈（同样 NoCollision、不写 Transform）。
3. Project Settings → Catfishing Fishing Presentation → `ChumFieldPresentationClass` 选它 → 重启。
4. 验收：选中窝料、左键松开打窝后看到圈，约 3 分钟后消失，与 debug 绿圈重合。

---

## 任务 4：搏斗持续状态动画（AnimBP / 角色层）

**目的**：拖拽循环、松线姿态这类"按住期间一直播"的动作。数据源是 **ViewBridge**（不是 Ability 钩子——钩子只有瞬间）。

在角色 AnimBP（或角色 BP 里定时 0.1s 刷新）：
```
CreateFishingViewBridge(Outer=self)   ← 存成变量，只建一次
FindFishingSessionForPlayerState(自己的 PlayerState) → BindSession
绑定 OnViewStateChangedBP →
    Phase == HookedFight 且 bReeling  → 拖拽循环姿态
    Phase == HookedFight 且 bSlacking → 松开线杯的喘气姿态
    Phase == ExhaustedReel            → 鱼耗尽体力后的持续收线姿态
```
会话是抛竿后才生成的 Actor，所以 **Find 要轮询到非空再 Bind**（比如 0.5s Timer，绑上后停）；会话终结后 ViewBridge 会收到终态一次，之后重新开始轮询下一次会话。

验收：搏斗中按住左键/右键，姿态实时切换，与屏幕 debug 文案 `[拖][松线]` 一致。

---

## 任务 5：结果演出 + HUD（可选）

同一个 ViewBridge 的 `OnViewStateChangedBP` 里比较前后状态：
- `bPerfectHook` 变 true → 完美中鱼特效/音效（★与 debug 文案同步）
- `Outcome` 变化 → 断线(LineBroken)/落水(CatInWater)/跑鱼(FishEscaped)/抄网捕获(Captured)/拖上岸(Landed) 各播一段演出
- HUD 进度条直接绑 `NormalizedFishStamina`（鱼体力%）、`Phase` 文案

可用字段全表：`FishingSessionId / Revision / Phase / Outcome / FishDefinitionId / NormalizedFishStamina / bReeling / bSlacking / bPerfectHook / FishMotionIntent`。

---

## 总验收（全部做完后关 debug 试一次）

控制台 `cat.Fishing.Debug 0`，纯靠你的表现走完整流程：
选中窝料、左键蓄力打窝见圈 → 选中鱼竿拿杆 → 左键抛竿见漂飞落水 → 咬钩提示 → 提竿见鱼 → 左右键搏斗姿态切换 → 鱼进入抄网范围后放杆、选中抄网并左键使用，直接叼嘴（体力不限），或耗尽鱼体力后继续收线并用 E 拾取岸上的鱼。
哪一环看不见了，就说明那一环还依赖 debug——回对应任务补。测完记得 `cat.Fishing.Debug 1` 打开继续开发。

## 常见坑速查

| 症状 | 原因 |
|---|---|
| 新事件在蓝图里搜不到 | 没重启 Editor（反射变更） |
| 换了 BP 类后按键失灵 | BP override 了 ActivateAbility |
| 放竿失败/猫被顶开 | 表现 Mesh 不是 NoCollision |
| Project Settings 改了没生效 | DeveloperSettings 启动才读 ini，重启 |
| PIE 运行中保存资产"成功"但重启丢失 | PIE 中保存被静默拒绝——**停 PIE 再保存** |
| 鱼 Mesh 和 debug 球位置不一致 | 蓝图写了 Actor Transform（禁止），删掉 |

构建证据：本轮功能最终版 `Saved/ScoopChum/BuildFinal2.log`（Editor）与 `BuildGameFinal.log`（Game）均 Succeeded；之后仅补正调试提示/注释。收尾 `BuildDeliveryEditor.log` 因并行咬钩模型修改与旧 `CatFishingBiteTimingWorldTests.cpp` 的 `FCatFishingBiteTimingDistribution/BuildDistribution` 引用尚未衔接而失败，未覆盖或修补该并行工作，不能声称当前整个共享工作区仍然构建通过。资产删除后的独立重载审计在 `InputMigrationFinal2.log` 成功，再运行删除 0 项，正式映射和能力集保持有效。
