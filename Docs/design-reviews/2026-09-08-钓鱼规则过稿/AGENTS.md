# 任务：钓鱼规则整册过稿的「口径零变更」复核

你是独立复核人。本目录里有两份游戏规格文档各两个版本：`主文-v1.1.md` → `主文-v1.2.md`（钓鱼规则），`附篇-v1.1.md` → `附篇-v1.2.md`（多人钓鱼）。v1.2 是对 v1.1 的整册文风过稿：去过程痕迹、去死概念、拆长句、去重、删验算、补引导句、删附录 B（裁决出处索引）与附录 C（集中缺口）。过稿的硬要求是**口径零变更**——除下面「允许的变更」外，任何数值、判定条件、公式、清单成员、事件顺序、责任归属都不得丢失或改变。请只读两份文档本身，不要假设你知道这个游戏。

## 允许的变更（不算问题；但若 v1.2 的落地与这里描述不符，要报）

1. 鱼体力改由鱼行为模型维护、钓鱼只读；v1.1 §4.4「服务器每秒一次 …`鱼体力−=F_total×0.08`」删除；§4.1 表与 §6.3 接口相应改写，§6.3 新增「钓鱼 → 鱼行为」一行。
2. 移动附加消耗改常数：前移 1.5 点/秒、后退 3 点/秒，不再乘力量对比系数；v1.1 §4.2 的力量三档定义（0.8～1.25 相近档）与 §4.4 移动消耗段（3 点/秒×系数、相近档 2.25／4.5、猫弱行另加 2）整体退役。
3. 附录 B、附录 C 整体删除。附录 C 中「已补／已裁」的规则内容应当已经在 v1.2 正文中有落点；仍开放的待办转派活单，不在文档里——**请单独列出附录 C 里哪些「已补／已裁」的规则内容在 v1.2 正文找不到落点**。附录 C 里的原文引用块（```text 块）本身不需要出现在 v1.2。
4. 窝料专题移到道具册（`道具册-v1.31.md` §3.1 第 7 节）：v1.1 §2.6 特殊窝料、§2.5 表中「品质加概率」「总存量加概率」两行，在 v1.2 只留指针；请核对道具册那一节是否完整承接了这三项的原意（数值、范围、待裁状态）。
5. §7.1「鱼向外发力」行的「具体回复数值见集中缺口」删除（对应 open-07 已裁：放线不恢复）；§5.6「错饵摆尾同揭」删除（错饵摆尾演出已删）；§7.1 完美行改为「瞳孔放大还是缩成竖线、专属声效，待表现集定」；§7.1 碾压行的「（保留待核）」删除。
6. 附篇 §2.1 角色表重构：原「体力消耗：标准速率／快速速率（150%）／自然恢复（200%）」改为「力量计入／消耗倍率 K_pos／恢复」三列（150% 与 K_pos 1.5 是同一个数）；「左键蓄力」改「左键收线／顶住」；§2.3 落地 open-07（放线不恢复、辅助不必退出）；§3.2 落地 gap-31（连携限演出向）；§5 缺口指针表与附录索引删除。
7. 头部：状态行改「现行规格」；章标题从二级提到一级；「（09-08 李前臻拍）」「（快照，参数页为准）」标签大量删除——标签删除不算口径变更，但**数值本身**必须仍在。
8. 62 与道具册的其他改动不在本次复核范围。

## 要做的事（按重要性）

1. **逐条抽取 v1.1 主文第 1～7 章与附录 A 里的每一个口径单元**（数值、阈值、公式、判定条件、事件顺序、清单成员、责任归属、禁止事项），在 v1.2 中找对应。报告三类：**丢失**（v1.2 找不到）、**改变**（数值或条件不同、含义变化）、**新增**（v1.2 有而 v1.1 没有的规则性内容，含我在 §6.3 新增的那一行——评估它是否越出 v1.1 已有信息）。每条给 `文件:行号`、v1.1 原句、v1.2 新句（或「无」）。
2. 对附篇做同样的事。
3. 附录 C 的「已补／已裁」内容落点核对（见允许变更第 3 条）。
4. 允许变更第 4 条：道具册窝料节的承接完整性。

## 输出

把完整报告写到本目录 `复核报告.md`（不要只在最终回答里给链接；最终回答再附一份摘要）。结构：

- A. 口径丢失或改变（必须修）——每条：位置、原句、新句、后果一句话
- B. 新增或措辞可能改变含义（需人判）
- C. 附录 C 已补内容未落点清单
- D. 道具册窝料节承接核对结果
- E. 逐章「查过、无问题」清单（写明覆盖了哪些章节，让读者分清「没发现」和「没看」）

不要提泛泛建议（补测试、补文档、加说明）。不要修改本目录除 `复核报告.md` 以外的任何文件。允许变更清单里的内容不要当成问题报，但落地与描述不符要报。
