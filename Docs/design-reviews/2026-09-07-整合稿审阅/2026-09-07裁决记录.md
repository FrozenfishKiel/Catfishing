# 2026-09-07 钓鱼规则裁决记录（李前臻逐条拍定，十个决策点）

> 本地 SSOT。上 50 裁决账本与参数页待授权。逐节落点见整合稿内「已裁（09-07）」共 34 处。

1. **搏斗消耗模型**：位移驱动为纲——`C = ΔD × (1+V/V_base) × (F/F_base) × K_dir × K_resist × R_base`。ΔD＝鱼当秒**意图游动量**（相对水，非鱼猫距变化；鱼歇免费、鱼闹收费、僵持照扣）；K_dir 角度连续化 `f(θ)`，锚点 `f(0°)=1.0/f(180°)=0.2`，θ 参照鱼→猫连线反向；K_resist＝力量对比×猫操作查表；R_base 单位**点/米**。判定表定性为意图说明，运行时全公式算。移动附加 S 列取 `2.25`（×1.5 规则）。
2. **W/S**：判标注笔误。全程唯一：W＝前移（靠水）、S＝后退（离水），钓鱼中不切相机反转；判定表猫强/相近 8 行已更正。
3. **多人移动＝力量加权合力**：`ρ=|F_net|/S` 三档（阈值 0.8/0.2 起测）；僵局分共线对顶（顶牛/拔河）与打转；甩飞恰 3 人／连环摔倒 ≥4 人；摸鱼不进计票、不拖慢队伍、+5 回体。
4. **窝点**：几何归属＝近者优先/等距早者优先/边界算内（投料与浮漂共用）；存量扣减＝**真咬成立**与饵同事务点、鱼逃不回滚、同刻服务器判序；基础池不扣转正；空池 8 倍断层判有意；聚鱼 RC 单源化（投料源、范围窝点、不加存量、冷却从结束后起算）。
5. **搏斗判定细节**：碾压＝瞬时判定（开始时＋合力变动时）；猫弱向岸补 S/放线两行；相近档比值 `0.8—1.25` 取等入内；字段三处一名（鱼册力量＝F_fish＝鱼力）、完美削减入场落字段全程用削后值（碾压判定同）；竿强度瞬断与 L_max **都接**（巨影装备闸门／放线预算）。
6. **抛竿流程**：待机＝装备即状态、站位只看落点；偏移＝均匀圆盘按漂精准度 `0.5/1/1.5 米`、落岸免损、越界 clamp；不设离开判定、切饵不重置计时、归属落水定死；放弃＝长按收竿键 `1.5 秒` 限主钓手、图鉴按鱼逃口径。
7. **抄网收鱼**：翻肚后固定速率拖回（建议 `2 米/秒`）、D 随收线归零、落岸在收线者最近岸点；时间戳并列按**席位序**、同 tick 结算序**收鱼→苏醒→落水**；挥网与拾取统一交互半径 `1.5 米`、不加朝向判定。
8. **多人与边界**：鱼体力扣减用**总力量**；当前力量＝基础×成长、不随体力衰减；两套消耗**替代**收束（`鱼力×0.12` 废除，K_pos 主 1.0/辅 1.5）；位置落水只判主钓手、取等不落水、体力出口优先、**放线不判位置落水**；辅助位 F 键 `1.5 米` 入退、归零弹出 `≥20%` 重入、上限随联机人数；恢复三处同源（基数 `2.5×` 场景乘数，+5 与 200% 同数、被换下不叠加）；入夜中断按事务时点导出（试探零损、真咬后按鱼逃）、**窝点翻天清空**。
9. **渔网存量边界**：扣存量（逐条按偏好属性，复用钓走扣法）、条数 Total 封顶不凑数、无窝点/空池撒网基础池低保 `1—2 条` 不扣。
10. **房主两层论**：游戏机制层无房主（旧草稿意图转正）；房间管理层房主保留（踢人/开局/翻天兜底，移交最早加入者）。62 不动，钱改 10.1。

**数字**：整合稿 ⚠️ 62 → 32（余者属张佳数据/王甜甜配方/表现集/UI/数值阶段与钱的少量闭环）；钱成澔待办 41 → 10（2 清理＋8 共担，其中 2 条只剩执行）。
