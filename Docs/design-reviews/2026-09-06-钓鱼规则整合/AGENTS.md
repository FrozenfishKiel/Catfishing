# 任务：把钓鱼规则整合成一份可直接上线的完整正文

请中文作答。你是执行者，不是裁决者——**遇到需要拍板的地方标出来，不要自己拍**。

## 背景（一句话）

《小猫钓鱼》的「钓鱼规则」页是全项目唯一的钓鱼实现规格书，程序照它做、验收照它验。设计作者钱成澔 2026-09-03 把该页第三～七章删了，正在自己的个人空间重写成「钓鱼全流程 9.3」，但**只改了一半，也没搬回知识库**。现在由你把散在几处的内容整合回一份完整正文。

## 材料

`材料/` 下 14 份，`材料/00_材料清单.md` 是索引。**不要读本目录以外的文件**，上级目录有其他评审的结论，读了会污染这次整合。

## 优先级（这条最重要）

冲突时严格按此裁，不要自己权衡：

| 级 | 来源 | 说明 |
|---|---|---|
| **0（凌驾一切）** | 李前臻 2026-09-04 起的裁决 | 见 `50_裁决账本` 的 09-04 / 09-05 两节，**外加下面「账本还没记的裁决」**。9.3 最后改于 09-03 17:55，不可能反映这些 |
| **1** | `10_钱-钓鱼全流程9.3` | 口径之争以它为准 |
| **2** | `20_知识库-钓鱼规则-现状` | 幸存的第一、二章 |
| **3** | `30_0902完整备份` | 补 9.3 缺的章节 |
| 参考（不作出处） | `40` `41` 旧多人方案（账本 09-01 已定性「不作为实现依据」）、`42` 08-10 老原稿 | 仅供查漏，不要据此写规格 |

### 账本还没记的裁决（2026-09-06，李前臻拍，与第 0 级同权）

窝点模型由「气味浓度」改为「**存量**」：

- `{X腥, P香, J酵}` 是**窝点里鱼的条数**，按偏好属性分类；打窝＝往里加对应偏好的鱼。原文写的「气味属性向量／浓度」口径作废。
- **钓走一条鱼，从它偏好的那一格扣**（钓走腥鱼 → X−1）。原文完全没有这一步，必须补上。
- 衰减 `×0.9 / 30 秒` **保留**，含义变成鱼自己游走。
- 咬钩间隔 `T = T_base/(1+Total/K)` **先保留**。
- 抽鱼是**先看饵、再出鱼**：某个浮漂计时到点，就按这个漂主人当前挂的饵，从该窝点里抽一条给他。不是「先抽出一条鱼、再看谁的饵对得上」。原文「谁的饵对就上谁的钩；几个人的饵都命中时随机」这句会被读成后者，要改写。

这三处仍未定，**标 ⚠️ 待定、不要自己定**：①衰减会产生小数，12.4 条鱼怎么处理；②某属性只剩 0.6 条时钓走一条怎么扣；③无窝料基础池（Total=0）钓走扣不扣。

## 怎么合

对每一节，先判它属于哪一类，再动手：

1. **口径冲突**（两处都成立，说法不同）→ 按优先级取，不要折中调和。
2. **9.3 自己错了**（可验算的错误：量纲重复、判定表自相矛盾、占优策略、数值断层）→ **不要照抄成规格**。原样保留它的意图，把错处标 ⚠️ 并写清错在哪、你验算的结果是什么。`51_钱的同步页` 里有一份 09-03 对 9.3 的复核清单，钱只做了一半——那是线索，但**你要自己复算，别直接引用结论**。
3. **9.3 没写、备份里有** → 保留备份的。特别注意：**第五章「抄网与多人抢抄」（6 节）、6.4「游戏结束与结算」、2.7 携带口径、2.8 数据流转总流程，只存在于 `30_0902完整备份`，9.3 里一个字都没有。** 抢抄是 W2 验收项，不能丢。
4. **两边都没有** → 标 ⚠️ 缺口，写清缺什么、该由谁给。**不要发明规则。**

## 硬要求

- **不要创造新机制。**你的产物必须每一句都能追溯到某份材料或第 0 级裁决。
- **算给我看。**凡涉及数值、公式、概率，把算式跑出来（可用 Python）。
- **章节编号连续**，不要留 9.3 那种跳号（它跳过第五、第八章）。
- 排版：`## 第N章`、`### N.x`，不用 callout / 颜色 / 分隔线。数值后标「（快照，参数页为准）」。

## 输出两个文件

**① `钓鱼规则-整合稿.md`** —— 一份可以直接覆盖知识库那一页的完整正文。开头写三行头：属主：钱成澔 ／ 状态 ／ 出处。

**② `合并决策表.md`** —— 逐节一行，这是我审核的抓手：

| 章节 | 内容来自 | 有无冲突 | 怎么定的 | 我标了什么 ⚠️ |

最后附一节「**我改动了原文含义的地方**」，逐条列出：哪一句、原文怎么说、你写成什么、依据是哪一级。这一节我会重点看。
