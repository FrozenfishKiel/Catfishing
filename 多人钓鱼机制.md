# 多人钓鱼机制

# 小猫钓鱼 · 多人合力钓鱼完整流程逻辑书

## 一、核心设计原则

## 二、位置定义与权限

### 2\.1 三个位置状态

### 2\.2 位置切换规则

- 任何人都可以随时从岸上进入辅助位（点击“帮忙”按钮），参与拉线。

- 辅助位人数设上限（受屏幕内可交互人数限制，最多3\~4人）。

- 任何人从钓鱼位移到岸上的唯一途径：换人（体力耗尽请求替换）。

- 任何人从辅助位移到岸上：随时可以主动松开左键并点击“E”按钮退出辅助，或体力耗尽自动退出。

## 三、力量操作机制

### 3\.1 蓄力与衰减规则（主钓手与辅助手通用）

蓄力速度：固定速率，从0到满约 2秒（可调）。无点击频率、无爆发增量等复杂机制。

### 3\.2 主钓手特殊规则

- 主钓手按住左键（力量 \> 0%） = 收线状态。

- 主钓手松开左键（力量 = 0%） = 放松状态（判定瞬间切换）。

- 主钓手的力量从100%降到0%的过程（1秒），在达到零即被判定为“放线状态”。

### 3\.3 辅助手规则

- 辅助手力量 \> 0% = 发力状态（计入总力量，消耗体力）。

- 辅助手力量 = 0% = 卸力状态（不贡献，不消耗体力）。

## 四、力量合成公式

### 4\.1 当前力量值（各角色实时）

text

主钓手当前力量 = 主钓手自身力量属性 × 当前力量百分比（0%\~100%）辅助手当前力量 = 辅助手自身力量属性 × 当前力量百分比（0%\~75%）

### 4\.2 总力量值（合力）

text

总力量 = 主钓手当前力量 \+ Σ\(每位辅助手当前力量\)

## 五、体力消耗逻辑

### 5\.1 消耗速率（按当前力量百分比动态计算）

举例：

- 主钓手基础消耗 = 10点/秒，当前力量80% → 消耗 8点/秒

- 辅助手基础消耗 = 10点/秒，当前力量60% → 消耗 10×1\.5×0\.6 = 9点/秒

### 5\.2 “捣乱”惩罚公式

当主钓手处于放线状态（力量=0%），而任意辅助手力量 \> 0%（正在发力）：

text

主钓手额外体力消耗 = Σ\(所有捣乱辅助手当前体力消耗\) × 50%

举例：3个辅助手在捣乱，各自当前消耗为 9点/秒、6点/秒、3点/秒，总额外消耗 = \(9\+6\+3\) × 50% = 9点/秒 附加给主钓手。

> 辅助手自己的体力依然正常扣除（捣乱有成本）。
>
>

### 5\.3 体力归零后果

## 六、换人机制（全自愿 · 拼手速）

### 6\.1 换人触发条件

- 主钓手体力 ≤ 5% 时，屏幕出现闪烁提示 “体力不支！按 E 请求换人”。

- 主钓手按下 E键 后，发起换人请求。

### 6\.2 换人流程

【第一步】主钓手A按E发起换人请求

↓

【第二步】所有在岸上的替补屏幕弹出提示：

“【A】体力不支！谁来接替钓鱼位？（按 E 接手）”

↓

【第三步】岸上替补自由决定是否按E接手（全自愿，无强制）

↓

【第四步】若有多人同时按E → 先按先得（拼手速）

↓

【第五步】按E成功的替补B 与 主钓手A 交换位置：

\- A → 从钓鱼位移到岸上（进入虚脱休息，体力恢复速度×2）

\- B → 从岸上坐到钓鱼位（获得完整控制权）

↓

【第六步】状态继承：

\- 鱼的状态（拉力、位置）完全继承

\- 鱼竿耐久度完全继承

\- B的体力 = 自身当前体力（接手时是多少就是多少）

\- B的当前力量百分比 = 0%（需重新蓄力）

### 6\.3 换人规则

### 6\.4 被换下后的体力恢复

- 被换下的猫进入“虚脱休息”状态：体力恢复速度 × 2（200%速度）。

- 体力恢复到 50%以上 后，可重新从岸上竞争接替钓鱼位。

## 七、鱼竿耐久度系统

### 7\.1 核心参数

### 7\.2 耐久消耗公式（指数增长）

设 R = 当前总力量 / 杆子临界点（力量比值）

数值参考：

### 7\.3 耐久归零后果（断杆）

- 鱼直接脱钩逃跑（钓鱼失败）。

- 所有正在拉线的猫被弹飞（倒地动画1秒）。

- 鱼竿进入“损坏”状态，需修复后才能再次使用。

## 八、失败判定

### 8\.1 “被拖下水”触发条件

同时满足以下两个条件：

1. 主钓手处于放线状态（力量 = 0%，即松开左键）。

2. 当前 总力量值 \< 鱼当前拉力。

### 8\.2 被拖下水后果

- 钓鱼强制失败，鱼逃跑。

- 参与拉线的所有猫被拖入水中（落水扑腾动画3秒，无法操作）。

### 8\.3 其他失败场景

## 九、视觉反馈与UI

### 9\.1 力量反馈

- 主钓手：屏幕中央大弧形条，实时显示当前力量百分比（0%\~100%），标注“收线/放线”状态。

- 每位辅助手：屏幕侧边小弧形条，显示自身力量百分比（0%\~75%）。

- 总力量值：屏幕上方显示当前总力量数值及与鱼拉力的对比

### 9\.2 警告提示

### 9\.3 左下角环形耐久度

- 颜色随耐久变化：绿（\>60%）→ 黄（30%\~60%）→ 红（10%\~30%）→ 闪烁红（\<10%）。

- 显示当前R值（力量/临界点），让玩家实时监控风险。

## 十、固定数值

## 十一、完整状态机流程图

【钓鱼进行中】

│

├─ 主钓手按住左键 → 蓄力（力量上升，0→100%）

│     ├─ 力量 \> 0% = 收线状态

│     ├─ 辅助手按住左键蓄力 → 正常合作（总力量叠加）

│     └─ 辅助手松开左键衰减 → 单人硬拉

│

├─ 主钓手松开左键 → 力量在 1 秒内衰减，归零后进入放线状态

│     ├─ 辅助手按住左键蓄力 → 捣乱惩罚（主钓手额外耗体）

│     └─ 辅助手松开左键衰减 → 全员休息（体力缓回）

│

├─ 主钓手体力 ≤ 5% → 按E换人

│     ├─ 岸上有人按E接手 → 位置交换，继续钓鱼

│     └─ 10秒无人接手 → 钓鱼失败

│

├─ 辅助手体力归零 → 自动卸力，可退到岸上恢复

│

├─ 总力量 \< 鱼拉力 \+ 主钓手放线 → 被拖下水 → 钓鱼失败

│

├─ 鱼竿耐久归零 → 断杆 → 钓鱼失败

│

└─ 鱼耐力归零（外部系统） → ✅ 钓鱼成功！

## 十二、多人场景示例

### 场景一：4人合作钓巨鱼

- 主钓手A按住左键蓄力，力量达到100%（收线状态，贡献100）

- 辅助手B、C、D同时按住左键蓄力，力量各达到75%（各贡献75×3=225）

- 总力量 = 100 \+ 225 = 325

- 鱼拉力300 → 鱼被拉近 ✅

- 体力消耗：A每秒10点，B/C/D每秒各15点（满力量时）

### 场景二：主钓手想休息但队友在捣乱

- 主钓手A松开左键（力量归零，放线状态）

- 辅助手B没注意，依然按住左键蓄力到75%

- 捣乱惩罚生效：B当前消耗15点/秒 × 50% = 7\.5点/秒附加给A

- A体力加速见底，被迫换人

### 场景三：极限换人接力

- 主钓手A体力剩3%，按E请求换人

- 岸上替补C和D同时按E → 系统判定C帧数更快（先按先得）

- C接手钓鱼位（体力当前80%，从0开始蓄力）

- A退到岸上（体力恢复速度×2）

- 鱼的状态不变，钓鱼继续

## 十三、手持鱼竿与运动约束（现行实现口径）

> 本节覆盖前文“固定钓鱼位/换人时交换站位”的旧草案口径。前文仍保留为多人协作设计参考；当前运行机制不再锁定角色位置，也不通过瞬移模拟拉扯。多人蓄力、合力和独立体力结算的现行口径见 13.5。

### 13.1 鱼竿是会话载体

- 钓鱼会话绑定到具体鱼竿 Actor，而不是绑定到某个玩家的固定钓鱼位。
- 鱼竿只有 `Held`（手持）和 `Grounded`（落地）两种权威姿态。第一位操作者是当前持竿者；持竿者离开后，有候补操作者则原子晋升，没有候补则同一根鱼竿落地。
- 放下鱼竿不会销毁会话、鱼钩、鱼、已放线长、鱼剩余体力或本场鱼线耐久，也不会中断等待、试探、咬钩、搏斗、近岸和力竭收线阶段。
- 拾起时复用同一鱼竿 Actor 和同一会话。新持竿者继承鱼、钩、线和阶段状态；玩家力量、搏斗体力和输入序号按接手者的会话规则重新绑定，不复制上一人的角色属性或剩余体力。
- 玩家手持鱼竿时可正常行走；系统不再调用 `DisableMovement`、不把角色吸附到固定站位，也不由钓鱼代码瞬移角色。

### 13.2 服务器权威姿态

- 手持姿态由服务器根据当前持竿 Pawn、控制朝向和规范 Grip 偏移计算；Gameplay 不读取客户端动画 Socket 作为权威依据。
- 规范竿向为 `Grip → RodTip`。服务器每帧只在 `Held` 时更新鱼竿 Transform、竿尖位置和竿尖速度；`Grounded` 时停止该姿态 Tick，保持落地点稳定。
- Actor 移动复制负责让其他客户端看到鱼竿位置；杆向、持有者和姿态模式随鱼竿表现状态复制。

### 13.3 固定步约束输入

每个搏斗固定步统一采样：

- 竿尖世界位置与速度；
- 规范竿向；
- 持竿玩家的服务器速度；
- 鱼的世界位置、游向、速度、力量和剩余体力；
- 猫的力量、剩余体力、收线/松线输入；
- 已放线长、最大线长、鱼线强度和本场鱼线耐久。

竿向与鱼线方向的对齐度为：

```text
A_rod = clamp(dot(normalize(RodForward), normalize(Fish - RodTip)), 0, 1)
K_leverage = lerp(K_min, 1, A_rod)
```

默认 `K_min = 0.4`。竿尖朝向鱼时杠杆接近 1；横竿或背向鱼时不会让力量瞬间归零，但有效杠杆下降、猫维持同样约束所需的体力增加。

玩家和竿尖沿鱼线反方向移动时形成运动发力：

```text
V_away = max(0, -dot(RodTipVelocity, LineDirection), -dot(CarrierVelocity, LineDirection))
M_move = clamp(V_away / V_ref, 0, 1)
Strength_active = Strength_primary × Power_primary + Σ(Strength_helper_i × Power_helper_i)
Strength_effective = Strength_active × K_leverage × (1 + MoveBoost × M_move)
```

默认 `V_ref = 300 cm/s`、`MoveBoost = 0.35`。后退拉竿能提高本步有效力量；向鱼移动只会改变竿尖距离和余线，不产生反向力量奖励。

### 13.4 双向运动结果

- `Strength_effective` 进入现有鱼体力消耗公式，因此鱼的基础体力和原有平静/挣扎消耗档位不重写，只把实际杆向与玩家运动合成到猫的有效力量中。
- 杆向不佳或玩家额外做功会提高猫的体力成本；鱼向外游、玩家反向移动、收线和线长边界共同决定真实约束负载。
- 鱼线/鱼竿磨损按实际约束负载计算，不再仅由静态力量比决定。没有绷紧就不会因为玩家原地转身凭空磨损。
- 绷紧时，鱼的对抗力按负载和双方力量比例生成水平牵引加速度，交给服务器 `CharacterMovement::AddForce`。默认满负载最大加速度为 `1200 cm/s²`；碰撞、地面和移动模式仍由 CharacterMovement 处理。
- 固定步求解器裁决鱼位置、双方体力、线长、磨损和终局；Chaos/动画只负责角色和鱼竿的物理表现，不能在各客户端分别决定玩法结果。

### 13.5 多人蓄力、合力与独立体力（现行实现）

- 鱼竿 `OperatorPlayerStates[0]` 是主位，其余成员是辅助位；当前默认容量为 2，提高 `MaximumRodOperatorSlots` 后同一套算法可处理更多辅助位。
- 每位参与者加入搏斗或接管主位时，力量百分比都从 0 开始。按住左键以固定速率蓄力，2 秒到达本角色上限；松开后以固定速率衰减，1 秒从满值降到 0。主位上限为 100%，辅助位上限为 75%。
- 主位力量仍大于 0 时保持收线；自然衰减到 0 后自动放线。主位右键是兼容的主动立即放线，会清空当前力量；辅助位右键不控制线杯，也不改变其左键蓄力。
- 每位角色的力量贡献为 `自身 FishingStrength × 当前力量百分比`，全部贡献相加后再统一乘竿向杠杆和持竿移动修正。鱼的剩余体力继续使用既有平静/挣扎、张力和性格倍率公式，只把输入猫力换成这份实时有效合力，不改鱼基础体力。
- 主位基础体力消耗为 `10 × 主位力量百分比 /秒`，再乘竿向不佳和持竿者额外做功形成的成本倍率；每位辅助位独立消耗 `10 × 1.5 × 自身力量百分比 /秒`。所有扣减分别写入各自 ASC，不再按鱼力量倒推猫体力消耗。
- 主位力量为 0、但任一辅助位仍在发力时，主位不获得放线恢复，而是额外承担 `全部辅助位当前消耗合计 × 50%`；辅助位自身仍照常扣除。
- 主位完全放线且没有辅助位继续发力时，按当前配置恢复体力（默认 3 点/秒）。辅助位体力归零会被服务器清空力量并自动移出鱼竿操作位；无正体力的玩家加入辅助位会被拒绝并自动卸位。
- 放下鱼竿不会继续结算离开玩家的体力。重新拾起或主位接管时读取接手者 ASC 的当前体力，不补满、不继承上一人的体力，并从 0 重新蓄力；鱼、位置、阶段、线长、鱼体力和本场鱼线耐久继续继承。
- 服务器每秒限频记录 `fishing_cooperative_power_sample`，包含主位力量百分比、主/辅助/合计力量、辅助人数、主位与鱼的本步体力消耗以及捣乱附加消耗；`fishing_helper_exhausted` 记录辅助位力竭自动退出。

### 13.6 仍待接入的策划层

- “主位体力低于 5% 主动广播换人、10 秒无人接手失败、先到先得接替”的专用请求和 UI 尚未接入；当前仍通过公共鱼竿操作位离开/加入完成接力。
- 被换下后的“虚脱休息 2 倍恢复”和恢复到 50% 才能再次竞争主位尚未接入。正式换人 UI 与恢复状态落地前，不把这两项写成已实现行为。

## 十四、放下、拾起与主动切线

### 14.1 任意阶段放下与拾起

- 玩家可以在所有非终态阶段放下鱼竿。最后一位操作者离开时，鱼竿在角色附近做地面探测后转为 `Grounded`；鱼和鱼钩继续交互。
- 上钩后的无人值守阶段自动松线：鱼可以继续外游并带出线；到达最大线长后才按真实外向负载磨损本场鱼线。
- 任意合法玩家可从公共交互锚点拾起未满员的鱼竿。服务器按鱼竿 Revision 原子写入操作者；若会话接管失败，则精确回滚本次占位。
- 拾起与放下只改变操作者和鱼竿姿态，不重置鱼的位置、体力、阶段、线长或耐久。

### 14.2 主动切线止损

- 上钩后的 `HookedFight`、`NearShore`、`ExhaustedReel` 和 `AutoHauling` 阶段允许主动切线；当前 X/取消输入在这些阶段路由为切线，上钩前仍是普通取消。
- 当前持竿者可以切线；鱼竿已落地且无人值守时，原持竿者或竿主在 250 cm 内也可以直接切线，不需要先拾起。
- 切线由服务器校验 `FishingSessionId + CastAttemptId + Revision + RequestId`，同一请求幂等重放，第一个终态提交者获胜。
- 切线结果是独立的 `LineCut`：本场鱼和已消耗鱼饵丢失，会话终止；不会追加最后一笔鱼线磨损，不会把可复用鱼竿标记为损坏，也不会改变鱼竿当前手持/落地姿态。
- UI/动画通过独立 `Cosmetic.Fishing.LineCut` 事件表现，不能借用自然断线或被拖下水的结果提示。
